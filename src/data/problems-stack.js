// Seed: stack
export const STACK_PROBLEMS = [
  {
    id: 'valid-parentheses',
    functionName: 'isValid',
    number: 7,
    title: 'Valid Parentheses',
    difficulty: 'Easy',
    topic: 'stack',
    tags: ['stack', 'string'],
    leetcodeUrl: 'https://leetcode.com/problems/valid-parentheses/',
    description: 'Given a string `s` containing just the characters `(`, `)`, `{`, `}`, `[` and `]`, determine if the input string is valid.\n\nAn input is valid if:\n1. Open brackets must be closed by the same type of brackets.\n2. Open brackets must be closed in the correct order.\n3. Every close bracket has a corresponding open bracket of the same type.',
    examples: [
      { input: 's = "()"', output: 'true' },
      { input: 's = "()[]{}"', output: 'true' },
      { input: 's = "(]"', output: 'false' },
    ],
    constraints: ['1 <= s.length <= 10^4', 's consists of parentheses only.'],
    hints: ['A stack gives you last-in, first-out — perfect for matching nested pairs.'],
    approaches: [{ name: 'Stack', summary: 'Push every opening bracket onto a stack. For each closing bracket, pop and check it matches. At the end, the stack must be empty.' }],
    starterCode: {
      javascript: 'function isValid(s) {\n  // your code here\n  return false;\n}',
      python: 'def is_valid(s):\n    # your code here\n    return False',
      java: 'class Solution {\n    public boolean isValid(String s) {\n        return false;\n    }\n}',
      cpp: 'bool isValid(string s) {\n    return false;\n}',
      go: 'func isValid(s string) bool {\n    return false\n}',
    },
    solutions: {
      javascript: 'function isValid(s) {\n  const stack = [];\n  const map = { ")": "(", "]": "[", "}": "{" };\n  for (const c of s) {\n    if (c === "(" || c === "[" || c === "{") stack.push(c);\n    else if (stack.pop() !== map[c]) return false;\n  }\n  return stack.length === 0;\n}',
      python: 'def is_valid(s):\n    stack = []\n    pairs = {")": "(", "]": "[", "}": "{"}\n    for c in s:\n        if c in "([{":\n            stack.append(c)\n        elif not stack or stack.pop() != pairs[c]:\n            return False\n    return not stack',
      java: 'class Solution {\n    public boolean isValid(String s) {\n        java.util.Deque<Character> stack = new java.util.ArrayDeque<>();\n        for (char c : s.toCharArray()) {\n            if (c == \'(\' || c == \'[\' || c == \'{\') stack.push(c);\n            else if (stack.isEmpty()) return false;\n            else {\n                char open = stack.pop();\n                if ((c == \')\' && open != \'(\') || (c == \']\' && open != \'[\') || (c == \'}\' && open != \'{\')) return false;\n            }\n        }\n        return stack.isEmpty();\n    }\n}',
      cpp: 'bool isValid(string s) {\n    std::stack<char> st;\n    for (char c : s) {\n        if (c == \'(\' || c == \'[\' || c == \'{\') st.push(c);\n        else {\n            if (st.empty()) return false;\n            char o = st.top(); st.pop();\n            if ((c == \')\' && o != \'(\') || (c == \']\' && o != \'[\') || (c == \'}\' && o != \'{\')) return false;\n        }\n    }\n    return st.empty();\n}',
      go: 'func isValid(s string) bool {\n    stack := []rune{}\n    pairs := map[rune]rune{\')\': \'(\', \']\': \'[\', \'}\': \'{\'}\n    for _, c := range s {\n        if c == \'(\' || c == \'[\' || c == \'{\' {\n            stack = append(stack, c)\n        } else {\n            if len(stack) == 0 || stack[len(stack)-1] != pairs[c] {\n                return false\n            }\n            stack = stack[:len(stack)-1]\n        }\n    }\n    return len(stack) == 0\n}',
    },
    complexity: { time: 'O(n)', space: 'O(n)' },
    testCases: [
      { input: { s: '()[]{}' }, expected: true,  hidden: false },
      { input: { s: '(]' },     expected: false, hidden: false },
      { input: { s: '{[]}' },   expected: true,  hidden: true },
    ],
  },
  {
    id: 'daily-temperatures',
    functionName: 'dailyTemperatures',
    number: 132,
    title: 'Daily Temperatures',
    difficulty: 'Medium',
    topic: 'stack',
    tags: ['stack', 'monotonic-stack', 'array'],
    leetcodeUrl: 'https://leetcode.com/problems/daily-temperatures/',
    description: 'Given an array of integers `temperatures` representing daily temperatures, return an array `answer` such that `answer[i]` is the number of days you have to wait after day `i` to get a warmer temperature. If no future day is warmer, set `answer[i] = 0`.',
    examples: [
      { input: 'temperatures = [73,74,75,71,69,72,76,73]', output: '[1,1,4,2,1,1,0,0]' },
      { input: 'temperatures = [30,40,50,60]', output: '[1,1,1,0]' },
    ],
    constraints: ['1 <= temperatures.length <= 10^5', '30 <= temperatures[i] <= 100'],
    hints: ['A monotonic (decreasing) stack of indices gives O(n).'],
    approaches: [{ name: 'Monotonic Stack', summary: 'Maintain a stack of indices whose temperatures are waiting for a warmer day. For each day, pop indices colder than today and record how many days they waited.' }],
    starterCode: {
      javascript: 'function dailyTemperatures(temperatures) {\n  return [];\n}',
      python: 'def daily_temperatures(temperatures):\n    return []',
      java: 'class Solution {\n    public int[] dailyTemperatures(int[] temperatures) {\n        return new int[0];\n    }\n}',
      cpp: 'vector<int> dailyTemperatures(vector<int>& temperatures) {\n    return {};\n}',
      go: 'func dailyTemperatures(temperatures []int) []int {\n    return nil\n}',
    },
    solutions: {
      javascript: 'function dailyTemperatures(t) {\n  const n = t.length, ans = new Array(n).fill(0), stack = [];\n  for (let i = 0; i < n; i++) {\n    while (stack.length && t[stack[stack.length-1]] < t[i]) {\n      const j = stack.pop();\n      ans[j] = i - j;\n    }\n    stack.push(i);\n  }\n  return ans;\n}',
      python: 'def daily_temperatures(t):\n    n, ans, stack = len(t), [0]*len(t), []\n    for i in range(n):\n        while stack and t[stack[-1]] < t[i]:\n            j = stack.pop()\n            ans[j] = i - j\n        stack.append(i)\n    return ans',
      java: 'class Solution {\n    public int[] dailyTemperatures(int[] t) {\n        int n = t.length;\n        int[] ans = new int[n];\n        java.util.Deque<Integer> stack = new java.util.ArrayDeque<>();\n        for (int i = 0; i < n; i++) {\n            while (!stack.isEmpty() && t[stack.peek()] < t[i]) {\n                int j = stack.pop();\n                ans[j] = i - j;\n            }\n            stack.push(i);\n        }\n        return ans;\n    }\n}',
      cpp: 'vector<int> dailyTemperatures(vector<int>& t) {\n    int n = t.size(); vector<int> ans(n, 0);\n    std::stack<int> st;\n    for (int i = 0; i < n; i++) {\n        while (!st.empty() && t[st.top()] < t[i]) {\n            int j = st.top(); st.pop();\n            ans[j] = i - j;\n        }\n        st.push(i);\n    }\n    return ans;\n}',
      go: 'func dailyTemperatures(t []int) []int {\n    n := len(t); ans := make([]int, n)\n    stack := []int{}\n    for i := 0; i < n; i++ {\n        for len(stack) > 0 && t[stack[len(stack)-1]] < t[i] {\n            j := stack[len(stack)-1]; stack = stack[:len(stack)-1]\n            ans[j] = i - j\n        }\n        stack = append(stack, i)\n    }\n    return ans\n}',
    },
    complexity: { time: 'O(n)', space: 'O(n)' },
    testCases: [
      { input: { temperatures: [73,74,75,71,69,72,76,73] }, expected: [1,1,4,2,1,1,0,0], hidden: false },
      { input: { temperatures: [30,40,50,60] },             expected: [1,1,1,0],       hidden: false },
      { input: { temperatures: [30,60,90] },                expected: [1,1,0],         hidden: true },
    ],
  },
  {
    id: 'evaluate-reverse-polish-notation',
    functionName: 'evalRPN',
    number: 133,
    title: 'Evaluate Reverse Polish Notation',
    difficulty: 'Medium',
    topic: 'stack',
    tags: ['stack', 'array', 'math'],
    leetcodeUrl: 'https://leetcode.com/problems/evaluate-reverse-polish-notation/',
    description: 'Evaluate the value of an arithmetic expression in **Reverse Polish Notation**. Valid operators are `+`, `-`, `*`, `/`. Each operand may be an integer or another expression. Division between two integers should **truncate toward zero**.\n\nIt is guaranteed the expression is valid and the result fits in a 32-bit integer.',
    examples: [
      { input: 'tokens = ["2","1","+","3","*"]', output: '9', explanation: '((2 + 1) * 3) = 9' },
      { input: 'tokens = ["4","13","5","/","+"]', output: '6', explanation: '(4 + (13 / 5)) = 6' },
    ],
    constraints: ['1 <= tokens.length <= 10^4', 'tokens[i] is either an operator or an integer in [-200, 200].'],
    hints: ['Each operator pops two operands and pushes the result.'],
    approaches: [{ name: 'Stack', summary: 'Walk the tokens. If it is a number, push. If it is an operator, pop two operands, apply the operator, push the result.' }],
    starterCode: {
      javascript: 'function evalRPN(tokens) {\n  return 0;\n}',
      python: 'def eval_rpn(tokens):\n    return 0',
      java: 'class Solution {\n    public int evalRPN(String[] tokens) {\n        return 0;\n    }\n}',
      cpp: 'int evalRPN(vector<string>& tokens) {\n    return 0;\n}',
      go: 'func evalRPN(tokens []string) int {\n    return 0\n}',
    },
    solutions: {
      javascript: 'function evalRPN(tokens) {\n  const stack = [];\n  for (const t of tokens) {\n    if (t === "+" || t === "-" || t === "*" || t === "/") {\n      const b = +stack.pop(), a = +stack.pop();\n      if (t === "+") stack.push(a + b);\n      else if (t === "-") stack.push(a - b);\n      else if (t === "*") stack.push(a * b);\n      else stack.push(Math.trunc(a / b));\n    } else stack.push(+t);\n  }\n  return stack[0];\n}',
      python: 'def eval_rpn(tokens):\n    stack = []\n    for t in tokens:\n        if t in "+-*/":\n            b, a = stack.pop(), stack.pop()\n            if t == "+": stack.append(a + b)\n            elif t == "-": stack.append(a - b)\n            elif t == "*": stack.append(a * b)\n            else: stack.append(int(a / b))\n        else:\n            stack.append(int(t))\n    return stack[0]',
      java: 'class Solution {\n    public int evalRPN(String[] tokens) {\n        java.util.Deque<Integer> stack = new java.util.ArrayDeque<>();\n        for (String t : tokens) {\n            switch (t) {\n                case "+": stack.push(stack.pop() + stack.pop()); break;\n                case "-": { int b = stack.pop(); int a = stack.pop(); stack.push(a - b); break; }\n                case "*": stack.push(stack.pop() * stack.pop()); break;\n                case "/": { int b = stack.pop(); int a = stack.pop(); stack.push(a / b); break; }\n                default: stack.push(Integer.parseInt(t));\n            }\n        }\n        return stack.pop();\n    }\n}',
      cpp: 'int evalRPN(vector<string>& tokens) {\n    std::stack<int> st;\n    for (auto& t : tokens) {\n      if (t == "+" || t == "-" || t == "*" || t == "/") {\n        int b = st.top(); st.pop(); int a = st.top(); st.pop();\n        if (t == "+") st.push(a + b);\n        else if (t == "-") st.push(a - b);\n        else if (t == "*") st.push(a * b);\n        else st.push(a / b);\n      } else st.push(stoi(t));\n    }\n    return st.top();\n}',
      go: 'func evalRPN(tokens []string) int {\n    stack := []int{}\n    for _, t := range tokens {\n        switch t {\n        case "+": stack[len(stack)-2] = stack[len(stack)-2] + stack[len(stack)-1]; stack = stack[:len(stack)-1]\n        case "-": stack[len(stack)-2] = stack[len(stack)-2] - stack[len(stack)-1]; stack = stack[:len(stack)-1]\n        case "*": stack[len(stack)-2] = stack[len(stack)-2] * stack[len(stack)-1]; stack = stack[:len(stack)-1]\n        case "/": stack[len(stack)-2] = stack[len(stack)-2] / stack[len(stack)-1]; stack = stack[:len(stack)-1]\n        default: n, _ := strconv.Atoi(t); stack = append(stack, n)\n        }\n    }\n    return stack[0]\n}',
    },
    complexity: { time: 'O(n)', space: 'O(n)' },
    testCases: [
      { input: { tokens: ['2','1','+','3','*'] }, expected: 9, hidden: false },
      { input: { tokens: ['4','13','5','/','+'] }, expected: 6, hidden: false },
      { input: { tokens: ['10','6','9','3','+','-11','*','/','*','17','+','5','+'] }, expected: 22, hidden: true },
    ],
  },
  {
    id: 'generate-parentheses',
    functionName: 'generateParenthesis',
    number: 134,
    title: 'Generate Parentheses',
    difficulty: 'Medium',
    topic: 'stack',
    tags: ['stack', 'backtracking', 'string'],
    leetcodeUrl: 'https://leetcode.com/problems/generate-parentheses/',
    description: 'Given `n` pairs of parentheses, generate all combinations of well-formed parentheses.',
    examples: [
      { input: 'n = 3', output: '["((()))","(()())","(())()","()(())","()()()"]' },
      { input: 'n = 1', output: '["()"]' },
    ],
    constraints: ['1 <= n <= 8'],
    hints: ['You can only add `(` if there are open slots left, and `)` if there are unmatched opens.'],
    approaches: [{ name: 'Backtracking', summary: 'Build the string step by step. Add `(` while open count < n. Add `)` while close count < open count. Append the string when its length reaches 2n.' }],
    starterCode: {
      javascript: 'function generateParenthesis(n) {\n  return [];\n}',
      python: 'def generate_parenthesis(n):\n    return []',
      java: 'class Solution {\n    public List<String> generateParenthesis(int n) {\n        return new ArrayList<>();\n    }\n}',
      cpp: 'vector<string> generateParenthesis(int n) {\n    return {};\n}',
      go: 'func generateParenthesis(n int) []string {\n    return nil\n}',
    },
    solutions: {
      javascript: 'function generateParenthesis(n) {\n  const res = [];\n  const go = (s, open, close) => {\n    if (s.length === 2 * n) { res.push(s); return; }\n    if (open < n) go(s + "(", open + 1, close);\n    if (close < open) go(s + ")", open, close + 1);\n  };\n  go("", 0, 0);\n  return res;\n}',
      python: 'def generate_parenthesis(n):\n    res = []\n    def go(s, o, c):\n        if len(s) == 2*n: res.append(s); return\n        if o < n: go(s+"(", o+1, c)\n        if c < o: go(s+")", o, c+1)\n    go("", 0, 0)\n    return res',
      java: 'class Solution {\n    public List<String> generateParenthesis(int n) {\n        List<String> res = new ArrayList<>();\n        go(res, new StringBuilder(), 0, 0, n);\n        return res;\n    }\n    void go(List<String> res, StringBuilder s, int o, int c, int n) {\n        if (s.length() == 2*n) { res.add(s.toString()); return; }\n        if (o < n) { s.append("("); go(res, s, o+1, c, n); s.deleteCharAt(s.length()-1); }\n        if (c < o) { s.append(")"); go(res, s, o, c+1, n); s.deleteCharAt(s.length()-1); }\n    }\n}',
      cpp: 'vector<string> generateParenthesis(int n) {\n    vector<string> res;\n    function<void(string,int,int)> go = [&](string s, int o, int c) {\n        if ((int)s.size() == 2*n) { res.push_back(s); return; }\n        if (o < n) go(s+"(", o+1, c);\n        if (c < o) go(s+")", o, c+1);\n    };\n    go("", 0, 0);\n    return res;\n}',
      go: 'func generateParenthesis(n int) []string {\n    res := []string{}\n    var go func(s string, o, c int)\n    go = func(s string, o, c int) {\n        if len(s) == 2*n { res = append(res, s); return }\n        if o < n { go(s+"(", o+1, c) }\n        if c < o { go(s+")", o, c+1) }\n    }\n    go("", 0, 0)\n    return res\n}',
    },
    complexity: { time: 'O(4^n / sqrt(n))', space: 'O(n)' },
    testCases: [
      { input: { n: 3 }, expected: ['((()))','(()())','(())()','()(())','()()()'], hidden: false },
      { input: { n: 1 }, expected: ['()'], hidden: false },
      { input: { n: 2 }, expected: ['(())','()()'], hidden: true },
    ],
  },
]