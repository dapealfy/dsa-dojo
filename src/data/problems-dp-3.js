// dp topic - third batch
export const DP_PROBLEMS_3 = [
  {
    id: 'edit-distance',
    functionName: 'minDistance',
    number: 125,
    title: 'Edit Distance',
    difficulty: 'Hard',
    topic: 'dp',
    tags: ['dynamic-programming', 'string'],
    leetcodeUrl: 'https://leetcode.com/problems/edit-distance/',
    description: 'Given two strings `word1` and `word2`, return the **minimum number of operations** required to convert `word1` to `word2`. Allowed operations: insert, delete, or replace a character.',
    examples: [
      { input: 'word1 = "horse", word2 = "ros"', output: '3', explanation: 'horse → rorse (replace h→r) → rose (delete r) → ros (delete e).' },
      { input: 'word1 = "intention", word2 = "execution"', output: '5' },
    ],
    constraints: ['0 <= word1.length, word2.length <= 500'],
    hints: ['dp[i][j] = edit distance between word1[:i] and word2[:j]. If last chars equal: dp[i][j]=dp[i-1][j-1]. Else: 1 + min(replace, delete, insert).'],
    approaches: [{ name: 'Wagner-Fischer DP', summary: 'Initialize dp[i][0]=i and dp[0][j]=j. If last chars match, dp[i][j]=dp[i-1][j-1]; else dp[i][j]=1+min(dp[i-1][j], dp[i][j-1], dp[i-1][j-1]).' }],
    starterCode: {
      javascript: 'function minDistance(word1, word2) {\n  // your code here\n  return 0;\n}',
      python: 'def min_distance(word1, word2):\n    # your code here\n    return 0',
      java: 'class Solution {\n    public int minDistance(String word1, String word2) {\n        return 0;\n    }\n}',
      cpp: 'int minDistance(string word1, string word2) {\n    return 0;\n}',
      go: 'func minDistance(word1 string, word2 string) int {\n    return 0\n}',
    },
    solutions: {
      javascript: 'function minDistance(w1, w2) {\n  const m = w1.length, n = w2.length;\n  const dp = Array.from({length: m + 1}, () => new Array(n + 1).fill(0));\n  for (let i = 0; i <= m; i++) dp[i][0] = i;\n  for (let j = 0; j <= n; j++) dp[0][j] = j;\n  for (let i = 1; i <= m; i++) for (let j = 1; j <= n; j++) {\n    if (w1[i-1] === w2[j-1]) dp[i][j] = dp[i-1][j-1];\n    else dp[i][j] = 1 + Math.min(dp[i-1][j], dp[i][j-1], dp[i-1][j-1]);\n  }\n  return dp[m][n];\n}',
      python: 'def min_distance(word1, word2):\n    m, n = len(word1), len(word2)\n    dp = [[0]*(n+1) for _ in range(m+1)]\n    for i in range(m+1): dp[i][0] = i\n    for j in range(n+1): dp[0][j] = j\n    for i in range(1, m+1):\n        for j in range(1, n+1):\n            if word1[i-1] == word2[j-1]: dp[i][j] = dp[i-1][j-1]\n            else: dp[i][j] = 1 + min(dp[i-1][j], dp[i][j-1], dp[i-1][j-1])\n    return dp[m][n]',
      java: 'class Solution {\n    public int minDistance(String w1, String w2) {\n        int m = w1.length(), n = w2.length();\n        int[] dp = new int[n + 1];\n        for (int j = 0; j <= n; j++) dp[j] = j;\n        for (int i = 1; i <= m; i++) {\n            int prev = dp[0]; dp[0] = i;\n            for (int j = 1; j <= n; j++) {\n                int tmp = dp[j];\n                if (w1.charAt(i-1) == w2.charAt(j-1)) dp[j] = prev;\n                else dp[j] = 1 + Math.min(prev, Math.min(dp[j], dp[j-1]));\n                prev = tmp;\n            }\n        }\n        return dp[n];\n    }\n}',
      cpp: 'int minDistance(string w1, string w2) {\n    int m = w1.size(), n = w2.size();\n    vector<int> dp(n + 1);\n    for (int j = 0; j <= n; j++) dp[j] = j;\n    for (int i = 1; i <= m; i++) {\n        int prev = dp[0]; dp[0] = i;\n        for (int j = 1; j <= n; j++) {\n            int tmp = dp[j];\n            if (w1[i-1] == w2[j-1]) dp[j] = prev;\n            else dp[j] = 1 + min({prev, dp[j], dp[j-1]});\n            prev = tmp;\n        }\n    }\n    return dp[n];\n}',
      go: 'func minDistance(word1 string, word2 string) int {\n    m, n := len(word1), len(word2)\n    dp := make([]int, n+1)\n    for j := 0; j <= n; j++ { dp[j] = j }\n    for i := 1; i <= m; i++ {\n        prev := dp[0]; dp[0] = i\n        for j := 1; j <= n; j++ {\n            tmp := dp[j]\n            if word1[i-1] == word2[j-1] { dp[j] = prev }\n            else {\n                minv := dp[j]\n                if dp[j-1] < minv { minv = dp[j-1] }\n                if prev < minv { minv = prev }\n                dp[j] = 1 + minv\n            }\n            prev = tmp\n        }\n    }\n    return dp[n]\n}',
    },
    complexity: { time: 'O(m * n)', space: 'O(n)' },
    testCases: [
      { input: { w1: 'horse', w2: 'ros' }, expected: 3, hidden: false },
      { input: { w1: 'intention', w2: 'execution' }, expected: 5, hidden: false },
      { input: { w1: 'abc', w2: 'abc' }, expected: 0, hidden: true },
    ],
  },
  {
    id: 'best-time-to-buy-and-sell-stock-with-cooldown',
    functionName: 'maxProfit',
    number: 126,
    title: 'Best Time to Buy and Sell Stock with Cooldown',
    difficulty: 'Medium',
    topic: 'dp',
    tags: ['dynamic-programming', 'array'],
    leetcodeUrl: 'https://leetcode.com/problems/best-time-to-buy-and-sell-stock-with-cooldown/',
    description: 'You are given an array `prices` where `prices[i]` is the price of a stock on day `i`. You may complete as many transactions as you like, but after you sell you must **cooldown** for one day before buying again. Return the maximum profit.',
    examples: [
      { input: 'prices = [1,2,3,0,2]', output: '3', explanation: 'Buy day 0, sell day 2 → 2. Cooldown day 3. Buy day 4 → cooldown; sell later.' },
      { input: 'prices = [1]', output: '0' },
    ],
    constraints: ['1 <= prices.length <= 5000', '0 <= prices[i] <= 1000'],
    hints: ['Track two states per day: holding (have a stock) and not holding (free to buy).'],
    approaches: [{ name: 'State DP', summary: 'hold[i] = max(hold[i-1], free[i-1] - prices[i]). free[i] = max(free[i-1], sell[i-1]). sell[i] = hold[i-1] + prices[i]. Answer = max(free[n-1], sell[n-1]).' }],
    starterCode: {
      javascript: 'function maxProfit(prices) {\n  // your code here\n  return 0;\n}',
      python: 'def max_profit_cooldown(prices):\n    # your code here\n    return 0',
      java: 'class Solution {\n    public int maxProfit(int[] prices) {\n        return 0;\n    }\n}',
      cpp: 'int maxProfit(vector<int>& prices) {\n    return 0;\n}',
      go: 'func maxProfit(prices []int) int {\n    return 0\n}',
    },
    solutions: {
      javascript: 'function maxProfit(prices) {\n  let hold = -prices[0], free = 0, sell = 0;\n  for (let i = 1; i < prices.length; i++) {\n    const prevHold = hold, prevFree = free, prevSell = sell;\n    hold = Math.max(prevHold, prevFree - prices[i]);\n    sell = prevHold + prices[i];\n    free = Math.max(prevFree, prevSell);\n  }\n  return Math.max(free, sell);\n}',
      python: 'def max_profit_cooldown(prices):\n    hold, free, sell = -prices[0], 0, 0\n    for x in prices[1:]:\n        hold, free, sell = max(hold, free - x), max(free, sell), hold + x\n    return max(free, sell)',
      java: 'class Solution {\n    public int maxProfit(int[] prices) {\n        int hold = -prices[0], free = 0, sell = 0;\n        for (int i = 1; i < prices.length; i++) {\n            int ph = hold, pf = free, ps = sell;\n            hold = Math.max(ph, pf - prices[i]);\n            sell = ph + prices[i];\n            free = Math.max(pf, ps);\n        }\n        return Math.max(free, sell);\n    }\n}',
      cpp: 'int maxProfit(vector<int>& prices) {\n    int hold = -prices[0], free = 0, sell = 0;\n    for (int i = 1; i < (int)prices.size(); i++) {\n        int ph = hold, pf = free, ps = sell;\n        hold = max(ph, pf - prices[i]);\n        sell = ph + prices[i];\n        free = max(pf, ps);\n    }\n    return max(free, sell);\n}',
      go: 'func maxProfit(prices []int) int {\n    hold, free, sell := -prices[0], 0, 0\n    for _, x := range prices[1:] {\n        ph, pf, ps := hold, free, sell\n        if pf-x > ph { hold = pf - x }\n        sell = ph + x\n        if ps > pf { pf = ps }\n        free = pf\n    }\n    if free > sell { return free }\n    return sell\n}',
    },
    complexity: { time: 'O(n)', space: 'O(1)' },
    testCases: [
      { input: { prices: [1,2,3,0,2] }, expected: 3, hidden: false },
      { input: { prices: [1] }, expected: 0, hidden: false },
      { input: { prices: [1,4,2] }, expected: 3, hidden: true },
    ],
  },
  {
    id: 'coin-change-ii',
    functionName: 'change',
    number: 127,
    title: 'Coin Change II',
    difficulty: 'Medium',
    topic: 'dp',
    tags: ['dynamic-programming', 'combinatorics'],
    leetcodeUrl: 'https://leetcode.com/problems/coin-change-ii/',
    description: 'Given an array of coin denominations and an amount, return the **number of combinations** of coins that sum to that amount. Order does not matter.',
    examples: [
      { input: 'amount = 5, coins = [1,2,5]', output: '4', explanation: '5=5, 4+1=2+1+1+1, 2+2+1, 1+1+1+1+1 → 4 ways.' },
      { input: 'amount = 3, coins = [2]', output: '0' },
    ],
    constraints: ['1 <= coins.length <= 300', '1 <= coins[i] <= 5000', '0 <= amount <= 5000'],
    hints: ['Outer loop over coins, inner over amounts. This ordering ensures combinations, not permutations.'],
    approaches: [{ name: '2-D / 1-D DP', summary: 'dp[a] = #ways. For each coin c, for a from c to amount, dp[a] += dp[a-c]. Initial dp[0] = 1.' }],
    starterCode: {
      javascript: 'function change(amount, coins) {\n  // your code here\n  return 0;\n}',
      python: 'def change(amount, coins):\n    # your code here\n    return 0',
      java: 'class Solution {\n    public int change(int amount, int[] coins) {\n        return 0;\n    }\n}',
      cpp: 'int change(int amount, vector<int>& coins) {\n    return 0;\n}',
      go: 'func change(amount int, coins []int) int {\n    return 0\n}',
    },
    solutions: {
      javascript: 'function change(amount, coins) {\n  const dp = new Array(amount + 1).fill(0);\n  dp[0] = 1;\n  for (const c of coins) for (let a = c; a <= amount; a++) dp[a] += dp[a - c];\n  return dp[amount];\n}',
      python: 'def change(amount, coins):\n    dp = [0] * (amount + 1); dp[0] = 1\n    for c in coins:\n        for a in range(c, amount + 1): dp[a] += dp[a - c]\n    return dp[amount]',
      java: 'class Solution {\n    public int change(int amount, int[] coins) {\n        int[] dp = new int[amount + 1]; dp[0] = 1;\n        for (int c : coins) for (int a = c; a <= amount; a++) dp[a] += dp[a - c];\n        return dp[amount];\n    }\n}',
      cpp: 'int change(int amount, vector<int>& coins) {\n    vector<int> dp(amount + 1, 0); dp[0] = 1;\n    for (int c : coins) for (int a = c; a <= amount; a++) dp[a] += dp[a - c];\n    return dp[amount];\n}',
      go: 'func change(amount int, coins []int) int {\n    dp := make([]int, amount+1); dp[0] = 1\n    for _, c := range coins {\n        for a := c; a <= amount; a++ { dp[a] += dp[a-c] }\n    }\n    return dp[amount]\n}',
    },
    complexity: { time: 'O(n * amount)', space: 'O(amount)' },
    testCases: [
      { input: { amount: 5, coins: [1,2,5] }, expected: 4, hidden: false },
      { input: { amount: 3, coins: [2] }, expected: 0, hidden: false },
      { input: { amount: 10, coins: [10] }, expected: 1, hidden: true },
    ],
  },
  {
    id: 'decode-ways',
    functionName: 'numDecodings',
    number: 128,
    title: 'Decode Ways',
    difficulty: 'Medium',
    topic: 'dp',
    tags: ['dynamic-programming', 'string'],
    leetcodeUrl: 'https://leetcode.com/problems/decode-ways/',
    description: 'A message containing letters is encoded as digits `A=1, B=2, ..., Z=26`. Given a string `s` of digits, return the **number of ways** to decode it.',
    examples: [
      { input: 's = "12"', output: '2', explanation: '"AB" or "L".' },
      { input: 's = "226"', output: '3' },
      { input: 's = "06"', output: '0', explanation: '"06" cannot be mapped to "F" (leading zero).' },
    ],
    constraints: ['1 <= s.length <= 100', 's contains only digits and may have leading zeros.'],
    hints: ['dp[i] = ways to decode s[:i]. dp[i] += dp[i-1] if s[i-1] ≠ 0; dp[i] += dp[i-2] if s[i-2..i-1] forms 10..26.'],
    approaches: [{ name: 'Linear DP', summary: 'dp[0]=1. For i from 1 to n: add dp[i-1] if s[i-1] is non-zero; add dp[i-2] if the last two digits form 10..26.' }],
    starterCode: {
      javascript: 'function numDecodings(s) {\n  // your code here\n  return 0;\n}',
      python: 'def num_decodings(s):\n    # your code here\n    return 0',
      java: 'class Solution {\n    public int numDecodings(String s) {\n        return 0;\n    }\n}',
      cpp: 'int numDecodings(string s) {\n    return 0;\n}',
      go: 'func numDecodings(s string) int {\n    return 0\n}',
    },
    solutions: {
      javascript: 'function numDecodings(s) {\n  const n = s.length;\n  const dp = new Array(n + 1).fill(0);\n  dp[0] = 1;\n  for (let i = 1; i <= n; i++) {\n    if (s[i-1] !== "0") dp[i] += dp[i-1];\n    if (i >= 2 && s[i-2] !== "0" && parseInt(s.slice(i-2, i)) <= 26) dp[i] += dp[i-2];\n  }\n  return dp[n];\n}',
      python: 'def num_decodings(s):\n    n = len(s); dp = [0]*(n+1); dp[0] = 1\n    for i in range(1, n+1):\n        if s[i-1] != "0": dp[i] += dp[i-1]\n        if i >= 2 and s[i-2] != "0" and int(s[i-2:i]) <= 26: dp[i] += dp[i-2]\n    return dp[n]',
      java: 'class Solution {\n    public int numDecodings(String s) {\n        int n = s.length(); int[] dp = new int[n + 1]; dp[0] = 1;\n        for (int i = 1; i <= n; i++) {\n            if (s.charAt(i-1) != \'0\') dp[i] += dp[i-1];\n            if (i >= 2 && s.charAt(i-2) != \'0\' && Integer.parseInt(s.substring(i-2, i)) <= 26) dp[i] += dp[i-2];\n        }\n        return dp[n];\n    }\n}',
      cpp: 'int numDecodings(string s) {\n    int n = s.size(); vector<int> dp(n + 1, 0); dp[0] = 1;\n    for (int i = 1; i <= n; i++) {\n        if (s[i-1] != \'0\') dp[i] += dp[i-1];\n        if (i >= 2 && s[i-2] != \'0\' && stoi(s.substr(i-2, 2)) <= 26) dp[i] += dp[i-2];\n    }\n    return dp[n];\n}',
      go: 'func numDecodings(s string) int {\n    n := len(s); dp := make([]int, n+1); dp[0] = 1\n    for i := 1; i <= n; i++ {\n        if s[i-1] != \'0\' { dp[i] += dp[i-1] }\n        if i >= 2 && s[i-2] != \'0\' {\n            v := (int(s[i-2])-48)*10 + (int(s[i-1])-48)\n            if v <= 26 { dp[i] += dp[i-2] }\n        }\n    }\n    return dp[n]\n}',
    },
    complexity: { time: 'O(n)', space: 'O(n)' },
    testCases: [
      { input: { s: '12' }, expected: 2, hidden: false },
      { input: { s: '226' }, expected: 3, hidden: false },
      { input: { s: '06' }, expected: 0, hidden: true },
    ],
  },
  {
    id: 'longest-common-substring',
    functionName: 'longestCommonSubstring',
    number: 129,
    title: 'Longest Common Substring',
    difficulty: 'Medium',
    topic: 'dp',
    tags: ['dynamic-programming', 'string'],
    leetcodeUrl: 'https://leetcode.com/problems/longest-common-substring/',
    description: 'Given two strings `s` and `t`, return the length of the longest common **substring** of the two (contiguous).',
    examples: [
      { input: 's = "abcdxyz", t = "xyzabcd"', output: '4', explanation: '"abcd" or "xyz".' },
      { input: 's = "abc", t = "def"', output: '0' },
    ],
    constraints: ['1 <= s.length, t.length <= 1000'],
    hints: ['dp[i][j] = length of LCS ending at s[i-1] and t[j-1]. If equal: dp[i][j] = dp[i-1][j-1]+1; else 0.'],
    approaches: [{ name: '2-D DP', summary: 'dp[i][j] = 1 + dp[i-1][j-1] if s[i-1]==t[j-1] else 0. Track max over the table.' }],
    starterCode: {
      javascript: 'function longestCommonSubstring(s, t) {\n  // your code here\n  return 0;\n}',
      python: 'def longest_common_substring(s, t):\n    # your code here\n    return 0',
      java: 'class Solution {\n    public int longestCommonSubstring(String s, String t) {\n        return 0;\n    }\n}',
      cpp: 'int longestCommonSubstring(string s, string t) {\n    return 0;\n}',
      go: 'func longestCommonSubstring(s string, t string) int {\n    return 0\n}',
    },
    solutions: {
      javascript: 'function longestCommonSubstring(s, t) {\n  const m = s.length, n = t.length;\n  let best = 0;\n  const dp = Array.from({length: m + 1}, () => new Array(n + 1).fill(0));\n  for (let i = 1; i <= m; i++) for (let j = 1; j <= n; j++) {\n    if (s[i-1] === t[j-1]) { dp[i][j] = dp[i-1][j-1] + 1; best = Math.max(best, dp[i][j]); }\n  }\n  return best;\n}',
      python: 'def longest_common_substring(s, t):\n    m, n = len(s), len(t)\n    dp = [[0]*(n+1) for _ in range(m+1)]\n    best = 0\n    for i in range(1, m+1):\n        for j in range(1, n+1):\n            if s[i-1] == t[j-1]:\n                dp[i][j] = dp[i-1][j-1] + 1\n                if dp[i][j] > best: best = dp[i][j]\n    return best',
      java: 'class Solution {\n    public int longestCommonSubstring(String s, String t) {\n        int m = s.length(), n = t.length(), best = 0;\n        int[] dp = new int[n + 1];\n        for (int i = 1; i <= m; i++) {\n            int prev = 0;\n            for (int j = 1; j <= n; j++) {\n                int tmp = dp[j];\n                if (s.charAt(i-1) == t.charAt(j-1)) { dp[j] = prev + 1; best = Math.max(best, dp[j]); }\n                else dp[j] = 0;\n                prev = tmp;\n            }\n        }\n        return best;\n    }\n}',
      cpp: 'int longestCommonSubstring(string s, string t) {\n    int m = s.size(), n = t.size(), best = 0;\n    vector<int> dp(n + 1, 0);\n    for (int i = 1; i <= m; i++) {\n        int prev = 0;\n        for (int j = 1; j <= n; j++) {\n            int tmp = dp[j];\n            if (s[i-1] == t[j-1]) { dp[j] = prev + 1; best = max(best, dp[j]); }\n            else dp[j] = 0;\n            prev = tmp;\n        }\n    }\n    return best;\n}',
      go: 'func longestCommonSubstring(s string, t string) int {\n    m, n := len(s), len(t); best := 0\n    dp := make([]int, n+1)\n    for i := 1; i <= m; i++ {\n        prev := 0\n        for j := 1; j <= n; j++ {\n            tmp := dp[j]\n            if s[i-1] == t[j-1] { dp[j] = prev + 1; if dp[j] > best { best = dp[j] } } else { dp[j] = 0 }\n            prev = tmp\n        }\n    }\n    return best\n}',
    },
    complexity: { time: 'O(m * n)', space: 'O(n)' },
    testCases: [
      { input: { s: 'abcdxyz', t: 'xyzabcd' }, expected: 4, hidden: false },
      { input: { s: 'abc', t: 'def' }, expected: 0, hidden: false },
      { input: { s: 'abcabc', t: 'abcabc' }, expected: 6, hidden: true },
    ],
  },
  {
    id: 'max-profit-with-transactions',
    functionName: 'maxProfit',
    number: 130,
    title: 'Best Time to Buy and Sell Stock with Transaction Fee',
    difficulty: 'Medium',
    topic: 'dp',
    tags: ['dynamic-programming', 'array'],
    leetcodeUrl: 'https://leetcode.com/problems/best-time-to-buy-and-sell-stock-with-transaction-fee/',
    description: 'You are given an array `prices` where `prices[i]` is the price on day `i`, and a `fee` for each transaction. You may complete as many transactions as you like, but you pay the fee for each completed sale. Return the maximum profit.',
    examples: [
      { input: 'prices = [1,3,2,8,4,9], fee = 2', output: '8' },
      { input: 'prices = [1,3,7,5,10,3], fee = 3', output: '6' },
    ],
    constraints: ['1 <= prices.length <= 5*10^4', '0 <= prices[i] < 5*10^4', '0 <= fee < 5*10^4'],
    hints: ['Track two states: cash (no stock) and hold (own stock). After selling, subtract the fee once.'],
    approaches: [{ name: 'State DP', summary: 'hold[i] = max(hold[i-1], cash[i-1] - prices[i]). cash[i] = max(cash[i-1], hold[i-1] + prices[i] - fee). Answer = cash[n-1].' }],
    starterCode: {
      javascript: 'function maxProfit(prices, fee) {\n  // your code here\n  return 0;\n}',
      python: 'def max_profit_fee(prices, fee):\n    # your code here\n    return 0',
      java: 'class Solution {\n    public int maxProfit(int[] prices, int fee) {\n        return 0;\n    }\n}',
      cpp: 'int maxProfit(vector<int>& prices, int fee) {\n    return 0;\n}',
      go: 'func maxProfit(prices []int, fee int) int {\n    return 0\n}',
    },
    solutions: {
      javascript: 'function maxProfit(prices, fee) {\n  let cash = 0, hold = -prices[0];\n  for (let i = 1; i < prices.length; i++) {\n    cash = Math.max(cash, hold + prices[i] - fee);\n    hold = Math.max(hold, cash - prices[i]);\n  }\n  return cash;\n}',
      python: 'def max_profit_fee(prices, fee):\n    cash, hold = 0, -prices[0]\n    for x in prices[1:]:\n        cash = max(cash, hold + x - fee)\n        hold = max(hold, cash - x)\n    return cash',
      java: 'class Solution {\n    public int maxProfit(int[] prices, int fee) {\n        int cash = 0, hold = -prices[0];\n        for (int i = 1; i < prices.length; i++) {\n            cash = Math.max(cash, hold + prices[i] - fee);\n            hold = Math.max(hold, cash - prices[i]);\n        }\n        return cash;\n    }\n}',
      cpp: 'int maxProfit(vector<int>& prices, int fee) {\n    int cash = 0, hold = -prices[0];\n    for (int i = 1; i < (int)prices.size(); i++) {\n        cash = max(cash, hold + prices[i] - fee);\n        hold = max(hold, cash - prices[i]);\n    }\n    return cash;\n}',
      go: 'func maxProfit(prices []int, fee int) int {\n    cash, hold := 0, -prices[0]\n    for _, x := range prices[1:] {\n        if hold+x-fee > cash { cash = hold + x - fee }\n        if cash-x > hold { hold = cash - x }\n    }\n    return cash\n}',
    },
    complexity: { time: 'O(n)', space: 'O(1)' },
    testCases: [
      { input: { prices: [1,3,2,8,4,9], fee: 2 }, expected: 8, hidden: false },
      { input: { prices: [1,3,7,5,10,3], fee: 3 }, expected: 6, hidden: false },
      { input: { prices: [1], fee: 1 }, expected: 0, hidden: true },
    ],
  },
  {
    id: 'distinct-subsequences',
    functionName: 'numDistinct',
    number: 131,
    title: 'Distinct Subsequences',
    difficulty: 'Hard',
    topic: 'dp',
    tags: ['dynamic-programming', 'string'],
    leetcodeUrl: 'https://leetcode.com/problems/distinct-subsequences/',
    description: 'Given two strings `s` and `t`, return the number of distinct subsequences of `s` which equals `t`.',
    examples: [
      { input: 's = "rabbbit", t = "rabbit"', output: '3' },
      { input: 's = "babgbag", t = "bag"', output: '5' },
    ],
    constraints: ['1 <= s.length, t.length <= 1000'],
    hints: ['dp[i][j] = #ways to form t[:j] from s[:i]. If s[i-1]==t[j-1], dp[i][j] = dp[i-1][j-1] + dp[i-1][j]; else dp[i-1][j].'],
    approaches: [{ name: '2-D DP', summary: 'dp[0][0]=1; if j>0, dp[0][j]=0. Otherwise: dp[i][j] = dp[i-1][j] + (s[i-1]==t[j-1] ? dp[i-1][j-1] : 0). Can be reduced to one row.' }],
    starterCode: {
      javascript: 'function numDistinct(s, t) {\n  // your code here\n  return 0;\n}',
      python: 'def num_distinct(s, t):\n    # your code here\n    return 0',
      java: 'class Solution {\n    public int numDistinct(String s, String t) {\n        return 0;\n    }\n}',
      cpp: 'int numDistinct(string s, string t) {\n    return 0;\n}',
      go: 'func numDistinct(s string, t string) int {\n    return 0\n}',
    },
    solutions: {
      javascript: 'function numDistinct(s, t) {\n  const m = s.length, n = t.length;\n  const dp = new Array(n + 1).fill(0);\n  dp[0] = 1;\n  for (let i = 1; i <= m; i++)\n    for (let j = n; j >= 1; j--)\n      if (s[i-1] === t[j-1]) dp[j] += dp[j-1];\n  return dp[n];\n}',
      python: 'def num_distinct(s, t):\n    m, n = len(s), len(t)\n    dp = [0]*(n+1); dp[0] = 1\n    for i in range(1, m+1):\n        for j in range(n, 0, -1):\n            if s[i-1] == t[j-1]: dp[j] += dp[j-1]\n    return dp[n]',
      java: 'class Solution {\n    public int numDistinct(String s, String t) {\n        int m = s.length(), n = t.length();\n        long[] dp = new long[n + 1]; dp[0] = 1;\n        for (int i = 1; i <= m; i++)\n            for (int j = n; j >= 1; j--)\n                if (s.charAt(i-1) == t.charAt(j-1)) dp[j] += dp[j-1];\n        return (int) dp[n];\n    }\n}',
      cpp: 'int numDistinct(string s, string t) {\n    int m = s.size(), n = t.size();\n    vector<unsigned long long> dp(n + 1, 0); dp[0] = 1;\n    for (int i = 1; i <= m; i++)\n        for (int j = n; j >= 1; j--)\n            if (s[i-1] == t[j-1]) dp[j] += dp[j-1];\n    return (int)dp[n];\n}',
      go: 'func numDistinct(s string, t string) int {\n    m, n := len(s), len(t)\n    dp := make([]int, n+1); dp[0] = 1\n    for i := 1; i <= m; i++ {\n        for j := n; j >= 1; j-- {\n            if s[i-1] == t[j-1] { dp[j] += dp[j-1] }\n        }\n    }\n    return dp[n]\n}',
    },
    complexity: { time: 'O(m * n)', space: 'O(n)' },
    testCases: [
      { input: { s: 'rabbbit', t: 'rabbit' }, expected: 3, hidden: false },
      { input: { s: 'babgbag', t: 'bag' }, expected: 5, hidden: false },
      { input: { s: 'a', t: 'a' }, expected: 1, hidden: true },
    ],
  },
]
