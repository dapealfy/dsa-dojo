// linkedlist topic (continued)
export const LINKEDLIST_PROBLEMS = [
  {
    id: 'reverse-linked-list',
    functionName: 'reverseList',
    number: 6,
    title: 'Reverse Linked List',
    difficulty: 'Easy',
    topic: 'linkedlist',
    tags: ['linked-list', 'recursion'],
    leetcodeUrl: 'https://leetcode.com/problems/reverse-linked-list/',
    description: 'Given the `head` of a singly linked list, reverse the list, and return the reversed list.',
    examples: [
      { input: 'head = [1,2,3,4,5]', output: '[5,4,3,2,1]' },
      { input: 'head = [1,2]', output: '[2,1]' },
    ],
    constraints: ['The number of nodes is in [0, 5000].', '-5000 <= Node.val <= 5000'],
    hints: ['You need three pointers: prev, curr, next.'],
    approaches: [{ name: 'Iterative 3-pointer', summary: 'Walk the list. For each node, point its next to the previous node, then advance prev and curr. Return prev at the end.' }],
    starterCode: {
      javascript: 'function reverseList(head) {\n  // your code here\n  return head;\n}',
      python: 'def reverse_list(head):\n    # your code here\n    return head',
      java: 'class Solution {\n    public ListNode reverseList(ListNode head) {\n        return head;\n    }\n}',
      cpp: 'ListNode* reverseList(ListNode* head) {\n    return head;\n}',
      go: 'func reverseList(head *ListNode) *ListNode {\n    return head\n}',
    },
    solutions: {
      javascript: 'function reverseList(head) {\n  let prev = null, curr = head;\n  while (curr) {\n    const next = curr.next;\n    curr.next = prev;\n    prev = curr;\n    curr = next;\n  }\n  return prev;\n}',
      python: 'def reverse_list(head):\n    prev, curr = None, head\n    while curr:\n        curr.next, prev, curr = prev, curr, curr.next\n    return prev',
      java: 'class Solution {\n    public ListNode reverseList(ListNode head) {\n        ListNode prev = null, curr = head;\n        while (curr != null) {\n            ListNode next = curr.next;\n            curr.next = prev;\n            prev = curr;\n            curr = next;\n        }\n        return prev;\n    }\n}',
      cpp: 'ListNode* reverseList(ListNode* head) {\n    ListNode *prev = nullptr, *curr = head;\n    while (curr) {\n        ListNode *next = curr->next;\n        curr->next = prev;\n        prev = curr;\n        curr = next;\n    }\n    return prev;\n}',
      go: 'func reverseList(head *ListNode) *ListNode {\n    var prev *ListNode\n    curr := head\n    for curr != nil {\n        next := curr.Next\n        curr.Next = prev\n        prev = curr\n        curr = next\n    }\n    return prev\n}',
    },
    complexity: { time: 'O(n)', space: 'O(1)' },
    testCases: [
      { input: { vals: [1,2,3,4,5] }, expected: [5,4,3,2,1], hidden: false },
      { input: { vals: [1,2] },       expected: [2,1],     hidden: false },
    ],
  },
  {
    id: 'merge-two-sorted-lists',
    functionName: 'mergeTwoLists',
    number: 89,
    title: 'Merge Two Sorted Lists',
    difficulty: 'Easy',
    topic: 'linkedlist',
    tags: ['linked-list', 'recursion'],
    leetcodeUrl: 'https://leetcode.com/problems/merge-two-sorted-lists/',
    description: 'Merge two sorted linked lists and return it as a **sorted** list. The list should be made by splicing together the nodes of the first two lists.',
    examples: [
      { input: 'list1 = [1,2,4], list2 = [1,3,4]', output: '[1,1,2,3,4,4]' },
      { input: 'list1 = [], list2 = []', output: '[]' },
    ],
    constraints: ['0 <= nodes <= 50', '-100 <= Node.val <= 100', 'Both lists are sorted in non-decreasing order.'],
    hints: ['Use a dummy head. Compare the heads of each list and append the smaller one until one is empty.'],
    approaches: [{ name: 'Dummy-head iteration', summary: 'Use a sentinel head. Walk both lists, attach the smaller head, advance it. Append the remainder. O(n + m).' }],
    starterCode: {
      javascript: 'function mergeTwoLists(list1, list2) {\n  // your code here\n  return null;\n}',
      python: 'def merge_two_lists(list1, list2):\n    # your code here\n    return None',
      java: 'class Solution {\n    public ListNode mergeTwoLists(ListNode list1, ListNode list2) {\n        return null;\n    }\n}',
      cpp: 'ListNode* mergeTwoLists(ListNode* list1, ListNode* list2) {\n    return nullptr;\n}',
      go: 'func mergeTwoLists(list1, list2 *ListNode) *ListNode {\n    return nil\n}',
    },
    solutions: {
      javascript: 'function mergeTwoLists(l1, l2) {\n  const dummy = { val: 0, next: null };\n  let tail = dummy;\n  while (l1 && l2) {\n    if (l1.val <= l2.val) { tail.next = l1; l1 = l1.next; }\n    else { tail.next = l2; l2 = l2.next; }\n    tail = tail.next;\n  }\n  tail.next = l1 || l2;\n  return dummy.next;\n}',
      python: 'def merge_two_lists(l1, l2):\n    dummy = ListNode(0); tail = dummy\n    while l1 and l2:\n        if l1.val <= l2.val:\n            tail.next = l1; l1 = l1.next\n        else:\n            tail.next = l2; l2 = l2.next\n        tail = tail.next\n    tail.next = l1 or l2\n    return dummy.next',
      java: 'class Solution {\n    public ListNode mergeTwoLists(ListNode list1, ListNode list2) {\n        ListNode dummy = new ListNode(0), tail = dummy;\n        while (list1 != null && list2 != null) {\n            if (list1.val <= list2.val) { tail.next = list1; list1 = list1.next; }\n            else { tail.next = list2; list2 = list2.next; }\n            tail = tail.next;\n        }\n        tail.next = (list1 != null) ? list1 : list2;\n        return dummy.next;\n    }\n}',
      cpp: 'ListNode* mergeTwoLists(ListNode* l1, ListNode* l2) {\n    ListNode dummy(0), *tail = &dummy;\n    while (l1 && l2) {\n        if (l1->val <= l2->val) { tail->next = l1; l1 = l1->next; }\n        else { tail->next = l2; l2 = l2->next; }\n        tail = tail->next;\n    }\n    tail->next = l1 ? l1 : l2;\n    return dummy.next;\n}',
      go: 'func mergeTwoLists(l1, l2 *ListNode) *ListNode {\n    dummy := &ListNode{Val: 0}\n    tail := dummy\n    for l1 != nil && l2 != nil {\n        if l1.Val <= l2.Val { tail.Next = l1; l1 = l1.Next }\n        else { tail.Next = l2; l2 = l2.Next }\n        tail = tail.Next\n    }\n    if l1 != nil { tail.Next = l1 } else { tail.Next = l2 }\n    return dummy.Next\n}',
    },
    complexity: { time: 'O(n+m)', space: 'O(1)' },
    testCases: [
      { input: { list1: [1,2,4], list2: [1,3,4] }, expected: [1,1,2,3,4,4], hidden: false },
      { input: { list1: [], list2: [] }, expected: [], hidden: false },
      { input: { list1: [], list2: [0] }, expected: [0], hidden: true },
    ],
  },
  {
    id: 'linked-list-cycle',
    functionName: 'hasCycle',
    number: 90,
    title: 'Linked List Cycle',
    difficulty: 'Easy',
    topic: 'linkedlist',
    tags: ['linked-list', 'two-pointers', 'hash-set'],
    leetcodeUrl: 'https://leetcode.com/problems/linked-list-cycle/',
    description: 'Given `head`, the head of a linked list, determine if the linked list has a cycle in it. There is a cycle if some node in the list can be reached again by continuously following the `next` pointer.',
    examples: [
      { input: 'head = [3,2,0,-4], pos = 1', output: 'true', explanation: 'Tail connects to node index 1.' },
      { input: 'head = [1,2], pos = -1', output: 'false' },
    ],
    constraints: ['0 <= nodes <= 10^4', '-10^5 <= Node.val <= 10^5', 'pos is -1 or a valid index.'],
    hints: ['Fast/slow pointer: if fast ever meets slow, there is a cycle; if fast hits null, there is not.'],
    approaches: [{ name: 'Floyd tortoise and hare', summary: 'Move slow by 1 and fast by 2. If they ever meet, a cycle exists. If fast reaches null, no cycle. O(1) extra space.' }],
    starterCode: {
      javascript: 'function hasCycle(head) {\n  // your code here\n  return false;\n}',
      python: 'def has_cycle(head):\n    # your code here\n    return False',
      java: 'class Solution {\n    public boolean hasCycle(ListNode head) {\n        return false;\n    }\n}',
      cpp: 'bool hasCycle(ListNode *head) {\n    return false;\n}',
      go: 'func hasCycle(head *ListNode) bool {\n    return false\n}',
    },
    solutions: {
      javascript: 'function hasCycle(head) {\n  let slow = head, fast = head;\n  while (fast && fast.next) {\n    slow = slow.next;\n    fast = fast.next.next;\n    if (slow === fast) return true;\n  }\n  return false;\n}',
      python: 'def has_cycle(head):\n    slow = fast = head\n    while fast and fast.next:\n        slow = slow.next\n        fast = fast.next.next\n        if slow is fast:\n            return True\n    return False',
      java: 'class Solution {\n    public boolean hasCycle(ListNode head) {\n        ListNode slow = head, fast = head;\n        while (fast != null && fast.next != null) {\n            slow = slow.next;\n            fast = fast.next.next;\n            if (slow == fast) return true;\n        }\n        return false;\n    }\n}',
      cpp: 'bool hasCycle(ListNode *head) {\n    ListNode *slow = head, *fast = head;\n    while (fast && fast->next) {\n        slow = slow->next;\n        fast = fast->next->next;\n        if (slow == fast) return true;\n    }\n    return false;\n}',
      go: 'func hasCycle(head *ListNode) bool {\n    slow, fast := head, head\n    for fast != nil && fast.Next != nil {\n        slow = slow.Next\n        fast = fast.Next.Next\n        if slow == fast { return true }\n    }\n    return false\n}',
    },
    complexity: { time: 'O(n)', space: 'O(1)' },
    testCases: [
      { input: { vals: [3,2,0,-4], pos: 1 }, expected: true, hidden: false },
      { input: { vals: [1,2], pos: -1 }, expected: false, hidden: false },
      { input: { vals: [1], pos: -1 }, expected: false, hidden: true },
    ],
  },
  {
    id: 'reorder-list',
    functionName: 'reorderList',
    number: 91,
    title: 'Reorder List',
    difficulty: 'Medium',
    topic: 'linkedlist',
    tags: ['linked-list', 'two-pointers', 'stack'],
    leetcodeUrl: 'https://leetcode.com/problems/reorder-list/',
    description: 'You are given the head of a singly linked list. Reorder it to follow the pattern: L0 → L1 → … → Ln-1 → Ln → L0 → Ln-1 → L1 → Ln-2 → …\n\nDo not modify the node values; only change the next pointers.',
    examples: [
      { input: 'head = [1,2,3,4]', output: '[1,4,2,3]' },
      { input: 'head = [1,2,3,4,5]', output: '[1,5,2,4,3]' },
    ],
    constraints: ['1 <= nodes <= 5*10^4', '-1000 <= Node.val <= 1000'],
    hints: ['Split in half, reverse the second half, then interleave the two halves.'],
    approaches: [{ name: 'Split + reverse + merge', summary: 'Find the middle with slow/fast, reverse the second half, then weave it into the first half by alternating next pointers.' }],
    starterCode: {
      javascript: 'function reorderList(head) {\n  // your code here\n}',
      python: 'def reorder_list(head):\n    # your code here\n    pass',
      java: 'class Solution {\n    public void reorderList(ListNode head) {}\n}',
      cpp: 'void reorderList(ListNode* head) {}',
      go: 'func reorderList(head *ListNode) {}',
    },
    solutions: {
      javascript: 'function reorderList(head) {\n  if (!head || !head.next) return;\n  let slow = head, fast = head;\n  while (fast.next && fast.next.next) { slow = slow.next; fast = fast.next.next; }\n  let prev = null, curr = slow.next;\n  slow.next = null;\n  while (curr) { const nx = curr.next; curr.next = prev; prev = curr; curr = nx; }\n  let a = head, b = prev;\n  while (b) { const nx = a.next; a.next = b; b = b.next; a.next.next = nx; a = nx; }\n}',
      python: 'def reorder_list(head):\n    if not head or not head.next: return\n    slow = fast = head\n    while fast.next and fast.next.next:\n        slow = slow.next; fast = fast.next.next\n    prev, curr = None, slow.next\n    slow.next = None\n    while curr:\n        curr.next, prev, curr = prev, curr, curr.next\n    a, b = head, prev\n    while b:\n        nx = a.next\n        a.next = b; b = b.next\n        a.next.next = nx; a = nx',
      java: 'class Solution {\n    public void reorderList(ListNode head) {\n        if (head == null || head.next == null) return;\n        ListNode slow = head, fast = head;\n        while (fast.next != null && fast.next.next != null) { slow = slow.next; fast = fast.next.next; }\n        ListNode prev = null, curr = slow.next; slow.next = null;\n        while (curr != null) { ListNode nx = curr.next; curr.next = prev; prev = curr; curr = nx; }\n        ListNode a = head, b = prev;\n        while (b != null) { ListNode nx = a.next; a.next = b; b = b.next; a.next.next = nx; a = nx; }\n    }\n}',
      cpp: 'void reorderList(ListNode* head) {\n    if (!head || !head->next) return;\n    ListNode *slow = head, *fast = head;\n    while (fast->next && fast->next->next) { slow = slow->next; fast = fast->next->next; }\n    ListNode *prev = nullptr, *curr = slow->next; slow->next = nullptr;\n    while (curr) { ListNode* nx = curr->next; curr->next = prev; prev = curr; curr = nx; }\n    ListNode *a = head, *b = prev;\n    while (b) { ListNode* nx = a->next; a->next = b; b = b->next; a->next->next = nx; a = nx; }\n}',
      go: 'func reorderList(head *ListNode) {\n    if head == nil || head.Next == nil { return }\n    slow, fast := head, head\n    for fast.Next != nil && fast.Next.Next != nil { slow = slow.Next; fast = fast.Next.Next }\n    var prev *ListNode; curr := slow.Next; slow.Next = nil\n    for curr != nil { nx := curr.Next; curr.Next = prev; prev = curr; curr = nx }\n    a, b := head, prev\n    for b != nil { nx := a.Next; a.Next = b; b = b.Next; a.Next.Next = nx; a = nx }\n}',
    },
    complexity: { time: 'O(n)', space: 'O(1)' },
    testCases: [
      { input: { vals: [1,2,3,4] }, expected: [1,4,2,3], hidden: false },
      { input: { vals: [1,2,3,4,5] }, expected: [1,5,2,4,3], hidden: false },
      { input: { vals: [1] }, expected: [1], hidden: true },
    ],
  },
  {
    id: 'remove-nth-node-from-end',
    functionName: 'removeNthFromEnd',
    number: 92,
    title: 'Remove Nth Node From End of List',
    difficulty: 'Medium',
    topic: 'linkedlist',
    tags: ['linked-list', 'two-pointers'],
    leetcodeUrl: 'https://leetcode.com/problems/remove-nth-node-from-end-of-list/',
    description: 'Given the `head` of a linked list, remove the `n`-th node from the end of the list and return its head.',
    examples: [
      { input: 'head = [1,2,3,4,5], n = 2', output: '[1,2,3,5]' },
      { input: 'head = [1], n = 1', output: '[]' },
    ],
    constraints: ['1 <= nodes <= 30', '1 <= n <= nodes'],
    hints: ['Advance a fast pointer n steps first, then move slow and fast together until fast hits null — slow ends at the node before the target.'],
    approaches: [{ name: 'Two-pointer single pass', summary: 'Use a dummy head. Advance fast by n steps. Walk slow and fast until fast.next is null. Remove slow.next.' }],
    starterCode: {
      javascript: 'function removeNthFromEnd(head, n) {\n  // your code here\n  return head;\n}',
      python: 'def remove_nth_from_end(head, n):\n    # your code here\n    return head',
      java: 'class Solution {\n    public ListNode removeNthFromEnd(ListNode head, int n) {\n        return head;\n    }\n}',
      cpp: 'ListNode* removeNthFromEnd(ListNode* head, int n) {\n    return head;\n}',
      go: 'func removeNthFromEnd(head *ListNode, n int) *ListNode {\n    return head\n}',
    },
    solutions: {
      javascript: 'function removeNthFromEnd(head, n) {\n  const dummy = { val: 0, next: head };\n  let fast = dummy, slow = dummy;\n  for (let i = 0; i < n; i++) fast = fast.next;\n  while (fast.next) { fast = fast.next; slow = slow.next; }\n  slow.next = slow.next.next;\n  return dummy.next;\n}',
      python: 'def remove_nth_from_end(head, n):\n    dummy = ListNode(0, head)\n    fast = slow = dummy\n    for _ in range(n):\n        fast = fast.next\n    while fast.next:\n        fast = fast.next; slow = slow.next\n    slow.next = slow.next.next\n    return dummy.next',
      java: 'class Solution {\n    public ListNode removeNthFromEnd(ListNode head, int n) {\n        ListNode dummy = new ListNode(0, head);\n        ListNode fast = dummy, slow = dummy;\n        for (int i = 0; i < n; i++) fast = fast.next;\n        while (fast.next != null) { fast = fast.next; slow = slow.next; }\n        slow.next = slow.next.next;\n        return dummy.next;\n    }\n}',
      cpp: 'ListNode* removeNthFromEnd(ListNode* head, int n) {\n    ListNode dummy(0); dummy.next = head;\n    ListNode *fast = &dummy, *slow = &dummy;\n    for (int i = 0; i < n; i++) fast = fast->next;\n    while (fast->next) { fast = fast->next; slow = slow->next; }\n    slow->next = slow->next->next;\n    return dummy.next;\n}',
      go: 'func removeNthFromEnd(head *ListNode, n int) *ListNode {\n    dummy := &ListNode{Next: head}\n    fast, slow := dummy, dummy\n    for i := 0; i < n; i++ { fast = fast.Next }\n    for fast.Next != nil { fast = fast.Next; slow = slow.Next }\n    slow.Next = slow.Next.Next\n    return dummy.Next\n}',
    },
    complexity: { time: 'O(n)', space: 'O(1)' },
    testCases: [
      { input: { vals: [1,2,3,4,5], n: 2 }, expected: [1,2,3,5], hidden: false },
      { input: { vals: [1], n: 1 }, expected: [], hidden: false },
      { input: { vals: [1,2], n: 1 }, expected: [1], hidden: true },
    ],
  },
  {
    id: 'copy-list-with-random-pointer',
    functionName: 'copyRandomList',
    number: 93,
    title: 'Copy List with Random Pointer',
    difficulty: 'Medium',
    topic: 'linkedlist',
    tags: ['linked-list', 'hash-map'],
    leetcodeUrl: 'https://leetcode.com/problems/copy-list-with-random-pointer/',
    description: 'A linked list of length `n` is given such that each node contains an additional random pointer, which can point to any node in the list, or `null`.\n\nConstruct a **deep copy** of the list.',
    examples: [
      { input: 'head = [[7,null],[13,0],[11,4],[10,2],[1,0]]', output: 'deep copy of same structure' },
      { input: 'head = [[1,1],[2,1]]', output: 'deep copy' },
    ],
    constraints: ['0 <= n <= 1000', '-10^4 <= Node.val <= 10^4'],
    hints: ['Two-pass with hashmap: first create clone nodes keyed by original, then rewire next/random using the map.'],
    approaches: [{ name: 'HashMap interleave', summary: 'Map original → clone, then for each original, set clone.next/random from map[original.next/random].' }],
    starterCode: {
      javascript: 'function copyRandomList(head) {\n  // your code here\n  return null;\n}',
      python: 'def copy_random_list(head):\n    # your code here\n    return None',
      java: 'class Solution {\n    public Node copyRandomList(Node head) {\n        return null;\n    }\n}',
      cpp: 'Node* copyRandomList(Node* head) {\n    return nullptr;\n}',
      go: 'func copyRandomList(head *Node) *Node {\n    return nil\n}',
    },
    solutions: {
      javascript: 'function copyRandomList(head) {\n  if (!head) return null;\n  const map = new Map();\n  let n = head;\n  while (n) { map.set(n, { val: n.val, next: null, random: null }); n = n.next; }\n  n = head;\n  while (n) { map.get(n).next = map.get(n.next) || null; map.get(n).random = map.get(n.random) || null; n = n.next; }\n  return map.get(head);\n}',
      python: 'def copy_random_list(head):\n    if not head: return None\n    m = {}\n    n = head\n    while n:\n        m[n] = Node(n.val, None, None)\n        n = n.next\n    n = head\n    while n:\n        m[n].next = m.get(n.next)\n        m[n].random = m.get(n.random)\n        n = n.next\n    return m[head]',
      java: 'class Solution {\n    public Node copyRandomList(Node head) {\n        if (head == null) return null;\n        java.util.Map<Node,Node> m = new java.util.HashMap<>();\n        for (Node n = head; n != null; n = n.next) m.put(n, new Node(n.val, null, null));\n        for (Node n = head; n != null; n = n.next) {\n            m.get(n).next = m.get(n.next);\n            m.get(n).random = m.get(n.random);\n        }\n        return m.get(head);\n    }\n}',
      cpp: 'Node* copyRandomList(Node* head) {\n    if (!head) return nullptr;\n    unordered_map<Node*, Node*> m;\n    for (Node* n = head; n; n = n->next) m[n] = new Node(n->val, nullptr, nullptr);\n    for (Node* n = head; n; n = n->next) {\n        m[n]->next = m[n->next];\n        m[n]->random = m[n->random];\n    }\n    return m[head];\n}',
      go: 'func copyRandomList(head *Node) *Node {\n    if head == nil { return nil }\n    m := map[*Node]*Node{}\n    for n := head; n != nil; n = n.Next { m[n] = &Node{Val: n.Val} }\n    for n := head; n != nil; n = n.Next {\n        m[n].Next = m[n.Next]\n        m[n].Random = m[n.Random]\n    }\n    return m[head]\n}',
    },
    complexity: { time: 'O(n)', space: 'O(n)' },
    testCases: [
      { input: { vals: [7,13,11,10,1], randoms: [null,0,4,2,0] }, expected: { vals: [7,13,11,10,1], randoms: [null,0,4,2,0] }, hidden: false },
      { input: { vals: [1,2], randoms: [1,1] }, expected: { vals: [1,2], randoms: [1,1] }, hidden: false },
      { input: { vals: [], randoms: [] }, expected: null, hidden: true },
    ],
  },
]
