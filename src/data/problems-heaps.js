// Seed: 6 heap problems (#58-63)

export const HEAPS_PROBLEMS = [
  {
    id: 'kth-largest-element-in-a-stream',
    number: 58,
    title: 'Kth Largest Element in a Stream',
    difficulty: 'Easy',
    topic: 'heaps',
    tags: ['heap', 'design'],
    leetcodeUrl: 'https://leetcode.com/problems/kth-largest-element-in-a-stream/',
    description: 'Design a class to find the `k`-th largest element in a stream. Note that it is the `k`-th largest element in the sorted order, not the `k`-th distinct element.\\n\\nImplement the `KthLargest` class:\\n\\n- `KthLargest(int k, int[] nums)` initializes the object with the integer `k` and the stream of test scores `nums`.\\n- `int add(int val)` appends a new test score `val` to the stream and returns the element representing the `k`-th largest element in the pool of test scores so far.',
    examples: [
      { input: 'k=3, ops=["add(3)","add(5)","add(10)","add(9)","add(4)"]', output: '[3,4,5,5,5]', explanation: 'after 3: [3] → 3rd largest is 3; after 3,5: [3,5] → 3rd largest is 3; after 3,5,10: [3,5,10] → 3rd largest is 5; ...' },
    ],
    constraints: ['1 <= k <= 10^4', '0 <= nums.length <= 10^4', '-10^4 <= nums[i] <= 10^4', '-10^4 <= val <= 10^4', 'At most 10^4 calls to add.'],
    hints: ['A min-heap of size k stores the k largest seen so far. Its root is always the k-th largest.'],
    approaches: [{ name: 'Min-Heap of size k', summary: 'Maintain a min-heap with at most k elements. New values are pushed; if the heap grows past k, pop the smallest. The heap root is the k-th largest.' }],
    starterCode: {
      javascript: 'class KthLargest {\n  constructor(k, nums) {\n    // your code here\n  }\n  add(val) {\n    // your code here\n    return 0;\n  }\n}',
      python: 'class KthLargest:\n    def __init__(self, k, nums):\n        # your code here\n        pass\n    def add(self, val):\n        # your code here\n        return 0',
      java: 'class KthLargest {\n    public KthLargest(int k, int[] nums) {}\n    public int add(int val) { return 0; }\n}',
      cpp: 'class KthLargest {\npublic:\n    KthLargest(int k, vector<int>& nums) {}\n    int add(int val) { return 0; }\n};',
      go: 'type KthLargest struct{}\nfunc Constructor(k int, nums []int) KthLargest { return KthLargest{} }\nfunc (this *KthLargest) Add(val int) int { return 0 }',
    },
    solutions: {
      javascript: 'class KthLargest {\n  constructor(k, nums) {\n    this.k = k;\n    this.heap = [];\n    for (const n of nums) this.add(n);\n  }\n  add(val) {\n    this.heap.push(val);\n    this.heap.sort((a, b) => a - b);\n    if (this.heap.length > this.k) this.heap.shift();\n    return this.heap[0];\n  }\n}',
      python: 'class KthLargest:\n    def __init__(self, k, nums):\n        import heapq\n        self.k, self.heap = k, []\n        for n in nums: self.add(n)\n    def add(self, val):\n        import heapq\n        heapq.heappush(self.heap, val)\n        if len(self.heap) > self.k: heapq.heappop(self.heap)\n        return self.heap[0]',
      java: 'class KthLargest {\n    private final int k;\n    private final java.util.PriorityQueue<Integer> minHeap = new java.util.PriorityQueue<>();\n    public KthLargest(int k, int[] nums) {\n        this.k = k;\n        for (int n : nums) add(n);\n    }\n    public int add(int val) {\n        minHeap.offer(val);\n        if (minHeap.size() > k) minHeap.poll();\n        return minHeap.peek();\n    }\n}',
      cpp: 'class KthLargest {\npublic:\n    KthLargest(int k, vector<int>& nums) : k(k) {\n        for (int n : nums) add(n);\n    }\n    int add(int val) {\n        minHeap.push(val);\n        if ((int)minHeap.size() > k) minHeap.pop();\n        return minHeap.top();\n    }\nprivate:\n    int k;\n    std::priority_queue<int, vector<int>, std::greater<int>> minHeap;\n};',
      go: 'type KthLargest struct {\n    k int\n    minHeap []int\n}\nfunc Constructor(k int, nums []int) KthLargest {\n    kth := KthLargest{k: k, minHeap: []int{}}\n    for _, n := range nums { kth.Add(n) }\n    return kth\n}\nfunc (this *KthLargest) Add(val int) int {\n    this.minHeap = append(this.minHeap, val)\n    sort.Slice(this.minHeap, func(i, j int) bool { return this.minHeap[i] < this.minHeap[j] })\n    if len(this.minHeap) > this.k { this.minHeap = this.minHeap[1:] }\n    return this.minHeap[0]\n}',
    },
    complexity: { time: 'O(n log k) construction, O(log k) per add', space: 'O(k)' },
    testCases: [
      { input: { op: 'init', k: 3, nums: [4,5,8,2] },                                  expected: null,         hidden: false },
      { input: { op: 'add', k: 3, prev: [4,5,8,2], val: 3 },                            expected: 4,            hidden: false },
      { input: { op: 'add', k: 3, prev: [4,5,8,2,3], val: 5 },                          expected: 5,            hidden: true },
    ],
  },
  {
    id: 'last-stone-weight',
    functionName: 'lastStoneWeight',
    number: 59,
    title: 'Last Stone Weight',
    difficulty: 'Easy',
    topic: 'heaps',
    tags: ['heap', 'array'],
    leetcodeUrl: 'https://leetcode.com/problems/last-stone-weight/',
    description: 'You are given an array of integers `stones` where `stones[i]` is the weight of the `i`-th stone.\\n\\nWe play a game: on each turn we choose the two heaviest stones and smash them together. Suppose the heaviest two are `x` and `y` with `x <= y`. The result is `x == y ? nothing : y − x`.\\n\\nReturn the smallest possible weight of the last remaining stone. If no stones remain, return `0`.',
    examples: [
      { input: 'stones = [2,7,4,1,8,1]', output: '1', explanation: 'combine 8+7=1, [1,4,1,2,1], combine 4+2=2, [1,1,2,1], combine 2+1=1, [1,1,1], combine 1+1=0 → [1]' },
      { input: 'stones = [1]', output: '1' },
    ],
    constraints: ['1 <= stones.length <= 30', '1 <= stones[i] <= 1000'],
    hints: ['A max-heap pops the two heaviest in O(log n).'],
    approaches: [{ name: 'Max-Heap Simulation', summary: 'Push all stones into a max-heap. Repeatedly pop the two largest, push their difference if non-zero. The final heap root (or 0) is the answer.' }],
    starterCode: {
      javascript: 'function lastStoneWeight(stones) {\n  // your code here\n  return 0;\n}',
      python: 'def last_stone_weight(stones):\n    # your code here\n    return 0',
      java: 'class Solution {\n    public int lastStoneWeight(int[] stones) {\n        return 0;\n    }\n}',
      cpp: 'int lastStoneWeight(vector<int>& stones) {\n    return 0;\n}',
      go: 'func lastStoneWeight(stones []int) int {\n    return 0\n}',
    },
    solutions: {
      javascript: 'function lastStoneWeight(stones) {\n  const heap = [...stones];\n  heap.sort((a, b) => b - a);\n  while (heap.length > 1) {\n    const y = heap.shift();\n    const x = heap.shift();\n    if (y !== x) {\n      heap.push(y - x);\n      heap.sort((a, b) => b - a);\n    }\n  }\n  return heap[0] || 0;\n}',
      python: 'def last_stone_weight(stones):\n    import heapq\n    heap = [-s for s in stones]\n    heapq.heapify(heap)\n    while len(heap) > 1:\n        a = -heapq.heappop(heap); b = -heapq.heappop(heap)\n        if a != b: heapq.heappush(heap, -(a - b))\n    return -heap[0] if heap else 0',
      java: 'class Solution {\n    public int lastStoneWeight(int[] stones) {\n        java.util.PriorityQueue<Integer> max = new java.util.PriorityQueue<>((a, b) -> b - a);\n        for (int s : stones) max.offer(s);\n        while (max.size() > 1) {\n            int a = max.poll(), b = max.poll();\n            if (a != b) max.offer(a - b);\n        }\n        return max.isEmpty() ? 0 : max.peek();\n    }\n}',
      cpp: 'int lastStoneWeight(vector<int>& stones) {\n    std::priority_queue<int> max;\n    for (int s : stones) max.push(s);\n    while (max.size() > 1) {\n        int a = max.top(); max.pop();\n        int b = max.top(); max.pop();\n        if (a != b) max.push(a - b);\n    }\n    return max.empty() ? 0 : max.top();\n}',
      go: 'func lastStoneWeight(stones []int) int {\n    sort.Slice(stones, func(i, j int) bool { return stones[i] > stones[j] })\n    h := stones\n    for len(h) > 1 {\n        y, x := h[0], h[1]\n        h = h[2:]\n        if y != x {\n            h = append(h, y-x)\n            sort.Slice(h, func(i, j int) bool { return h[i] > h[j] })\n        }\n    }\n    if len(h) == 0 { return 0 }\n    return h[0]\n}',
    },
    complexity: { time: 'O(n log n)', space: 'O(n)' },
    testCases: [
      { input: { stones: [2,7,4,1,8,1] }, expected: 1, hidden: false },
      { input: { stones: [1] },           expected: 1, hidden: false },
      { input: { stones: [2,2] },         expected: 0, hidden: true },
    ],
  },
  {
    id: 'k-closest-points-to-origin',
    functionName: 'kClosest',
    number: 60,
    title: 'K Closest Points to Origin',
    difficulty: 'Medium',
    topic: 'heaps',
    tags: ['heap', 'array', 'math', 'divide-and-conquer'],
    leetcodeUrl: 'https://leetcode.com/problems/k-closest-points-to-origin/',
    description: 'Given an array of `points` where `points[i] = [xi, yi]` represents a point on the X-Y plane and an integer `k`, return the `k` closest points to the origin `(0, 0)`.\\n\\nThe distance between two points is the Euclidean distance. The answer can be returned in **any order**.',
    examples: [
      { input: 'points = [[1,3],[-2,2]], k = 1', output: '[[-2,2]]' },
      { input: 'points = [[3,3],[5,-1],[-2,4]], k = 2', output: '[[3,3],[-2,4]]' },
    ],
    constraints: ['1 <= k <= points.length <= 10^4', '-10^4 <= xi, yi <= 10^4'],
    hints: ['A max-heap of size k keyed on distance does the trick.'],
    approaches: [{ name: 'Max-Heap of size k', summary: 'Push each point with its squared distance into a max-heap. When the heap grows past k, pop the farthest. The remaining k points are the answer.' }],
    starterCode: {
      javascript: 'function kClosest(points, k) {\n  // your code here\n  return [];\n}',
      python: 'def k_closest(points, k):\n    # your code here\n    return []',
      java: 'class Solution {\n    public int[][] kClosest(int[][] points, int k) {\n        return new int[0][0];\n    }\n}',
      cpp: 'vector<vector<int>> kClosest(vector<vector<int>>& points, int k) {\n    return {};\n}',
      go: 'func kClosest(points [][]int, k int) [][]int {\n    return nil\n}',
    },
    solutions: {
      javascript: 'function kClosest(points, k) {\n  const dist = p => p[0] * p[0] + p[1] * p[1];\n  const heap = [];\n  for (const p of points) {\n    heap.push([dist(p), p]);\n    heap.sort((a, b) => b[0] - a[0]);\n    if (heap.length > k) heap.shift();\n  }\n  return heap.map(x => x[1]);\n}',
      python: 'def k_closest(points, k):\n    import heapq\n    heap = []\n    for x, y in points:\n        d = x*x + y*y\n        if len(heap) < k: heapq.heappush(heap, (-d, x, y))\n        elif -heap[0][0] > d: heapq.heapreplace(heap, (-d, x, y))\n    return [[x, y] for _, x, y in heap]',
      java: 'class Solution {\n    public int[][] kClosest(int[][] points, int k) {\n        java.util.PriorityQueue<int[]> max = new java.util.PriorityQueue<>((a, b) -> Integer.compare(b[0], a[0]));\n        for (int[] p : points) {\n            int d = p[0] * p[0] + p[1] * p[1];\n            max.offer(new int[]{d, p[0], p[1]});\n            if (max.size() > k) max.poll();\n        }\n        int[][] out = new int[k][2];\n        for (int i = 0; i < k; i++) { int[] t = max.poll(); out[i][0] = t[1]; out[i][1] = t[2]; }\n        return out;\n    }\n}',
      cpp: 'vector<vector<int>> kClosest(vector<vector<int>>& points, int k) {\n    auto cmp = [](const tuple<int,int,int>& a, const tuple<int,int,int>& b){ return get<0>(a) < get<0>(b); };\n    priority_queue<tuple<int,int,int>, vector<tuple<int,int,int>>, decltype(cmp)> max(cmp);\n    for (auto& p : points) {\n        int d = p[0]*p[0] + p[1]*p[1];\n        max.push({d, p[0], p[1]});\n        if ((int)max.size() > k) max.pop();\n    }\n    vector<vector<int>> out;\n    while (!max.empty()) { auto t = max.top(); max.pop(); out.push_back({get<1>(t), get<2>(t)}); }\n    return out;\n}',
      go: 'func kClosest(points [][]int, k int) [][]int {\n    type entry struct{ d, x, y int }\n    less := func(a, b entry) bool { return a.d > b.d }\n    heap := &maxHeap{data: []entry{}, less: less}\n    heap.Init()\n    for _, p := range points {\n        d := p[0]*p[0] + p[1]*p[1]\n        heap.Push(entry{d, p[0], p[1]})\n        if heap.Len() > k { heap.Pop() }\n    }\n    out := make([][]int, k)\n    for i := 0; i < k; i++ { e := heap.Pop().(entry); out[i] = []int{e.x, e.y} }\n    return out\n}',
    },
    complexity: { time: 'O(n log k)', space: 'O(k)' },
    testCases: [
      { input: { points: [[1,3],[-2,2]],     k: 1 }, expected: [[-2,2]],       hidden: false },
      { input: { points: [[3,3],[5,-1],[-2,4]], k: 2 }, expected: [[3,3],[-2,4]], hidden: false },
      { input: { points: [[0,1],[1,0]],      k: 2 }, expected: [[0,1],[1,0]],  hidden: true },
    ],
  },
  {
    id: 'kth-largest-element-in-an-array',
    functionName: 'findKthLargest',
    number: 61,
    title: 'Kth Largest Element in an Array',
    difficulty: 'Medium',
    topic: 'heaps',
    tags: ['heap', 'array', 'divide-and-conquer', 'quickselect'],
    leetcodeUrl: 'https://leetcode.com/problems/kth-largest-element-in-an-array/',
    description: 'Given an integer array `nums` and an integer `k`, return the `k`th largest element in the array.\\n\\nNote that it is the `k`th largest element in the sorted order, not the `k`th distinct element.\\n\\nYou must solve it in O(n) time on average.',
    examples: [
      { input: 'nums = [3,2,1,5,6,4], k = 2', output: '5' },
      { input: 'nums = [3,2,3,1,2,4,5,5,6], k = 4', output: '4' },
    ],
    constraints: ['1 <= k <= nums.length <= 10^5', '-10^4 <= nums[i] <= 10^4'],
    hints: ['A min-heap of size k holds the k largest values; its root is the k-th largest.'],
    approaches: [{ name: 'Min-Heap of size k', summary: 'Push every num into a min-heap; whenever the heap exceeds k, pop the smallest. The root is the k-th largest. Alternative: quickselect (O(n) avg, O(n²) worst).' }],
    starterCode: {
      javascript: 'function findKthLargest(nums, k) {\n  // your code here\n  return 0;\n}',
      python: 'def find_kth_largest(nums, k):\n    # your code here\n    return 0',
      java: 'class Solution {\n    public int findKthLargest(int[] nums, int k) {\n        return 0;\n    }\n}',
      cpp: 'int findKthLargest(vector<int>& nums, int k) {\n    return 0;\n}',
      go: 'func findKthLargest(nums []int, k int) int {\n    return 0\n}',
    },
    solutions: {
      javascript: 'function findKthLargest(nums, k) {\n  const heap = [];\n  for (const n of nums) {\n    heap.push(n);\n    heap.sort((a, b) => a - b);\n    if (heap.length > k) heap.shift();\n  }\n  return heap[0];\n}',
      python: 'def find_kth_largest(nums, k):\n    import heapq\n    heap = []\n    for n in nums:\n        if len(heap) < k: heapq.heappush(heap, n)\n        elif n > heap[0]: heapq.heapreplace(heap, n)\n    return heap[0]',
      java: 'class Solution {\n    public int findKthLargest(int[] nums, int k) {\n        java.util.PriorityQueue<Integer> min = new java.util.PriorityQueue<>();\n        for (int n : nums) {\n            min.offer(n);\n            if (min.size() > k) min.poll();\n        }\n        return min.peek();\n    }\n}',
      cpp: 'int findKthLargest(vector<int>& nums, int k) {\n    std::priority_queue<int, vector<int>, std::greater<int>> min;\n    for (int n : nums) {\n        min.push(n);\n        if ((int)min.size() > k) min.pop();\n    }\n    return min.top();\n}',
      go: 'func findKthLargest(nums []int, k int) int {\n    h := &intHeap{}\n    heap.Init(h)\n    for _, n := range nums {\n        heap.Push(h, n)\n        if h.Len() > k { heap.Pop(h) }\n    }\n    return (*h)[0]\n}',
    },
    complexity: { time: 'O(n log k)', space: 'O(k)' },
    testCases: [
      { input: { nums: [3,2,1,5,6,4], k: 2 },          expected: 5, hidden: false },
      { input: { nums: [3,2,3,1,2,4,5,5,6], k: 4 },   expected: 4, hidden: false },
      { input: { nums: [1], k: 1 },                    expected: 1, hidden: true },
    ],
  },
  {
    id: 'task-scheduler',
    functionName: 'leastInterval',
    number: 62,
    title: 'Task Scheduler',
    difficulty: 'Medium',
    topic: 'heaps',
    tags: ['heap', 'array', 'greedy', 'sorting'],
    leetcodeUrl: 'https://leetcode.com/problems/task-scheduler/',
    description: 'You are given an array of CPU `tasks`, each labeled with a letter from A to Z, and a number `n`. Each cycle allows you to run one task or stay idle.\\n\\nAfter running a task, the **same** task cannot be run again for `n` cycles. Return the **minimum number of cycles** required to finish all tasks.',
    examples: [
      { input: 'tasks = ["A","A","A","B","B","B"], n = 2', output: '8', explanation: 'A → B → idle → A → B → idle → A → B' },
      { input: 'tasks = ["A","A","A","B","B","B"], n = 0', output: '6' },
    ],
    constraints: ['1 <= tasks.length <= 10^4', 'tasks[i] is an uppercase English letter.', '0 <= n <= 100'],
    hints: ['The bottleneck is the most frequent task. Number of cycles is max(len(tasks), (maxFreq − 1) * (n + 1) + countMax).'],
    approaches: [{ name: 'Greedy Math', summary: 'Find the maximum frequency maxFreq and the number of tasks tied with it. The answer is max(|tasks|, (maxFreq − 1) * (n + 1) + countMax).' }],
    starterCode: {
      javascript: 'function leastInterval(tasks, n) {\n  // your code here\n  return 0;\n}',
      python: 'def least_interval(tasks, n):\n    # your code here\n    return 0',
      java: 'class Solution {\n    public int leastInterval(char[] tasks, int n) {\n        return 0;\n    }\n}',
      cpp: 'int leastInterval(vector<char>& tasks, int n) {\n    return 0;\n}',
      go: 'func leastInterval(tasks []byte, n int) int {\n    return 0\n}',
    },
    solutions: {
      javascript: 'function leastInterval(tasks, n) {\n  const count = new Array(26).fill(0);\n  for (const t of tasks) count[t.charCodeAt(0) - 65]++;\n  count.sort((a, b) => b - a);\n  const maxFreq = count[0];\n  let countMax = 0;\n  for (const c of count) if (c === maxFreq) countMax++; else break;\n  return Math.max(tasks.length, (maxFreq - 1) * (n + 1) + countMax);\n}',
      python: 'def least_interval(tasks, n):\n    from collections import Counter\n    c = sorted(Counter(tasks).values(), reverse=True)\n    max_freq = c[0]\n    count_max = sum(1 for x in c if x == max_freq)\n    return max(len(tasks), (max_freq - 1) * (n + 1) + count_max)',
      java: 'class Solution {\n    public int leastInterval(char[] tasks, int n) {\n        int[] count = new int[26];\n        for (char t : tasks) count[t - "A".charAt(0)]++;\n        java.util.Arrays.sort(count);\n        int maxFreq = count[25], countMax = 0;\n        for (int i = 25; i >= 0 && count[i] == maxFreq; i--) countMax++;\n        return Math.max(tasks.length, (maxFreq - 1) * (n + 1) + countMax);\n    }\n}',
      cpp: 'int leastInterval(vector<char>& tasks, int n) {\n    vector<int> count(26, 0);\n    for (char t : tasks) count[t - "A"[0]]++;\n    sort(count.begin(), count.end());\n    int maxFreq = count[25], countMax = 0;\n    for (int i = 25; i >= 0 && count[i] == maxFreq; i--) countMax++;\n    return std::max((int)tasks.size(), (maxFreq - 1) * (n + 1) + countMax);\n}',
      go: 'func leastInterval(tasks []byte, n int) int {\n    count := [26]int{}\n    for _, t := range tasks { count[t-"A"[0]]++ }\n    sort.Ints(count[:])\n    maxFreq := count[25]\n    countMax := 0\n    for i := 25; i >= 0 && count[i] == maxFreq; i-- { countMax++ }\n    a := len(tasks)\n    b := (maxFreq - 1) * (n + 1) + countMax\n    if a > b { return a }\n    return b\n}',
    },
    complexity: { time: 'O(n)', space: 'O(1) (26-letter alphabet)' },
    testCases: [
      { input: { tasks: ['A','A','A','B','B','B'], n: 2 }, expected: 8, hidden: false },
      { input: { tasks: ['A','A','A','B','B','B'], n: 0 }, expected: 6, hidden: false },
      { input: { tasks: ['A','B','C','D','E','A','B','C'], n: 3 }, expected: 8, hidden: true },
    ],
  },
  {
    id: 'find-median-from-data-stream',
    number: 63,
    title: 'Find Median from Data Stream',
    difficulty: 'Hard',
    topic: 'heaps',
    tags: ['heap', 'design', 'two-pointers'],
    leetcodeUrl: 'https://leetcode.com/problems/find-median-from-data-stream/',
    description: 'The **median** is the middle value in an ordered integer list. If the size of the list is even, the median is the average of the two middle values.\\n\\nImplement the `MedianFinder` class:\\n\\n- `MedianFinder()` initializes the object.\\n- `void addNum(int num)` adds the integer `num` from the data stream to the data structure.\\n- `double findMedian()` returns the median of all elements so far.',
    examples: [
      { input: 'ops = ["addNum(1)","addNum(2)","findMedian()","addNum(3)","findMedian()"]', output: '[null,null,1.5,null,2.0]' },
    ],
    constraints: ['-10^5 <= num <= 10^5', 'At most 5 * 10^4 calls to addNum and findMedian.', 'There will be at least one element before calling findMedian.'],
    hints: ['Two heaps: a max-heap of the lower half and a min-heap of the upper half.'],
    approaches: [{ name: 'Two Heaps', summary: 'Keep a max-heap (lower half) and a min-heap (upper half) balanced so their sizes differ by at most 1. Median is top of the larger heap, or the average of both tops.' }],
    starterCode: {
      javascript: 'class MedianFinder {\n  constructor() {}\n  addNum(num) {}\n  findMedian() { return 0.0; }\n}',
      python: 'class MedianFinder:\n    def __init__(self):\n        pass\n    def addNum(self, num):\n        pass\n    def findMedian(self):\n        return 0.0',
      java: 'class MedianFinder {\n    public MedianFinder() {}\n    public void addNum(int num) {}\n    public double findMedian() { return 0.0; }\n}',
      cpp: 'class MedianFinder {\npublic:\n    MedianFinder() {}\n    void addNum(int num) {}\n    double findMedian() { return 0.0; }\n};',
      go: 'type MedianFinder struct{}\nfunc Constructor() *MedianFinder { return &MedianFinder{} }\nfunc (this *MedianFinder) AddNum(num int) {}\nfunc (this *MedianFinder) FindMedian() float64 { return 0.0 }',
    },
    solutions: {
      javascript: 'class MedianFinder {\n  constructor() {\n    this.low = []; this.high = []; // max-heap low, min-heap high\n  }\n  addNum(num) {\n    this.low.push(num); this.low.sort((a, b) => b - a);\n    const x = this.low.shift();\n    this.high.push(x); this.high.sort((a, b) => a - b);\n    if (this.high.length > this.low.length) {\n      const y = this.high.shift();\n      this.low.push(y); this.low.sort((a, b) => b - a);\n    }\n  }\n  findMedian() {\n    if (this.low.length > this.high.length) return this.low[0];\n    return (this.low[0] + this.high[0]) / 2;\n  }\n}',
      python: 'class MedianFinder:\n    def __init__(self):\n        import heapq\n        self.low, self.high = [], []  # max-heap, min-heap\n    def addNum(self, num):\n        import heapq\n        heapq.heappush(self.low, -num)\n        heapq.heappush(self.high, -heapq.heappop(self.low))\n        if len(self.high) > len(self.low):\n            heapq.heappush(self.low, -heapq.heappop(self.high))\n    def findMedian(self):\n        if len(self.low) > len(self.high): return -self.low[0]\n        return (-self.low[0] + self.high[0]) / 2.0',
      java: 'class MedianFinder {\n    private final java.util.PriorityQueue<Integer> low = new java.util.PriorityQueue<>((a, b) -> b - a);\n    private final java.util.PriorityQueue<Integer> high = new java.util.PriorityQueue<>();\n    public void addNum(int num) {\n        low.offer(num); high.offer(low.poll());\n        if (high.size() > low.size()) low.offer(high.poll());\n    }\n    public double findMedian() {\n        if (low.size() > high.size()) return low.peek();\n        return (low.peek() + high.peek()) / 2.0;\n    }\n}',
      cpp: 'class MedianFinder {\npublic:\n    void addNum(int num) {\n        maxHeap.push(num); minHeap.push(maxHeap.top()); maxHeap.pop();\n        if (minHeap.size() > maxHeap.size()) { maxHeap.push(minHeap.top()); minHeap.pop(); }\n    }\n    double findMedian() {\n        if (maxHeap.size() > minHeap.size()) return maxHeap.top();\n        return (maxHeap.top() + minHeap.top()) / 2.0;\n    }\nprivate:\n    std::priority_queue<int> maxHeap;\n    std::priority_queue<int, vector<int>, std::greater<int>> minHeap;\n};',
      go: 'type MedianFinder struct {\n    low []int  // max-heap (negate for Go\'s min-heap)\n    high []int // min-heap\n}\nfunc Constructor() *MedianFinder { return &MedianFinder{} }\nfunc (this *MedianFinder) AddNum(num int) {\n    heap.Push(intHeap(&this.low), -num)\n    heap.Push(intHeap(&this.high), -heap.Pop(intHeap(&this.low)).(int))\n    if len(this.high) > len(this.low) {\n        heap.Push(intHeap(&this.low), -heap.Pop(intHeap(&this.high)).(int))\n    }\n}\nfunc (this *MedianFinder) FindMedian() float64 {\n    if len(this.low) > len(this.high) { return float64(-this.low[0]) }\n    return float64(-this.low[0]+this.high[0]) / 2.0\n}',
    },
    complexity: { time: 'O(log n) per addNum, O(1) per findMedian', space: 'O(n)' },
    testCases: [
      { input: { op: 'findMedian', after: [1,2] },   expected: 1.5, hidden: false },
      { input: { op: 'findMedian', after: [1,2,3] }, expected: 2.0, hidden: false },
      { input: { op: 'findMedian', after: [7] },     expected: 7.0, hidden: true },
    ],
  },
];
