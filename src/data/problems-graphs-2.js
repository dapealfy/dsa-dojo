// graphs topic - second batch
export const GRAPHS_PROBLEMS_2 = [
  {
    id: 'course-schedule-ii',
    functionName: 'findOrder',
    number: 105,
    title: 'Course Schedule II',
    difficulty: 'Medium',
    topic: 'graphs',
    tags: ['bfs', 'dfs', 'topological-sort'],
    leetcodeUrl: 'https://leetcode.com/problems/course-schedule-ii/',
    description: 'There are a total of `numCourses` courses labeled from `0` to `numCourses - 1`. You are given an array `prerequisites` where `prerequisites[i] = [ai, bi]` means you must take course `bi` first if you want to take course `ai`.\n\nReturn **any** ordering of courses you can take to finish all courses. If it is impossible, return an empty array.',
    examples: [
      { input: 'numCourses = 2, prerequisites = [[1,0]]', output: '[0,1]' },
      { input: 'numCourses = 4, prerequisites = [[1,0],[2,0],[3,1],[3,2]]', output: '[0,2,1,3]' },
    ],
    constraints: ['1 <= numCourses <= 2000', '0 <= prerequisites.length <= 5000'],
    hints: ['Kahn\'s BFS topological sort: start with indegree-0 nodes, peel them off, decrement neighbors.'],
    approaches: [{ name: 'BFS topological sort', summary: 'Compute indegrees, push all 0-indegree nodes into a queue. Pop, append to order, decrement neighbors. If order length < numCourses, cycle → return [].' }],
    starterCode: {
      javascript: 'function findOrder(numCourses, prerequisites) {\n  // your code here\n  return [];\n}',
      python: 'def find_order(num_courses, prerequisites):\n    # your code here\n    return []',
      java: 'class Solution {\n    public int[] findOrder(int numCourses, int[][] prerequisites) {\n        return new int[0];\n    }\n}',
      cpp: 'vector<int> findOrder(int numCourses, vector<vector<int>>& prerequisites) {\n    return {};\n}',
      go: 'func findOrder(numCourses int, prerequisites [][]int) []int {\n    return nil\n}',
    },
    solutions: {
      javascript: 'function findOrder(n, prereq) {\n  const adj = Array.from({length: n}, () => []);\n  const indeg = new Array(n).fill(0);\n  for (const [a, b] of prereq) { adj[b].push(a); indeg[a]++; }\n  const q = []; for (let i = 0; i < n; i++) if (indeg[i] === 0) q.push(i);\n  const order = [];\n  while (q.length) {\n    const u = q.shift(); order.push(u);\n    for (const v of adj[u]) if (--indeg[v] === 0) q.push(v);\n  }\n  return order.length === n ? order : [];\n}',
      python: 'def find_order(num_courses, prerequisites):\n    from collections import deque\n    adj = [[] for _ in range(num_courses)]\n    indeg = [0] * num_courses\n    for a, b in prerequisites: adj[b].append(a); indeg[a] += 1\n    q = deque(i for i in range(num_courses) if indeg[i] == 0)\n    order = []\n    while q:\n        u = q.popleft(); order.append(u)\n        for v in adj[u]:\n            indeg[v] -= 1\n            if indeg[v] == 0: q.append(v)\n    return order if len(order) == num_courses else []',
      java: 'class Solution {\n    public int[] findOrder(int n, int[][] prereq) {\n        java.util.List<Integer>[] adj = new java.util.ArrayList[n];\n        for (int i = 0; i < n; i++) adj[i] = new java.util.ArrayList<>();\n        int[] indeg = new int[n];\n        for (int[] e : prereq) { adj[e[1]].add(e[0]); indeg[e[0]]++; }\n        java.util.Queue<Integer> q = new java.util.LinkedList<>();\n        for (int i = 0; i < n; i++) if (indeg[i] == 0) q.offer(i);\n        int[] order = new int[n]; int idx = 0;\n        while (!q.isEmpty()) {\n            int u = q.poll(); order[idx++] = u;\n            for (int v : adj[u]) if (--indeg[v] == 0) q.offer(v);\n        }\n        return idx == n ? order : new int[0];\n    }\n}',
      cpp: 'vector<int> findOrder(int n, vector<vector<int>>& prereq) {\n    vector<vector<int>> adj(n);\n    vector<int> indeg(n, 0);\n    for (auto& e : prereq) { adj[e[1]].push_back(e[0]); indeg[e[0]]++; }\n    queue<int> q; for (int i = 0; i < n; i++) if (indeg[i] == 0) q.push(i);\n    vector<int> order; while (!q.empty()) {\n        int u = q.front(); q.pop(); order.push_back(u);\n        for (int v : adj[u]) if (--indeg[v] == 0) q.push(v);\n    }\n    return (int)order.size() == n ? order : vector<int>{};\n}',
      go: 'func findOrder(numCourses int, prerequisites [][]int) []int {\n    adj := make([][]int, numCourses); indeg := make([]int, numCourses)\n    for _, e := range prerequisites { adj[e[1]] = append(adj[e[1]], e[0]); indeg[e[0]]++ }\n    q := []int{}\n    for i := 0; i < numCourses; i++ { if indeg[i] == 0 { q = append(q, i) } }\n    order := []int{}\n    for len(q) > 0 {\n        u := q[0]; q = q[1:]; order = append(order, u)\n        for _, v := range adj[u] { indeg[v]--; if indeg[v] == 0 { q = append(q, v) } }\n    }\n    if len(order) == numCourses { return order }\n    return []int{}\n}',
    },
    complexity: { time: 'O(V + E)', space: 'O(V + E)' },
    testCases: [
      { input: { numCourses: 2, prerequisites: [[1,0]] }, expected: [0,1], hidden: false },
      { input: { numCourses: 4, prerequisites: [[1,0],[2,0],[3,1],[3,2]] }, expected: [0,2,1,3], hidden: false },
      { input: { numCourses: 2, prerequisites: [[1,0],[0,1]] }, expected: [], hidden: true },
    ],
  },
  {
    id: 'redundant-connection',
    functionName: 'findRedundantConnection',
    number: 106,
    title: 'Redundant Connection',
    difficulty: 'Medium',
    topic: 'graphs',
    tags: ['union-find', 'dfs', 'bfs'],
    leetcodeUrl: 'https://leetcode.com/problems/redundant-connection/',
    description: 'In a forest (tree) problem, you are given a graph that started as a tree with `n` nodes labeled `1` to `n`, with one extra edge added. The added edge has two different vertices chosen from `1` to `n`, and was not an edge that already existed.\n\nThe graph is given as an array `edges` of length `n`. Return an edge that can be removed to make the resulting graph a tree of `n` nodes.',
    examples: [
      { input: 'edges = [[1,2],[1,3],[2,3]]', output: '[2,3]' },
      { input: 'edges = [[1,2],[2,3],[3,4],[1,4],[1,5]]', output: '[1,4]' },
    ],
    constraints: ['3 <= n <= 1000', 'edges[i].length == 2', 'No self-loops or parallel edges.'],
    hints: ['Union-Find: the first edge whose two endpoints are already connected is the redundant one.'],
    approaches: [{ name: 'Union-Find', summary: 'Process edges in order. If union(u, v) finds u and v already in the same component, that edge is the answer.' }],
    starterCode: {
      javascript: 'function findRedundantConnection(edges) {\n  // your code here\n  return [];\n}',
      python: 'def find_redundant_connection(edges):\n    # your code here\n    return []',
      java: 'class Solution {\n    public int[] findRedundantConnection(int[][] edges) {\n        return new int[0];\n    }\n}',
      cpp: 'vector<int> findRedundantConnection(vector<vector<int>>& edges) {\n    return {};\n}',
      go: 'func findRedundantConnection(edges [][]int) []int {\n    return nil\n}',
    },
    solutions: {
      javascript: 'function findRedundantConnection(edges) {\n  const parent = Array.from({length: edges.length + 1}, (_, i) => i);\n  function find(x) { while (parent[x] !== x) { parent[x] = parent[parent[x]]; x = parent[x]; } return x; }\n  for (const [u, v] of edges) {\n    const pu = find(u), pv = find(v);\n    if (pu === pv) return [u, v];\n    parent[pu] = pv;\n  }\n  return [];\n}',
      python: 'def find_redundant_connection(edges):\n    parent = list(range(len(edges) + 1))\n    def find(x):\n        while parent[x] != x: parent[x] = parent[parent[x]]; x = parent[x]\n        return x\n    for u, v in edges:\n        pu, pv = find(u), find(v)\n        if pu == pv: return [u, v]\n        parent[pu] = pv\n    return []',
      java: 'class Solution {\n    int[] parent;\n    int find(int x) { while (parent[x] != x) { parent[x] = parent[parent[x]]; x = parent[x]; } return x; }\n    public int[] findRedundantConnection(int[][] edges) {\n        parent = new int[edges.length + 1];\n        for (int i = 0; i < parent.length; i++) parent[i] = i;\n        for (int[] e : edges) {\n            int pu = find(e[0]), pv = find(e[1]);\n            if (pu == pv) return e;\n            parent[pu] = pv;\n        }\n        return new int[0];\n    }\n}',
      cpp: 'vector<int> parent;\nint find(int x) { while (parent[x] != x) { parent[x] = parent[parent[x]]; x = parent[x]; } return x; }\nvector<int> findRedundantConnection(vector<vector<int>>& edges) {\n    int n = edges.size(); parent.assign(n + 1, 0);\n    for (int i = 1; i <= n; i++) parent[i] = i;\n    for (auto& e : edges) {\n        int pu = find(e[0]), pv = find(e[1]);\n        if (pu == pv) return e;\n        parent[pu] = pv;\n    }\n    return {};\n}',
      go: 'func findRedundantConnection(edges [][]int) []int {\n    parent := make([]int, len(edges)+1)\n    for i := range parent { parent[i] = i }\n    var find func(int) int\n    find = func(x int) int { for parent[x] != x { parent[x] = parent[parent[x]]; x = parent[x] }; return x }\n    for _, e := range edges {\n        pu, pv := find(e[0]), find(e[1])\n        if pu == pv { return e }\n        parent[pu] = pv\n    }\n    return nil\n}',
    },
    complexity: { time: 'O(n α(n))', space: 'O(n)' },
    testCases: [
      { input: { edges: [[1,2],[1,3],[2,3]] }, expected: [2,3], hidden: false },
      { input: { edges: [[1,2],[2,3],[3,4],[1,4],[1,5]] }, expected: [1,4], hidden: false },
      { input: { edges: [[1,2],[2,1]] }, expected: [2,1], hidden: true },
    ],
  },
  {
    id: 'word-ladder',
    functionName: 'ladderLength',
    number: 107,
    title: 'Word Ladder',
    difficulty: 'Hard',
    topic: 'graphs',
    tags: ['bfs', 'hash-map'],
    leetcodeUrl: 'https://leetcode.com/problems/word-ladder/',
    description: 'Given two words `beginWord` and `endWord`, and a dictionary `wordList`, return the **length** of the shortest transformation sequence from `beginWord` to `endWord`.\n\nRules:\n- Only one letter can be changed at a time.\n- Each transformed word must exist in the wordList.\n- `beginWord` does not need to be in the wordList.\n- Return 0 if no such sequence exists.',
    examples: [
      { input: 'beginWord = "hit", endWord = "cog", wordList = ["hot","dot","dog","lot","log","cog"]', output: '5', explanation: 'hit → hot → dot → dog → cog' },
      { input: 'beginWord = "hit", endWord = "cog", wordList = ["hot","dot","dog","lot","log"]', output: '0' },
    ],
    constraints: ['1 <= beginWord.length == endWord.length <= 10', '1 <= wordList.length <= 5000'],
    hints: ['BFS over word space. For each word, enumerate all 1-letter variants and check the set.'],
    approaches: [{ name: 'BFS with intermediate states', summary: 'Put wordList in a set. BFS from beginWord; for each word, try every * wildcard variant; if the matched word is the end, return depth+1.' }],
    starterCode: {
      javascript: 'function ladderLength(beginWord, endWord, wordList) {\n  // your code here\n  return 0;\n}',
      python: 'def ladder_length(begin_word, end_word, word_list):\n    # your code here\n    return 0',
      java: 'class Solution {\n    public int ladderLength(String beginWord, String endWord, List<String> wordList) {\n        return 0;\n    }\n}',
      cpp: 'int ladderLength(string beginWord, string endWord, vector<string>& wordList) {\n    return 0;\n}',
      go: 'func ladderLength(beginWord string, endWord string, wordList []string) int {\n    return 0\n}',
    },
    solutions: {
      javascript: 'function ladderLength(begin, end, list) {\n  const set = new Set(list);\n  if (!set.has(end)) return 0;\n  const q = [[begin, 1]];\n  while (q.length) {\n    const [w, d] = q.shift();\n    if (w === end) return d;\n    for (let i = 0; i < w.length; i++) {\n      for (let c = 97; c <= 122; c++) {\n        const nw = w.slice(0, i) + String.fromCharCode(c) + w.slice(i+1);\n        if (set.has(nw)) { set.delete(nw); q.push([nw, d+1]); }\n      }\n    }\n  }\n  return 0;\n}',
      python: 'def ladder_length(begin_word, end_word, word_list):\n    s = set(word_list)\n    if end_word not in s: return 0\n    from collections import deque\n    q = deque([(begin_word, 1)])\n    while q:\n        w, d = q.popleft()\n        if w == end_word: return d\n        for i in range(len(w)):\n            for c in \"abcdefghijklmnopqrstuvwxyz\":\n                nw = w[:i] + c + w[i+1:]\n                if nw in s:\n                    s.remove(nw); q.append((nw, d+1))\n    return 0',
      java: 'class Solution {\n    public int ladderLength(String begin, String end, List<String> list) {\n        java.util.Set<String> set = new java.util.HashSet<>(list);\n        if (!set.contains(end)) return 0;\n        java.util.Queue<String[]> q = new java.util.LinkedList<>();\n        q.offer(new String[]{begin, "1"});\n        while (!q.isEmpty()) {\n            String[] cur = q.poll();\n            String w = cur[0]; int d = Integer.parseInt(cur[1]);\n            if (w.equals(end)) return d;\n            char[] arr = w.toCharArray();\n            for (int i = 0; i < arr.length; i++) {\n                char orig = arr[i];\n                for (char c = \'a\'; c <= \'z\'; c++) {\n                    arr[i] = c;\n                    String nw = new String(arr);\n                    if (set.contains(nw)) { set.remove(nw); q.offer(new String[]{nw, String.valueOf(d+1)}); }\n                }\n                arr[i] = orig;\n            }\n        }\n        return 0;\n    }\n}',
      cpp: 'int ladderLength(string begin, string end, vector<string>& list) {\n    unordered_set<string> s(list.begin(), list.end());\n    if (!s.count(end)) return 0;\n    queue<pair<string,int>> q; q.push({begin, 1});\n    while (!q.empty()) {\n        auto [w, d] = q.front(); q.pop();\n        if (w == end) return d;\n        for (int i = 0; i < (int)w.size(); i++) {\n            char orig = w[i];\n            for (char c = \'a\'; c <= \'z\'; c++) {\n                w[i] = c;\n                if (s.count(w)) { s.erase(w); q.push({w, d+1}); }\n            }\n            w[i] = orig;\n        }\n    }\n    return 0;\n}',
      go: 'func ladderLength(beginWord string, endWord string, wordList []string) int {\n    s := make(map[string]bool)\n    for _, w := range wordList { s[w] = true }\n    if !s[endWord] { return 0 }\n    type pair struct { w string; d int }\n    q := []pair{{beginWord, 1}}\n    for len(q) > 0 {\n        p := q[0]; q = q[1:]\n        if p.w == endWord { return p.d }\n        bs := []byte(p.w)\n        for i := 0; i < len(bs); i++ {\n            orig := bs[i]\n            for c := byte(\'a\'); c <= \'z\'; c++ {\n                bs[i] = c; nw := string(bs)\n                if s[nw] { delete(s, nw); q = append(q, pair{nw, p.d+1}) }\n            }\n            bs[i] = orig\n        }\n    }\n    return 0\n}',
    },
    complexity: { time: 'O(L * 26 * N)', space: 'O(N)' },
    testCases: [
      { input: { begin: 'hit', end: 'cog', list: ['hot','dot','dog','lot','log','cog'] }, expected: 5, hidden: false },
      { input: { begin: 'hit', end: 'cog', list: ['hot','dot','dog','lot','log'] }, expected: 0, hidden: false },
      { input: { begin: 'a', end: 'c', list: ['a','b','c'] }, expected: 2, hidden: true },
    ],
  },
  {
    id: 'network-delay-time',
    functionName: 'networkDelayTime',
    number: 108,
    title: 'Network Delay Time',
    difficulty: 'Medium',
    topic: 'graphs',
    tags: ['dijkstra', 'bfs', 'heap'],
    leetcodeUrl: 'https://leetcode.com/problems/network-delay-time/',
    description: 'You are given a network of `n` nodes, labeled `1` to `n`. You are also given `times`, a list of travel times as directed edges `times[i] = (ui, vi, wi)`, where `ui` is the source, `vi` is the target, and `wi` is the time it takes for a signal to travel from source to target.\n\nWe send a signal from node `k`. Return the **minimum time** it takes for all `n` nodes to receive the signal. If it is impossible, return `-1`.',
    examples: [
      { input: 'times = [[2,1,1],[2,3,1],[3,4,1]], n = 4, k = 2', output: '2' },
      { input: 'times = [[1,2,1]], n = 2, k = 1', output: '1' },
    ],
    constraints: ['1 <= n <= 100', '1 <= times.length <= 6000', 'times[i].length == 3', '1 <= ui, vi <= n', 'No multi-edges or self-loops.'],
    hints: ['Dijkstra from k; the max distance to any node is the answer.'],
    approaches: [{ name: 'Dijkstra', summary: 'Run Dijkstra from k. The answer is the max of the shortest distances to every node; if any node is unreachable, return -1.' }],
    starterCode: {
      javascript: 'function networkDelayTime(times, n, k) {\n  // your code here\n  return 0;\n}',
      python: 'def network_delay_time(times, n, k):\n    # your code here\n    return 0',
      java: 'class Solution {\n    public int networkDelayTime(int[][] times, int n, int k) {\n        return 0;\n    }\n}',
      cpp: 'int networkDelayTime(vector<vector<int>>& times, int n, int k) {\n    return 0;\n}',
      go: 'func networkDelayTime(times [][]int, n int, k int) int {\n    return 0\n}',
    },
    solutions: {
      javascript: 'function networkDelayTime(times, n, k) {\n  const adj = Array.from({length: n + 1}, () => []);\n  for (const [u, v, w] of times) adj[u].push([v, w]);\n  const dist = new Array(n + 1).fill(Infinity);\n  dist[k] = 0;\n  const heap = new MinPriorityQueue({ priority: x => x[0] });\n  heap.enqueue([0, k]);\n  while (!heap.isEmpty()) {\n    const [d, u] = heap.dequeue().element;\n    if (d > dist[u]) continue;\n    for (const [v, w] of adj[u]) {\n      if (dist[u] + w < dist[v]) {\n        dist[v] = dist[u] + w;\n        heap.enqueue([dist[v], v]);\n      }\n    }\n  }\n  let ans = 0;\n  for (let i = 1; i <= n; i++) { if (dist[i] === Infinity) return -1; ans = Math.max(ans, dist[i]); }\n  return ans;\n}',
      python: 'def network_delay_time(times, n, k):\n    import heapq\n    adj = {}\n    for u, v, w in times:\n        adj.setdefault(u, []).append((v, w))\n    dist = {i: float(\"inf\") for i in range(1, n + 1)}\n    dist[k] = 0\n    heap = [(0, k)]\n    while heap:\n        d, u = heapq.heappop(heap)\n        if d > dist[u]: continue\n        for v, w in adj.get(u, []):\n            if d + w < dist[v]:\n                dist[v] = d + w; heapq.heappush(heap, (dist[v], v))\n    ans = max(dist.values())\n    return ans if ans < float(\"inf\") else -1',
      java: 'class Solution {\n    public int networkDelayTime(int[][] times, int n, int k) {\n        java.util.List<int[]>[] adj = new java.util.ArrayList[n+1];\n        for (int i = 0; i <= n; i++) adj[i] = new java.util.ArrayList<>();\n        for (int[] t : times) adj[t[0]].add(new int[]{t[1], t[2]});\n        int[] dist = new int[n+1]; java.util.Arrays.fill(dist, Integer.MAX_VALUE); dist[k] = 0;\n        java.util.PriorityQueue<int[]> pq = new java.util.PriorityQueue<>((a,b) -> a[0]-b[0]);\n        pq.offer(new int[]{0, k});\n        while (!pq.isEmpty()) {\n            int[] cur = pq.poll(); int d = cur[0], u = cur[1];\n            if (d > dist[u]) continue;\n            for (int[] e : adj[u]) { int v = e[0], w = e[1];\n                if (d + w < dist[v]) { dist[v] = d + w; pq.offer(new int[]{dist[v], v}); }\n            }\n        }\n        int ans = 0;\n        for (int i = 1; i <= n; i++) { if (dist[i] == Integer.MAX_VALUE) return -1; ans = Math.max(ans, dist[i]); }\n        return ans;\n    }\n}',
      cpp: 'int networkDelayTime(vector<vector<int>>& times, int n, int k) {\n    vector<vector<pair<int,int>>> adj(n+1);\n    for (auto& t : times) adj[t[0]].push_back({t[1], t[2]});\n    vector<int> dist(n+1, INT_MAX); dist[k] = 0;\n    priority_queue<pair<int,int>, vector<pair<int,int>>, greater<>> pq;\n    pq.push({0, k});\n    while (!pq.empty()) {\n        auto [d, u] = pq.top(); pq.pop();\n        if (d > dist[u]) continue;\n        for (auto [v, w] : adj[u])\n            if (d + w < dist[v]) { dist[v] = d + w; pq.push({dist[v], v}); }\n    }\n    int ans = 0;\n    for (int i = 1; i <= n; i++) { if (dist[i] == INT_MAX) return -1; ans = max(ans, dist[i]); }\n    return ans;\n}',
      go: 'func networkDelayTime(times [][]int, n int, k int) int {\n    adj := make([][][2]int, n+1)\n    for _, t := range times { adj[t[0]] = append(adj[t[0]], [2]int{t[1], t[2]}) }\n    dist := make([]int, n+1)\n    for i := range dist { dist[i] = math.MaxInt32 }\n    dist[k] = 0\n    pq := &pq2{}\n    heap.Init(pq); heap.Push(pq, [2]int{0, k})\n    for pq.Len() > 0 {\n        cur := heap.Pop(pq).([2]int); d, u := cur[0], cur[1]\n        if d > dist[u] { continue }\n        for _, e := range adj[u] { v, w := e[0], e[1]\n            if d+w < dist[v] { dist[v] = d+w; heap.Push(pq, [2]int{dist[v], v}) }\n        }\n    }\n    ans := 0\n    for i := 1; i <= n; i++ {\n        if dist[i] == math.MaxInt32 { return -1 }\n        if dist[i] > ans { ans = dist[i] }\n    }\n    return ans\n}\ntype pq2 [][2]int\nfunc (p pq2) Len() int { return len(p) }\nfunc (p pq2) Less(i, j int) bool { return p[i][0] < p[j][0] }\nfunc (p *pq2) Push(x any) { *p = append(*p, x.([2]int)) }\nfunc (p *pq2) Pop() any { old := *p; n := len(old); x := old[n-1]; *p = old[:n-1]; return x }',
    },
    complexity: { time: 'O(E log V)', space: 'O(V + E)' },
    testCases: [
      { input: { times: [[2,1,1],[2,3,1],[3,4,1]], n: 4, k: 2 }, expected: 2, hidden: false },
      { input: { times: [[1,2,1]], n: 2, k: 1 }, expected: 1, hidden: false },
      { input: { times: [[1,2,1]], n: 2, k: 2 }, expected: -1, hidden: true },
    ],
  },
  {
    id: 'cheapest-flights-within-k-stops',
    functionName: 'findCheapestPrice',
    number: 109,
    title: 'Cheapest Flights Within K Stops',
    difficulty: 'Medium',
    topic: 'graphs',
    tags: ['bfs', 'dijkstra', 'dynamic-programming'],
    leetcodeUrl: 'https://leetcode.com/problems/cheapest-flights-within-k-stops/',
    description: 'There are `n` cities connected by flights. Each flight `[u, v, w]` goes from city `u` to city `v` with cost `w`. Find the cheapest price from `src` to `dst` with **at most `k` stops**. If there is no such route, return `-1`.',
    examples: [
      { input: 'n = 4, flights = [[0,1,100],[1,2,100],[2,3,100],[0,2,500]], src = 0, dst = 3, k = 1', output: '500' },
      { input: 'n = 3, flights = [[0,1,100],[1,2,100],[0,2,500]], src = 0, dst = 2, k = 0', output: '500' },
    ],
    constraints: ['1 <= n <= 100', '0 <= flights.length <= n*(n-1)/2', '0 <= src, dst < n', '0 <= k < n'],
    hints: ['Modified Dijkstra / BFS over (cost, node, stops). Only relax if the new path uses ≤ k stops.'],
    approaches: [{ name: 'Bellman-Ford K iterations', summary: 'Run Bellman-Ford for k+1 rounds. dist[v] = min(dist[v], dist[u]+w). Answer is dist[dst].' }],
    starterCode: {
      javascript: 'function findCheapestPrice(n, flights, src, dst, k) {\n  // your code here\n  return 0;\n}',
      python: 'def find_cheapest_price(n, flights, src, dst, k):\n    # your code here\n    return 0',
      java: 'class Solution {\n    public int findCheapestPrice(int n, int[][] flights, int src, int dst, int k) {\n        return 0;\n    }\n}',
      cpp: 'int findCheapestPrice(int n, vector<vector<int>>& flights, int src, int dst, int k) {\n    return 0;\n}',
      go: 'func findCheapestPrice(n int, flights [][]int, src int, dst int, k int) int {\n    return 0\n}',
    },
    solutions: {
      javascript: 'function findCheapestPrice(n, flights, src, dst, k) {\n  let dist = new Array(n).fill(Infinity);\n  dist[src] = 0;\n  for (let i = 0; i <= k; i++) {\n    const next = dist.slice();\n    for (const [u, v, w] of flights) {\n      if (dist[u] !== Infinity && dist[u] + w < next[v]) next[v] = dist[u] + w;\n    }\n    dist = next;\n  }\n  return dist[dst] === Infinity ? -1 : dist[dst];\n}',
      python: 'def find_cheapest_price(n, flights, src, dst, k):\n    INF = float(\"inf\")\n    dist = [INF] * n; dist[src] = 0\n    for _ in range(k + 1):\n        nxt = dist[:]\n        for u, v, w in flights:\n            if dist[u] != INF and dist[u] + w < nxt[v]:\n                nxt[v] = dist[u] + w\n        dist = nxt\n    return -1 if dist[dst] == INF else dist[dst]',
      java: 'class Solution {\n    public int findCheapestPrice(int n, int[][] flights, int src, int dst, int k) {\n        int[] dist = new int[n]; java.util.Arrays.fill(dist, Integer.MAX_VALUE);\n        dist[src] = 0;\n        for (int i = 0; i <= k; i++) {\n            int[] nxt = dist.clone();\n            for (int[] f : flights) {\n                int u = f[0], v = f[1], w = f[2];\n                if (dist[u] != Integer.MAX_VALUE && dist[u] + w < nxt[v]) nxt[v] = dist[u] + w;\n            }\n            dist = nxt;\n        }\n        return dist[dst] == Integer.MAX_VALUE ? -1 : dist[dst];\n    }\n}',
      cpp: 'int findCheapestPrice(int n, vector<vector<int>>& flights, int src, int dst, int k) {\n    vector<int> dist(n, INT_MAX); dist[src] = 0;\n    for (int i = 0; i <= k; i++) {\n        vector<int> nxt = dist;\n        for (auto& f : flights) {\n            int u = f[0], v = f[1], w = f[2];\n            if (dist[u] != INT_MAX && dist[u] + w < nxt[v]) nxt[v] = dist[u] + w;\n        }\n        dist = nxt;\n    }\n    return dist[dst] == INT_MAX ? -1 : dist[dst];\n}',
      go: 'func findCheapestPrice(n int, flights [][]int, src int, dst int, k int) int {\n    dist := make([]int, n)\n    for i := range dist { dist[i] = math.MaxInt32 }\n    dist[src] = 0\n    for i := 0; i <= k; i++ {\n        nxt := make([]int, n); copy(nxt, dist)\n        for _, f := range flights {\n            u, v, w := f[0], f[1], f[2]\n            if dist[u] != math.MaxInt32 && dist[u]+w < nxt[v] { nxt[v] = dist[u] + w }\n        }\n        dist = nxt\n    }\n    if dist[dst] == math.MaxInt32 { return -1 }\n    return dist[dst]\n}',
    },
    complexity: { time: 'O((k+1) * E)', space: 'O(V)' },
    testCases: [
      { input: { n: 4, flights: [[0,1,100],[1,2,100],[2,3,100],[0,2,500]], src: 0, dst: 3, k: 1 }, expected: 500, hidden: false },
      { input: { n: 3, flights: [[0,1,100],[1,2,100],[0,2,500]], src: 0, dst: 2, k: 0 }, expected: 500, hidden: false },
      { input: { n: 4, flights: [[0,1,1],[0,2,5],[1,2,1],[2,3,1]], src: 0, dst: 3, k: 1 }, expected: 6, hidden: true },
    ],
  },
  {
    id: 'graph-valid-tree',
    functionName: 'validTree',
    number: 110,
    title: 'Graph Valid Tree',
    difficulty: 'Medium',
    topic: 'graphs',
    tags: ['dfs', 'bfs', 'union-find'],
    leetcodeUrl: 'https://leetcode.com/problems/graph-valid-tree/',
    description: 'Given `n` nodes labeled `0` to `n-1` and a list of undirected edges, determine if these edges form a valid tree.\n\nA graph is a valid tree if it is **connected** AND has **no cycles**, equivalently: it has exactly `n - 1` edges and is connected.',
    examples: [
      { input: 'n = 5, edges = [[0,1],[0,2],[0,3],[1,4]]', output: 'true' },
      { input: 'n = 5, edges = [[0,1],[1,2],[2,3],[1,3],[1,4]]', output: 'false' },
    ],
    constraints: ['1 <= n <= 2000', '0 <= edges.length <= n*1999/2', 'No duplicate edges or self-loops.'],
    hints: ['A valid tree has exactly n-1 edges AND is connected.'],
    approaches: [{ name: 'Union-Find', summary: 'If edges.length !== n-1 return false. Union each edge; if any union finds two already-connected nodes, there is a cycle → false. Else return true.' }],
    starterCode: {
      javascript: 'function validTree(n, edges) {\n  // your code here\n  return false;\n}',
      python: 'def valid_tree(n, edges):\n    # your code here\n    return False',
      java: 'class Solution {\n    public boolean validTree(int n, int[][] edges) {\n        return false;\n    }\n}',
      cpp: 'bool validTree(int n, vector<vector<int>>& edges) {\n    return false;\n}',
      go: 'func validTree(n int, edges [][]int) bool {\n    return false\n}',
    },
    solutions: {
      javascript: 'function validTree(n, edges) {\n  if (edges.length !== n - 1) return false;\n  const parent = Array.from({length: n}, (_, i) => i);\n  function find(x) { while (parent[x] !== x) { parent[x] = parent[parent[x]]; x = parent[x]; } return x; }\n  for (const [u, v] of edges) {\n    const pu = find(u), pv = find(v);\n    if (pu === pv) return false;\n    parent[pu] = pv;\n  }\n  return true;\n}',
      python: 'def valid_tree(n, edges):\n    if len(edges) != n - 1: return False\n    parent = list(range(n))\n    def find(x):\n        while parent[x] != x: parent[x] = parent[parent[x]]; x = parent[x]\n        return x\n    for u, v in edges:\n        pu, pv = find(u), find(v)\n        if pu == pv: return False\n        parent[pu] = pv\n    return True',
      java: 'class Solution {\n    int[] parent;\n    int find(int x) { while (parent[x] != x) { parent[x] = parent[parent[x]]; x = parent[x]; } return x; }\n    public boolean validTree(int n, int[][] edges) {\n        if (edges.length != n - 1) return false;\n        parent = new int[n]; for (int i = 0; i < n; i++) parent[i] = i;\n        for (int[] e : edges) {\n            int pu = find(e[0]), pv = find(e[1]);\n            if (pu == pv) return false;\n            parent[pu] = pv;\n        }\n        return true;\n    }\n}',
      cpp: 'vector<int> parent;\nint find(int x) { while (parent[x] != x) { parent[x] = parent[parent[x]]; x = parent[x]; } return x; }\nbool validTree(int n, vector<vector<int>>& edges) {\n    if ((int)edges.size() != n - 1) return false;\n    parent.resize(n); for (int i = 0; i < n; i++) parent[i] = i;\n    for (auto& e : edges) {\n        int pu = find(e[0]), pv = find(e[1]);\n        if (pu == pv) return false;\n        parent[pu] = pv;\n    }\n    return true;\n}',
      go: 'func validTree(n int, edges [][]int) bool {\n    if len(edges) != n-1 { return false }\n    parent := make([]int, n)\n    for i := range parent { parent[i] = i }\n    var find func(int) int\n    find = func(x int) int { for parent[x] != x { parent[x] = parent[parent[x]]; x = parent[x] }; return x }\n    for _, e := range edges {\n        pu, pv := find(e[0]), find(e[1])\n        if pu == pv { return false }\n        parent[pu] = pv\n    }\n    return true\n}',
    },
    complexity: { time: 'O(n α(n))', space: 'O(n)' },
    testCases: [
      { input: { n: 5, edges: [[0,1],[0,2],[0,3],[1,4]] }, expected: true, hidden: false },
      { input: { n: 5, edges: [[0,1],[1,2],[2,3],[1,3],[1,4]] }, expected: false, hidden: false },
      { input: { n: 1, edges: [] }, expected: true, hidden: true },
    ],
  },
]
