// trees topic (continued)
export const TREES_PROBLEMS = [
  {
    id: 'invert-binary-tree',
    functionName: 'invertTree',
    number: 4,
    title: 'Invert Binary Tree',
    difficulty: 'Easy',
    topic: 'trees',
    tags: ['binary-tree', 'dfs', 'bfs'],
    leetcodeUrl: 'https://leetcode.com/problems/invert-binary-tree/',
    description: 'Given the `root` of a binary tree, invert the tree, and return its root.\n\n**Inverting** means swapping the left and right children of every node.',
    examples: [
      { input: 'root = [4,2,7,1,3,6,9]', output: '[4,7,2,9,6,3,1]' },
      { input: 'root = [2,1,3]', output: '[2,3,1]' },
    ],
    constraints: ['The number of nodes is in [0, 100].', '-100 <= Node.val <= 100'],
    hints: ['You can solve this with recursion OR with a queue.'],
    approaches: [{ name: 'DFS (recursive)', summary: 'For each node, swap its left and right children, then recursively invert both subtrees. The base case is a null node — return null.' }],
    starterCode: {
      javascript: 'function invertTree(root) {\n  // your code here\n  return root;\n}',
      python: 'def invert_tree(root):\n    # your code here\n    return root',
      java: 'class Solution {\n    public TreeNode invertTree(TreeNode root) {\n        return root;\n    }\n}',
      cpp: 'TreeNode* invertTree(TreeNode* root) {\n    return root;\n}',
      go: 'func invertTree(root *TreeNode) *TreeNode {\n    return root\n}',
    },
    solutions: {
      javascript: 'function invertTree(root) {\n  if (!root) return null;\n  [root.left, root.right] = [invertTree(root.right), invertTree(root.left)];\n  return root;\n}',
      python: 'def invert_tree(root):\n    if not root: return None\n    root.left, root.right = invert_tree(root.right), invert_tree(root.left)\n    return root',
      java: 'class Solution {\n    public TreeNode invertTree(TreeNode root) {\n        if (root == null) return null;\n        TreeNode t = root.left;\n        root.left = invertTree(root.right);\n        root.right = invertTree(t);\n        return root;\n    }\n}',
      cpp: 'TreeNode* invertTree(TreeNode* root) {\n    if (!root) return nullptr;\n    swap(root->left, root->right);\n    invertTree(root->left);\n    invertTree(root->right);\n    return root;\n}',
      go: 'func invertTree(root *TreeNode) *TreeNode {\n    if root == nil { return nil }\n    root.Left, root.Right = invertTree(root.Right), invertTree(root.Left)\n    return root\n}',
    },
    complexity: { time: 'O(n)', space: 'O(h)' },
    testCases: [
      { input: { root: { val: 4, left: { val: 2, left: { val: 1 }, right: { val: 3 } }, right: { val: 7, left: { val: 6 }, right: { val: 9 } } } }, expected: { val: 4, left: { val: 7, left: { val: 9 }, right: { val: 6 } }, right: { val: 2, left: { val: 3 }, right: { val: 1 } } }, hidden: false },
      { input: { root: null }, expected: null, hidden: false },
    ],
  },
  {
    id: 'max-depth-of-binary-tree',
    functionName: 'maxDepth',
    number: 75,
    title: 'Maximum Depth of Binary Tree',
    difficulty: 'Easy',
    topic: 'trees',
    tags: ['binary-tree', 'dfs', 'bfs'],
    leetcodeUrl: 'https://leetcode.com/problems/maximum-depth-of-binary-tree/',
    description: 'Given the `root` of a binary tree, return its **maximum depth**. The maximum depth is the number of nodes along the longest path from the root down to the farthest leaf node.',
    examples: [
      { input: 'root = [3,9,20,null,null,15,7]', output: '3' },
      { input: 'root = [1,null,2]', output: '2' },
    ],
    constraints: ['0 <= number of nodes <= 10^4', '-100 <= Node.val <= 100'],
    hints: ['The depth of a node = 1 + max(depth(left), depth(right)). Base case: null returns 0.'],
    approaches: [{ name: 'DFS recursion', summary: 'Recursively return 1 + max(depth(left), depth(right)). Empty tree returns 0. Runs in O(n).' }],
    starterCode: {
      javascript: 'function maxDepth(root) {\n  // your code here\n  return 0;\n}',
      python: 'def max_depth(root):\n    # your code here\n    return 0',
      java: 'class Solution {\n    public int maxDepth(TreeNode root) {\n        return 0;\n    }\n}',
      cpp: 'int maxDepth(TreeNode* root) {\n    return 0;\n}',
      go: 'func maxDepth(root *TreeNode) int {\n    return 0\n}',
    },
    solutions: {
      javascript: 'function maxDepth(root) {\n  if (!root) return 0;\n  return 1 + Math.max(maxDepth(root.left), maxDepth(root.right));\n}',
      python: 'def max_depth(root):\n    if not root: return 0\n    return 1 + max(max_depth(root.left), max_depth(root.right))',
      java: 'class Solution {\n    public int maxDepth(TreeNode root) {\n        if (root == null) return 0;\n        return 1 + Math.max(maxDepth(root.left), maxDepth(root.right));\n    }\n}',
      cpp: 'int maxDepth(TreeNode* root) {\n    if (!root) return 0;\n    return 1 + std::max(maxDepth(root->left), maxDepth(root->right));\n}',
      go: 'func maxDepth(root *TreeNode) int {\n    if root == nil { return 0 }\n    l, r := maxDepth(root.Left), maxDepth(root.Right)\n    if l > r { return 1 + l }\n    return 1 + r\n}',
    },
    complexity: { time: 'O(n)', space: 'O(h)' },
    testCases: [
      { input: { root: { val: 3, left: { val: 9 }, right: { val: 20, left: { val: 15 }, right: { val: 7 } } } }, expected: 3, hidden: false },
      { input: { root: { val: 1, right: { val: 2 } } }, expected: 2, hidden: false },
      { input: { root: null }, expected: 0, hidden: true },
    ],
  },
  {
    id: 'same-tree',
    functionName: 'isSameTree',
    number: 76,
    title: 'Same Tree',
    difficulty: 'Easy',
    topic: 'trees',
    tags: ['binary-tree', 'dfs', 'bfs'],
    leetcodeUrl: 'https://leetcode.com/problems/same-tree/',
    description: 'Given the roots of two binary trees `p` and `q`, check if they are **the same** (structurally identical with the same node values).',
    examples: [
      { input: 'p = [1,2,3], q = [1,2,3]', output: 'true' },
      { input: 'p = [1,2], q = [1,null,2]', output: 'false' },
    ],
    constraints: ['0 <= nodes <= 100', '-10^4 <= Node.val <= 10^4'],
    hints: ['Both null → true. One null → false. Otherwise compare values, then recurse on left/right children.'],
    approaches: [{ name: 'DFS', summary: 'Base cases: both null → true; one null → false. Recurse: same value AND same left AND same right.' }],
    starterCode: {
      javascript: 'function isSameTree(p, q) {\n  // your code here\n  return false;\n}',
      python: 'def is_same_tree(p, q):\n    # your code here\n    return False',
      java: 'class Solution {\n    public boolean isSameTree(TreeNode p, TreeNode q) {\n        return false;\n    }\n}',
      cpp: 'bool isSameTree(TreeNode* p, TreeNode* q) {\n    return false;\n}',
      go: 'func isSameTree(p, q *TreeNode) bool {\n    return false\n}',
    },
    solutions: {
      javascript: 'function isSameTree(p, q) {\n  if (!p && !q) return true;\n  if (!p || !q) return false;\n  return p.val === q.val && isSameTree(p.left, q.left) && isSameTree(p.right, q.right);\n}',
      python: 'def is_same_tree(p, q):\n    if not p and not q: return True\n    if not p or not q: return False\n    return p.val == q.val and is_same_tree(p.left, q.left) and is_same_tree(p.right, q.right)',
      java: 'class Solution {\n    public boolean isSameTree(TreeNode p, TreeNode q) {\n        if (p == null && q == null) return true;\n        if (p == null || q == null) return false;\n        return p.val == q.val && isSameTree(p.left, q.left) && isSameTree(p.right, q.right);\n    }\n}',
      cpp: 'bool isSameTree(TreeNode* p, TreeNode* q) {\n    if (!p && !q) return true;\n    if (!p || !q) return false;\n    return p->val == q->val && isSameTree(p->left, q->left) && isSameTree(p->right, q->right);\n}',
      go: 'func isSameTree(p, q *TreeNode) bool {\n    if p == nil && q == nil { return true }\n    if p == nil || q == nil { return false }\n    return p.Val == q.Val && isSameTree(p.Left, q.Left) && isSameTree(p.Right, q.Right)\n}',
    },
    complexity: { time: 'O(n)', space: 'O(h)' },
    testCases: [
      { input: { p: { val: 1, left: { val: 2 }, right: { val: 3 } }, q: { val: 1, left: { val: 2 }, right: { val: 3 } } }, expected: true, hidden: false },
      { input: { p: { val: 1, left: { val: 2 } }, q: { val: 1, right: { val: 2 } } }, expected: false, hidden: false },
      { input: { p: { val: 1, left: { val: 2, right: { val: 3 } } }, q: { val: 1, left: { val: 2, right: { val: 3 } } } }, expected: true, hidden: true },
    ],
  },
  {
    id: 'subtree-of-another-tree',
    functionName: 'isSubtree',
    number: 77,
    title: 'Subtree of Another Tree',
    difficulty: 'Easy',
    topic: 'trees',
    tags: ['binary-tree', 'dfs'],
    leetcodeUrl: 'https://leetcode.com/problems/subtree-of-another-tree/',
    description: 'Given the roots of two binary trees `root` and `subRoot`, return `true` if there is a subtree of `root` with the same structure and node values as `subRoot` and `false` otherwise.\n\nA subtree of a tree `root` is a tree that consists of a node in `root` and all of its descendants.',
    examples: [
      { input: 'root = [3,4,5,1,2], subRoot = [4,1,2]', output: 'true' },
      { input: 'root = [3,4,5,1,2,null,null,null,null,0], subRoot = [4,1,2]', output: 'false' },
    ],
    constraints: ['0 <= nodes <= 10^4', '-10^4 <= Node.val <= 10^4'],
    hints: ['At every node of root, check whether the subtree rooted there equals subRoot.'],
    approaches: [{ name: 'DFS with same-tree helper', summary: 'DFS root. At each node, run isSameTree(root, subRoot). Return true on any match, false otherwise.' }],
    starterCode: {
      javascript: 'function isSubtree(root, subRoot) {\n  // your code here\n  return false;\n}',
      python: 'def is_subtree(root, sub_root):\n    # your code here\n    return False',
      java: 'class Solution {\n    public boolean isSubtree(TreeNode root, TreeNode subRoot) {\n        return false;\n    }\n}',
      cpp: 'bool isSubtree(TreeNode* root, TreeNode* subRoot) {\n    return false;\n}',
      go: 'func isSubtree(root, subRoot *TreeNode) bool {\n    return false\n}',
    },
    solutions: {
      javascript: 'function isSame(p, q) {\n  if (!p && !q) return true;\n  if (!p || !q) return false;\n  return p.val === q.val && isSame(p.left, q.left) && isSame(p.right, q.right);\n}\nfunction isSubtree(root, subRoot) {\n  if (!root) return false;\n  if (isSame(root, subRoot)) return true;\n  return isSubtree(root.left, subRoot) || isSubtree(root.right, subRoot);\n}',
      python: 'def is_same(p, q):\n    if not p and not q: return True\n    if not p or not q: return False\n    return p.val == q.val and is_same(p.left, q.left) and is_same(p.right, q.right)\n\ndef is_subtree(root, sub_root):\n    if not root: return False\n    if is_same(root, sub_root): return True\n    return is_subtree(root.left, sub_root) or is_subtree(root.right, sub_root)',
      java: 'class Solution {\n    public boolean isSubtree(TreeNode root, TreeNode subRoot) {\n        if (root == null) return false;\n        if (isSame(root, subRoot)) return true;\n        return isSubtree(root.left, subRoot) || isSubtree(root.right, subRoot);\n    }\n    boolean isSame(TreeNode p, TreeNode q) {\n        if (p == null && q == null) return true;\n        if (p == null || q == null) return false;\n        return p.val == q.val && isSame(p.left, q.left) && isSame(p.right, q.right);\n    }\n}',
      cpp: 'bool isSame(TreeNode* p, TreeNode* q) {\n    if (!p && !q) return true;\n    if (!p || !q) return false;\n    return p->val == q->val && isSame(p->left, q->left) && isSame(p->right, q->right);\n}\nbool isSubtree(TreeNode* root, TreeNode* subRoot) {\n    if (!root) return false;\n    if (isSame(root, subRoot)) return true;\n    return isSubtree(root->left, subRoot) || isSubtree(root->right, subRoot);\n}',
      go: 'func isSame(p, q *TreeNode) bool {\n    if p == nil && q == nil { return true }\n    if p == nil || q == nil { return false }\n    return p.Val == q.Val && isSame(p.Left, q.Left) && isSame(p.Right, q.Right)\n}\nfunc isSubtree(root, subRoot *TreeNode) bool {\n    if root == nil { return false }\n    if isSame(root, subRoot) { return true }\n    return isSubtree(root.Left, subRoot) || isSubtree(root.Right, subRoot)\n}',
    },
    complexity: { time: 'O(n*m)', space: 'O(h)' },
    testCases: [
      { input: { root: { val: 3, left: { val: 4, left: { val: 1 }, right: { val: 2 } }, right: { val: 5 } }, subRoot: { val: 4, left: { val: 1 }, right: { val: 2 } } }, expected: true, hidden: false },
      { input: { root: { val: 3, left: { val: 4, left: { val: 1 }, right: { val: 2, left: { val: 0 } } }, right: { val: 5 } }, subRoot: { val: 4, left: { val: 1 }, right: { val: 2 } } }, expected: false, hidden: false },
      { input: { root: { val: 1 }, subRoot: { val: 1 } }, expected: true, hidden: true },
    ],
  },
  {
    id: 'lowest-common-ancestor-of-bst',
    functionName: 'lowestCommonAncestor',
    number: 78,
    title: 'Lowest Common Ancestor of a BST',
    difficulty: 'Easy',
    topic: 'trees',
    tags: ['binary-search-tree', 'dfs'],
    leetcodeUrl: 'https://leetcode.com/problems/lowest-common-ancestor-of-a-binary-search-tree/',
    description: 'Given a binary search tree (BST), find the **lowest common ancestor (LCA)** of two given nodes `p` and `q`.\n\nThe LCA is the lowest node that has both `p` and `q` as descendants (a node can be a descendant of itself).',
    examples: [
      { input: 'root = [6,2,8,0,4,7,9], p = 2, q = 8', output: '6' },
      { input: 'root = [6,2,8,0,4,7,9], p = 2, q = 4', output: '2' },
    ],
    constraints: ['2 <= nodes <= 10^5', '-10^9 <= Node.val <= 10^9', 'All Node.val are unique.', 'p != q', 'p and q exist in the BST.'],
    hints: ['Because of BST ordering: if both p and q are smaller than root, LCA is in left subtree; if both larger, in right; otherwise root is the split point.'],
    approaches: [{ name: 'Iterative BST walk', summary: 'Walk down from root. If both p and q are greater, go right; if both smaller, go left; otherwise root is the split and is the LCA.' }],
    starterCode: {
      javascript: 'function lowestCommonAncestor(root, p, q) {\n  // your code here\n  return root;\n}',
      python: 'def lowest_common_ancestor(root, p, q):\n    # your code here\n    return root',
      java: 'class Solution {\n    public TreeNode lowestCommonAncestor(TreeNode root, TreeNode p, TreeNode q) {\n        return root;\n    }\n}',
      cpp: 'TreeNode* lowestCommonAncestor(TreeNode* root, TreeNode* p, TreeNode* q) {\n    return root;\n}',
      go: 'func lowestCommonAncestor(root, p, q *TreeNode) *TreeNode {\n    return root\n}',
    },
    solutions: {
      javascript: 'function lowestCommonAncestor(root, p, q) {\n  let cur = root;\n  while (cur) {\n    if (p.val < cur.val && q.val < cur.val) cur = cur.left;\n    else if (p.val > cur.val && q.val > cur.val) cur = cur.right;\n    else return cur;\n  }\n  return null;\n}',
      python: 'def lowest_common_ancestor(root, p, q):\n    cur = root\n    while cur:\n        if p.val < cur.val and q.val < cur.val:\n            cur = cur.left\n        elif p.val > cur.val and q.val > cur.val:\n            cur = cur.right\n        else:\n            return cur\n    return None',
      java: 'class Solution {\n    public TreeNode lowestCommonAncestor(TreeNode root, TreeNode p, TreeNode q) {\n        TreeNode cur = root;\n        while (cur != null) {\n            if (p.val < cur.val && q.val < cur.val) cur = cur.left;\n            else if (p.val > cur.val && q.val > cur.val) cur = cur.right;\n            else return cur;\n        }\n        return null;\n    }\n}',
      cpp: 'TreeNode* lowestCommonAncestor(TreeNode* root, TreeNode* p, TreeNode* q) {\n    TreeNode* cur = root;\n    while (cur) {\n        if (p->val < cur->val && q->val < cur->val) cur = cur->left;\n        else if (p->val > cur->val && q->val > cur->val) cur = cur->right;\n        else return cur;\n    }\n    return nullptr;\n}',
      go: 'func lowestCommonAncestor(root, p, q *TreeNode) *TreeNode {\n    cur := root\n    for cur != nil {\n        if p.Val < cur.Val && q.Val < cur.Val {\n            cur = cur.Left\n        } else if p.Val > cur.Val && q.Val > cur.Val {\n            cur = cur.Right\n        } else {\n            return cur\n        }\n    }\n    return nil\n}',
    },
    complexity: { time: 'O(h)', space: 'O(1)' },
    testCases: [
      { input: { root: { val: 6, left: { val: 2, left: { val: 0 }, right: { val: 4 } }, right: { val: 8, left: { val: 7 }, right: { val: 9 } } }, p: 2, q: 8 }, expected: 6, hidden: false },
      { input: { root: { val: 6, left: { val: 2, left: { val: 0 }, right: { val: 4 } }, right: { val: 8, left: { val: 7 }, right: { val: 9 } } }, p: 2, q: 4 }, expected: 2, hidden: false },
      { input: { root: { val: 2, left: { val: 1 } }, p: 1, q: 2 }, expected: 2, hidden: true },
    ],
  },
  {
    id: 'binary-tree-level-order-traversal',
    functionName: 'levelOrder',
    number: 79,
    title: 'Binary Tree Level Order Traversal',
    difficulty: 'Medium',
    topic: 'trees',
    tags: ['binary-tree', 'bfs'],
    leetcodeUrl: 'https://leetcode.com/problems/binary-tree-level-order-traversal/',
    description: 'Given the `root` of a binary tree, return the **level order traversal** of its nodes values (i.e., from left to right, level by level).',
    examples: [
      { input: 'root = [3,9,20,null,null,15,7]', output: '[[3],[9,20],[15,7]]' },
      { input: 'root = [1]', output: '[[1]]' },
    ],
    constraints: ['0 <= nodes <= 2000', '-1000 <= Node.val <= 1000'],
    hints: ['Process one level at a time using a queue: snapshot the queue size before popping each level.'],
    approaches: [{ name: 'BFS with level snapshots', summary: 'Use a queue. While it is non-empty, the level size is the queue length; pop that many nodes, collect their values, enqueue their children. Append the level to the answer.' }],
    starterCode: {
      javascript: 'function levelOrder(root) {\n  // your code here\n  return [];\n}',
      python: 'def level_order(root):\n    # your code here\n    return []',
      java: 'class Solution {\n    public List<List<Integer>> levelOrder(TreeNode root) {\n        return new java.util.ArrayList<>();\n    }\n}',
      cpp: 'vector<vector<int>> levelOrder(TreeNode* root) {\n    return {};\n}',
      go: 'func levelOrder(root *TreeNode) [][]int {\n    return nil\n}',
    },
    solutions: {
      javascript: 'function levelOrder(root) {\n  if (!root) return [];\n  const res = [], q = [root];\n  while (q.length) {\n    const level = [], sz = q.length;\n    for (let i = 0; i < sz; i++) {\n      const n = q.shift();\n      level.push(n.val);\n      if (n.left) q.push(n.left);\n      if (n.right) q.push(n.right);\n    }\n    res.push(level);\n  }\n  return res;\n}',
      python: 'def level_order(root):\n    if not root: return []\n    res, q = [], [root]\n    while q:\n        level, sz = [], len(q)\n        for _ in range(sz):\n            n = q.pop(0)\n            level.append(n.val)\n            if n.left: q.append(n.left)\n            if n.right: q.append(n.right)\n        res.append(level)\n    return res',
      java: 'class Solution {\n    public List<List<Integer>> levelOrder(TreeNode root) {\n        List<List<Integer>> res = new ArrayList<>();\n        if (root == null) return res;\n        Queue<TreeNode> q = new LinkedList<>();\n        q.offer(root);\n        while (!q.isEmpty()) {\n            int sz = q.size();\n            List<Integer> level = new ArrayList<>();\n            for (int i = 0; i < sz; i++) {\n                TreeNode n = q.poll();\n                level.add(n.val);\n                if (n.left != null) q.offer(n.left);\n                if (n.right != null) q.offer(n.right);\n            }\n            res.add(level);\n        }\n        return res;\n    }\n}',
      cpp: 'vector<vector<int>> levelOrder(TreeNode* root) {\n    vector<vector<int>> res;\n    if (!root) return res;\n    queue<TreeNode*> q; q.push(root);\n    while (!q.empty()) {\n        int sz = q.size();\n        vector<int> level;\n        for (int i = 0; i < sz; i++) {\n            TreeNode* n = q.front(); q.pop();\n            level.push_back(n->val);\n            if (n->left) q.push(n->left);\n            if (n->right) q.push(n->right);\n        }\n        res.push_back(level);\n    }\n    return res;\n}',
      go: 'func levelOrder(root *TreeNode) [][]int {\n    if root == nil { return nil }\n    res := [][]int{}\n    q := []*TreeNode{root}\n    for len(q) > 0 {\n        sz := len(q)\n        level := []int{}\n        for i := 0; i < sz; i++ {\n            n := q[0]; q = q[1:]\n            level = append(level, n.Val)\n            if n.Left != nil { q = append(q, n.Left) }\n            if n.Right != nil { q = append(q, n.Right) }\n        }\n        res = append(res, level)\n    }\n    return res\n}',
    },
    complexity: { time: 'O(n)', space: 'O(n)' },
    testCases: [
      { input: { root: { val: 3, left: { val: 9 }, right: { val: 20, left: { val: 15 }, right: { val: 7 } } } }, expected: [[3],[9,20],[15,7]], hidden: false },
      { input: { root: { val: 1 } }, expected: [[1]], hidden: false },
      { input: { root: null }, expected: [], hidden: true },
    ],
  },
  {
    id: 'binary-tree-right-side-view',
    functionName: 'rightSideView',
    number: 80,
    title: 'Binary Tree Right Side View',
    difficulty: 'Medium',
    topic: 'trees',
    tags: ['binary-tree', 'bfs', 'dfs'],
    leetcodeUrl: 'https://leetcode.com/problems/binary-tree-right-side-view/',
    description: 'Given the `root` of a binary tree, imagine yourself standing on the **right side** of it. Return the values of the nodes you can see ordered from top to bottom.',
    examples: [
      { input: 'root = [1,2,3,null,5,null,4]', output: '[1,3,4]' },
      { input: 'root = [1,null,3]', output: '[1,3]' },
    ],
    constraints: ['0 <= nodes <= 100', '-100 <= Node.val <= 100'],
    hints: ['The rightmost node of each level is visible. BFS level-by-level naturally exposes it as the last node you visit per level.'],
    approaches: [{ name: 'BFS', summary: 'BFS level by level; the last node of each level is the right-side view. O(n) time.' }],
    starterCode: {
      javascript: 'function rightSideView(root) {\n  // your code here\n  return [];\n}',
      python: 'def right_side_view(root):\n    # your code here\n    return []',
      java: 'class Solution {\n    public List<Integer> rightSideView(TreeNode root) {\n        return new java.util.ArrayList<>();\n    }\n}',
      cpp: 'vector<int> rightSideView(TreeNode* root) {\n    return {};\n}',
      go: 'func rightSideView(root *TreeNode) []int {\n    return nil\n}',
    },
    solutions: {
      javascript: 'function rightSideView(root) {\n  if (!root) return [];\n  const res = [], q = [root];\n  while (q.length) {\n    let rightmost = null;\n    for (let i = 0, sz = q.length; i < sz; i++) {\n      const n = q.shift();\n      rightmost = n.val;\n      if (n.left) q.push(n.left);\n      if (n.right) q.push(n.right);\n    }\n    res.push(rightmost);\n  }\n  return res;\n}',
      python: 'def right_side_view(root):\n    if not root: return []\n    res, q = [], [root]\n    while q:\n        rightmost = None\n        for _ in range(len(q)):\n            n = q.pop(0)\n            rightmost = n.val\n            if n.left: q.append(n.left)\n            if n.right: q.append(n.right)\n        res.append(rightmost)\n    return res',
      java: 'class Solution {\n    public List<Integer> rightSideView(TreeNode root) {\n        List<Integer> res = new ArrayList<>();\n        if (root == null) return res;\n        Queue<TreeNode> q = new LinkedList<>(); q.offer(root);\n        while (!q.isEmpty()) {\n            int sz = q.size(); Integer rightmost = null;\n            for (int i = 0; i < sz; i++) {\n                TreeNode n = q.poll(); rightmost = n.val;\n                if (n.left != null) q.offer(n.left);\n                if (n.right != null) q.offer(n.right);\n            }\n            res.add(rightmost);\n        }\n        return res;\n    }\n}',
      cpp: 'vector<int> rightSideView(TreeNode* root) {\n    vector<int> res;\n    if (!root) return res;\n    queue<TreeNode*> q; q.push(root);\n    while (!q.empty()) {\n        int rightmost = 0; int sz = q.size();\n        for (int i = 0; i < sz; i++) {\n            TreeNode* n = q.front(); q.pop();\n            rightmost = n->val;\n            if (n->left) q.push(n->left);\n            if (n->right) q.push(n->right);\n        }\n        res.push_back(rightmost);\n    }\n    return res;\n}',
      go: 'func rightSideView(root *TreeNode) []int {\n    if root == nil { return nil }\n    res := []int{}\n    q := []*TreeNode{root}\n    for len(q) > 0 {\n        sz := len(q); rightmost := 0\n        for i := 0; i < sz; i++ {\n            n := q[0]; q = q[1:]\n            rightmost = n.Val\n            if n.Left != nil { q = append(q, n.Left) }\n            if n.Right != nil { q = append(q, n.Right) }\n        }\n        res = append(res, rightmost)\n    }\n    return res\n}',
    },
    complexity: { time: 'O(n)', space: 'O(n)' },
    testCases: [
      { input: { root: { val: 1, left: { val: 2, right: { val: 5 } }, right: { val: 3, right: { val: 4 } } } }, expected: [1,3,4], hidden: false },
      { input: { root: { val: 1, right: { val: 3 } } }, expected: [1,3], hidden: false },
      { input: { root: null }, expected: [], hidden: true },
    ],
  },
  {
    id: 'count-good-nodes-in-binary-tree',
    functionName: 'goodNodes',
    number: 81,
    title: 'Count Good Nodes in Binary Tree',
    difficulty: 'Medium',
    topic: 'trees',
    tags: ['binary-tree', 'dfs'],
    leetcodeUrl: 'https://leetcode.com/problems/count-good-nodes-in-binary-tree/',
    description: 'Given a binary tree `root`, a node `x` in the tree is named **good** if in the path from root to `x` there are no nodes with a value greater than the value of `x`. Return the number of good nodes.',
    examples: [
      { input: 'root = [3,1,4,3,null,1,5]', output: '4', explanation: 'Nodes 3,4,5 and the root 3 are good.' },
      { input: 'root = [3,3,null,4,2]', output: '3' },
    ],
    constraints: ['1 <= nodes <= 10^5', '-10^4 <= Node.val <= 10^4'],
    hints: ['DFS carrying the max-so-far: a node is good iff its val >= max-so-far.'],
    approaches: [{ name: 'DFS with running max', summary: 'DFS the tree passing the max seen on the path. Increment the counter when current.val >= max. Update max = max(max, current.val).' }],
    starterCode: {
      javascript: 'function goodNodes(root) {\n  // your code here\n  return 0;\n}',
      python: 'def good_nodes(root):\n    # your code here\n    return 0',
      java: 'class Solution {\n    public int goodNodes(TreeNode root) {\n        return 0;\n    }\n}',
      cpp: 'int goodNodes(TreeNode* root) {\n    return 0;\n}',
      go: 'func goodNodes(root *TreeNode) int {\n    return 0\n}',
    },
    solutions: {
      javascript: 'function goodNodes(root) {\n  let count = 0;\n  function dfs(n, maxSoFar) {\n    if (!n) return;\n    if (n.val >= maxSoFar) count++;\n    const next = Math.max(maxSoFar, n.val);\n    dfs(n.left, next);\n    dfs(n.right, next);\n  }\n  dfs(root, -Infinity);\n  return count;\n}',
      python: 'def good_nodes(root):\n    count = [0]\n    def dfs(n, max_so_far):\n        if not n: return\n        if n.val >= max_so_far:\n            count[0] += 1\n        dfs(n.left, max(max_so_far, n.val))\n        dfs(n.right, max(max_so_far, n.val))\n    dfs(root, float(\"-inf\"))\n    return count[0]',
      java: 'class Solution {\n    int count = 0;\n    public int goodNodes(TreeNode root) {\n        dfs(root, Integer.MIN_VALUE);\n        return count;\n    }\n    void dfs(TreeNode n, int maxSoFar) {\n        if (n == null) return;\n        if (n.val >= maxSoFar) count++;\n        int next = Math.max(maxSoFar, n.val);\n        dfs(n.left, next); dfs(n.right, next);\n    }\n}',
      cpp: 'int count = 0;\nvoid dfs(TreeNode* n, int maxSoFar) {\n    if (!n) return;\n    if (n->val >= maxSoFar) count++;\n    int next = std::max(maxSoFar, n->val);\n    dfs(n->left, next); dfs(n->right, next);\n}\nint goodNodes(TreeNode* root) {\n    dfs(root, INT_MIN);\n    return count;\n}',
      go: 'var count int\nfunc dfs(n *TreeNode, maxSoFar int) {\n    if n == nil { return }\n    if n.Val >= maxSoFar { count++ }\n    next := maxSoFar\n    if n.Val > next { next = n.Val }\n    dfs(n.Left, next); dfs(n.Right, next)\n}\nfunc goodNodes(root *TreeNode) int {\n    count = 0\n    dfs(root, -1<<31)\n    return count\n}',
    },
    complexity: { time: 'O(n)', space: 'O(h)' },
    testCases: [
      { input: { root: { val: 3, left: { val: 1, left: { val: 3 } }, right: { val: 4, left: { val: 1 }, right: { val: 5 } } } }, expected: 4, hidden: false },
      { input: { root: { val: 3, left: { val: 3, left: { val: 4 }, right: { val: 2 } } } }, expected: 3, hidden: false },
      { input: { root: { val: 1 } }, expected: 1, hidden: true },
    ],
  },
]
