// linkedlist topic - second batch
export const LINKEDLIST_PROBLEMS_2 = [
  {
    id: 'add-two-numbers',
    functionName: 'addTwoNumbers',
    number: 94,
    title: 'Add Two Numbers',
    difficulty: 'Medium',
    topic: 'linkedlist',
    tags: ['linked-list', 'math'],
    leetcodeUrl: 'https://leetcode.com/problems/add-two-numbers/',
    description: 'You are given two non-empty linked lists representing two non-negative integers. The digits are stored in **reverse order**, and each node contains a single digit. Add the two numbers and return the sum as a linked list.',
    examples: [
      { input: 'l1 = [2,4,3], l2 = [5,6,4]', output: '[7,0,8]', explanation: '342 + 465 = 807' },
      { input: 'l1 = [9,9,9,9], l2 = [9,9,9,9,9]', output: '[8,9,9,0,0,1]' },
    ],
    constraints: ['1 <= nodes <= 100', '0 <= Node.val <= 9', 'No leading zeros except the number 0 itself.'],
    hints: ['Walk both lists, summing digits plus carry. Append digit % 10 to result and propagate carry // 10.'],
    approaches: [{ name: 'Carry-based digit walk', summary: 'Iterate while either list or carry is non-zero. Pull digit or 0 from each; new digit = (a+b+carry) % 10; carry = (a+b+carry) / 10.' }],
    starterCode: {
      javascript: 'function addTwoNumbers(l1, l2) {\n  // your code here\n  return null;\n}',
      python: 'def add_two_numbers(l1, l2):\n    # your code here\n    return None',
      java: 'class Solution {\n    public ListNode addTwoNumbers(ListNode l1, ListNode l2) {\n        return null;\n    }\n}',
      cpp: 'ListNode* addTwoNumbers(ListNode* l1, ListNode* l2) {\n    return nullptr;\n}',
      go: 'func addTwoNumbers(l1, l2 *ListNode) *ListNode {\n    return nil\n}',
    },
    solutions: {
      javascript: 'function addTwoNumbers(l1, l2) {\n  const dummy = { val: 0, next: null };\n  let tail = dummy, carry = 0;\n  while (l1 || l2 || carry) {\n    const sum = (l1 ? l1.val : 0) + (l2 ? l2.val : 0) + carry;\n    carry = Math.floor(sum / 10);\n    tail.next = { val: sum % 10, next: null };\n    tail = tail.next;\n    if (l1) l1 = l1.next;\n    if (l2) l2 = l2.next;\n  }\n  return dummy.next;\n}',
      python: 'def add_two_numbers(l1, l2):\n    dummy = tail = ListNode(0); carry = 0\n    while l1 or l2 or carry:\n        s = (l1.val if l1 else 0) + (l2.val if l2 else 0) + carry\n        carry, s = divmod(s, 10)\n        tail.next = ListNode(s); tail = tail.next\n        if l1: l1 = l1.next\n        if l2: l2 = l2.next\n    return dummy.next',
      java: 'class Solution {\n    public ListNode addTwoNumbers(ListNode l1, ListNode l2) {\n        ListNode dummy = new ListNode(0), tail = dummy;\n        int carry = 0;\n        while (l1 != null || l2 != null || carry != 0) {\n            int sum = carry + (l1 != null ? l1.val : 0) + (l2 != null ? l2.val : 0);\n            carry = sum / 10;\n            tail.next = new ListNode(sum % 10);\n            tail = tail.next;\n            if (l1 != null) l1 = l1.next;\n            if (l2 != null) l2 = l2.next;\n        }\n        return dummy.next;\n    }\n}',
      cpp: 'ListNode* addTwoNumbers(ListNode* l1, ListNode* l2) {\n    ListNode dummy(0), *tail = &dummy;\n    int carry = 0;\n    while (l1 || l2 || carry) {\n        int s = carry + (l1 ? l1->val : 0) + (l2 ? l2->val : 0);\n        carry = s / 10;\n        tail->next = new ListNode(s % 10);\n        tail = tail->next;\n        if (l1) l1 = l1->next;\n        if (l2) l2 = l2->next;\n    }\n    return dummy.next;\n}',
      go: 'func addTwoNumbers(l1, l2 *ListNode) *ListNode {\n    dummy := &ListNode{Val: 0}\n    tail := dummy; carry := 0\n    for l1 != nil || l2 != nil || carry != 0 {\n        s := carry\n        if l1 != nil { s += l1.Val; l1 = l1.Next }\n        if l2 != nil { s += l2.Val; l2 = l2.Next }\n        carry, s = s/10, s%10\n        tail.Next = &ListNode{Val: s}; tail = tail.Next\n    }\n    return dummy.Next\n}',
    },
    complexity: { time: 'O(max(n,m))', space: 'O(max(n,m))' },
    testCases: [
      { input: { l1: [2,4,3], l2: [5,6,4] }, expected: [7,0,8], hidden: false },
      { input: { l1: [9,9,9,9], l2: [9,9,9,9,9] }, expected: [8,9,9,0,0,1], hidden: false },
      { input: { l1: [0], l2: [0] }, expected: [0], hidden: true },
    ],
  },
  {
    id: 'find-the-duplicate-number',
    functionName: 'findDuplicate',
    number: 95,
    title: 'Find the Duplicate Number',
    difficulty: 'Medium',
    topic: 'linkedlist',
    tags: ['linked-list', 'array', 'two-pointers', 'binary-search'],
    leetcodeUrl: 'https://leetcode.com/problems/find-the-duplicate-number/',
    description: 'Given an array of integers `nums` containing `n + 1` integers where each integer is in the range `[1, n]` inclusive, there is only **one repeated number**. Return this repeated number without modifying the array and using only O(1) extra space.',
    examples: [
      { input: 'nums = [1,3,4,2,2]', output: '2' },
      { input: 'nums = [3,1,3,4,2]', output: '3' },
    ],
    constraints: ['1 <= n <= 10^5', 'nums.length == n + 1', '1 <= nums[i] <= n', 'Only one duplicate, but it may repeat more than once.'],
    hints: ['Treat the array as a linked list where nums[i] → nums[nums[i]]. The duplicate is the entry of a cycle — find it with Floyd.'],
    approaches: [{ name: 'Floyd cycle detection', summary: 'Phase 1: tortoise and hare meet inside the cycle. Phase 2: reset one pointer to start; advance both by 1; they meet at the cycle entry, which is the duplicate.' }],
    starterCode: {
      javascript: 'function findDuplicate(nums) {\n  // your code here\n  return 0;\n}',
      python: 'def find_duplicate(nums):\n    # your code here\n    return 0',
      java: 'class Solution {\n    public int findDuplicate(int[] nums) {\n        return 0;\n    }\n}',
      cpp: 'int findDuplicate(vector<int>& nums) {\n    return 0;\n}',
      go: 'func findDuplicate(nums []int) int {\n    return 0\n}',
    },
    solutions: {
      javascript: 'function findDuplicate(nums) {\n  let slow = nums[0], fast = nums[0];\n  do { slow = nums[slow]; fast = nums[nums[fast]]; } while (slow !== fast);\n  slow = nums[0];\n  while (slow !== fast) { slow = nums[slow]; fast = nums[fast]; }\n  return slow;\n}',
      python: 'def find_duplicate(nums):\n    slow = fast = nums[0]\n    while True:\n        slow = nums[slow]; fast = nums[nums[fast]]\n        if slow == fast: break\n    slow = nums[0]\n    while slow != fast:\n        slow = nums[slow]; fast = nums[fast]\n    return slow',
      java: 'class Solution {\n    public int findDuplicate(int[] nums) {\n        int slow = nums[0], fast = nums[0];\n        do { slow = nums[slow]; fast = nums[nums[fast]]; } while (slow != fast);\n        slow = nums[0];\n        while (slow != fast) { slow = nums[slow]; fast = nums[fast]; }\n        return slow;\n    }\n}',
      cpp: 'int findDuplicate(vector<int>& nums) {\n    int slow = nums[0], fast = nums[0];\n    do { slow = nums[slow]; fast = nums[nums[fast]]; } while (slow != fast);\n    slow = nums[0];\n    while (slow != fast) { slow = nums[slow]; fast = nums[fast]; }\n    return slow;\n}',
      go: 'func findDuplicate(nums []int) int {\n    slow, fast := nums[0], nums[0]\n    for {\n        slow = nums[slow]; fast = nums[nums[fast]]\n        if slow == fast { break }\n    }\n    slow = nums[0]\n    for slow != fast { slow = nums[slow]; fast = nums[fast] }\n    return slow\n}',
    },
    complexity: { time: 'O(n)', space: 'O(1)' },
    testCases: [
      { input: { nums: [1,3,4,2,2] }, expected: 2, hidden: false },
      { input: { nums: [3,1,3,4,2] }, expected: 3, hidden: false },
      { input: { nums: [1,1] }, expected: 1, hidden: true },
    ],
  },
  {
    id: 'lru-cache',
    number: 96,
    title: 'LRU Cache',
    difficulty: 'Medium',
    topic: 'linkedlist',
    tags: ['linked-list', 'design', 'hash-map', 'doubly-linked-list'],
    leetcodeUrl: 'https://leetcode.com/problems/lru-cache/',
    description: 'Design a data structure that follows the constraints of a **Least Recently Used (LRU) cache**.\n\nImplement the `LRUCache` class:\n- `LRUCache(int capacity)` initialize with positive capacity.\n- `int get(int key)` returns the value if key exists, otherwise -1.\n- `void put(int key, int value)` insert/update; if the number of keys exceeds capacity, evict the least recently used key.\n\nThe functions `get` and `put` must each run in **O(1)** average time.',
    examples: [
      { input: 'LRUCache(2); put(1,1); put(2,2); get(1); put(3,3); get(2); put(4,4); get(1); get(3); get(4)', output: '[null,null,null,1,null,-1,null,-1,3,4]' },
    ],
    constraints: ['1 <= capacity <= 3000', '0 <= key, value <= 10^4', 'At most 2*10^5 calls.'],
    hints: ['Combine a doubly linked list (for O(1) move-to-front and evict-from-tail) with a hashmap from key → node.'],
    approaches: [{ name: 'HashMap + Doubly Linked List', summary: 'Maintain a doubly linked list of (key, value) pairs ordered by recency. The hashmap gives O(1) node lookup. get/put both move the node to the head. Evict the tail when over capacity.' }],
    starterCode: {
      javascript: 'class LRUCache {\n  constructor(capacity) {\n    // your code here\n  }\n  get(key) {\n    // your code here\n    return -1;\n  }\n  put(key, value) {\n    // your code here\n  }\n}',
      python: 'class LRUCache:\n    def __init__(self, capacity: int):\n        # your code here\n        pass\n    def get(self, key: int) -> int:\n        # your code here\n        return -1\n    def put(self, key: int, value: int) -> None:\n        # your code here\n        pass',
      java: 'class LRUCache {\n    public LRUCache(int capacity) {}\n    public int get(int key) { return -1; }\n    public void put(int key, int value) {}\n}',
      cpp: 'class LRUCache {\npublic:\n    LRUCache(int capacity) {}\n    int get(int key) { return -1; }\n    void put(int key, int value) {}\n};',
      go: 'type LRUCache struct{}\nfunc Constructor(capacity int) LRUCache { return LRUCache{} }\nfunc (c *LRUCache) Get(key int) int { return -1 }\nfunc (c *LRUCache) Put(key, value int) {}',
    },
    solutions: {
      javascript: 'class Node { constructor(k, v) { this.k = k; this.v = v; this.prev = null; this.next = null; } }\nclass LRUCache {\n  constructor(cap) {\n    this.cap = cap; this.map = new Map();\n    this.head = new Node(0, 0); this.tail = new Node(0, 0);\n    this.head.next = this.tail; this.tail.prev = this.head;\n  }\n  _remove(n) { n.prev.next = n.next; n.next.prev = n.prev; }\n  _add(n) { n.prev = this.head; n.next = this.head.next; this.head.next.prev = n; this.head.next = n; }\n  get(k) {\n    if (!this.map.has(k)) return -1;\n    const n = this.map.get(k); this._remove(n); this._add(n);\n    return n.v;\n  }\n  put(k, v) {\n    if (this.map.has(k)) { this._remove(this.map.get(k)); }\n    const n = new Node(k, v); this._add(n); this.map.set(k, n);\n    if (this.map.size > this.cap) { const lru = this.tail.prev; this._remove(lru); this.map.delete(lru.k); }\n  }\n}',
      python: 'class Node:\n    __slots__ = (\"k\",\"v\",\"prev\",\"next\")\n    def __init__(self, k, v):\n        self.k = k; self.v = v; self.prev = None; self.next = None\nclass LRUCache:\n    def __init__(self, capacity):\n        self.cap = capacity; self.m = {}\n        self.head = self.tail = Node(0, 0)\n        self.head.next = self.tail; self.tail.prev = self.head\n    def _remove(self, n):\n        n.prev.next = n.next; n.next.prev = n.prev\n    def _add(self, n):\n        n.prev = self.head; n.next = self.head.next\n        self.head.next.prev = n; self.head.next = n\n    def get(self, key):\n        if key not in self.m: return -1\n        n = self.m[key]; self._remove(n); self._add(n); return n.v\n    def put(self, key, value):\n        if key in self.m: self._remove(self.m[key])\n        n = Node(key, value); self._add(n); self.m[key] = n\n        if len(self.m) > self.cap:\n            lru = self.tail.prev; self._remove(lru); del self.m[lru.k]',
      java: 'class LRUCache {\n    class Node { int k, v; Node prev, next; Node(int k, int v) { this.k = k; this.v = v; } }\n    java.util.Map<Integer,Node> m = new java.util.HashMap<>();\n    Node head = new Node(0,0), tail = new Node(0,0); int cap;\n    public LRUCache(int capacity) {\n        cap = capacity; head.next = tail; tail.prev = head;\n    }\n    void remove(Node n) { n.prev.next = n.next; n.next.prev = n.prev; }\n    void add(Node n) { n.prev = head; n.next = head.next; head.next.prev = n; head.next = n; }\n    public int get(int key) {\n        Node n = m.get(key); if (n == null) return -1;\n        remove(n); add(n); return n.v;\n    }\n    public void put(int key, int value) {\n        if (m.containsKey(key)) remove(m.get(key));\n        Node n = new Node(key, value); add(n); m.put(key, n);\n        if (m.size() > cap) { Node lru = tail.prev; remove(lru); m.remove(lru.k); }\n    }\n}',
      cpp: 'class LRUCache {\n    struct Node { int k, v; Node *prev, *next; Node(int kk,int vv):k(kk),v(vv),prev(nullptr),next(nullptr){} };\n    unordered_map<int,Node*> m;\n    Node *head = new Node(0,0), *tail = new Node(0,0);\n    int cap;\n    void remove(Node* n) { n->prev->next = n->next; n->next->prev = n->prev; }\n    void add(Node* n) { n->prev = head; n->next = head->next; head->next->prev = n; head->next = n; }\npublic:\n    LRUCache(int capacity) {\n        cap = capacity; head->next = tail; tail->prev = head;\n    }\n    int get(int key) {\n        auto it = m.find(key); if (it == m.end()) return -1;\n        remove(it->second); add(it->second); return it->second->v;\n    }\n    void put(int key, int value) {\n        if (m.count(key)) remove(m[key]);\n        Node* n = new Node(key, value); add(n); m[key] = n;\n        if ((int)m.size() > cap) { Node* lru = tail->prev; remove(lru); m.erase(lru->k); delete lru; }\n    }\n};',
      go: 'type node struct { k, v int; prev, next *node }\ntype LRUCache struct {\n    cap int; m map[int]*node\n    head, tail *node\n}\nfunc Constructor(capacity int) LRUCache {\n    h := &node{}; t := &node{}\n    h.next = t; t.prev = h\n    return LRUCache{cap: capacity, m: map[int]*node{}, head: h, tail: t}\n}\nfunc (c *LRUCache) remove(n *node) { n.prev.next = n.next; n.next.prev = n.prev }\nfunc (c *LRUCache) add(n *node) { n.prev = c.head; n.next = c.head.next; c.head.next.prev = n; c.head.next = n }\nfunc (c *LRUCache) Get(key int) int {\n    if n, ok := c.m[key]; ok { c.remove(n); c.add(n); return n.v }\n    return -1\n}\nfunc (c *LRUCache) Put(key, value int) {\n    if n, ok := c.m[key]; ok { c.remove(n) }\n    n := &node{k: key, v: value}; c.add(n); c.m[key] = n\n    if len(c.m) > c.cap { lru := c.tail.prev; c.remove(lru); delete(c.m, lru.k) }\n}',
    },
    complexity: { time: 'O(1) per op', space: 'O(capacity)' },
    testCases: [
      { input: { ops: 'LRUCache(2)', calls: 'put,1,1;put,2,2;get,1;put,3,3;get,2;put,4,4;get,1;get,3;get,4' }, expected: [null,null,null,1,null,-1,null,-1,3,4], hidden: false },
      { input: { ops: 'LRUCache(1)', calls: 'put,2,1;get,2;put,3,2;get,2;get,3' }, expected: [null,null,1,null,-1,2], hidden: false },
      { input: { ops: 'LRUCache(2)', calls: 'get,2;put,2,6;get,2' }, expected: [-1,null,6], hidden: true },
    ],
  },
  {
    id: 'merge-k-sorted-lists',
    functionName: 'mergeKLists',
    number: 97,
    title: 'Merge k Sorted Lists',
    difficulty: 'Hard',
    topic: 'linkedlist',
    tags: ['linked-list', 'heap', 'divide-and-conquer'],
    leetcodeUrl: 'https://leetcode.com/problems/merge-k-sorted-lists/',
    description: 'You are given an array of `k` linked lists, each sorted in ascending order. Merge all the linked lists into one sorted linked list and return it.',
    examples: [
      { input: 'lists = [[1,4,5],[1,3,4],[2,6]]', output: '[1,1,2,3,4,4,5,6]' },
      { input: 'lists = []', output: '[]' },
    ],
    constraints: ['0 <= k <= 10^4', '0 <= lists[i].length <= 500', '-10^4 <= Node.val <= 10^4'],
    hints: ['Push each list head into a min-heap. Pop the smallest, advance, push its next.' ],
    approaches: [{ name: 'Min-heap', summary: 'Push all non-null heads into a heap keyed by val. Repeatedly pop the smallest, append it to the result, push its successor. O(N log k).' }],
    starterCode: {
      javascript: 'function mergeKLists(lists) {\n  // your code here\n  return null;\n}',
      python: 'def merge_k_lists(lists):\n    # your code here\n    return None',
      java: 'class Solution {\n    public ListNode mergeKLists(ListNode[] lists) {\n        return null;\n    }\n}',
      cpp: 'ListNode* mergeKLists(vector<ListNode*>& lists) {\n    return nullptr;\n}',
      go: 'func mergeKLists(lists []*ListNode) *ListNode {\n    return nil\n}',
    },
    solutions: {
      javascript: 'function mergeKLists(lists) {\n  const heap = new MinPriorityQueue({ priority: n => n.val });\n  for (const h of lists) if (h) heap.enqueue(h);\n  const dummy = { val: 0, next: null };\n  let tail = dummy;\n  while (!heap.isEmpty()) {\n    const n = heap.dequeue().element;\n    tail.next = n; tail = n;\n    if (n.next) heap.enqueue(n.next);\n  }\n  return dummy.next;\n}',
      python: 'def merge_k_lists(lists):\n    import heapq\n    heap = []\n    for i, h in enumerate(lists):\n        if h: heapq.heappush(heap, (h.val, i, h))\n    dummy = tail = ListNode(0)\n    while heap:\n        v, i, n = heapq.heappop(heap)\n        tail.next = n; tail = n\n        if n.next: heapq.heappush(heap, (n.next.val, i, n.next))\n    return dummy.next',
      java: 'class Solution {\n    public ListNode mergeKLists(ListNode[] lists) {\n        PriorityQueue<ListNode> pq = new PriorityQueue<>((a, b) -> a.val - b.val);\n        for (ListNode h : lists) if (h != null) pq.offer(h);\n        ListNode dummy = new ListNode(0), tail = dummy;\n        while (!pq.isEmpty()) {\n            ListNode n = pq.poll(); tail.next = n; tail = n;\n            if (n.next != null) pq.offer(n.next);\n        }\n        return dummy.next;\n    }\n}',
      cpp: 'ListNode* mergeKLists(vector<ListNode*>& lists) {\n    auto cmp = [](ListNode* a, ListNode* b) { return a->val > b->val; };\n    priority_queue<ListNode*, vector<ListNode*>, decltype(cmp)> pq(cmp);\n    for (auto h : lists) if (h) pq.push(h);\n    ListNode dummy(0), *tail = &dummy;\n    while (!pq.empty()) {\n        ListNode* n = pq.top(); pq.pop();\n        tail->next = n; tail = n;\n        if (n->next) pq.push(n->next);\n    }\n    return dummy.next;\n}',
      go: 'type hp []*ListNode\nfunc (h hp) Len() int { return len(h) }\nfunc (h hp) Less(i, j int) bool { return h[i].Val < h[j].Val }\nfunc (h *hp) Push(x any) { *h = append(*h, x.(*ListNode)) }\nfunc (h *hp) Pop() any { old := *h; n := len(old); x := old[n-1]; *h = old[:n-1]; return x }\nfunc mergeKLists(lists []*ListNode) *ListNode {\n    h := &hp{}\n    for _, n := range lists { if n != nil { heap.Push(h, n) } }\n    dummy := &ListNode{Val: 0}; tail := dummy\n    for h.Len() > 0 {\n        n := heap.Pop(h).(*ListNode)\n        tail.Next = n; tail = n\n        if n.Next != nil { heap.Push(h, n.Next) }\n    }\n    return dummy.Next\n}',
    },
    complexity: { time: 'O(N log k)', space: 'O(k)' },
    testCases: [
      { input: { lists: [[1,4,5],[1,3,4],[2,6]] }, expected: [1,1,2,3,4,4,5,6], hidden: false },
      { input: { lists: [] }, expected: [], hidden: false },
      { input: { lists: [[]] }, expected: [], hidden: true },
    ],
  },
  {
    id: 'reverse-nodes-in-k-group',
    functionName: 'reverseKGroup',
    number: 98,
    title: 'Reverse Nodes in k-Group',
    difficulty: 'Hard',
    topic: 'linkedlist',
    tags: ['linked-list', 'recursion'],
    leetcodeUrl: 'https://leetcode.com/problems/reverse-nodes-in-k-group/',
    description: 'Given the `head` of a linked list, reverse the nodes of the list `k` at a time, and return the modified list. If the number of nodes is not a multiple of `k`, the remaining nodes at the end should stay in their original order.',
    examples: [
      { input: 'head = [1,2,3,4,5], k = 2', output: '[2,1,4,3,5]' },
      { input: 'head = [1,2,3,4,5], k = 3', output: '[3,2,1,4,5]' },
    ],
    constraints: ['0 <= nodes <= 5000', '1 <= k <= 5000'],
    hints: ['Check if there are k nodes ahead; if so reverse them and recurse on the rest, else leave them alone.'],
    approaches: [{ name: 'Reverse-k + recurse', summary: 'Check k nodes ahead. If available, reverse them and recurse on the (k+1)-th node; otherwise leave the tail as-is. O(n) total.' }],
    starterCode: {
      javascript: 'function reverseKGroup(head, k) {\n  // your code here\n  return head;\n}',
      python: 'def reverse_k_group(head, k):\n    # your code here\n    return head',
      java: 'class Solution {\n    public ListNode reverseKGroup(ListNode head, int k) {\n        return head;\n    }\n}',
      cpp: 'ListNode* reverseKGroup(ListNode* head, int k) {\n    return head;\n}',
      go: 'func reverseKGroup(head *ListNode, k int) *ListNode {\n    return head\n}',
    },
    solutions: {
      javascript: 'function reverseKGroup(head, k) {\n  let count = 0, n = head;\n  while (n && count < k) { n = n.next; count++; }\n  if (count < k) return head;\n  let prev = null, curr = head;\n  for (let i = 0; i < k; i++) { const nx = curr.next; curr.next = prev; prev = curr; curr = nx; }\n  head.next = reverseKGroup(curr, k);\n  return prev;\n}',
      python: 'def reverse_k_group(head, k):\n    count = 0; n = head\n    while n and count < k: n = n.next; count += 1\n    if count < k: return head\n    prev, curr = None, head\n    for _ in range(k):\n        nx = curr.next; curr.next = prev; prev = curr; curr = nx\n    head.next = reverse_k_group(curr, k)\n    return prev',
      java: 'class Solution {\n    public ListNode reverseKGroup(ListNode head, int k) {\n        ListNode n = head; int count = 0;\n        while (n != null && count < k) { n = n.next; count++; }\n        if (count < k) return head;\n        ListNode prev = null, curr = head;\n        for (int i = 0; i < k; i++) { ListNode nx = curr.next; curr.next = prev; prev = curr; curr = nx; }\n        head.next = reverseKGroup(curr, k);\n        return prev;\n    }\n}',
      cpp: 'ListNode* reverseKGroup(ListNode* head, int k) {\n    ListNode* n = head; int count = 0;\n    while (n && count < k) { n = n->next; count++; }\n    if (count < k) return head;\n    ListNode *prev = nullptr, *curr = head;\n    for (int i = 0; i < k; i++) { ListNode* nx = curr->next; curr->next = prev; prev = curr; curr = nx; }\n    head->next = reverseKGroup(curr, k);\n    return prev;\n}',
      go: 'func reverseKGroup(head *ListNode, k int) *ListNode {\n    n := head; count := 0\n    for n != nil && count < k { n = n.Next; count++ }\n    if count < k { return head }\n    var prev *ListNode; curr := head\n    for i := 0; i < k; i++ { nx := curr.Next; curr.Next = prev; prev = curr; curr = nx }\n    head.Next = reverseKGroup(curr, k)\n    return prev\n}',
    },
    complexity: { time: 'O(n)', space: 'O(n/k)' },
    testCases: [
      { input: { vals: [1,2,3,4,5], k: 2 }, expected: [2,1,4,3,5], hidden: false },
      { input: { vals: [1,2,3,4,5], k: 3 }, expected: [3,2,1,4,5], hidden: false },
      { input: { vals: [1,2], k: 3 }, expected: [1,2], hidden: true },
    ],
  },
]
