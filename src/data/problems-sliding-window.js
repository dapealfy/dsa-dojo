// Seed: 6 sliding-window problems (#47-52)

export const SLIDINGWINDOW_PROBLEMS = [
  {
    id: 'best-time-to-buy-and-sell-stock',
    functionName: 'maxProfit',
    number: 47,
    title: 'Best Time to Buy and Sell Stock',
    difficulty: 'Easy',
    topic: 'sliding-window',
    tags: ['sliding-window', 'array', 'greedy'],
    leetcodeUrl: 'https://leetcode.com/problems/best-time-to-buy-and-sell-stock/',
    description: 'You are given an array `prices` where `prices[i]` is the price of a given stock on the `i`-th day.\\n\\nYou want to maximize your profit by choosing a **single day** to buy and a **different day in the future** to sell. Return the maximum profit; if no profit is possible, return `0`.',
    examples: [
      { input: 'prices = [7,1,5,3,6,4]', output: '5', explanation: 'Buy day 1 (price=1), sell day 4 (price=6), profit=5' },
      { input: 'prices = [7,6,4,3,1]',   output: '0' },
    ],
    constraints: ['1 <= prices.length <= 10^5', '0 <= prices[i] <= 10^4'],
    hints: ['Track the running minimum so far and the best (price − min) seen.'],
    approaches: [{ name: 'Running Minimum', summary: 'Walk once. Maintain the smallest price seen so far and update best = max(best, price − min). O(n) time, O(1) space.' }],
    starterCode: {
      javascript: 'function maxProfit(prices) {\n  // your code here\n  return 0;\n}',
      python: 'def max_profit(prices):\n    # your code here\n    return 0',
      java: 'class Solution {\n    public int maxProfit(int[] prices) {\n        return 0;\n    }\n}',
      cpp: 'int maxProfit(vector<int>& prices) {\n    return 0;\n}',
      go: 'func maxProfit(prices []int) int {\n    return 0\n}',
    },
    solutions: {
      javascript: 'function maxProfit(prices) {\n  let minP = Infinity, best = 0;\n  for (const p of prices) {\n    if (p < minP) minP = p;\n    else if (p - minP > best) best = p - minP;\n  }\n  return best;\n}',
      python: 'def max_profit(prices):\n    min_p, best = float("inf"), 0\n    for p in prices:\n        if p < min_p: min_p = p\n        elif p - min_p > best: best = p - min_p\n    return best',
      java: 'class Solution {\n    public int maxProfit(int[] prices) {\n        int minP = Integer.MAX_VALUE, best = 0;\n        for (int p : prices) {\n            if (p < minP) minP = p;\n            else if (p - minP > best) best = p - minP;\n        }\n        return best;\n    }\n}',
      cpp: 'int maxProfit(vector<int>& prices) {\n    int minP = INT_MAX, best = 0;\n    for (int p : prices) {\n        if (p < minP) minP = p;\n        else if (p - minP > best) best = p - minP;\n    }\n    return best;\n}',
      go: 'func maxProfit(prices []int) int {\n    minP, best := 1<<30, 0\n    for _, p := range prices {\n        if p < minP { minP = p } else if p-minP > best { best = p - minP }\n    }\n    return best\n}',
    },
    complexity: { time: 'O(n)', space: 'O(1)' },
    testCases: [
      { input: { prices: [7,1,5,3,6,4] }, expected: 5, hidden: false },
      { input: { prices: [7,6,4,3,1] },   expected: 0, hidden: false },
      { input: { prices: [2,4,1] },       expected: 2, hidden: true },
    ],
  },
  {
    id: 'longest-substring-without-repeating-characters',
    functionName: 'lengthOfLongestSubstring',
    number: 48,
    title: 'Longest Substring Without Repeating Characters',
    difficulty: 'Medium',
    topic: 'sliding-window',
    tags: ['sliding-window', 'hash-set', 'string'],
    leetcodeUrl: 'https://leetcode.com/problems/longest-substring-without-repeating-characters/',
    description: 'Given a string `s`, find the length of the **longest substring** without repeating characters.',
    examples: [
      { input: 's = "abcabcbb"', output: '3', explanation: '"abc" has length 3' },
      { input: 's = "bbbbb"',     output: '1' },
      { input: 's = "pwwkew"',    output: '3', explanation: '"wke" has length 3' },
    ],
    constraints: ['0 <= s.length <= 5 * 10^4', 's consists of English letters, digits, symbols and spaces.'],
    hints: ['Use a sliding window with a Set; when a duplicate is found, shrink from the left until the window is valid again.'],
    approaches: [{ name: 'Sliding Window + Set', summary: 'Move right pointer through the string. If s[right] is already in the set, advance left and remove s[left] until it is gone. Track the largest window size seen.' }],
    starterCode: {
      javascript: 'function lengthOfLongestSubstring(s) {\n  // your code here\n  return 0;\n}',
      python: 'def length_of_longest_substring(s):\n    # your code here\n    return 0',
      java: 'class Solution {\n    public int lengthOfLongestSubstring(String s) {\n        return 0;\n    }\n}',
      cpp: 'int lengthOfLongestSubstring(string s) {\n    return 0;\n}',
      go: 'func lengthOfLongestSubstring(s string) int {\n    return 0\n}',
    },
    solutions: {
      javascript: 'function lengthOfLongestSubstring(s) {\n  const set = new Set();\n  let l = 0, best = 0;\n  for (let r = 0; r < s.length; r++) {\n    while (set.has(s[r])) { set.delete(s[l]); l++; }\n    set.add(s[r]);\n    if (r - l + 1 > best) best = r - l + 1;\n  }\n  return best;\n}',
      python: 'def length_of_longest_substring(s):\n    seen = set()\n    l = best = 0\n    for r, c in enumerate(s):\n        while c in seen:\n            seen.remove(s[l])\n            l += 1\n        seen.add(c)\n        if r - l + 1 > best: best = r - l + 1\n    return best',
      java: 'class Solution {\n    public int lengthOfLongestSubstring(String s) {\n        java.util.Set<Character> seen = new java.util.HashSet<>();\n        int l = 0, best = 0;\n        for (int r = 0; r < s.length(); r++) {\n            while (seen.contains(s.charAt(r))) {\n                seen.remove(s.charAt(l));\n                l++;\n            }\n            seen.add(s.charAt(r));\n            if (r - l + 1 > best) best = r - l + 1;\n        }\n        return best;\n    }\n}',
      cpp: 'int lengthOfLongestSubstring(string s) {\n    std::unordered_set<char> seen;\n    int l = 0, best = 0;\n    for (int r = 0; r < (int)s.size(); r++) {\n        while (seen.count(s[r])) { seen.erase(s[l]); l++; }\n        seen.insert(s[r]);\n        if (r - l + 1 > best) best = r - l + 1;\n    }\n    return best;\n}',
      go: 'func lengthOfLongestSubstring(s string) int {\n    seen := make(map[byte]bool)\n    l, best := 0, 0\n    for r := 0; r < len(s); r++ {\n        for seen[s[r]] { delete(seen, s[l]); l++ }\n        seen[s[r]] = true\n        if r-l+1 > best { best = r - l + 1 }\n    }\n    return best\n}',
    },
    complexity: { time: 'O(n)', space: 'O(min(n, alphabet))' },
    testCases: [
      { input: { s: 'abcabcbb' }, expected: 3, hidden: false },
      { input: { s: 'bbbbb' },     expected: 1, hidden: false },
      { input: { s: 'pwwkew' },    expected: 3, hidden: true },
    ],
  },
  {
    id: 'longest-repeating-character-replacement',
    functionName: 'characterReplacement',
    number: 49,
    title: 'Longest Repeating Character Replacement',
    difficulty: 'Medium',
    topic: 'sliding-window',
    tags: ['sliding-window', 'string', 'hash-map'],
    leetcodeUrl: 'https://leetcode.com/problems/longest-repeating-character-replacement/',
    description: 'You are given a string `s` and an integer `k`. You can choose any character of the string and change it to any other uppercase English character. You can perform this operation at most `k` times.\\n\\nReturn the length of the longest substring containing the same letter you can get after performing the above operations.',
    examples: [
      { input: 's = "ABAB", k = 2', output: '4', explanation: 'Replace two "A"s with "B"s → "BBBB"' },
      { input: 's = "AABABBA", k = 1', output: '4' },
    ],
    constraints: ['1 <= s.length <= 10^5', '0 <= k <= s.length', 's consists of only uppercase English letters.'],
    hints: ['Window is valid if windowSize − countOfMostFrequent <= k.'],
    approaches: [{ name: 'Sliding Window with Frequency Count', summary: 'Track the most frequent letter in the current window. While windowSize − maxFreq > k, shrink from the left. The maximum window size during this walk is the answer.' }],
    starterCode: {
      javascript: 'function characterReplacement(s, k) {\n  // your code here\n  return 0;\n}',
      python: 'def character_replacement(s, k):\n    # your code here\n    return 0',
      java: 'class Solution {\n    public int characterReplacement(String s, int k) {\n        return 0;\n    }\n}',
      cpp: 'int characterReplacement(string s, int k) {\n    return 0;\n}',
      go: 'func characterReplacement(s string, k int) int {\n    return 0\n}',
    },
    solutions: {
      javascript: 'function characterReplacement(s, k) {\n  const count = new Array(26).fill(0);\n  let l = 0, maxFreq = 0, best = 0;\n  for (let r = 0; r < s.length; r++) {\n    count[s.charCodeAt(r) - 65]++;\n    if (count[s.charCodeAt(r) - 65] > maxFreq) maxFreq = count[s.charCodeAt(r) - 65];\n    while (r - l + 1 - maxFreq > k) {\n      count[s.charCodeAt(l) - 65]--;\n      l++;\n    }\n    if (r - l + 1 > best) best = r - l + 1;\n  }\n  return best;\n}',
      python: 'def character_replacement(s, k):\n    count = [0] * 26\n    l = max_freq = best = 0\n    for r, c in enumerate(s):\n        count[ord(c) - 65] += 1\n        if count[ord(c) - 65] > max_freq: max_freq = count[ord(c) - 65]\n        while r - l + 1 - max_freq > k:\n            count[ord(s[l]) - 65] -= 1\n            l += 1\n        if r - l + 1 > best: best = r - l + 1\n    return best',
      java: 'class Solution {\n    public int characterReplacement(String s, int k) {\n        int[] count = new int[26];\n        int l = 0, maxFreq = 0, best = 0;\n        for (int r = 0; r < s.length(); r++) {\n            count[s.charAt(r) - "A".charAt(0)]++;\n            if (count[s.charAt(r) - "A".charAt(0)] > maxFreq) maxFreq = count[s.charAt(r) - "A".charAt(0)];\n            while (r - l + 1 - maxFreq > k) {\n                count[s.charAt(l) - "A".charAt(0)]--;\n                l++;\n            }\n            if (r - l + 1 > best) best = r - l + 1;\n        }\n        return best;\n    }\n}',
      cpp: 'int characterReplacement(string s, int k) {\n    vector<int> count(26, 0);\n    int l = 0, maxFreq = 0, best = 0;\n    for (int r = 0; r < (int)s.size(); r++) {\n        count[s[r] - "A"[0]]++;\n        if (count[s[r] - "A"[0]] > maxFreq) maxFreq = count[s[r] - "A"[0]];\n        while (r - l + 1 - maxFreq > k) { count[s[l] - "A"[0]]--; l++; }\n        if (r - l + 1 > best) best = r - l + 1;\n    }\n    return best;\n}',
      go: 'func characterReplacement(s string, k int) int {\n    count := [26]int{}\n    l, maxFreq, best := 0, 0, 0\n    for r := 0; r < len(s); r++ {\n        count[s[r]-"A"[0]]++\n        if count[s[r]-"A"[0]] > maxFreq { maxFreq = count[s[r]-"A"[0]] }\n        for r-l+1-maxFreq > k { count[s[l]-"A"[0]]--; l++ }\n        if r-l+1 > best { best = r - l + 1 }\n    }\n    return best\n}',
    },
    complexity: { time: 'O(n)', space: 'O(1) (26-letter alphabet)' },
    testCases: [
      { input: { s: 'ABAB', k: 2 },     expected: 4, hidden: false },
      { input: { s: 'AABABBA', k: 1 },  expected: 4, hidden: false },
      { input: { s: 'ABCDE', k: 1 },    expected: 2, hidden: true },
    ],
  },
  {
    id: 'permutation-in-string',
    functionName: 'checkInclusion',
    number: 50,
    title: 'Permutation in String',
    difficulty: 'Medium',
    topic: 'sliding-window',
    tags: ['sliding-window', 'hash-map', 'string'],
    leetcodeUrl: 'https://leetcode.com/problems/permutation-in-string/',
    description: 'Given two strings `s1` and `s2`, return `true` if `s2` contains a **permutation** of `s1`, or `false` otherwise.\\n\\nIn other words, return `true` if one of `s1`\'s permutations is a substring of `s2`.',
    examples: [
      { input: 's1 = "ab", s2 = "eidbaooo"', output: 'true', explanation: 's2 contains "ba"' },
      { input: 's1 = "ab", s2 = "eidboaoo"', output: 'false' },
    ],
    constraints: ['1 <= s1.length, s2.length <= 10^4', 's1 and s2 consist of lowercase English letters.'],
    hints: ['A fixed-size sliding window of length |s1| over s2 keeps character counts. Match them against s1\'s counts.'],
    approaches: [{ name: 'Fixed-Size Sliding Window', summary: 'Count chars of s1. Slide a window of that length across s2, decrement count for the char leaving the window, increment for the entering char, and check whether the count array is all zero.' }],
    starterCode: {
      javascript: 'function checkInclusion(s1, s2) {\n  // your code here\n  return false;\n}',
      python: 'def check_inclusion(s1, s2):\n    # your code here\n    return False',
      java: 'class Solution {\n    public boolean checkInclusion(String s1, String s2) {\n        return false;\n    }\n}',
      cpp: 'bool checkInclusion(string s1, string s2) {\n    return false;\n}',
      go: 'func checkInclusion(s1 string, s2 string) bool {\n    return false\n}',
    },
    solutions: {
      javascript: 'function checkInclusion(s1, s2) {\n  if (s1.length > s2.length) return false;\n  const need = new Array(26).fill(0);\n  const have = new Array(26).fill(0);\n  for (let i = 0; i < s1.length; i++) {\n    need[s1.charCodeAt(i) - 97]++;\n    have[s2.charCodeAt(i) - 97]++;\n  }\n  let matches = 26 - (need.join("|") !== have.join("|") ? 1 : 0);\n  for (let i = 0; i < 26; i++) if (need[i] === have[i]) matches++;\n  for (let i = s1.length; i < s2.length; i++) {\n    if (matches === 26) return true;\n    have[s2.charCodeAt(i) - 97]++;\n    have[s2.charCodeAt(i - s1.length) - 97]--;\n    matches = 0;\n    for (let j = 0; j < 26; j++) if (need[j] === have[j]) matches++;\n  }\n  return matches === 26;\n}',
      python: 'def check_inclusion(s1, s2):\n    if len(s1) > len(s2): return False\n    need = [0] * 26; have = [0] * 26\n    for i in range(len(s1)):\n        need[ord(s1[i]) - 97] += 1\n        have[ord(s2[i]) - 97] += 1\n    matches = sum(1 for i in range(26) if need[i] == have[i])\n    for i in range(len(s1), len(s2)):\n        if matches == 26: return True\n        have[ord(s2[i]) - 97] += 1\n        have[ord(s2[i - len(s1)]) - 97] -= 1\n        matches = sum(1 for j in range(26) if need[j] == have[j])\n    return matches == 26',
      java: 'class Solution {\n    public boolean checkInclusion(String s1, String s2) {\n        if (s1.length() > s2.length()) return false;\n        int[] need = new int[26], have = new int[26];\n        for (int i = 0; i < s1.length(); i++) {\n            need[s1.charAt(i) - "a".charAt(0)]++;\n            have[s2.charAt(i) - "a".charAt(0)]++;\n        }\n        int matches = 0;\n        for (int j = 0; j < 26; j++) if (need[j] == have[j]) matches++;\n        for (int i = s1.length(); i < s2.length(); i++) {\n            if (matches == 26) return true;\n            have[s2.charAt(i) - "a".charAt(0)]++;\n            have[s2.charAt(i - s1.length()) - "a".charAt(0)]--;\n            matches = 0;\n            for (int j = 0; j < 26; j++) if (need[j] == have[j]) matches++;\n        }\n        return matches == 26;\n    }\n}',
      cpp: 'bool checkInclusion(string s1, string s2) {\n    if (s1.size() > s2.size()) return false;\n    vector<int> need(26, 0), have(26, 0);\n    for (size_t i = 0; i < s1.size(); i++) { need[s1[i]-"a"[0]]++; have[s2[i]-"a"[0]]++; }\n    int matches = 0; for (int j = 0; j < 26; j++) if (need[j] == have[j]) matches++;\n    for (size_t i = s1.size(); i < s2.size(); i++) {\n        if (matches == 26) return true;\n        have[s2[i]-"a"[0]]++;\n        have[s2[i-s1.size()]-"a"[0]]--;\n        matches = 0; for (int j = 0; j < 26; j++) if (need[j] == have[j]) matches++;\n    }\n    return matches == 26;\n}',
      go: 'func checkInclusion(s1, s2 string) bool {\n    if len(s1) > len(s2) { return false }\n    need, have := [26]int{}, [26]int{}\n    for i := 0; i < len(s1); i++ { need[s1[i]-"a"[0]]++; have[s2[i]-"a"[0]]++ }\n    matches := 0\n    for j := 0; j < 26; j++ { if need[j] == have[j] { matches++ } }\n    for i := len(s1); i < len(s2); i++ {\n        if matches == 26 { return true }\n        have[s2[i]-"a"[0]]++\n        have[s2[i-len(s1)]-"a"[0]]--\n        matches = 0\n        for j := 0; j < 26; j++ { if need[j] == have[j] { matches++ } }\n    }\n    return matches == 26\n}',
    },
    complexity: { time: 'O(n)', space: 'O(1) (26-letter alphabet)' },
    testCases: [
      { input: { s1: 'ab', s2: 'eidbaooo' }, expected: true,  hidden: false },
      { input: { s1: 'ab', s2: 'eidboaoo' }, expected: false, hidden: false },
      { input: { s1: 'adc', s2: 'dcda' },    expected: true,  hidden: true },
    ],
  },
  {
    id: 'minimum-window-substring',
    functionName: 'minWindow',
    number: 51,
    title: 'Minimum Window Substring',
    difficulty: 'Hard',
    topic: 'sliding-window',
    tags: ['sliding-window', 'hash-map', 'string'],
    leetcodeUrl: 'https://leetcode.com/problems/minimum-window-substring/',
    description: 'Given two strings `s` and `t`, return the **minimum window substring** of `s` such that every character in `t` (including duplicates) is included in the window. If no such substring exists, return `""`.',
    examples: [
      { input: 's = "ADOBECODEBANC", t = "ABC"', output: '"BANC"' },
      { input: 's = "a", t = "a"',                output: '"a"' },
      { input: 's = "a", t = "aa"',               output: '""' },
    ],
    constraints: ['1 <= s.length, t.length <= 10^5', 's and t consist of uppercase and lowercase English letters.'],
    hints: ['Expand the right edge until the window is "valid" (has enough of every char in t), then shrink from the left as long as it stays valid.'],
    approaches: [{ name: 'Sliding Window + Counter', summary: 'Maintain a running count of matched "required" characters. Expand right; when the window has every required char, try to shrink from the left while keeping it valid. Track the smallest valid window.' }],
    starterCode: {
      javascript: 'function minWindow(s, t) {\n  // your code here\n  return "";\n}',
      python: 'def min_window(s, t):\n    # your code here\n    return ""',
      java: 'class Solution {\n    public String minWindow(String s, String t) {\n        return "";\n    }\n}',
      cpp: 'string minWindow(string s, string t) {\n    return "";\n}',
      go: 'func minWindow(s string, t string) string {\n    return ""\n}',
    },
    solutions: {
      javascript: 'function minWindow(s, t) {\n  if (t === "" || s.length < t.length) return "";\n  const need = new Map();\n  for (const c of t) need.set(c, (need.get(c) || 0) + 1);\n  const have = new Map();\n  let l = 0, haveCount = 0, needCount = need.size, best = [0, Infinity];\n  for (let r = 0; r < s.length; r++) {\n    const c = s[r];\n    if (need.has(c)) {\n      have.set(c, (have.get(c) || 0) + 1);\n      if (have.get(c) === need.get(c)) haveCount++;\n    }\n    while (haveCount === needCount) {\n      if (r - l < best[1] - best[0]) best = [l, r];\n      const cl = s[l];\n      if (need.has(cl)) {\n        have.set(cl, have.get(cl) - 1);\n        if (have.get(cl) < need.get(cl)) haveCount--;\n      }\n      l++;\n    }\n  }\n  return best[1] === Infinity ? "" : s.slice(best[0], best[1] + 1);\n}',
      python: 'def min_window(s, t):\n    if not t or len(s) < len(t): return ""\n    from collections import Counter\n    need = Counter(t)\n    have = {}\n    l = have_count = 0\n    best = [0, float("inf")]\n    for r, c in enumerate(s):\n        if c in need:\n            have[c] = have.get(c, 0) + 1\n            if have[c] == need[c]: have_count += 1\n        while have_count == len(need):\n            if r - l < best[1] - best[0]: best = [l, r]\n            cl = s[l]\n            if cl in need:\n                have[cl] -= 1\n                if have[cl] < need[cl]: have_count -= 1\n            l += 1\n    return "" if best[1] == float("inf") else s[best[0]:best[1] + 1]',
      java: 'class Solution {\n    public String minWindow(String s, String t) {\n        if (t.isEmpty() || s.length() < t.length()) return "";\n        int[] need = new int[128], have = new int[128];\n        int needCount = 0;\n        for (char c : t.toCharArray()) { if (need[c]++ == 0) needCount++; }\n        int l = 0, haveCount = 0, bestL = 0, bestLen = Integer.MAX_VALUE;\n        for (int r = 0; r < s.length(); r++) {\n            char c = s.charAt(r);\n            if (++have[c] == need[c]) haveCount++;\n            while (haveCount == needCount) {\n                if (r - l + 1 < bestLen) { bestLen = r - l + 1; bestL = l; }\n                char cl = s.charAt(l++);\n                if (have[cl]-- == need[cl]) haveCount--;\n            }\n        }\n        return bestLen == Integer.MAX_VALUE ? "" : s.substring(bestL, bestL + bestLen);\n    }\n}',
      cpp: 'string minWindow(string s, string t) {\n    if (t.empty() || s.size() < t.size()) return "";\n    vector<int> need(128, 0), have(128, 0);\n    int needCount = 0;\n    for (char c : t) if (need[c]++ == 0) needCount++;\n    int l = 0, haveCount = 0, bestLen = INT_MAX, bestL = 0;\n    for (int r = 0; r < (int)s.size(); r++) {\n        if (++have[s[r]] == need[s[r]]) haveCount++;\n        while (haveCount == needCount) {\n            if (r - l + 1 < bestLen) { bestLen = r - l + 1; bestL = l; }\n            if (have[s[l]]-- == need[s[l]]) haveCount--;\n            l++;\n        }\n    }\n    return bestLen == INT_MAX ? "" : s.substr(bestL, bestLen);\n}',
      go: 'func minWindow(s string, t string) string {\n    if len(t) == 0 || len(s) < len(t) { return "" }\n    need, have := make(map[byte]int), make(map[byte]int)\n    for i := 0; i < len(t); i++ { need[t[i]]++ }\n    l, haveCount, bestL, bestLen := 0, 0, 0, 1<<30\n    for r := 0; r < len(s); r++ {\n        have[s[r]]++\n        if have[s[r]] == need[s[r]] { haveCount++ }\n        for haveCount == len(need) {\n            if r-l+1 < bestLen { bestLen = r - l + 1; bestL = l }\n            have[s[l]]--\n            if have[s[l]] < need[s[l]] { haveCount-- }\n            l++\n        }\n    }\n    if bestLen == 1<<30 { return "" }\n    return s[bestL : bestL+bestLen]\n}',
    },
    complexity: { time: 'O(|s| + |t|)', space: 'O(|alphabet|)' },
    testCases: [
      { input: { s: 'ADOBECODEBANC', t: 'ABC' }, expected: 'BANC', hidden: false },
      { input: { s: 'a', t: 'a' },               expected: 'a',    hidden: false },
      { input: { s: 'a', t: 'aa' },              expected: '',     hidden: true },
    ],
  },
  {
    id: 'sliding-window-maximum',
    functionName: 'maxSlidingWindow',
    number: 52,
    title: 'Sliding Window Maximum',
    difficulty: 'Hard',
    topic: 'sliding-window',
    tags: ['sliding-window', 'deque', 'array'],
    leetcodeUrl: 'https://leetcode.com/problems/sliding-window-maximum/',
    description: 'You are given an array of integers `nums` and a sliding window of size `k` that moves from the very left to the very right. You can only see the `k` numbers in the window. Each time the sliding window moves right by one position, return the **max** of the current window.',
    examples: [
      { input: 'nums = [1,3,-1,-3,5,3,6,7], k = 3', output: '[3,3,5,5,6,7]' },
      { input: 'nums = [1], k = 1',                  output: '[1]' },
    ],
    constraints: ['1 <= nums.length <= 10^5', '-10^4 <= nums[i] <= 10^4', '1 <= k <= nums.length'],
    hints: ['A monotonic deque stores candidate indices in decreasing value order; drop indices that fall out of the window.'],
    approaches: [{ name: 'Monotonic Deque', summary: 'Maintain a deque of candidate indices in decreasing nums order. Pop from the back while the new element is larger, drop from the front if the index left the window, and record the front as the window max.' }],
    starterCode: {
      javascript: 'function maxSlidingWindow(nums, k) {\n  // your code here\n  return [];\n}',
      python: 'def max_sliding_window(nums, k):\n    # your code here\n    return []',
      java: 'class Solution {\n    public int[] maxSlidingWindow(int[] nums, int k) {\n        return new int[0];\n    }\n}',
      cpp: 'vector<int> maxSlidingWindow(vector<int>& nums, int k) {\n    return {};\n}',
      go: 'func maxSlidingWindow(nums []int, k int) []int {\n    return nil\n}',
    },
    solutions: {
      javascript: 'function maxSlidingWindow(nums, k) {\n  const dq = []; const out = [];\n  for (let i = 0; i < nums.length; i++) {\n    while (dq.length && nums[dq[dq.length - 1]] <= nums[i]) dq.pop();\n    dq.push(i);\n    if (dq[0] <= i - k) dq.shift();\n    if (i >= k - 1) out.push(nums[dq[0]]);\n  }\n  return out;\n}',
      python: 'def max_sliding_window(nums, k):\n    from collections import deque\n    dq = deque(); out = []\n    for i, n in enumerate(nums):\n        while dq and nums[dq[-1]] <= n: dq.pop()\n        dq.append(i)\n        if dq[0] <= i - k: dq.popleft()\n        if i >= k - 1: out.append(nums[dq[0]])\n    return out',
      java: 'class Solution {\n    public int[] maxSlidingWindow(int[] nums, int k) {\n        java.util.ArrayDeque<Integer> dq = new java.util.ArrayDeque<>();\n        int[] out = new int[nums.length - k + 1];\n        for (int i = 0; i < nums.length; i++) {\n            while (!dq.isEmpty() && nums[dq.peekLast()] <= nums[i]) dq.pollLast();\n            dq.offer(i);\n            if (dq.peekFirst() <= i - k) dq.pollFirst();\n            if (i >= k - 1) out[i - k + 1] = nums[dq.peekFirst()];\n        }\n        return out;\n    }\n}',
      cpp: 'vector<int> maxSlidingWindow(vector<int>& nums, int k) {\n    std::deque<int> dq;\n    vector<int> out;\n    for (int i = 0; i < (int)nums.size(); i++) {\n        while (!dq.empty() && nums[dq.back()] <= nums[i]) dq.pop_back();\n        dq.push_back(i);\n        if (dq.front() <= i - k) dq.pop_front();\n        if (i >= k - 1) out.push_back(nums[dq.front()]);\n    }\n    return out;\n}',
      go: 'func maxSlidingWindow(nums []int, k int) []int {\n    dq := []int{}\n    out := []int{}\n    for i, n := range nums {\n        for len(dq) > 0 && nums[dq[len(dq)-1]] <= n { dq = dq[:len(dq)-1] }\n        dq = append(dq, i)\n        if dq[0] <= i-k { dq = dq[1:] }\n        if i >= k-1 { out = append(out, nums[dq[0]]) }\n    }\n    return out\n}',
    },
    complexity: { time: 'O(n)', space: 'O(k)' },
    testCases: [
      { input: { nums: [1,3,-1,-3,5,3,6,7], k: 3 }, expected: [3,3,5,5,6,7], hidden: false },
      { input: { nums: [1], k: 1 },                  expected: [1],           hidden: false },
      { input: { nums: [9,11], k: 2 },               expected: [11],          hidden: true },
    ],
  },
];
