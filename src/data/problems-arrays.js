// Seed: 2 starter array problems. Rest will be filled by parallel subagents.

export const ARRAYS_PROBLEMS = [
  {
    id: 'two-sum',
    functionName: 'twoSum',
    number: 1,
    title: 'Two Sum',
    difficulty: 'Easy',
    topic: 'arrays',
    tags: ['hash-map'],
    leetcodeUrl: 'https://leetcode.com/problems/two-sum/',
    description: 'Given an array of integers `nums` and an integer `target`, return **indices** of the two numbers such that they add up to `target`.\n\nYou may assume each input has exactly one solution, and you may not use the same element twice. Return the answer in any order.',
    examples: [
      { input: 'nums = [2,7,11,15], target = 9', output: '[0,1]', explanation: 'nums[0] + nums[1] == 9' },
      { input: 'nums = [3,2,4], target = 6',     output: '[1,2]' },
      { input: 'nums = [3,3], target = 6',       output: '[0,1]' },
    ],
    constraints: ['2 <= nums.length <= 10^4', '-10^9 <= nums[i] <= 10^9', 'Only one valid answer exists.'],
    hints: [
      'The brute force O(n²) approach compares every pair — can you cut that down?',
      'For each number, ask: have I already seen the value I need to pair with it?',
    ],
    approaches: [{ name: 'Hash Map (one pass)', summary: 'Walk the array once. For each value n, check if (target − n) is already in a map of seen values → indices. If yes, return both indices; otherwise store n → its index and continue.' }],
    starterCode: {
      javascript: 'function twoSum(nums, target) {\n  // your code here\n  return [];\n}',
      python: 'def two_sum(nums, target):\n    # your code here\n    return []',
      java: 'class Solution {\n    public int[] twoSum(int[] nums, int target) {\n        return new int[0];\n    }\n}',
      cpp: 'vector<int> twoSum(vector<int>& nums, int target) {\n    return {};\n}',
      go: 'func twoSum(nums []int, target int) []int {\n    return nil\n}',
    },
    solutions: {
      javascript: 'function twoSum(nums, target) {\n  const seen = new Map();\n  for (let i = 0; i < nums.length; i++) {\n    const need = target - nums[i];\n    if (seen.has(need)) return [seen.get(need), i];\n    seen.set(nums[i], i);\n  }\n  return [];\n}',
      python: 'def two_sum(nums, target):\n    seen = {}\n    for i, n in enumerate(nums):\n        if target - n in seen:\n            return [seen[target - n], i]\n        seen[n] = i\n    return []',
      java: 'class Solution {\n    public int[] twoSum(int[] nums, int target) {\n        java.util.Map<Integer,Integer> seen = new java.util.HashMap<>();\n        for (int i = 0; i < nums.length; i++) {\n            int need = target - nums[i];\n            if (seen.containsKey(need)) return new int[]{seen.get(need), i};\n            seen.put(nums[i], i);\n        }\n        return new int[0];\n    }\n}',
      cpp: 'vector<int> twoSum(vector<int>& nums, int target) {\n    std::unordered_map<int,int> seen;\n    for (int i = 0; i < (int)nums.size(); i++) {\n      int need = target - nums[i];\n      auto it = seen.find(need);\n      if (it != seen.end()) return {it->second, i};\n      seen[nums[i]] = i;\n    }\n    return {};\n}',
      go: 'func twoSum(nums []int, target int) []int {\n    seen := make(map[int]int)\n    for i, n := range nums {\n        if j, ok := seen[target-n]; ok {\n            return []int{j, i}\n        }\n        seen[n] = i\n    }\n    return nil\n}',
    },
    complexity: { time: 'O(n)', space: 'O(n)' },
    testCases: [
      { input: { nums: [2,7,11,15], target: 9 }, expected: [0,1], hidden: false },
      { input: { nums: [3,2,4], target: 6 },     expected: [1,2], hidden: false },
      { input: { nums: [3,3], target: 6 },       expected: [0,1], hidden: true },
    ],
  },
  {
    id: 'contains-duplicate',
    functionName: 'containsDuplicate',
    number: 2,
    title: 'Contains Duplicate',
    difficulty: 'Easy',
    topic: 'arrays',
    tags: ['hash-set'],
    leetcodeUrl: 'https://leetcode.com/problems/contains-duplicate/',
    description: 'Given an integer array `nums`, return `true` if any value appears **at least twice**, and `false` if every element is distinct.',
    examples: [
      { input: 'nums = [1,2,3,1]', output: 'true' },
      { input: 'nums = [1,2,3,4]', output: 'false' },
    ],
    constraints: ['1 <= nums.length <= 10^5', '-10^9 <= nums[i] <= 10^9'],
    hints: ['A set has O(1) membership — could you use one to detect repeats?'],
    approaches: [{ name: 'Hash Set', summary: 'Walk the array, adding each number to a set. If you ever try to add something that\'s already there, return true. Otherwise return false at the end.' }],
    starterCode: {
      javascript: 'function containsDuplicate(nums) {\n  // your code here\n  return false;\n}',
      python: 'def contains_duplicate(nums):\n    # your code here\n    return False',
      java: 'class Solution {\n    public boolean containsDuplicate(int[] nums) {\n        return false;\n    }\n}',
      cpp: 'bool containsDuplicate(vector<int>& nums) {\n    return false;\n}',
      go: 'func containsDuplicate(nums []int) bool {\n    return false\n}',
    },
    solutions: {
      javascript: 'function containsDuplicate(nums) {\n  const seen = new Set();\n  for (const n of nums) {\n    if (seen.has(n)) return true;\n    seen.add(n);\n  }\n  return false;\n}',
      python: 'def contains_duplicate(nums):\n    seen = set()\n    for n in nums:\n        if n in seen:\n            return True\n        seen.add(n)\n    return False',
      java: 'class Solution {\n    public boolean containsDuplicate(int[] nums) {\n        java.util.Set<Integer> seen = new java.util.HashSet<>();\n        for (int n : nums) if (!seen.add(n)) return true;\n        return false;\n    }\n}',
      cpp: 'bool containsDuplicate(vector<int>& nums) {\n    std::unordered_set<int> seen;\n    for (int n : nums) if (!seen.insert(n).second) return true;\n    return false;\n}',
      go: 'func containsDuplicate(nums []int) bool {\n    seen := make(map[int]bool)\n    for _, n := range nums {\n        if seen[n] { return true }\n        seen[n] = true\n    }\n    return false\n}',
    },
    complexity: { time: 'O(n)', space: 'O(n)' },
    testCases: [
      { input: { nums: [1,2,3,1] },     expected: true,  hidden: false },
      { input: { nums: [1,2,3,4] },     expected: false, hidden: false },
      { input: { nums: [1,1,1,3,3,4,6] }, expected: true,  hidden: true },
    ],
  },
  {
    id: 'top-k-frequent-elements',
    functionName: 'topKFrequent',
    number: 9,
    title: 'Top K Frequent Elements',
    difficulty: 'Medium',
    topic: 'arrays',
    tags: ['hash-map', 'bucket-sort', 'heap'],
    leetcodeUrl: 'https://leetcode.com/problems/top-k-frequent-elements/',
    description: 'Given an integer array `nums` and an integer `k`, return the `k` most frequent elements. You may return the answer in **any order**.\n\nThe solution must run in better than O(n log n) time.',
    examples: [
      { input: 'nums = [1,1,1,2,2,3], k = 2', output: '[1,2]' },
      { input: 'nums = [1], k = 1',           output: '[1]' },
    ],
    constraints: ['1 <= nums.length <= 10^5', 'k is in the range [1, the number of distinct elements in nums].', '-10^4 <= nums[i] <= 10^4'],
    hints: ['Can you bucket elements by their frequency and then walk buckets from highest to lowest?'],
    approaches: [{ name: 'Bucket Sort by Frequency', summary: 'Count occurrences with a Map, then place each value into a bucket at index = its count. Finally walk buckets from highest to lowest, collecting elements until you have k.' }],
    starterCode: {
      javascript: 'function topKFrequent(nums, k) {\n  // your code here\n  return [];\n}',
      python: 'def top_k_frequent(nums, k):\n    # your code here\n    return []',
      java: 'class Solution {\n    public int[] topKFrequent(int[] nums, int k) {\n        return new int[0];\n    }\n}',
      cpp: 'vector<int> topKFrequent(vector<int>& nums, int k) {\n    return {};\n}',
      go: 'func topKFrequent(nums []int, k int) []int {\n    return nil\n}',
    },
    solutions: {
      javascript: 'function topKFrequent(nums, k) {\n  const freq = new Map();\n  for (const n of nums) freq.set(n, (freq.get(n) || 0) + 1);\n  const bucket = Array.from({ length: nums.length + 1 }, () => []);\n  for (const [n, c] of freq) bucket[c].push(n);\n  const out = [];\n  for (let i = bucket.length - 1; i >= 0 && out.length < k; i--) {\n    out.push(...bucket[i]);\n  }\n  return out.slice(0, k);\n}',
      python: 'def top_k_frequent(nums, k):\n    from collections import Counter\n    freq = Counter(nums)\n    bucket = [[] for _ in range(len(nums) + 1)]\n    for n, c in freq.items():\n        bucket[c].append(n)\n    out = []\n    for i in range(len(bucket) - 1, -1, -1):\n        out.extend(bucket[i])\n        if len(out) >= k:\n            return out[:k]\n    return out',
      java: 'class Solution {\n    public int[] topKFrequent(int[] nums, int k) {\n        java.util.Map<Integer,Integer> freq = new java.util.HashMap<>();\n        for (int n : nums) freq.merge(n, 1, Integer::sum);\n        java.util.List<Integer>[] bucket = new java.util.List[nums.length + 1];\n        for (int i = 0; i < bucket.length; i++) bucket[i] = new java.util.ArrayList<>();\n        for (var e : freq.entrySet()) bucket[e.getValue()].add(e.getKey());\n        int[] out = new int[k];\n        int idx = 0;\n        for (int i = bucket.length - 1; i >= 0 && idx < k; i--)\n            for (int v : bucket[i]) out[idx++] = v;\n        return out;\n    }\n}',
      cpp: 'vector<int> topKFrequent(vector<int>& nums, int k) {\n    std::unordered_map<int,int> freq;\n    for (int n : nums) freq[n]++;\n    vector<vector<int>> bucket(nums.size() + 1);\n    for (auto& [n, c] : freq) bucket[c].push_back(n);\n    vector<int> out;\n    for (int i = (int)bucket.size() - 1; i >= 0 && (int)out.size() < k; i--)\n        for (int v : bucket[i]) out.push_back(v);\n    if ((int)out.size() > k) out.resize(k);\n    return out;\n}',
      go: 'func topKFrequent(nums []int, k int) []int {\n    freq := make(map[int]int)\n    for _, n := range nums { freq[n]++ }\n    bucket := make([][]int, len(nums)+1)\n    for n, c := range freq { bucket[c] = append(bucket[c], n) }\n    out := []int{}\n    for i := len(bucket) - 1; i >= 0 && len(out) < k; i-- {\n        out = append(out, bucket[i]...)\n    }\n    if len(out) > k { out = out[:k] }\n    return out\n}',
    },
    complexity: { time: 'O(n)', space: 'O(n)' },
    testCases: [
      { input: { nums: [1,1,1,2,2,3], k: 2 }, expected: [1,2], hidden: false },
      { input: { nums: [1], k: 1 },           expected: [1],   hidden: false },
      { input: { nums: [4,4,4,5,5,6,7], k: 3 }, expected: [4,5,6], hidden: true },
    ],
  },
  {
    id: 'encode-and-decode-strings',
    functionName: 'encode',
    number: 10,
    title: 'Encode and Decode Strings',
    difficulty: 'Medium',
    topic: 'arrays',
    tags: ['string', 'design'],
    leetcodeUrl: 'https://leetcode.com/problems/encode-and-decode-strings/',
    description: 'Design an algorithm to **encode** a list of strings into a single string. The encoded string is then sent over the network and decoded back to the original list of strings.\n\nYou may not use any class member / global / static variables to store state. Implement `encode` and `decode` as methods on the same class.',
    examples: [
      { input: 'strs = ["hello","world"]', output: 'encode → "5#hello5#world", decode → ["hello","world"]' },
      { input: 'strs = [""]',              output: 'encode → "0#", decode → [""]' },
    ],
    constraints: ['0 <= strs.length <= 100', '0 <= strs[i].length <= 200', 'strs[i] contains any possible characters.'],
    hints: ['The naive join(",") breaks if strings can contain the delimiter. Try length-prefixing each one.'],
    approaches: [{ name: 'Length-Prefix Encoding', summary: 'Concatenate `${length}#${str}` for each string. To decode, read until `#` to get the length, then take exactly that many characters and repeat.' }],
    starterCode: {
      javascript: 'function encode(strs) {\n  // your code here\n  return "";\n}\nfunction decode(str) {\n  // your code here\n  return [];\n}',
      python: 'def encode(strs):\n    # your code here\n    return ""\n\ndef decode(s):\n    # your code here\n    return []',
      java: 'class Codec {\n    public String encode(List<String> strs) {\n        return "";\n    }\n    public List<String> decode(String s) {\n        return new java.util.ArrayList<>();\n    }\n}',
      cpp: 'string encode(vector<string>& strs) {\n    return "";\n}\nvector<string> decode(string s) {\n    return {};\n}',
      go: 'func encode(strs []string) string {\n    return ""\n}\nfunc decode(s string) []string {\n    return nil\n}',
    },
    solutions: {
      javascript: 'function encode(strs) {\n  return strs.map(s => `${s.length}#${s}`).join("");\n}\nfunction decode(str) {\n  const result = [];\n  let i = 0;\n  while (i < str.length) {\n    const j = str.indexOf("#", i);\n    const len = parseInt(str.slice(i, j), 10);\n    result.push(str.slice(j + 1, j + 1 + len));\n    i = j + 1 + len;\n  }\n  return result;\n}',
      python: 'def encode(strs):\n    return "".join(f"{len(s)}#{s}" for s in strs)\n\ndef decode(s):\n    out, i = [], 0\n    while i < len(s):\n        j = s.index("#", i)\n        length = int(s[i:j])\n        out.append(s[j+1:j+1+length])\n        i = j + 1 + length\n    return out',
      java: 'class Codec {\n    public String encode(List<String> strs) {\n        StringBuilder sb = new StringBuilder();\n        for (String s : strs) sb.append(s.length()).append("#").append(s);\n        return sb.toString();\n    }\n    public List<String> decode(String s) {\n        List<String> out = new ArrayList<>();\n        int i = 0;\n        while (i < s.length()) {\n            int j = s.indexOf("#", i);\n            int len = Integer.parseInt(s.substring(i, j));\n            out.add(s.substring(j + 1, j + 1 + len));\n            i = j + 1 + len;\n        }\n        return out;\n    }\n}',
      cpp: 'string encode(vector<string>& strs) {\n    string out;\n    for (auto& s : strs) { out += to_string(s.size()) + "#" + s; }\n    return out;\n}\nvector<string> decode(string s) {\n    vector<string> out;\n    int i = 0;\n    while (i < (int)s.size()) {\n        int j = s.find("#", i);\n        int len = stoi(s.substr(i, j - i));\n        out.push_back(s.substr(j + 1, len));\n        i = j + 1 + len;\n    }\n    return out;\n}',
      go: 'func encode(strs []string) string {\n    var sb strings.Builder\n    for _, s := range strs {\n        sb.WriteString(strconv.Itoa(len(s)))\n        sb.WriteByte("#")\n        sb.WriteString(s)\n    }\n    return sb.String()\n}\nfunc decode(s string) []string {\n    out := []string{}\n    i := 0\n    for i < len(s) {\n        j := strings.IndexByte(s[i:], "#") + i\n        length, _ := strconv.Atoi(s[i:j])\n        out = append(out, s[j+1:j+1+length])\n        i = j + 1 + length\n    }\n    return out\n}',
    },
    complexity: { time: 'O(n)', space: 'O(n)' },
    testCases: [
      { input: { op: 'decode', str: '5#hello5#world' },                 expected: ['hello','world'], hidden: false },
      { input: { op: 'decode', str: '0#' },                             expected: [''],             hidden: false },
      { input: { op: 'decode', str: '0#1#a0#' },                        expected: ['','a',''],      hidden: true },
    ],
  },
  {
    id: 'longest-consecutive-sequence',
    functionName: 'longestConsecutive',
    number: 11,
    title: 'Longest Consecutive Sequence',
    difficulty: 'Medium',
    topic: 'arrays',
    tags: ['hash-set', 'union-find'],
    leetcodeUrl: 'https://leetcode.com/problems/longest-consecutive-sequence/',
    description: 'Given an unsorted array of integers `nums`, return the length of the **longest consecutive elements sequence**.\n\nYou must write an algorithm that runs in **O(n)** time.',
    examples: [
      { input: 'nums = [100,4,200,1,3,2]',  output: '4', explanation: 'The longest sequence is [1,2,3,4]' },
      { input: 'nums = [0,3,7,2,5,8,4,6,0,1]', output: '9' },
    ],
    constraints: ['0 <= nums.length <= 10^5', '-10^9 <= nums[i] <= 10^9'],
    hints: ['Only start counting from the *start* of a sequence (an n where n−1 is NOT in the set).'],
    approaches: [{ name: 'Hash Set Seed', summary: 'Put all numbers in a set. For each n, if n−1 is absent, walk forward as long as n+1, n+2, … are in the set, tracking the streak length. Return the max.' }],
    starterCode: {
      javascript: 'function longestConsecutive(nums) {\n  // your code here\n  return 0;\n}',
      python: 'def longest_consecutive(nums):\n    # your code here\n    return 0',
      java: 'class Solution {\n    public int longestConsecutive(int[] nums) {\n        return 0;\n    }\n}',
      cpp: 'int longestConsecutive(vector<int>& nums) {\n    return 0;\n}',
      go: 'func longestConsecutive(nums []int) int {\n    return 0\n}',
    },
    solutions: {
      javascript: 'function longestConsecutive(nums) {\n  const set = new Set(nums);\n  let best = 0;\n  for (const n of set) {\n    if (!set.has(n - 1)) {\n      let len = 1, cur = n;\n      while (set.has(cur + 1)) { cur++; len++; }\n      best = Math.max(best, len);\n    }\n  }\n  return best;\n}',
      python: 'def longest_consecutive(nums):\n    s = set(nums)\n    best = 0\n    for n in s:\n        if n - 1 not in s:\n            cur, length = n, 1\n            while cur + 1 in s:\n                cur += 1\n                length += 1\n            best = max(best, length)\n    return best',
      java: 'class Solution {\n    public int longestConsecutive(int[] nums) {\n        java.util.Set<Integer> set = new java.util.HashSet<>();\n        for (int n : nums) set.add(n);\n        int best = 0;\n        for (int n : set) {\n            if (!set.contains(n - 1)) {\n                int cur = n, len = 1;\n                while (set.contains(cur + 1)) { cur++; len++; }\n                best = Math.max(best, len);\n            }\n        }\n        return best;\n    }\n}',
      cpp: 'int longestConsecutive(vector<int>& nums) {\n    std::unordered_set<int> s(nums.begin(), nums.end());\n    int best = 0;\n    for (int n : s) {\n        if (!s.count(n - 1)) {\n            int cur = n, len = 1;\n            while (s.count(cur + 1)) { cur++; len++; }\n            best = std::max(best, len);\n        }\n    }\n    return best;\n}',
      go: 'func longestConsecutive(nums []int) int {\n    s := make(map[int]bool)\n    for _, n := range nums { s[n] = true }\n    best := 0\n    for n := range s {\n        if !s[n-1] {\n            cur, length := n, 1\n            for s[cur+1] { cur++; length++ }\n            if length > best { best = length }\n        }\n    }\n    return best\n}',
    },
    complexity: { time: 'O(n)', space: 'O(n)' },
    testCases: [
      { input: { nums: [100,4,200,1,3,2] },          expected: 4, hidden: false },
      { input: { nums: [0,3,7,2,5,8,4,6,0,1] },     expected: 9, hidden: false },
      { input: { nums: [] },                         expected: 0, hidden: true },
    ],
  },
  {
    id: 'group-anagrams',
    functionName: 'groupAnagrams',
    number: 12,
    title: 'Group Anagrams',
    difficulty: 'Medium',
    topic: 'arrays',
    tags: ['hash-map', 'sorting'],
    leetcodeUrl: 'https://leetcode.com/problems/group-anagrams/',
    description: 'Given an array of strings `strs`, group the **anagrams** together. You can return the answer in **any order**.',
    examples: [
      { input: 'strs = ["eat","tea","tan","ate","nat","bat"]', output: '[["bat"],["nat","tan"],["ate","eat","tea"]]' },
      { input: 'strs = [""]',                                  output: '[[""]]' },
    ],
    constraints: ['1 <= strs.length <= 10^4', '0 <= strs[i].length <= 100', 'strs[i] consists of lowercase English letters.'],
    hints: ['Two strings are anagrams iff their sorted characters are equal.'],
    approaches: [{ name: 'Hash Map by Sorted Key', summary: 'For each string, compute its sorted characters as the key, append to a map value list, and return all values at the end.' }],
    starterCode: {
      javascript: 'function groupAnagrams(strs) {\n  // your code here\n  return [];\n}',
      python: 'def group_anagrams(strs):\n    # your code here\n    return []',
      java: 'class Solution {\n    public java.util.List<java.util.List<String>> groupAnagrams(String[] strs) {\n        return new java.util.ArrayList<>();\n    }\n}',
      cpp: 'vector<vector<string>> groupAnagrams(vector<string>& strs) {\n    return {};\n}',
      go: 'func groupAnagrams(strs []string) [][]string {\n    return nil\n}',
    },
    solutions: {
      javascript: 'function groupAnagrams(strs) {\n  const map = new Map();\n  for (const s of strs) {\n    const key = s.split("").sort().join("");\n    if (!map.has(key)) map.set(key, []);\n    map.get(key).push(s);\n  }\n  return [...map.values()];\n}',
      python: 'def group_anagrams(strs):\n    from collections import defaultdict\n    buckets = defaultdict(list)\n    for s in strs:\n        buckets["".join(sorted(s))].append(s)\n    return list(buckets.values())',
      java: 'class Solution {\n    public java.util.List<java.util.List<String>> groupAnagrams(String[] strs) {\n        java.util.Map<String, java.util.List<String>> map = new java.util.HashMap<>();\n        for (String s : strs) {\n            char[] cs = s.toCharArray();\n            java.util.Arrays.sort(cs);\n            String key = new String(cs);\n            map.computeIfAbsent(key, k -> new java.util.ArrayList<>()).add(s);\n        }\n        return new java.util.ArrayList<>(map.values());\n    }\n}',
      cpp: 'vector<vector<string>> groupAnagrams(vector<string>& strs) {\n    std::unordered_map<string, vector<string>> m;\n    for (auto& s : strs) {\n        string key = s;\n        sort(key.begin(), key.end());\n        m[key].push_back(s);\n    }\n    vector<vector<string>> out;\n    for (auto& [_, v] : m) out.push_back(v);\n    return out;\n}',
      go: 'func groupAnagrams(strs []string) [][]string {\n    m := make(map[string][]string)\n    for _, s := range strs {\n        cs := []byte(s)\n        sort.Slice(cs, func(i, j int) bool { return cs[i] < cs[j] })\n        key := string(cs)\n        m[key] = append(m[key], s)\n    }\n    out := make([][]string, 0, len(m))\n    for _, v := range m { out = append(out, v) }\n    return out\n}',
    },
    complexity: { time: 'O(n * k log k)', space: 'O(n * k)' },
    testCases: [
      { input: { strs: ['eat','tea','tan','ate','nat','bat'] }, expected: [['eat','tea','ate'],['tan','nat'],['bat']], hidden: false },
      { input: { strs: [''] },                                  expected: [['']], hidden: false },
      { input: { strs: ['a'] },                                 expected: [['a']], hidden: true },
    ],
  },
  {
    id: 'product-of-array-except-self',
    functionName: 'productExceptSelf',
    number: 13,
    title: 'Product of Array Except Self',
    difficulty: 'Medium',
    topic: 'arrays',
    tags: ['prefix-sum', 'array'],
    leetcodeUrl: 'https://leetcode.com/problems/product-of-array-except-self/',
    description: 'Given an integer array `nums`, return an array `answer` such that `answer[i]` is equal to the product of all the elements of `nums` except `nums[i]`.\n\nYou must write an algorithm that runs in **O(n)** time and **without using the division operation**.',
    examples: [
      { input: 'nums = [1,2,3,4]',     output: '[24,12,8,6]' },
      { input: 'nums = [-1,1,0,-3,3]', output: '[0,0,9,0,0]' },
    ],
    constraints: ['2 <= nums.length <= 10^5', '-30 <= nums[i] <= 30', 'The product of any prefix/suffix fits in a 32-bit integer.'],
    hints: ['Compute the prefix products left-to-right, then suffix products right-to-left — multiplying them on the way back.'],
    approaches: [{ name: 'Prefix × Suffix', summary: 'Build the prefix-product array, then walk right-to-left multiplying each result by a running suffix product. O(n) time, O(1) extra space if you reuse the output array.' }],
    starterCode: {
      javascript: 'function productExceptSelf(nums) {\n  // your code here\n  return [];\n}',
      python: 'def product_except_self(nums):\n    # your code here\n    return []',
      java: 'class Solution {\n    public int[] productExceptSelf(int[] nums) {\n        return new int[0];\n    }\n}',
      cpp: 'vector<int> productExceptSelf(vector<int>& nums) {\n    return {};\n}',
      go: 'func productExceptSelf(nums []int) []int {\n    return nil\n}',
    },
    solutions: {
      javascript: 'function productExceptSelf(nums) {\n  const n = nums.length;\n  const out = new Array(n);\n  let prefix = 1;\n  for (let i = 0; i < n; i++) { out[i] = prefix; prefix *= nums[i]; }\n  let suffix = 1;\n  for (let i = n - 1; i >= 0; i--) { out[i] *= suffix; suffix *= nums[i]; }\n  return out;\n}',
      python: 'def product_except_self(nums):\n    n = len(nums)\n    out = [1] * n\n    prefix = 1\n    for i in range(n):\n        out[i] = prefix\n        prefix *= nums[i]\n    suffix = 1\n    for i in range(n - 1, -1, -1):\n        out[i] *= suffix\n        suffix *= nums[i]\n    return out',
      java: 'class Solution {\n    public int[] productExceptSelf(int[] nums) {\n        int n = nums.length;\n        int[] out = new int[n];\n        int prefix = 1;\n        for (int i = 0; i < n; i++) { out[i] = prefix; prefix *= nums[i]; }\n        int suffix = 1;\n        for (int i = n - 1; i >= 0; i--) { out[i] *= suffix; suffix *= nums[i]; }\n        return out;\n    }\n}',
      cpp: 'vector<int> productExceptSelf(vector<int>& nums) {\n    int n = nums.size();\n    vector<int> out(n, 1);\n    int prefix = 1;\n    for (int i = 0; i < n; i++) { out[i] = prefix; prefix *= nums[i]; }\n    int suffix = 1;\n    for (int i = n - 1; i >= 0; i--) { out[i] *= suffix; suffix *= nums[i]; }\n    return out;\n}',
      go: 'func productExceptSelf(nums []int) []int {\n    n := len(nums)\n    out := make([]int, n)\n    prefix := 1\n    for i := 0; i < n; i++ { out[i] = prefix; prefix *= nums[i] }\n    suffix := 1\n    for i := n - 1; i >= 0; i-- { out[i] *= suffix; suffix *= nums[i] }\n    return out\n}',
    },
    complexity: { time: 'O(n)', space: 'O(1) excluding output' },
    testCases: [
      { input: { nums: [1,2,3,4] },     expected: [24,12,8,6],  hidden: false },
      { input: { nums: [-1,1,0,-3,3] }, expected: [0,0,9,0,0],  hidden: false },
      { input: { nums: [2,3] },         expected: [3,2],        hidden: true },
    ],
  },
  {
    id: 'valid-sudoku',
    functionName: 'isValidSudoku',
    number: 14,
    title: 'Valid Sudoku',
    difficulty: 'Medium',
    topic: 'arrays',
    tags: ['hash-set', 'matrix'],
    leetcodeUrl: 'https://leetcode.com/problems/valid-sudoku/',
    description: 'Determine if a `9 × 9` Sudoku board is valid. Only the filled cells need to be validated according to the following rules:\n\n- Each row must contain the digits `1-9` without repetition.\n- Each column must contain the digits `1-9` without repetition.\n- Each of the nine `3 × 3` sub-boxes must contain the digits `1-9` without repetition.',
    examples: [
      { input: 'board = [["5","3",".",".","7",".",".",".","."],...] (valid)', output: 'true' },
      { input: 'board with duplicate "8" in top-left box', output: 'false' },
    ],
    constraints: ['board.length == 9', 'board[i].length == 9', 'board[i][j] is a digit or ".".'],
    hints: ['Use three independent sets of trackers: one per row, one per column, one per 3×3 box (9 of each).'],
    approaches: [{ name: 'Hash Sets per Row/Col/Box', summary: 'Iterate every cell. For each digit seen, check it is not already in its row set, column set, or 3×3 box set, then add it to all three. If any check fails, return false.' }],
    starterCode: {
      javascript: 'function isValidSudoku(board) {\n  // your code here\n  return true;\n}',
      python: 'def is_valid_sudoku(board):\n    # your code here\n    return True',
      java: 'class Solution {\n    public boolean isValidSudoku(char[][] board) {\n        return true;\n    }\n}',
      cpp: 'bool isValidSudoku(vector<vector<char>>& board) {\n    return true;\n}',
      go: 'func isValidSudoku(board [][]byte) bool {\n    return true\n}',
    },
    solutions: {
      javascript: 'function isValidSudoku(board) {\n  const rows = Array.from({length:9}, ()=>new Set());\n  const cols = Array.from({length:9}, ()=>new Set());\n  const boxes = Array.from({length:9}, ()=>new Set());\n  for (let r = 0; r < 9; r++) {\n    for (let c = 0; c < 9; c++) {\n      const v = board[r][c];\n      if (v === ".") continue;\n      const b = Math.floor(r/3)*3 + Math.floor(c/3);\n      if (rows[r].has(v) || cols[c].has(v) || boxes[b].has(v)) return false;\n      rows[r].add(v); cols[c].add(v); boxes[b].add(v);\n    }\n  }\n  return true;\n}',
      python: 'def is_valid_sudoku(board):\n    rows = [set() for _ in range(9)]\n    cols = [set() for _ in range(9)]\n    boxes = [set() for _ in range(9)]\n    for r in range(9):\n        for c in range(9):\n            v = board[r][c]\n            if v == ".": continue\n            b = (r // 3) * 3 + (c // 3)\n            if v in rows[r] or v in cols[c] or v in boxes[b]: return False\n            rows[r].add(v); cols[c].add(v); boxes[b].add(v)\n    return True',
      java: 'class Solution {\n    public boolean isValidSudoku(char[][] board) {\n        java.util.Set<Character>[] rows = new java.util.HashSet[9];\n        java.util.Set<Character>[] cols = new java.util.HashSet[9];\n        java.util.Set<Character>[] boxes = new java.util.HashSet[9];\n        for (int i = 0; i < 9; i++) { rows[i] = new java.util.HashSet<>(); cols[i] = new java.util.HashSet<>(); boxes[i] = new java.util.HashSet<>(); }\n        for (int r = 0; r < 9; r++)\n            for (int c = 0; c < 9; c++) {\n                char v = board[r][c];\n                if (v == \".\") continue;\n                int b = (r / 3) * 3 + (c / 3);\n                if (rows[r].contains(v) || cols[c].contains(v) || boxes[b].contains(v)) return false;\n                rows[r].add(v); cols[c].add(v); boxes[b].add(v);\n            }\n        return true;\n    }\n}',
      cpp: 'bool isValidSudoku(vector<vector<char>>& board) {\n    std::unordered_set<char> rows[9], cols[9], boxes[9];\n    for (int r = 0; r < 9; r++)\n        for (int c = 0; c < 9; c++) {\n            char v = board[r][c];\n            if (v == \".\") continue;\n            int b = (r / 3) * 3 + (c / 3);\n            if (rows[r].count(v) || cols[c].count(v) || boxes[b].count(v)) return false;\n            rows[r].insert(v); cols[c].insert(v); boxes[b].insert(v);\n        }\n    return true;\n}',
      go: 'func isValidSudoku(board [][]byte) bool {\n    rows := [9]map[byte]bool{}\n    cols := [9]map[byte]bool{}\n    boxes := [9]map[byte]bool{}\n    for i := 0; i < 9; i++ { rows[i] = map[byte]bool{}; cols[i] = map[byte]bool{}; boxes[i] = map[byte]bool{} }\n    for r := 0; r < 9; r++ {\n        for c := 0; c < 9; c++ {\n            v := board[r][c]\n            if v == 46 { continue }\n            b := (r/3)*3 + c/3\n            if rows[r][v] || cols[c][v] || boxes[b][v] { return false }\n            rows[r][v] = true; cols[c][v] = true; boxes[b][v] = true\n        }\n    }\n    return true\n}',
    },
    complexity: { time: 'O(81) = O(1)', space: 'O(81) = O(1)' },
    testCases: [
      { input: { board: [['5','3','.','.','7','.','.','.','.'],['6','.','.','1','9','5','.','.','.'],['.','9','8','.','.','.','.','6','.'],['8','.','.','.','6','.','.','.','3'],['4','.','.','8','.','3','.','.','1'],['7','.','.','.','2','.','.','.','6'],['.','6','.','.','.','.','2','8','.'],['.','.','.','4','1','9','.','.','5'],['.','.','.','.','8','.','.','7','9']] }, expected: true, hidden: false },
      { input: { board: [['8','3','.','.','7','.','.','.','.'],['6','.','.','1','9','5','.','.','.'],['.','9','8','.','.','.','.','6','.'],['8','.','.','.','6','.','.','.','3'],['4','.','.','8','.','3','.','.','1'],['7','.','.','.','2','.','.','.','6'],['.','6','.','.','.','.','2','8','.'],['.','.','.','4','1','9','.','.','5'],['.','.','.','.','8','.','.','7','9']] }, expected: false, hidden: false },
      { input: { board: [['.','.','.','.','.','.','.','.','.'],['.','.','.','.','.','.','.','.','.'],['.','.','.','.','.','.','.','.','.'],['.','.','.','.','.','.','.','.','.'],['.','.','.','.','.','.','.','.','.'],['.','.','.','.','.','.','.','.','.'],['.','.','.','.','.','.','.','.','.'],['.','.','.','.','.','.','.','.','.'],['.','.','.','.','.','.','.','.','.']] }, expected: true, hidden: true },
    ],
  },
  {
    id: 'find-all-duplicates-in-array',
    functionName: 'findDuplicates',
    number: 15,
    title: 'Find All Duplicates in an Array',
    difficulty: 'Medium',
    topic: 'arrays',
    tags: ['in-place', 'hash-set'],
    leetcodeUrl: 'https://leetcode.com/problems/find-all-duplicates-in-an-array/',
    description: 'Given an integer array `nums` of length `n` where all the integers of `nums` are in the range `[1, n]` and each integer appears **once or twice**, return an array of all the integers that appear **twice**.\n\nYou must write an algorithm that runs in **O(n)** time and uses only **O(1)** extra space.',
    examples: [
      { input: 'nums = [4,3,2,7,8,2,3,1]', output: '[2,3]' },
      { input: 'nums = [1,1,2]',          output: '[1]' },
    ],
    constraints: ['n == nums.length', '1 <= n <= 10^5', '1 <= nums[i] <= n', 'Each element appears once or twice.'],
    hints: ['Use the sign of `nums[|nums[i]| − 1]` as a "have I seen this?" flag.'],
    approaches: [{ name: 'In-Place Marking', summary: 'For each value v, look at index |v| − 1. If nums[|v| − 1] is already negative, v is a duplicate. Otherwise flip it negative. O(n) time, O(1) extra space.' }],
    starterCode: {
      javascript: 'function findDuplicates(nums) {\n  // your code here\n  return [];\n}',
      python: 'def find_duplicates(nums):\n    # your code here\n    return []',
      java: 'class Solution {\n    public java.util.List<Integer> findDuplicates(int[] nums) {\n        return new java.util.ArrayList<>();\n    }\n}',
      cpp: 'vector<int> findDuplicates(vector<int>& nums) {\n    return {};\n}',
      go: 'func findDuplicates(nums []int) []int {\n    return nil\n}',
    },
    solutions: {
      javascript: 'function findDuplicates(nums) {\n  const out = [];\n  for (let i = 0; i < nums.length; i++) {\n    const idx = Math.abs(nums[i]) - 1;\n    if (nums[idx] < 0) out.push(idx + 1);\n    else nums[idx] = -nums[idx];\n  }\n  return out;\n}',
      python: 'def find_duplicates(nums):\n    out = []\n    for n in nums:\n        idx = abs(n) - 1\n        if nums[idx] < 0:\n            out.append(idx + 1)\n        else:\n            nums[idx] = -nums[idx]\n    return out',
      java: 'class Solution {\n    public java.util.List<Integer> findDuplicates(int[] nums) {\n        java.util.List<Integer> out = new java.util.ArrayList<>();\n        for (int i = 0; i < nums.length; i++) {\n            int idx = Math.abs(nums[i]) - 1;\n            if (nums[idx] < 0) out.add(idx + 1);\n            else nums[idx] = -nums[idx];\n        }\n        return out;\n    }\n}',
      cpp: 'vector<int> findDuplicates(vector<int>& nums) {\n    vector<int> out;\n    for (int i = 0; i < (int)nums.size(); i++) {\n        int idx = std::abs(nums[i]) - 1;\n        if (nums[idx] < 0) out.push_back(idx + 1);\n        else nums[idx] = -nums[idx];\n    }\n    return out;\n}',
      go: 'func findDuplicates(nums []int) []int {\n    out := []int{}\n    for _, n := range nums {\n        idx := abs(n) - 1\n        if nums[idx] < 0 {\n            out = append(out, idx+1)\n        } else {\n            nums[idx] = -nums[idx]\n        }\n    }\n    return out\n}',
    },
    complexity: { time: 'O(n)', space: 'O(1) excluding output' },
    testCases: [
      { input: { nums: [4,3,2,7,8,2,3,1] }, expected: [2,3], hidden: false },
      { input: { nums: [1,1,2] },          expected: [1],   hidden: false },
      { input: { nums: [1] },               expected: [],    hidden: true },
    ],
  },
];