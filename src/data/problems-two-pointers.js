// Seed: 5 two-pointers problems (#42-46)

export const TWOPOINTERS_PROBLEMS = [
  {
    id: 'valid-palindrome',
    functionName: 'isPalindrome',
    number: 42,
    title: 'Valid Palindrome',
    difficulty: 'Easy',
    topic: 'two-pointers',
    tags: ['two-pointers', 'string'],
    leetcodeUrl: 'https://leetcode.com/problems/valid-palindrome/',
    description: 'A phrase is a **palindrome** if, after converting all uppercase letters to lowercase letters and removing all non-alphanumeric characters, it reads the same forward and backward.\\n\\nGiven a string `s`, return `true` if it is a palindrome, or `false` otherwise.',
    examples: [
      { input: 's = "A man, a plan, a canal: Panama"', output: 'true', explanation: '"amanaplanacanalpanama" is a palindrome' },
      { input: 's = "race a car"',                    output: 'false' },
      { input: 's = " "',                             output: 'true', explanation: 'empty string after filtering' },
    ],
    constraints: ['1 <= s.length <= 2 * 10^5', 's consists only of printable ASCII characters.'],
    hints: ['Two pointers at the ends, skip non-alphanumeric characters on each step.'],
    approaches: [{ name: 'Two Pointers', summary: 'Place one pointer at the start and one at the end. Skip non-alphanumeric characters, then compare the lowercased values. If any pair mismatches, return false; if the pointers cross, return true.' }],
    starterCode: {
      javascript: 'function isPalindrome(s) {\n  // your code here\n  return true;\n}',
      python: 'def is_palindrome(s):\n    # your code here\n    return True',
      java: 'class Solution {\n    public boolean isPalindrome(String s) {\n        return true;\n    }\n}',
      cpp: 'bool isPalindrome(string s) {\n    return true;\n}',
      go: 'func isPalindrome(s string) bool {\n    return true\n}',
    },
    solutions: {
      javascript: 'function isPalindrome(s) {\n  const isAlnum = c => (c >= "a" && c <= "z") || (c >= "0" && c <= "9") || (c >= "A" && c <= "Z");\n  let l = 0, r = s.length - 1;\n  while (l < r) {\n    while (l < r && !isAlnum(s[l])) l++;\n    while (l < r && !isAlnum(s[r])) r--;\n    if (s[l].toLowerCase() !== s[r].toLowerCase()) return false;\n    l++; r--;\n  }\n  return true;\n}',
      python: 'def is_palindrome(s):\n    l, r = 0, len(s) - 1\n    while l < r:\n        while l < r and not s[l].isalnum(): l += 1\n        while l < r and not s[r].isalnum(): r -= 1\n        if s[l].lower() != s[r].lower(): return False\n        l += 1; r -= 1\n    return True',
      java: 'class Solution {\n    public boolean isPalindrome(String s) {\n        int l = 0, r = s.length() - 1;\n        while (l < r) {\n            while (l < r && !Character.isLetterOrDigit(s.charAt(l))) l++;\n            while (l < r && !Character.isLetterOrDigit(s.charAt(r))) r--;\n            if (Character.toLowerCase(s.charAt(l)) != Character.toLowerCase(s.charAt(r))) return false;\n            l++; r--;\n        }\n        return true;\n    }\n}',
      cpp: 'bool isPalindrome(string s) {\n    int l = 0, r = (int)s.size() - 1;\n    auto alnum = [](char c){ return isalnum((unsigned char)c); };\n    while (l < r) {\n        while (l < r && !alnum(s[l])) l++;\n        while (l < r && !alnum(s[r])) r--;\n        if (tolower((unsigned char)s[l]) != tolower((unsigned char)s[r])) return false;\n        l++; r--;\n    }\n    return true;\n}',
      go: 'func isPalindrome(s string) bool {\n    alnum := func(c byte) bool { return (c >= "a"[0] && c <= "z"[0]) || (c >= "A"[0] && c <= "Z"[0]) || (c >= "0"[0] && c <= "9"[0]) }\n    l, r := 0, len(s)-1\n    for l < r {\n        for l < r && !alnum(s[l]) { l++ }\n        for l < r && !alnum(s[r]) { r-- }\n        if strings.ToLower(string(s[l])) != strings.ToLower(string(s[r])) { return false }\n        l++; r--\n    }\n    return true\n}',
    },
    complexity: { time: 'O(n)', space: 'O(1)' },
    testCases: [
      { input: { s: 'A man, a plan, a canal: Panama' }, expected: true,  hidden: false },
      { input: { s: 'race a car' },                    expected: false, hidden: false },
      { input: { s: ' ' },                             expected: true,  hidden: true },
    ],
  },
  {
    id: 'two-sum-ii-input-array-is-sorted',
    functionName: 'twoSum',
    number: 43,
    title: 'Two Sum II - Input Array Is Sorted',
    difficulty: 'Medium',
    topic: 'two-pointers',
    tags: ['two-pointers', 'array', 'binary-search'],
    leetcodeUrl: 'https://leetcode.com/problems/two-sum-ii-input-array-is-sorted/',
    description: 'Given a **1-indexed** array of integers `numbers` that is already sorted in **non-decreasing order**, find two numbers such that they add up to a specific `target` number.\\n\\nReturn the indices of the two numbers (1-indexed) added by one, as an integer array `[index1, index2]` of length 2.\\n\\nYou may assume each input has exactly one solution, and you may not use the same element twice. The solution must use only constant extra space.',
    examples: [
      { input: 'numbers = [2,7,11,15], target = 9', output: '[1,2]', explanation: '2 + 7 = 9' },
      { input: 'numbers = [2,3,4],     target = 6', output: '[1,3]' },
    ],
    constraints: ['2 <= numbers.length <= 3 * 10^4', '-1000 <= numbers[i] <= 1000', 'numbers is sorted in non-decreasing order.', 'Only one valid answer exists.'],
    hints: ['Sorted input means a two-pointer scan can prune one side at every step.'],
    approaches: [{ name: 'Two Pointers', summary: 'Start with l=0, r=n−1. If numbers[l]+numbers[r] < target, l++. If greater, r−−. On equality, return (l+1, r+1). The sorted order guarantees O(n) convergence.' }],
    starterCode: {
      javascript: 'function twoSum(numbers, target) {\n  // your code here\n  return [0, 0];\n}',
      python: 'def two_sum(numbers, target):\n    # your code here\n    return [0, 0]',
      java: 'class Solution {\n    public int[] twoSum(int[] numbers, int target) {\n        return new int[2];\n    }\n}',
      cpp: 'vector<int> twoSum(vector<int>& numbers, int target) {\n    return {0, 0};\n}',
      go: 'func twoSum(numbers []int, target int) []int {\n    return []int{0, 0}\n}',
    },
    solutions: {
      javascript: 'function twoSum(numbers, target) {\n  let l = 0, r = numbers.length - 1;\n  while (l < r) {\n    const sum = numbers[l] + numbers[r];\n    if (sum === target) return [l + 1, r + 1];\n    if (sum < target) l++;\n    else r--;\n  }\n  return [-1, -1];\n}',
      python: 'def two_sum(numbers, target):\n    l, r = 0, len(numbers) - 1\n    while l < r:\n        s = numbers[l] + numbers[r]\n        if s == target: return [l + 1, r + 1]\n        if s < target: l += 1\n        else: r -= 1\n    return [-1, -1]',
      java: 'class Solution {\n    public int[] twoSum(int[] numbers, int target) {\n        int l = 0, r = numbers.length - 1;\n        while (l < r) {\n            int sum = numbers[l] + numbers[r];\n            if (sum == target) return new int[]{l + 1, r + 1};\n            if (sum < target) l++;\n            else r--;\n        }\n        return new int[]{-1, -1};\n    }\n}',
      cpp: 'vector<int> twoSum(vector<int>& numbers, int target) {\n    int l = 0, r = (int)numbers.size() - 1;\n    while (l < r) {\n        int sum = numbers[l] + numbers[r];\n        if (sum == target) return {l + 1, r + 1};\n        if (sum < target) l++;\n        else r--;\n    }\n    return {-1, -1};\n}',
      go: 'func twoSum(numbers []int, target int) []int {\n    l, r := 0, len(numbers)-1\n    for l < r {\n        sum := numbers[l] + numbers[r]\n        if sum == target { return []int{l + 1, r + 1} }\n        if sum < target { l++ } else { r-- }\n    }\n    return []int{-1, -1}\n}',
    },
    complexity: { time: 'O(n)', space: 'O(1)' },
    testCases: [
      { input: { numbers: [2,7,11,15], target: 9 }, expected: [1,2], hidden: false },
      { input: { numbers: [2,3,4],     target: 6 }, expected: [1,3], hidden: false },
      { input: { numbers: [-1,0],      target: -1 }, expected: [1,2], hidden: true },
    ],
  },
  {
    id: '3sum',
    functionName: 'threeSum',
    number: 44,
    title: '3Sum',
    difficulty: 'Medium',
    topic: 'two-pointers',
    tags: ['two-pointers', 'array', 'sorting'],
    leetcodeUrl: 'https://leetcode.com/problems/3sum/',
    description: 'Given an integer array `nums`, return all the triplets `[nums[i], nums[j], nums[k]]` such that `i != j`, `i != k`, and `j != k`, and `nums[i] + nums[j] + nums[k] == 0`.\\n\\nThe solution set must not contain duplicate triplets.',
    examples: [
      { input: 'nums = [-1,0,1,2,-1,-4]', output: '[[-1,-1,2],[-1,0,1]]' },
      { input: 'nums = [0,1,1]',          output: '[]' },
    ],
    constraints: ['3 <= nums.length <= 3000', '-10^5 <= nums[i] <= 10^5'],
    hints: ['Sort the array, then for each i use the two-sum-II two-pointer scan on the right side. Skip duplicates after every move.'],
    approaches: [{ name: 'Sort + Two Pointers', summary: 'Sort nums. For each index i, run a two-pointer scan on the suffix looking for pairs summing to −nums[i]. Skip duplicates at i, l, and r to avoid repeated triplets. O(n²) overall.' }],
    starterCode: {
      javascript: 'function threeSum(nums) {\n  // your code here\n  return [];\n}',
      python: 'def three_sum(nums):\n    # your code here\n    return []',
      java: 'class Solution {\n    public java.util.List<java.util.List<Integer>> threeSum(int[] nums) {\n        return new java.util.ArrayList<>();\n    }\n}',
      cpp: 'vector<vector<int>> threeSum(vector<int>& nums) {\n    return {};\n}',
      go: 'func threeSum(nums []int) [][]int {\n    return nil\n}',
    },
    solutions: {
      javascript: 'function threeSum(nums) {\n  nums.sort((a, b) => a - b);\n  const res = [];\n  for (let i = 0; i < nums.length - 2; i++) {\n    if (i > 0 && nums[i] === nums[i - 1]) continue;\n    let l = i + 1, r = nums.length - 1;\n    while (l < r) {\n      const sum = nums[i] + nums[l] + nums[r];\n      if (sum === 0) {\n        res.push([nums[i], nums[l], nums[r]]);\n        while (l < r && nums[l] === nums[l + 1]) l++;\n        while (l < r && nums[r] === nums[r - 1]) r--;\n        l++; r--;\n      } else if (sum < 0) l++;\n      else r--;\n    }\n  }\n  return res;\n}',
      python: 'def three_sum(nums):\n    nums.sort()\n    res = []\n    for i in range(len(nums) - 2):\n        if i > 0 and nums[i] == nums[i - 1]: continue\n        l, r = i + 1, len(nums) - 1\n        while l < r:\n            s = nums[i] + nums[l] + nums[r]\n            if s == 0:\n                res.append([nums[i], nums[l], nums[r]])\n                while l < r and nums[l] == nums[l + 1]: l += 1\n                while l < r and nums[r] == nums[r - 1]: r -= 1\n                l += 1; r -= 1\n            elif s < 0: l += 1\n            else: r -= 1\n    return res',
      java: 'class Solution {\n    public java.util.List<java.util.List<Integer>> threeSum(int[] nums) {\n        java.util.Arrays.sort(nums);\n        java.util.List<java.util.List<Integer>> res = new java.util.ArrayList<>();\n        for (int i = 0; i < nums.length - 2; i++) {\n            if (i > 0 && nums[i] == nums[i - 1]) continue;\n            int l = i + 1, r = nums.length - 1;\n            while (l < r) {\n                int s = nums[i] + nums[l] + nums[r];\n                if (s == 0) {\n                    res.add(java.util.Arrays.asList(nums[i], nums[l], nums[r]));\n                    while (l < r && nums[l] == nums[l + 1]) l++;\n                    while (l < r && nums[r] == nums[r - 1]) r--;\n                    l++; r--;\n                } else if (s < 0) l++;\n                else r--;\n            }\n        }\n        return res;\n    }\n}',
      cpp: 'vector<vector<int>> threeSum(vector<int>& nums) {\n    sort(nums.begin(), nums.end());\n    vector<vector<int>> res;\n    for (int i = 0; i < (int)nums.size() - 2; i++) {\n        if (i > 0 && nums[i] == nums[i - 1]) continue;\n        int l = i + 1, r = (int)nums.size() - 1;\n        while (l < r) {\n            int s = nums[i] + nums[l] + nums[r];\n            if (s == 0) {\n                res.push_back({nums[i], nums[l], nums[r]});\n                while (l < r && nums[l] == nums[l + 1]) l++;\n                while (l < r && nums[r] == nums[r - 1]) r--;\n                l++; r--;\n            } else if (s < 0) l++;\n            else r--;\n        }\n    }\n    return res;\n}',
      go: 'func threeSum(nums []int) [][]int {\n    sort.Ints(nums)\n    res := [][]int{}\n    for i := 0; i < len(nums)-2; i++ {\n        if i > 0 && nums[i] == nums[i-1] { continue }\n        l, r := i+1, len(nums)-1\n        for l < r {\n            s := nums[i] + nums[l] + nums[r]\n            if s == 0 {\n                res = append(res, []int{nums[i], nums[l], nums[r]})\n                for l < r && nums[l] == nums[l+1] { l++ }\n                for l < r && nums[r] == nums[r-1] { r-- }\n                l++; r--\n            } else if s < 0 { l++ } else { r-- }\n        }\n    }\n    return res\n}',
    },
    complexity: { time: 'O(n²)', space: 'O(1) excluding output' },
    testCases: [
      { input: { nums: [-1,0,1,2,-1,-4] }, expected: [[-1,-1,2],[-1,0,1]], hidden: false },
      { input: { nums: [0,1,1] },          expected: [],                hidden: false },
      { input: { nums: [0,0,0] },          expected: [[0,0,0]],         hidden: true },
    ],
  },
  {
    id: 'container-with-most-water',
    functionName: 'maxArea',
    number: 45,
    title: 'Container With Most Water',
    difficulty: 'Medium',
    topic: 'two-pointers',
    tags: ['two-pointers', 'array', 'greedy'],
    leetcodeUrl: 'https://leetcode.com/problems/container-with-most-water/',
    description: 'You are given an integer array `height` of length `n`. There are `n` vertical lines drawn such that the two endpoints of the `i`-th line are `(i, 0)` and `(i, height[i])`.\\n\\nFind two lines that together with the x-axis form a container that holds the **most water**. Return the maximum amount of water a container can store.',
    examples: [
      { input: 'height = [1,8,6,2,5,4,8,3,7]', output: '49', explanation: 'lines at indices 1 and 8 yield 7 * (8-1) = 49' },
      { input: 'height = [1,1]',               output: '1' },
    ],
    constraints: ['2 <= height.length <= 10^5', '0 <= height[i] <= 10^4'],
    hints: ['Greedy choice: at every step, moving the smaller line inward can never worsen the answer.'],
    approaches: [{ name: 'Greedy Two Pointers', summary: 'Start with the widest container at the two ends. Each step, move the pointer at the shorter line inward (the wider one cannot yield a taller minimum). Track the best area.' }],
    starterCode: {
      javascript: 'function maxArea(height) {\n  // your code here\n  return 0;\n}',
      python: 'def max_area(height):\n    # your code here\n    return 0',
      java: 'class Solution {\n    public int maxArea(int[] height) {\n        return 0;\n    }\n}',
      cpp: 'int maxArea(vector<int>& height) {\n    return 0;\n}',
      go: 'func maxArea(height []int) int {\n    return 0\n}',
    },
    solutions: {
      javascript: 'function maxArea(height) {\n  let l = 0, r = height.length - 1, best = 0;\n  while (l < r) {\n    const area = Math.min(height[l], height[r]) * (r - l);\n    if (area > best) best = area;\n    if (height[l] < height[r]) l++;\n    else r--;\n  }\n  return best;\n}',
      python: 'def max_area(height):\n    l, r, best = 0, len(height) - 1, 0\n    while l < r:\n        best = max(best, min(height[l], height[r]) * (r - l))\n        if height[l] < height[r]: l += 1\n        else: r -= 1\n    return best',
      java: 'class Solution {\n    public int maxArea(int[] height) {\n        int l = 0, r = height.length - 1, best = 0;\n        while (l < r) {\n            int area = Math.min(height[l], height[r]) * (r - l);\n            if (area > best) best = area;\n            if (height[l] < height[r]) l++;\n            else r--;\n        }\n        return best;\n    }\n}',
      cpp: 'int maxArea(vector<int>& height) {\n    int l = 0, r = (int)height.size() - 1, best = 0;\n    while (l < r) {\n        best = std::max(best, std::min(height[l], height[r]) * (r - l));\n        if (height[l] < height[r]) l++;\n        else r--;\n    }\n    return best;\n}',
      go: 'func maxArea(height []int) int {\n    l, r, best := 0, len(height)-1, 0\n    for l < r {\n        h := height[l]\n        if height[r] < h { h = height[r] }\n        area := h * (r - l)\n        if area > best { best = area }\n        if height[l] < height[r] { l++ } else { r-- }\n    }\n    return best\n}',
    },
    complexity: { time: 'O(n)', space: 'O(1)' },
    testCases: [
      { input: { height: [1,8,6,2,5,4,8,3,7] }, expected: 49, hidden: false },
      { input: { height: [1,1] },               expected: 1,  hidden: false },
      { input: { height: [4,3,2,1,4] },         expected: 16, hidden: true },
    ],
  },
  {
    id: 'trapping-rain-water',
    functionName: 'trap',
    number: 46,
    title: 'Trapping Rain Water',
    difficulty: 'Hard',
    topic: 'two-pointers',
    tags: ['two-pointers', 'array', 'dynamic-programming', 'stack'],
    leetcodeUrl: 'https://leetcode.com/problems/trapping-rain-water/',
    description: 'Given `n` non-negative integers representing an elevation map where the width of each bar is `1`, compute how much water it can trap after raining.',
    examples: [
      { input: 'height = [0,1,0,2,1,0,1,3,2,1,2,1]', output: '6' },
      { input: 'height = [4,2,0,3,2,5]',              output: '9' },
    ],
    constraints: ['n == height.length', '1 <= n <= 2 * 10^5', '0 <= height[i] <= 10^5'],
    hints: ['Water at index i depends on the max height to the left and right of i — bounded by the smaller of the two.'],
    approaches: [{ name: 'Two Pointers (O(1) space)', summary: 'Walk with l and r while tracking the best leftMax and rightMax seen so far. The water at the smaller side is (sideMax − height[side]), then advance that pointer. Total O(n) time, O(1) space.' }],
    starterCode: {
      javascript: 'function trap(height) {\n  // your code here\n  return 0;\n}',
      python: 'def trap(height):\n    # your code here\n    return 0',
      java: 'class Solution {\n    public int trap(int[] height) {\n        return 0;\n    }\n}',
      cpp: 'int trap(vector<int>& height) {\n    return 0;\n}',
      go: 'func trap(height []int) int {\n    return 0\n}',
    },
    solutions: {
      javascript: 'function trap(height) {\n  let l = 0, r = height.length - 1, lMax = 0, rMax = 0, water = 0;\n  while (l < r) {\n    if (height[l] < height[r]) {\n      if (height[l] >= lMax) lMax = height[l];\n      else water += lMax - height[l];\n      l++;\n    } else {\n      if (height[r] >= rMax) rMax = height[r];\n      else water += rMax - height[r];\n      r--;\n    }\n  }\n  return water;\n}',
      python: 'def trap(height):\n    l, r = 0, len(height) - 1\n    l_max = r_max = 0\n    water = 0\n    while l < r:\n        if height[l] < height[r]:\n            if height[l] >= l_max: l_max = height[l]\n            else: water += l_max - height[l]\n            l += 1\n        else:\n            if height[r] >= r_max: r_max = height[r]\n            else: water += r_max - height[r]\n            r -= 1\n    return water',
      java: 'class Solution {\n    public int trap(int[] height) {\n        int l = 0, r = height.length - 1, lMax = 0, rMax = 0, water = 0;\n        while (l < r) {\n            if (height[l] < height[r]) {\n                if (height[l] >= lMax) lMax = height[l];\n                else water += lMax - height[l];\n                l++;\n            } else {\n                if (height[r] >= rMax) rMax = height[r];\n                else water += rMax - height[r];\n                r--;\n            }\n        }\n        return water;\n    }\n}',
      cpp: 'int trap(vector<int>& height) {\n    int l = 0, r = (int)height.size() - 1, lMax = 0, rMax = 0, water = 0;\n    while (l < r) {\n        if (height[l] < height[r]) {\n            if (height[l] >= lMax) lMax = height[l];\n            else water += lMax - height[l];\n            l++;\n        } else {\n            if (height[r] >= rMax) rMax = height[r];\n            else water += rMax - height[r];\n            r--;\n        }\n    }\n    return water;\n}',
      go: 'func trap(height []int) int {\n    l, r := 0, len(height)-1\n    lMax, rMax, water := 0, 0, 0\n    for l < r {\n        if height[l] < height[r] {\n            if height[l] >= lMax { lMax = height[l] } else { water += lMax - height[l] }\n            l++\n        } else {\n            if height[r] >= rMax { rMax = height[r] } else { water += rMax - height[r] }\n            r--\n        }\n    }\n    return water\n}',
    },
    complexity: { time: 'O(n)', space: 'O(1)' },
    testCases: [
      { input: { height: [0,1,0,2,1,0,1,3,2,1,2,1] }, expected: 6, hidden: false },
      { input: { height: [4,2,0,3,2,5] },              expected: 9, hidden: false },
      { input: { height: [1,0,1] },                    expected: 1, hidden: true },
    ],
  },
];
