// Seed: strings topic starter problems
export const STRINGS_PROBLEMS = [
  {
    id: 'valid-anagram',
    functionName: 'isAnagram',
    number: 3,
    title: 'Valid Anagram',
    difficulty: 'Easy',
    topic: 'strings',
    tags: ['hash-map', 'sorting'],
    leetcodeUrl: 'https://leetcode.com/problems/valid-anagram/',
    description: 'Given two strings `s` and `t`, return `true` if `t` is an anagram of `s`, and `false` otherwise.\n\nAn **anagram** is a word formed by rearranging the letters of another word, using all original letters exactly once.',
    examples: [
      { input: 's = "anagram", t = "nagaram"', output: 'true' },
      { input: 's = "rat", t = "car"', output: 'false' },
    ],
    constraints: ['1 <= s.length, t.length <= 5 * 10^4', 's and t consist of lowercase English letters.'],
    hints: ['Two anagrams have the same letter frequencies.'],
    approaches: [{ name: 'Frequency Counter', summary: 'Count how many times each letter appears in s and t. If the counts match exactly, return true.' }],
    starterCode: {
      javascript: 'function isAnagram(s, t) {\n  // your code here\n  return false;\n}',
      python: 'def is_anagram(s, t):\n    # your code here\n    return False',
      java: 'class Solution {\n    public boolean isAnagram(String s, String t) {\n        return false;\n    }\n}',
      cpp: 'bool isAnagram(string s, string t) {\n    return false;\n}',
      go: 'func isAnagram(s, t string) bool {\n    return false\n}',
    },
    solutions: {
      javascript: 'function isAnagram(s, t) {\n  if (s.length !== t.length) return false;\n  const counts = {};\n  for (const c of s) counts[c] = (counts[c] || 0) + 1;\n  for (const c of t) {\n    if (!counts[c]) return false;\n    counts[c]--;\n  }\n  return true;\n}',
      python: 'def is_anagram(s, t):\n    if len(s) != len(t): return False\n    from collections import Counter\n    return Counter(s) == Counter(t)',
      java: 'class Solution {\n    public boolean isAnagram(String s, String t) {\n        if (s.length() != t.length()) return false;\n        int[] count = new int[26];\n        for (int i = 0; i < s.length(); i++) {\n            count[s.charAt(i) - \'a\']++;\n            count[t.charAt(i) - \'a\']--;\n        }\n        for (int c : count) if (c != 0) return false;\n        return true;\n    }\n}',
      cpp: 'bool isAnagram(string s, string t) {\n    if (s.size() != t.size()) return false;\n    int count[26] = {};\n    for (int i = 0; i < (int)s.size(); i++) {\n      count[s[i]-\'a\']++;\n      count[t[i]-\'a\']--;\n    }\n    for (int c : count) if (c != 0) return false;\n    return true;\n}',
      go: 'func isAnagram(s, t string) bool {\n    if len(s) != len(t) { return false }\n    count := [26]int{}\n    for i := 0; i < len(s); i++ {\n        count[s[i]-\'a\']++\n        count[t[i]-\'a\']--\n    }\n    for _, c := range count { if c != 0 { return false } }\n    return true\n}',
    },
    complexity: { time: 'O(n)', space: 'O(1)' },
    testCases: [
      { input: { s: 'anagram', t: 'nagaram' }, expected: true,  hidden: false },
      { input: { s: 'rat',     t: 'car' },    expected: false, hidden: false },
      { input: { s: 'listen',  t: 'silent' }, expected: true,  hidden: true  },
    ],
  },
  {
    id: 'longest-substring-without-repeating-characters',
    functionName: 'lengthOfLongestSubstring',
    number: 16,
    title: 'Longest Substring Without Repeating Characters',
    difficulty: 'Medium',
    topic: 'strings',
    tags: ['sliding-window', 'hash-set'],
    leetcodeUrl: 'https://leetcode.com/problems/longest-substring-without-repeating-characters/',
    description: 'Given a string `s`, find the length of the **longest substring** without repeating characters.',
    examples: [
      { input: 's = "abcabcbb"', output: '3', explanation: '"abc" has length 3' },
      { input: 's = "bbbbb"',    output: '1' },
      { input: 's = "pwwkew"',   output: '3', explanation: '"wke" has length 3' },
    ],
    constraints: ['0 <= s.length <= 5 * 10^4', 's consists of English letters, digits, symbols and spaces.'],
    hints: ['Use a sliding window. When you see a repeat, jump the left edge past the previous occurrence.'],
    approaches: [{ name: 'Sliding Window + Last-Seen Map', summary: 'Maintain a window [left, right] and a map of last-seen index per character. If s[right] was seen inside the current window, push left past it. Track max(right − left + 1).' }],
    starterCode: {
      javascript: 'function lengthOfLongestSubstring(s) {\n  // your code here\n  return 0;\n}',
      python: 'def length_of_longest_substring(s):\n    # your code here\n    return 0',
      java: 'class Solution {\n    public int lengthOfLongestSubstring(String s) {\n        return 0;\n    }\n}',
      cpp: 'int lengthOfLongestSubstring(string s) {\n    return 0;\n}',
      go: 'func lengthOfLongestSubstring(s string) int {\n    return 0\n}',
    },
    solutions: {
      javascript: 'function lengthOfLongestSubstring(s) {\n  const seen = new Map();\n  let best = 0, left = 0;\n  for (let right = 0; right < s.length; right++) {\n    if (seen.has(s[right]) && seen.get(s[right]) >= left) {\n      left = seen.get(s[right]) + 1;\n    }\n    seen.set(s[right], right);\n    best = Math.max(best, right - left + 1);\n  }\n  return best;\n}',
      python: 'def length_of_longest_substring(s):\n    seen = {}\n    best = left = 0\n    for right, c in enumerate(s):\n        if c in seen and seen[c] >= left:\n            left = seen[c] + 1\n        seen[c] = right\n        best = max(best, right - left + 1)\n    return best',
      java: 'class Solution {\n    public int lengthOfLongestSubstring(String s) {\n        java.util.Map<Character,Integer> seen = new java.util.HashMap<>();\n        int best = 0, left = 0;\n        for (int right = 0; right < s.length(); right++) {\n            char c = s.charAt(right);\n            Integer prev = seen.get(c);\n            if (prev != null && prev >= left) left = prev + 1;\n            seen.put(c, right);\n            best = Math.max(best, right - left + 1);\n        }\n        return best;\n    }\n}',
      cpp: 'int lengthOfLongestSubstring(string s) {\n    std::unordered_map<char,int> seen;\n    int best = 0, left = 0;\n    for (int right = 0; right < (int)s.size(); right++) {\n      auto it = seen.find(s[right]);\n      if (it != seen.end() && it->second >= left) left = it->second + 1;\n      seen[s[right]] = right;\n      best = std::max(best, right - left + 1);\n    }\n    return best;\n}',
      go: 'func lengthOfLongestSubstring(s string) int {\n    seen := map[byte]int{}\n    best, left := 0, 0\n    for right := 0; right < len(s); right++ {\n        if prev, ok := seen[s[right]]; ok && prev >= left {\n            left = prev + 1\n        }\n        seen[s[right]] = right\n        if right-left+1 > best { best = right - left + 1 }\n    }\n    return best\n}',
    },
    complexity: { time: 'O(n)', space: 'O(min(n, 26))' },
    testCases: [
      { input: { s: 'abcabcbb' }, expected: 3, hidden: false },
      { input: { s: 'bbbbb' },    expected: 1, hidden: false },
      { input: { s: 'pwwkew' },   expected: 3, hidden: true },
    ],
  },
  {
    id: 'longest-repeating-character-replacement',
    functionName: 'characterReplacement',
    number: 17,
    title: 'Longest Repeating Character Replacement',
    difficulty: 'Medium',
    topic: 'strings',
    tags: ['sliding-window'],
    leetcodeUrl: 'https://leetcode.com/problems/longest-repeating-character-replacement/',
    description: 'You are given a string `s` and an integer `k`. You can choose any character of the string and change it to any other uppercase English character. You can perform this operation at most `k` times.\n\nReturn the length of the longest substring containing the same letter you can get after performing the above operations.',
    examples: [
      { input: 's = "ABAB", k = 2',         output: '4' },
      { input: 's = "AABABBA", k = 1',      output: '4' },
    ],
    constraints: ['1 <= s.length <= 10^5', '0 <= k <= s.length', 's consists of only uppercase English letters.'],
    hints: ['Within a window, replacements needed = windowLength − mostFrequentCharCount.'],
    approaches: [{ name: 'Sliding Window with Max Count', summary: 'Slide a window, tracking per-char counts. Window is valid if (length − maxCount) ≤ k. When it grows too big, shrink from the left.' }],
    starterCode: {
      javascript: 'function characterReplacement(s, k) {\n  // your code here\n  return 0;\n}',
      python: 'def character_replacement(s, k):\n    # your code here\n    return 0',
      java: 'class Solution {\n    public int characterReplacement(String s, int k) {\n        return 0;\n    }\n}',
      cpp: 'int characterReplacement(string s, int k) {\n    return 0;\n}',
      go: 'func characterReplacement(s string, k int) int {\n    return 0\n}',
    },
    solutions: {
      javascript: 'function characterReplacement(s, k) {\n  const counts = {};\n  let best = 0, left = 0, maxCount = 0;\n  for (let right = 0; right < s.length; right++) {\n    counts[s[right]] = (counts[s[right]] || 0) + 1;\n    maxCount = Math.max(maxCount, counts[s[right]]);\n    while (right - left + 1 - maxCount > k) {\n      counts[s[left]]--;\n      left++;\n    }\n    best = Math.max(best, right - left + 1);\n  }\n  return best;\n}',
      python: 'def character_replacement(s, k):\n    from collections import Counter\n    counts = Counter()\n    best = left = max_count = 0\n    for right, c in enumerate(s):\n        counts[c] += 1\n        if counts[c] > max_count: max_count = counts[c]\n        while right - left + 1 - max_count > k:\n            counts[s[left]] -= 1\n            left += 1\n        best = max(best, right - left + 1)\n    return best',
      java: 'class Solution {\n    public int characterReplacement(String s, int k) {\n        int[] counts = new int[26];\n        int best = 0, left = 0, maxCount = 0;\n        for (int right = 0; right < s.length(); right++) {\n            int idx = s.charAt(right) - \'A\';\n            counts[idx]++;\n            if (counts[idx] > maxCount) maxCount = counts[idx];\n            while (right - left + 1 - maxCount > k) {\n                counts[s.charAt(left) - \'A\']--;\n                left++;\n            }\n            best = Math.max(best, right - left + 1);\n        }\n        return best;\n    }\n}',
      cpp: 'int characterReplacement(string s, int k) {\n    int counts[26] = {};\n    int best = 0, left = 0, maxCount = 0;\n    for (int right = 0; right < (int)s.size(); right++) {\n      counts[s[right] - \'A\']++;\n      maxCount = std::max(maxCount, counts[s[right] - \'A\']);\n      while (right - left + 1 - maxCount > k) {\n        counts[s[left] - \'A\']--;\n        left++;\n      }\n      best = std::max(best, right - left + 1);\n    }\n    return best;\n}',
      go: 'func characterReplacement(s string, k int) int {\n    counts := [26]int{}\n    best, left, maxCount := 0, 0, 0\n    for right := 0; right < len(s); right++ {\n        counts[s[right]-\'A\']++\n        if counts[s[right]-\'A\'] > maxCount { maxCount = counts[s[right]-\'A\'] }\n        for right-left+1-maxCount > k {\n            counts[s[left]-\'A\']--\n            left++\n        }\n        if right-left+1 > best { best = right - left + 1 }\n    }\n    return best\n}',
    },
    complexity: { time: 'O(n)', space: 'O(1)' },
    testCases: [
      { input: { s: 'ABAB', k: 2 },       expected: 4, hidden: false },
      { input: { s: 'AABABBA', k: 1 },    expected: 4, hidden: false },
      { input: { s: 'AAAA', k: 0 },       expected: 4, hidden: true },
    ],
  },
  {
    id: 'minimum-window-substring',
    functionName: 'minWindow',
    number: 18,
    title: 'Minimum Window Substring',
    difficulty: 'Hard',
    topic: 'strings',
    tags: ['sliding-window', 'hash-map'],
    leetcodeUrl: 'https://leetcode.com/problems/minimum-window-substring/',
    description: 'Given two strings `s` and `t`, return the **minimum window substring** of `s` such that every character in `t` (including duplicates) is included in the window. If no such substring exists, return `""`.\n\nYou may assume the answer is unique.',
    examples: [
      { input: 's = "ADOBECODEBANC", t = "ABC"', output: '"BANC"' },
      { input: 's = "a", t = "a"', output: '"a"' },
    ],
    constraints: ['1 <= s.length, t.length <= 10^5', 's and t consist of uppercase and lowercase English letters.'],
    hints: ['Two-pointer sliding window — expand right until window is "valid", then shrink left while it stays valid.'],
    approaches: [{ name: 'Sliding Window with Need Counter', summary: 'Track how many chars of t are still missing (missing). Expand right until missing == 0, then shrink left to minimize the window, recording the smallest one.' }],
    starterCode: {
      javascript: 'function minWindow(s, t) {\n  // your code here\n  return "";\n}',
      python: 'def min_window(s, t):\n    # your code here\n    return ""',
      java: 'class Solution {\n    public String minWindow(String s, String t) {\n        return "";\n    }\n}',
      cpp: 'string minWindow(string s, string t) {\n    return "";\n}',
      go: 'func minWindow(s, t string) string {\n    return ""\n}',
    },
    solutions: {
      javascript: 'function minWindow(s, t) {\n  if (!t) return "";\n  const need = {};\n  for (const c of t) need[c] = (need[c] || 0) + 1;\n  let missing = t.length, left = 0;\n  let best = [0, Infinity];\n  for (let right = 0; right < s.length; right++) {\n    if (need[s[right]] !== undefined) {\n      if (need[s[right]] > 0) missing--;\n      need[s[right]]--;\n    }\n    while (missing === 0) {\n      if (right - left < best[1] - best[0]) best = [left, right];\n      if (need[s[left]] !== undefined) {\n        need[s[left]]++;\n        if (need[s[left]] > 0) missing++;\n      }\n      left++;\n    }\n  }\n  return best[1] === Infinity ? "" : s.slice(best[0], best[1] + 1);\n}',
      python: 'def min_window(s, t):\n    from collections import Counter\n    if not t: return ""\n    need = Counter(t)\n    missing = len(t)\n    left = 0\n    best = (0, float("inf"))\n    for right, c in enumerate(s):\n        if c in need:\n            if need[c] > 0: missing -= 1\n            need[c] -= 1\n        while missing == 0:\n            if right - left < best[1] - best[0]: best = (left, right)\n            if s[left] in need:\n                need[s[left]] += 1\n                if need[s[left]] > 0: missing += 1\n            left += 1\n    return "" if best[1] == float("inf") else s[best[0]:best[1]+1]',
      java: 'class Solution {\n    public String minWindow(String s, String t) {\n        java.util.Map<Character,Integer> need = new java.util.HashMap<>();\n        for (int i = 0; i < t.length(); i++) need.merge(t.charAt(i), 1, Integer::sum);\n        int missing = t.length(), left = 0;\n        int bl = 0, br = Integer.MAX_VALUE;\n        for (int right = 0; right < s.length(); right++) {\n            char c = s.charAt(right);\n            if (need.containsKey(c)) {\n                if (need.get(c) > 0) missing--;\n                need.put(c, need.get(c) - 1);\n            }\n            while (missing == 0) {\n                if (right - left < br - bl) { bl = left; br = right; }\n                char cl = s.charAt(left);\n                if (need.containsKey(cl)) {\n                    need.put(cl, need.get(cl) + 1);\n                    if (need.get(cl) > 0) missing++;\n                }\n                left++;\n            }\n        }\n        return br == Integer.MAX_VALUE ? "" : s.substring(bl, br + 1);\n    }\n}',
      cpp: 'string minWindow(string s, string t) {\n    if (t.empty()) return "";\n    std::unordered_map<char,int> need;\n    for (char c : t) need[c]++;\n    int missing = t.size(), left = 0, bl = 0, br = INT_MAX;\n    for (int right = 0; right < (int)s.size(); right++) {\n      if (need.count(s[right])) { if (need[s[right]] > 0) missing--; need[s[right]]--; }\n      while (missing == 0) {\n        if (right - left < br - bl) { bl = left; br = right; }\n        if (need.count(s[left])) { if (++need[s[left]] > 0) missing++; }\n        left++;\n      }\n    }\n    return br == INT_MAX ? "" : s.substr(bl, br - bl + 1);\n}',
      go: 'func minWindow(s, t string) string {\n    if len(t) == 0 { return "" }\n    need := map[byte]int{}\n    for i := 0; i < len(t); i++ { need[t[i]]++ }\n    missing, left, bl, br := len(t), 0, 0, len(s)+1\n    for right := 0; right < len(s); right++ {\n        if _, ok := need[s[right]]; ok {\n            if need[s[right]] > 0 { missing-- }\n            need[s[right]]--\n        }\n        for missing == 0 {\n            if right-left < br-bl { bl, br = left, right }\n            if _, ok := need[s[left]]; ok {\n                need[s[left]]++\n                if need[s[left]] > 0 { missing++ }\n            }\n            left++\n        }\n    }\n    if br > len(s) { return "" }\n    return s[bl : br+1]\n}',
    },
    complexity: { time: 'O(n + m)', space: 'O(m)' },
    testCases: [
      { input: { s: 'ADOBECODEBANC', t: 'ABC' }, expected: 'BANC', hidden: false },
      { input: { s: 'a', t: 'a' },               expected: 'a',    hidden: false },
      { input: { s: 'a', t: 'aa' },              expected: '',     hidden: true },
    ],
  },
  {
    id: 'valid-palindrome',
    functionName: 'isPalindrome',
    number: 19,
    title: 'Valid Palindrome',
    difficulty: 'Easy',
    topic: 'strings',
    tags: ['two-pointers'],
    leetcodeUrl: 'https://leetcode.com/problems/valid-palindrome/',
    description: 'A phrase is a **palindrome** if, after converting all uppercase letters to lowercase and removing all non-alphanumeric characters, it reads the same forward and backward.\n\nGiven a string `s`, return `true` if it is a palindrome, or `false` otherwise.',
    examples: [
      { input: 's = "A man, a plan, a canal: Panama"', output: 'true' },
      { input: 's = "race a car"',                     output: 'false' },
    ],
    constraints: ['1 <= s.length <= 2 * 10^5', 's consists only of printable ASCII characters.'],
    hints: ['Two pointers walking inward, skipping non-alphanumerics, is simpler than building a cleaned copy.'],
    approaches: [{ name: 'Two Pointers In Place', summary: 'Strip non-alphanumerics and lowercase the string, then walk left and right pointers inward, comparing characters. Mismatch ⇒ false; otherwise true.' }],
    starterCode: {
      javascript: 'function isPalindrome(s) {\n  // your code here\n  return true;\n}',
      python: 'def is_palindrome(s):\n    # your code here\n    return True',
      java: 'class Solution {\n    public boolean isPalindrome(String s) {\n        return true;\n    }\n}',
      cpp: 'bool isPalindrome(string s) {\n    return true;\n}',
      go: 'func isPalindrome(s string) bool {\n    return true\n}',
    },
    solutions: {
      javascript: 'function isPalindrome(s) {\n  const clean = s.toLowerCase().replace(/[^a-z0-9]/g, "");\n  let l = 0, r = clean.length - 1;\n  while (l < r) {\n    if (clean[l] !== clean[r]) return false;\n    l++; r--;\n  }\n  return true;\n}',
      python: 'def is_palindrome(s):\n    clean = "".join(c.lower() for c in s if c.isalnum())\n    return clean == clean[::-1]',
      java: 'class Solution {\n    public boolean isPalindrome(String s) {\n        StringBuilder sb = new StringBuilder();\n        for (int i = 0; i < s.length(); i++) {\n            char c = Character.toLowerCase(s.charAt(i));\n            if (Character.isLetterOrDigit(c)) sb.append(c);\n        }\n        String clean = sb.toString();\n        int l = 0, r = clean.length() - 1;\n        while (l < r) {\n            if (clean.charAt(l) != clean.charAt(r)) return false;\n            l++; r--;\n        }\n        return true;\n    }\n}',
      cpp: 'bool isPalindrome(string s) {\n    string clean;\n    for (char c : s) if (isalnum((unsigned char)c)) clean += tolower(c);\n    int l = 0, r = (int)clean.size() - 1;\n    while (l < r) {\n      if (clean[l] != clean[r]) return false;\n      l++; r--;\n    }\n    return true;\n}',
      go: 'func isPalindrome(s string) bool {\n    l, r := 0, len(s)-1\n    for l < r {\n        for l < r && !isAlnum(s[l]) { l++ }\n        for l < r && !isAlnum(s[r]) { r-- }\n        if tolower(s[l]) != tolower(s[r]) { return false }\n        l++; r--\n    }\n    return true\n}\nfunc isAlnum(b byte) bool { return (b >= \'a\' && b <= \'z\') || (b >= \'A\' && b <= \'Z\') || (b >= \'0\' && b <= \'9\') }',
    },
    complexity: { time: 'O(n)', space: 'O(n)' },
    testCases: [
      { input: { s: 'A man, a plan, a canal: Panama' }, expected: true,  hidden: false },
      { input: { s: 'race a car' },                     expected: false, hidden: false },
      { input: { s: ' ' },                              expected: true,  hidden: true },
    ],
  },
  {
    id: 'longest-palindromic-substring',
    functionName: 'longestPalindrome',
    number: 20,
    title: 'Longest Palindromic Substring',
    difficulty: 'Medium',
    topic: 'strings',
    tags: ['dp', 'two-pointers'],
    leetcodeUrl: 'https://leetcode.com/problems/longest-palindromic-substring/',
    description: 'Given a string `s`, return the **longest palindromic substring**.',
    examples: [
      { input: 's = "babad"', output: '"bab" or "aba"' },
      { input: 's = "cbbd"',  output: '"bb"' },
    ],
    constraints: ['1 <= s.length <= 1000', 's consists of only digits and English letters.'],
    hints: ['Try expanding around each center — every palindrome has a center (one letter or a gap between two).'],
    approaches: [{ name: 'Expand Around Center', summary: 'For each index, expand two pointers outward assuming odd-length and even-length centers. Track the longest palindrome found.' }],
    starterCode: {
      javascript: 'function longestPalindrome(s) {\n  // your code here\n  return "";\n}',
      python: 'def longest_palindrome(s):\n    # your code here\n    return ""',
      java: 'class Solution {\n    public String longestPalindrome(String s) {\n        return "";\n    }\n}',
      cpp: 'string longestPalindrome(string s) {\n    return "";\n}',
      go: 'func longestPalindrome(s string) string {\n    return ""\n}',
    },
    solutions: {
      javascript: 'function longestPalindrome(s) {\n  if (s.length < 2) return s;\n  let start = 0, maxLen = 1;\n  function expand(l, r) {\n    while (l >= 0 && r < s.length && s[l] === s[r]) {\n      if (r - l + 1 > maxLen) { maxLen = r - l + 1; start = l; }\n      l--; r++;\n    }\n  }\n  for (let i = 0; i < s.length; i++) {\n    expand(i, i);\n    expand(i, i + 1);\n  }\n  return s.slice(start, start + maxLen);\n}',
      python: 'def longest_palindrome(s):\n    if len(s) < 2: return s\n    start, max_len = 0, 1\n    def expand(l, r):\n        nonlocal start, max_len\n        while l >= 0 and r < len(s) and s[l] == s[r]:\n            if r - l + 1 > max_len:\n                max_len = r - l + 1; start = l\n            l -= 1; r += 1\n    for i in range(len(s)):\n        expand(i, i)\n        expand(i, i + 1)\n    return s[start:start+max_len]',
      java: 'class Solution {\n    int start = 0, maxLen = 1;\n    public String longestPalindrome(String s) {\n        if (s.length() < 2) return s;\n        for (int i = 0; i < s.length(); i++) {\n            expand(s, i, i);\n            expand(s, i, i + 1);\n        }\n        return s.substring(start, start + maxLen);\n    }\n    private void expand(String s, int l, int r) {\n        while (l >= 0 && r < s.length() && s.charAt(l) == s.charAt(r)) {\n            if (r - l + 1 > maxLen) { maxLen = r - l + 1; start = l; }\n            l--; r++;\n        }\n    }\n}',
      cpp: 'class Solution {\npublic:\n    int start = 0, maxLen = 1;\n    string longestPalindrome(string s) {\n        if (s.size() < 2) return s;\n        for (int i = 0; i < (int)s.size(); i++) { expand(s, i, i); expand(s, i, i + 1); }\n        return s.substr(start, maxLen);\n    }\nprivate:\n    void expand(const string& s, int l, int r) {\n        while (l >= 0 && r < (int)s.size() && s[l] == s[r]) {\n            if (r - l + 1 > maxLen) { maxLen = r - l + 1; start = l; }\n            l--; r++;\n        }\n    }\n};',
      go: 'func longestPalindrome(s string) string {\n    if len(s) < 2 { return s }\n    start, maxLen := 0, 1\n    expand := func(l, r int) {\n        for l >= 0 && r < len(s) && s[l] == s[r] {\n            if r-l+1 > maxLen { maxLen, start = r-l+1, l }\n            l--; r++\n        }\n    }\n    for i := 0; i < len(s); i++ {\n        expand(i, i)\n        expand(i, i+1)\n    }\n    return s[start : start+maxLen]\n}',
    },
    complexity: { time: 'O(n^2)', space: 'O(1)' },
    testCases: [
      { input: { s: 'babad' }, expected: 'bab', hidden: false },
      { input: { s: 'cbbd' },  expected: 'bb',  hidden: false },
      { input: { s: 'a' },     expected: 'a',   hidden: true },
    ],
  },
  {
    id: 'palindromic-substrings',
    functionName: 'countSubstrings',
    number: 21,
    title: 'Palindromic Substrings',
    difficulty: 'Medium',
    topic: 'strings',
    tags: ['dp', 'two-pointers'],
    leetcodeUrl: 'https://leetcode.com/problems/palindromic-substrings/',
    description: 'Given a string `s`, return the **number of palindromic substrings** in it.\n\nA string is a palindrome when it reads the same backward as forward. Substrings are contiguous.',
    examples: [
      { input: 's = "abc"', output: '3', explanation: '"a", "b", "c"' },
      { input: 's = "aaa"', output: '6', explanation: '"a","a","a","aa","aa","aaa"' },
    ],
    constraints: ['1 <= s.length <= 1000', 's consists of lowercase English letters.'],
    hints: ['Same trick: expand around each center, counting how many palindromes you find.'],
    approaches: [{ name: 'Expand Around Center', summary: 'For each index, expand odd- and even-length palindromes. Each successful expansion is a new palindromic substring — increment a counter.' }],
    starterCode: {
      javascript: 'function countSubstrings(s) {\n  // your code here\n  return 0;\n}',
      python: 'def count_substrings(s):\n    # your code here\n    return 0',
      java: 'class Solution {\n    public int countSubstrings(String s) {\n        return 0;\n    }\n}',
      cpp: 'int countSubstrings(string s) {\n    return 0;\n}',
      go: 'func countSubstrings(s string) int {\n    return 0\n}',
    },
    solutions: {
      javascript: 'function countSubstrings(s) {\n  let count = 0;\n  function expand(l, r) {\n    while (l >= 0 && r < s.length && s[l] === s[r]) { count++; l--; r++; }\n  }\n  for (let i = 0; i < s.length; i++) { expand(i, i); expand(i, i + 1); }\n  return count;\n}',
      python: 'def count_substrings(s):\n    count = 0\n    def expand(l, r):\n        nonlocal count\n        while l >= 0 and r < len(s) and s[l] == s[r]:\n            count += 1; l -= 1; r += 1\n    for i in range(len(s)):\n        expand(i, i)\n        expand(i, i + 1)\n    return count',
      java: 'class Solution {\n    int count = 0;\n    public int countSubstrings(String s) {\n        for (int i = 0; i < s.length(); i++) {\n            expand(s, i, i);\n            expand(s, i, i + 1);\n        }\n        return count;\n    }\n    private void expand(String s, int l, int r) {\n        while (l >= 0 && r < s.length() && s.charAt(l) == s.charAt(r)) { count++; l--; r++; }\n    }\n}',
      cpp: 'class Solution {\n    int count = 0;\npublic:\n    int countSubstrings(string s) {\n        for (int i = 0; i < (int)s.size(); i++) { expand(s, i, i); expand(s, i, i + 1); }\n        return count;\n    }\nprivate:\n    void expand(const string& s, int l, int r) {\n        while (l >= 0 && r < (int)s.size() && s[l] == s[r]) { count++; l--; r++; }\n    }\n};',
      go: 'func countSubstrings(s string) int {\n    count := 0\n    expand := func(l, r int) {\n        for l >= 0 && r < len(s) && s[l] == s[r] { count++; l--; r++ }\n    }\n    for i := 0; i < len(s); i++ { expand(i, i); expand(i, i+1) }\n    return count\n}',
    },
    complexity: { time: 'O(n^2)', space: 'O(1)' },
    testCases: [
      { input: { s: 'abc' }, expected: 3, hidden: false },
      { input: { s: 'aaa' }, expected: 6, hidden: false },
      { input: { s: 'abba' }, expected: 6, hidden: true },
    ],
  },
];