// Seed: 3 trie problems (#64-66)

export const TRIES_PROBLEMS = [
  {
    id: 'implement-trie-prefix-tree',
    number: 64,
    title: 'Implement Trie (Prefix Tree)',
    difficulty: 'Medium',
    topic: 'tries',
    tags: ['trie', 'design', 'string'],
    leetcodeUrl: 'https://leetcode.com/problems/implement-trie-prefix-tree/',
    description: 'A **trie** (pronounced "try") or prefix tree is a tree data structure used to efficiently store and retrieve keys in a dataset of strings. There are various applications of this data structure, such as autocomplete and spellchecker.\\n\\nImplement the `Trie` class:\\n\\n- `Trie()` initializes the trie object.\\n- `void insert(String word)` inserts the string `word` into the trie.\\n- `boolean search(String word)` returns `true` if the string `word` is in the trie (i.e., was inserted before), and `false` otherwise.\\n- `boolean startsWith(String prefix)` returns `true` if there is a previously inserted string `word` that has the prefix `prefix`, and `false` otherwise.',
    examples: [
      { input: 'ops = ["Trie()","insert(\\"apple\\")","search(\\"apple\\")","search(\\"app\\")","startsWith(\\"app\\")","insert(\\"app\\")","search(\\"app\\")"]', output: '[null,null,true,false,true,null,true]' },
    ],
    constraints: ['1 <= word.length, prefix.length <= 2000', 'word and prefix consist only of lowercase English letters.', 'At most 3 * 10^4 calls to insert, search, and startsWith.'],
    hints: ['Each node holds a 26-element array of child pointers plus an `isEnd` flag.'],
    approaches: [{ name: 'Node Tree', summary: 'Each node has children (map char → node) and a flag isEnd. insert walks/creates nodes and sets the flag. search walks and checks the flag; startsWith just walks without the flag check.' }],
    starterCode: {
      javascript: 'class Trie {\n  constructor() {}\n  insert(word) {}\n  search(word) { return false; }\n  startsWith(prefix) { return false; }\n}',
      python: 'class Trie:\n    def __init__(self):\n        pass\n    def insert(self, word: str) -> None:\n        pass\n    def search(self, word: str) -> bool:\n        return False\n    def startsWith(self, prefix: str) -> bool:\n        return False',
      java: 'class Trie {\n    public Trie() {}\n    public void insert(String word) {}\n    public boolean search(String word) { return false; }\n    public boolean startsWith(String prefix) { return false; }\n}',
      cpp: 'class Trie {\npublic:\n    Trie() {}\n    void insert(string word) {}\n    bool search(string word) { return false; }\n    bool startsWith(string prefix) { return false; }\n};',
      go: 'type Trie struct{}\nfunc Constructor() Trie { return Trie{} }\nfunc (this *Trie) Insert(word string) {}\nfunc (this *Trie) Search(word string) bool { return false }\nfunc (this *Trie) StartsWith(prefix string) bool { return false }',
    },
    solutions: {
      javascript: 'class Trie {\n  constructor() { this.root = {}; }\n  insert(word) {\n    let n = this.root;\n    for (const c of word) { if (!n[c]) n[c] = {}; n = n[c]; }\n    n.isEnd = true;\n  }\n  _walk(prefix) {\n    let n = this.root;\n    for (const c of prefix) { if (!n[c]) return null; n = n[c]; }\n    return n;\n  }\n  search(word) { const n = this._walk(word); return n !== null && n.isEnd === true; }\n  startsWith(prefix) { return this._walk(prefix) !== null; }\n}',
      python: 'class Trie:\n    def __init__(self):\n        self.root = {}\n    def insert(self, word):\n        n = self.root\n        for c in word:\n            n = n.setdefault(c, {})\n        n["isEnd"] = True\n    def _walk(self, s):\n        n = self.root\n        for c in s:\n            if c not in n: return None\n            n = n[c]\n        return n\n    def search(self, word):\n        n = self._walk(word)\n        return n is not None and n.get("isEnd", False)\n    def startsWith(self, prefix):\n        return self._walk(prefix) is not None',
      java: 'class Trie {\n    private final TrieNode root = new TrieNode();\n    private static class TrieNode { TrieNode[] children = new TrieNode[26]; boolean isEnd; }\n    public void insert(String word) {\n        TrieNode n = root;\n        for (char c : word.toCharArray()) {\n            int i = c - "a".charAt(0);\n            if (n.children[i] == null) n.children[i] = new TrieNode();\n            n = n.children[i];\n        }\n        n.isEnd = true;\n    }\n    private TrieNode walk(String s) {\n        TrieNode n = root;\n        for (char c : s.toCharArray()) {\n            int i = c - "a".charAt(0);\n            if (n.children[i] == null) return null;\n            n = n.children[i];\n        }\n        return n;\n    }\n    public boolean search(String word) { TrieNode n = walk(word); return n != null && n.isEnd; }\n    public boolean startsWith(String prefix) { return walk(prefix) != null; }\n}',
      cpp: 'class Trie {\npublic:\n    Trie() : root(new TrieNode()) {}\n    void insert(string word) {\n        TrieNode* n = root;\n        for (char c : word) {\n            if (!n->children[c - "a"[0]]) n->children[c - "a"[0]] = new TrieNode();\n            n = n->children[c - "a"[0]];\n        }\n        n->isEnd = true;\n    }\n    bool search(string word) { TrieNode* n = walk(word); return n && n->isEnd; }\n    bool startsWith(string prefix) { return walk(prefix) != nullptr; }\nprivate:\n    struct TrieNode { TrieNode* children[26] = {}; bool isEnd = false; };\n    TrieNode* root;\n    TrieNode* walk(string s) {\n        TrieNode* n = root;\n        for (char c : s) {\n            if (!n->children[c - "a"[0]]) return nullptr;\n            n = n->children[c - "a"[0]];\n        }\n        return n;\n    }\n};',
      go: 'type TrieNode struct {\n    children map[byte]*TrieNode\n    isEnd bool\n}\ntype Trie struct { root *TrieNode }\nfunc Constructor() Trie {\n    return Trie{root: &TrieNode{children: map[byte]*TrieNode{}}}\n}\nfunc (this *Trie) walk(s string) *TrieNode {\n    n := this.root\n    for i := 0; i < len(s); i++ {\n        if n.children[s[i]] == nil { return nil }\n        n = n.children[s[i]]\n    }\n    return n\n}\nfunc (this *Trie) Insert(word string) {\n    n := this.root\n    for i := 0; i < len(word); i++ {\n        if n.children[word[i]] == nil { n.children[word[i]] = &TrieNode{children: map[byte]*TrieNode{}} }\n        n = n.children[word[i]]\n    }\n    n.isEnd = true\n}\nfunc (this *Trie) Search(word string) bool { n := this.walk(word); return n != nil && n.isEnd }\nfunc (this *Trie) StartsWith(prefix string) bool { return this.walk(prefix) != nil }',
    },
    complexity: { time: 'O(L) per operation, where L = word length', space: 'O(total chars)' },
    testCases: [
      { input: { op: 'search', word: 'apple', afterInserts: ['apple'] },                expected: true,  hidden: false },
      { input: { op: 'search', word: 'app',   afterInserts: ['apple'] },                expected: false, hidden: false },
      { input: { op: 'startsWith', prefix: 'app', afterInserts: ['apple','app'] },      expected: true,  hidden: true },
    ],
  },
  {
    id: 'design-add-and-search-words-data-structure',
    number: 65,
    title: 'Design Add and Search Words Data Structure',
    difficulty: 'Medium',
    topic: 'tries',
    tags: ['trie', 'design', 'string', 'dfs', 'bfs'],
    leetcodeUrl: 'https://leetcode.com/problems/design-add-and-search-words-data-structure/',
    description: 'Design a data structure that supports adding new words and finding if a string matches any previously added string.\\n\\nImplement the `WordDictionary` class:\\n\\n- `WordDictionary()` initializes the object.\\n- `void addWord(word)` adds `word` to the data structure.\\n- `bool search(word)` returns `true` if there is any string in the data structure that matches `word`, or `false` otherwise. The `word` may contain dots `.` where dots can be matched with any letter.',
    examples: [
      { input: 'ops = ["WordDictionary","addWord(\\"bad\\")","addWord(\\"dad\\")","addWord(\\"mad\\")","search(\\"pad\\")","search(\\"bad\\")","search(\\".ad\\")","search(\\"b..\\")"]', output: '[null,null,null,null,false,true,true,true]' },
    ],
    constraints: ['1 <= word.length <= 25', 'word in addWord consists of lowercase English letters.', 'word in search consists of "." or lowercase English letters.', 'At most 10^4 calls to addWord and search.'],
    hints: ['A `.` in the search pattern forces you to try every child of the current trie node.'],
    approaches: [{ name: 'Trie + DFS', summary: 'Insert normally. For search, walk the trie: if the char is `.`, recurse on every child; otherwise match exactly. If the walk exhausts the word and we are at an isEnd node, return true.' }],
    starterCode: {
      javascript: 'class WordDictionary {\n  constructor() {}\n  addWord(word) {}\n  search(word) { return false; }\n}',
      python: 'class WordDictionary:\n    def __init__(self):\n        pass\n    def addWord(self, word: str) -> None:\n        pass\n    def search(self, word: str) -> bool:\n        return False',
      java: 'class WordDictionary {\n    public WordDictionary() {}\n    public void addWord(String word) {}\n    public boolean search(String word) { return false; }\n}',
      cpp: 'class WordDictionary {\npublic:\n    WordDictionary() {}\n    void addWord(string word) {}\n    bool search(string word) { return false; }\n};',
      go: 'type WordDictionary struct{}\nfunc Constructor() WordDictionary { return WordDictionary{} }\nfunc (this *WordDictionary) AddWord(word string) {}\nfunc (this *WordDictionary) Search(word string) bool { return false }',
    },
    solutions: {
      javascript: 'class WordDictionary {\n  constructor() { this.root = {}; }\n  addWord(word) {\n    let n = this.root;\n    for (const c of word) { if (!n[c]) n[c] = {}; n = n[c]; }\n    n.isEnd = true;\n  }\n  search(word) {\n    const dfs = (n, i) => {\n      if (i === word.length) return n.isEnd === true;\n      const c = word[i];\n      if (c === ".") { for (const k in n) if (k !== "isEnd" && dfs(n[k], i + 1)) return true; return false; }\n      return n[c] ? dfs(n[c], i + 1) : false;\n    };\n    return dfs(this.root, 0);\n  }\n}',
      python: 'class WordDictionary:\n    def __init__(self):\n        self.root = {}\n    def addWord(self, word):\n        n = self.root\n        for c in word:\n            n = n.setdefault(c, {})\n        n["isEnd"] = True\n    def search(self, word):\n        def dfs(n, i):\n            if i == len(word): return n.get("isEnd", False)\n            c = word[i]\n            if c == ".":\n                for k, v in n.items():\n                    if k != "isEnd" and dfs(v, i + 1): return True\n                return False\n            return c in n and dfs(n[c], i + 1)\n        return dfs(self.root, 0)',
      java: 'class WordDictionary {\n    private static class Node { Node[] children = new Node[26]; boolean isEnd; }\n    private final Node root = new Node();\n    public void addWord(String word) {\n        Node n = root;\n        for (char c : word.toCharArray()) {\n            int i = c - "a".charAt(0);\n            if (n.children[i] == null) n.children[i] = new Node();\n            n = n.children[i];\n        }\n        n.isEnd = true;\n    }\n    public boolean search(String word) { return dfs(root, word, 0); }\n    private boolean dfs(Node n, String w, int i) {\n        if (i == w.length()) return n.isEnd;\n        char c = w.charAt(i);\n        if (c == ".") {\n            for (Node child : n.children) if (child != null && dfs(child, w, i + 1)) return true;\n            return false;\n        }\n        int idx = c - "a".charAt(0);\n        return n.children[idx] != null && dfs(n.children[idx], w, i + 1);\n    }\n}',
      cpp: 'class WordDictionary {\npublic:\n    void addWord(string word) {\n        Node* n = root;\n        for (char c : word) {\n            if (!n->children[c - "a"[0]]) n->children[c - "a"[0]] = new Node();\n            n = n->children[c - "a"[0]];\n        }\n        n->isEnd = true;\n    }\n    bool search(string word) { return dfs(root, word, 0); }\nprivate:\n    struct Node { Node* children[26] = {}; bool isEnd = false; };\n    Node* root = new Node();\n    bool dfs(Node* n, const string& w, int i) {\n        if (i == (int)w.size()) return n->isEnd;\n        if (w[i] == ".") {\n            for (Node* c : n->children) if (c && dfs(c, w, i + 1)) return true;\n            return false;\n        }\n        Node* c = n->children[w[i] - "a"[0]];\n        return c && dfs(c, w, i + 1);\n    }\n};',
      go: 'type wNode struct { children map[byte]*wNode; isEnd bool }\ntype WordDictionary struct { root *wNode }\nfunc Constructor() WordDictionary {\n    return WordDictionary{root: &wNode{children: map[byte]*wNode{}}}\n}\nfunc (this *WordDictionary) AddWord(word string) {\n    n := this.root\n    for i := 0; i < len(word); i++ {\n        if n.children[word[i]] == nil { n.children[word[i]] = &wNode{children: map[byte]*wNode{}} }\n        n = n.children[word[i]]\n    }\n    n.isEnd = true\n}\nfunc (this *WordDictionary) Search(word string) bool {\n    var dfs func(n *wNode, i int) bool\n    dfs = func(n *wNode, i int) bool {\n        if i == len(word) { return n.isEnd }\n        c := word[i]\n        if c == "." {\n            for _, child := range n.children { if dfs(child, i+1) { return true } }\n            return false\n        }\n        return n.children[c] != nil && dfs(n.children[c], i+1)\n    }\n    return dfs(this.root, 0)\n}',
    },
    complexity: { time: 'O(L) insert, O(26^D · L) worst-case search (D = #dots)', space: 'O(total chars)' },
    testCases: [
      { input: { op: 'search', word: 'pad', afterInserts: ['bad','dad','mad'] }, expected: false, hidden: false },
      { input: { op: 'search', word: '.ad', afterInserts: ['bad','dad','mad'] }, expected: true,  hidden: false },
      { input: { op: 'search', word: 'b..', afterInserts: ['bad','dad','mad'] }, expected: true,  hidden: true },
    ],
  },
  {
    id: 'word-search-ii',
    functionName: 'findWords',
    number: 66,
    title: 'Word Search II',
    difficulty: 'Hard',
    topic: 'tries',
    tags: ['trie', 'array', 'string', 'backtracking', 'matrix'],
    leetcodeUrl: 'https://leetcode.com/problems/word-search-ii/',
    description: 'Given an `m x n` `board` of characters and a list of strings `words`, return **all words on the board**.\\n\\nEach word must be constructed from letters of sequentially adjacent cells (horizontally or vertically). The same cell may not be used more than once in a word.',
    examples: [
      { input: 'board = [["o","a","a","n"],["e","t","a","e"],["i","h","k","r"],["i","f","l","v"]], words = ["oath","pea","eat","rain"]', output: '["eat","oath"]' },
    ],
    constraints: ['m == board.length', 'n == board[i].length', '1 <= m, n <= 12', '1 <= words.length <= 3 * 10^4', '1 <= words[i].length <= 10'],
    hints: ['Build a trie of all words, then DFS from each cell. Prune a trie branch when it no longer has any matches.'],
    approaches: [{ name: 'Trie + DFS + Pruning', summary: 'Build a trie of all words. From each cell, DFS in 4 directions, walking the trie. When a node is exhausted, prune it. Collect any word whose terminal node is reached.' }],
    starterCode: {
      javascript: 'function findWords(board, words) {\n  // your code here\n  return [];\n}',
      python: 'def find_words(board, words):\n    # your code here\n    return []',
      java: 'class Solution {\n    public java.util.List<String> findWords(char[][] board, String[] words) {\n        return new java.util.ArrayList<>();\n    }\n}',
      cpp: 'vector<string> findWords(vector<vector<char>>& board, vector<string>& words) {\n    return {};\n}',
      go: 'func findWords(board [][]byte, words []string) []string {\n    return nil\n}',
    },
    solutions: {
      javascript: 'function findWords(board, words) {\n  const root = {};\n  for (const w of words) { let n = root; for (const c of w) n = (n[c] = n[c] || {}); n.word = w; }\n  const out = [], R = board.length, C = board[0].length;\n  const dfs = (n, r, c) => {\n    const ch = board[r][c];\n    if (!n[ch]) return;\n    n = n[ch];\n    if (n.word) { out.push(n.word); n.word = null; }\n    board[r][c] = "#";\n    if (r > 0) dfs(n, r - 1, c);\n    if (c > 0) dfs(n, r, c - 1);\n    if (r + 1 < R) dfs(n, r + 1, c);\n    if (c + 1 < C) dfs(n, r, c + 1);\n    board[r][c] = ch;\n  };\n  for (let r = 0; r < R; r++) for (let c = 0; c < C; c++) dfs(root, r, c);\n  return out;\n}',
      python: 'def find_words(board, words):\n    root = {}\n    for w in words:\n        n = root\n        for c in w: n = n.setdefault(c, {})\n        n["word"] = w\n    R, C = len(board), len(board[0])\n    out = []\n    def dfs(n, r, c):\n        ch = board[r][c]\n        if ch not in n: return\n        n = n[ch]\n        if "word" in n: out.append(n.pop("word"))\n        board[r][c] = "#"\n        for dr, dc in ((-1,0),(1,0),(0,-1),(0,1)):\n            nr, nc = r + dr, c + dc\n            if 0 <= nr < R and 0 <= nc < C and board[nr][nc] != "#": dfs(n, nr, nc)\n        board[r][c] = ch\n    for r in range(R):\n        for c in range(C): dfs(root, r, c)\n    return out',
      java: 'class Solution {\n    private static class Node { Node[] children = new Node[26]; String word; }\n    public java.util.List<String> findWords(char[][] board, String[] words) {\n        Node root = new Node();\n        for (String w : words) {\n            Node n = root;\n            for (char c : w.toCharArray()) {\n                int i = c - "a".charAt(0);\n                if (n.children[i] == null) n.children[i] = new Node();\n                n = n.children[i];\n            }\n            n.word = w;\n        }\n        java.util.List<String> out = new java.util.ArrayList<>();\n        int R = board.length, C = board[0].length;\n        for (int r = 0; r < R; r++)\n            for (int c = 0; c < C; c++) dfs(board, root, r, c, out);\n        return out;\n    }\n    private void dfs(char[][] b, Node n, int r, int c, java.util.List<String> out) {\n        char ch = b[r][c];\n        if (ch == "#" || n.children[ch - "a".charAt(0)] == null) return;\n        n = n.children[ch - "a".charAt(0)];\n        if (n.word != null) { out.add(n.word); n.word = null; }\n        b[r][c] = "#";\n        int R = b.length, C = b[0].length;\n        if (r > 0) dfs(b, n, r - 1, c, out);\n        if (c > 0) dfs(b, n, r, c - 1, out);\n        if (r + 1 < R) dfs(b, n, r + 1, c, out);\n        if (c + 1 < C) dfs(b, n, r, c + 1, out);\n        b[r][c] = ch;\n    }\n}',
      cpp: 'vector<string> findWords(vector<vector<char>>& board, vector<string>& words) {\n    struct Node { Node* children[26] = {}; string word; };\n    Node* root = new Node();\n    for (auto& w : words) {\n        Node* n = root;\n        for (char c : w) {\n            if (!n->children[c - "a"[0]]) n->children[c - "a"[0]] = new Node();\n            n = n->children[c - "a"[0]];\n        }\n        n->word = w;\n    }\n    int R = board.size(), C = board[0].size();\n    vector<string> out;\n    function<void(Node*, int, int)> dfs = [&](Node* n, int r, int c) {\n        char ch = board[r][c];\n        if (ch == "#" || !n->children[ch - "a"[0]]) return;\n        n = n->children[ch - "a"[0]];\n        if (!n->word.empty()) { out.push_back(n->word); n->word.clear(); }\n        board[r][c] = "#";\n        if (r > 0) dfs(n, r - 1, c);\n        if (c > 0) dfs(n, r, c - 1);\n        if (r + 1 < R) dfs(n, r + 1, c);\n        if (c + 1 < C) dfs(n, r, c + 1);\n        board[r][c] = ch;\n    };\n    for (int r = 0; r < R; r++) for (int c = 0; c < C; c++) dfs(root, r, c);\n    return out;\n}',
      go: 'type tNode struct { children map[byte]*tNode; word string }\nfunc findWords(board [][]byte, words []string) []string {\n    root := &tNode{children: map[byte]*tNode{}}\n    for _, w := range words {\n        n := root\n        for i := 0; i < len(w); i++ {\n            if n.children[w[i]] == nil { n.children[w[i]] = &tNode{children: map[byte]*tNode{}} }\n            n = n.children[w[i]]\n        }\n        n.word = w\n    }\n    R, C := len(board), len(board[0])\n    out := []string{}\n    var dfs func(n *tNode, r, c int)\n    dfs = func(n *tNode, r, c int) {\n        ch := board[r][c]\n        if ch == "#" { return }\n        if n.children[ch] == nil { return }\n        n = n.children[ch]\n        if n.word != "" { out = append(out, n.word); n.word = "" }\n        board[r][c] = "#"\n        if r > 0 { dfs(n, r-1, c) }\n        if c > 0 { dfs(n, r, c-1) }\n        if r+1 < R { dfs(n, r+1, c) }\n        if c+1 < C { dfs(n, r, c+1) }\n        board[r][c] = ch\n    }\n    for r := 0; r < R; r++ { for c := 0; c < C; c++ { dfs(root, r, c) } }\n    return out\n}',
    },
    complexity: { time: 'O(m · n · 4^L) worst case, pruned heavily by trie', space: 'O(sum of word lengths)' },
    testCases: [
      { input: { board: [['o','a','a','n'],['e','t','a','e'],['i','h','k','r'],['i','f','l','v']], words: ['oath','pea','eat','rain'] }, expected: ['eat','oath'], hidden: false },
      { input: { board: [['a','b'],['c','d']], words: ['abcb'] },                                       expected: [],          hidden: false },
      { input: { board: [['a']], words: ['a'] },                                                       expected: ['a'],        hidden: true },
    ],
  },
];
