// Seed: 5 intervals problems (#53-57)

export const INTERVALS_PROBLEMS = [
  {
    id: 'insert-interval',
    functionName: 'insert',
    number: 53,
    title: 'Insert Interval',
    difficulty: 'Medium',
    topic: 'intervals',
    tags: ['intervals', 'array'],
    leetcodeUrl: 'https://leetcode.com/problems/insert-interval/',
    description: 'You are given an array of non-overlapping intervals `intervals` where `intervals[i] = [start_i, end_i]` and is sorted by `start_i`. You are also given an interval `newInterval = [start, end]`.\\n\\nInsert `newInterval` into `intervals` so that `intervals` is still sorted and contains no overlaps (merge if necessary). Return `intervals` after the insertion.',
    examples: [
      { input: 'intervals = [[1,3],[6,9]], newInterval = [2,5]', output: '[[1,5],[6,9]]' },
      { input: 'intervals = [[1,2],[3,5],[6,7],[8,10],[12,16]], newInterval = [4,8]', output: '[[1,2],[3,8],[9,10],[12,16]]', explanation: 'newInterval overlaps [3,5],[6,7],[8,10]' },
    ],
    constraints: ['0 <= intervals.length <= 10^4', 'intervals[i].length == 2', '0 <= start_i <= end_i <= 10^5', 'intervals is sorted by start_i and non-overlapping.'],
    hints: ['Three linear phases: add intervals ending before newInterval, merge all that overlap, then add the rest.'],
    approaches: [{ name: 'Linear Merge', summary: 'Walk once. Append every interval that ends before newInterval starts. Then merge every interval that overlaps newInterval (expand newInterval). Finally append the remainder.' }],
    starterCode: {
      javascript: 'function insert(intervals, newInterval) {\n  // your code here\n  return [];\n}',
      python: 'def insert(intervals, newInterval):\n    # your code here\n    return []',
      java: 'class Solution {\n    public int[][] insert(int[][] intervals, int[] newInterval) {\n        return new int[0][0];\n    }\n}',
      cpp: 'vector<vector<int>> insert(vector<vector<int>>& intervals, vector<int>& newInterval) {\n    return {};\n}',
      go: 'func insert(intervals [][]int, newInterval []int) [][]int {\n    return nil\n}',
    },
    solutions: {
      javascript: 'function insert(intervals, newInterval) {\n  const res = [];\n  let i = 0, n = intervals.length;\n  while (i < n && intervals[i][1] < newInterval[0]) res.push(intervals[i++]);\n  let [s, e] = newInterval;\n  while (i < n && intervals[i][0] <= e) { s = Math.min(s, intervals[i][0]); e = Math.max(e, intervals[i][1]); i++; }\n  res.push([s, e]);\n  while (i < n) res.push(intervals[i++]);\n  return res;\n}',
      python: 'def insert(intervals, newInterval):\n    res, i, n = [], 0, len(intervals)\n    while i < n and intervals[i][1] < newInterval[0]:\n        res.append(intervals[i]); i += 1\n    s, e = newInterval\n    while i < n and intervals[i][0] <= e:\n        s = min(s, intervals[i][0]); e = max(e, intervals[i][1]); i += 1\n    res.append([s, e])\n    while i < n: res.append(intervals[i]); i += 1\n    return res',
      java: 'class Solution {\n    public int[][] insert(int[][] intervals, int[] newInterval) {\n        java.util.List<int[]> res = new java.util.ArrayList<>();\n        int i = 0, n = intervals.length;\n        while (i < n && intervals[i][1] < newInterval[0]) { res.add(intervals[i++]); }\n        int s = newInterval[0], e = newInterval[1];\n        while (i < n && intervals[i][0] <= e) {\n            s = Math.min(s, intervals[i][0]); e = Math.max(e, intervals[i][1]); i++;\n        }\n        res.add(new int[]{s, e});\n        while (i < n) res.add(intervals[i++]);\n        return res.toArray(new int[0][]);\n    }\n}',
      cpp: 'vector<vector<int>> insert(vector<vector<int>>& intervals, vector<int>& newInterval) {\n    vector<vector<int>> res;\n    int i = 0, n = intervals.size();\n    while (i < n && intervals[i][1] < newInterval[0]) res.push_back(intervals[i++]);\n    int s = newInterval[0], e = newInterval[1];\n    while (i < n && intervals[i][0] <= e) {\n        s = std::min(s, intervals[i][0]); e = std::max(e, intervals[i][1]); i++;\n    }\n    res.push_back({s, e});\n    while (i < n) res.push_back(intervals[i++]);\n    return res;\n}',
      go: 'func insert(intervals [][]int, newInterval []int) [][]int {\n    res := [][]int{}\n    i, n := 0, len(intervals)\n    for i < n && intervals[i][1] < newInterval[0] {\n        res = append(res, intervals[i]); i++\n    }\n    s, e := newInterval[0], newInterval[1]\n    for i < n && intervals[i][0] <= e {\n        if intervals[i][0] < s { s = intervals[i][0] }\n        if intervals[i][1] > e { e = intervals[i][1] }\n        i++\n    }\n    res = append(res, []int{s, e})\n    for i < n { res = append(res, intervals[i]); i++ }\n    return res\n}',
    },
    complexity: { time: 'O(n)', space: 'O(n) for output' },
    testCases: [
      { input: { intervals: [[1,3],[6,9]], newInterval: [2,5] },                       expected: [[1,5],[6,9]],            hidden: false },
      { input: { intervals: [[1,2],[3,5],[6,7],[8,10],[12,16]], newInterval: [4,8] },  expected: [[1,2],[3,8],[9,10],[12,16]], hidden: false },
      { input: { intervals: [], newInterval: [5,7] },                                  expected: [[5,7]],                   hidden: true },
    ],
  },
  {
    id: 'merge-intervals',
    functionName: 'merge',
    number: 54,
    title: 'Merge Intervals',
    difficulty: 'Medium',
    topic: 'intervals',
    tags: ['intervals', 'array', 'sorting'],
    leetcodeUrl: 'https://leetcode.com/problems/merge-intervals/',
    description: 'Given an array of `intervals` where `intervals[i] = [start_i, end_i]`, merge all overlapping intervals and return an array of the non-overlapping intervals that cover all the input.',
    examples: [
      { input: 'intervals = [[1,3],[2,6],[8,10],[15,18]]', output: '[[1,6],[8,10],[15,18]]' },
      { input: 'intervals = [[1,4],[4,5]]',                output: '[[1,5]]' },
    ],
    constraints: ['1 <= intervals.length <= 10^4', 'intervals[i].length == 2', '0 <= start_i <= end_i <= 10^4'],
    hints: ['Sort by start, then sweep and merge whenever the next interval starts before the current end.'],
    approaches: [{ name: 'Sort + Sweep', summary: 'Sort intervals by start. Walk left-to-right, extending the current merged interval as long as the next one overlaps; otherwise push and start a new one.' }],
    starterCode: {
      javascript: 'function merge(intervals) {\n  // your code here\n  return [];\n}',
      python: 'def merge(intervals):\n    # your code here\n    return []',
      java: 'class Solution {\n    public int[][] merge(int[][] intervals) {\n        return new int[0][0];\n    }\n}',
      cpp: 'vector<vector<int>> merge(vector<vector<int>>& intervals) {\n    return {};\n}',
      go: 'func merge(intervals [][]int) [][]int {\n    return nil\n}',
    },
    solutions: {
      javascript: 'function merge(intervals) {\n  intervals.sort((a, b) => a[0] - b[0]);\n  const res = [];\n  for (const iv of intervals) {\n    if (res.length === 0 || iv[0] > res[res.length - 1][1]) res.push(iv);\n    else res[res.length - 1][1] = Math.max(res[res.length - 1][1], iv[1]);\n  }\n  return res;\n}',
      python: 'def merge(intervals):\n    intervals.sort(key=lambda x: x[0])\n    res = []\n    for iv in intervals:\n        if not res or iv[0] > res[-1][1]: res.append(iv)\n        else: res[-1][1] = max(res[-1][1], iv[1])\n    return res',
      java: 'class Solution {\n    public int[][] merge(int[][] intervals) {\n        java.util.Arrays.sort(intervals, (a, b) -> Integer.compare(a[0], b[0]));\n        java.util.List<int[]> res = new java.util.ArrayList<>();\n        for (int[] iv : intervals) {\n            if (res.isEmpty() || iv[0] > res.get(res.size() - 1)[1]) res.add(iv);\n            else res.get(res.size() - 1)[1] = Math.max(res.get(res.size() - 1)[1], iv[1]);\n        }\n        return res.toArray(new int[0][]);\n    }\n}',
      cpp: 'vector<vector<int>> merge(vector<vector<int>>& intervals) {\n    sort(intervals.begin(), intervals.end());\n    vector<vector<int>> res;\n    for (auto& iv : intervals) {\n        if (res.empty() || iv[0] > res.back()[1]) res.push_back(iv);\n        else res.back()[1] = std::max(res.back()[1], iv[1]);\n    }\n    return res;\n}',
      go: 'func merge(intervals [][]int) [][]int {\n    sort.Slice(intervals, func(i, j int) bool { return intervals[i][0] < intervals[j][0] })\n    res := [][]int{}\n    for _, iv := range intervals {\n        if len(res) == 0 || iv[0] > res[len(res)-1][1] {\n            res = append(res, iv)\n        } else if iv[1] > res[len(res)-1][1] {\n            res[len(res)-1][1] = iv[1]\n        }\n    }\n    return res\n}',
    },
    complexity: { time: 'O(n log n)', space: 'O(n) for output' },
    testCases: [
      { input: { intervals: [[1,3],[2,6],[8,10],[15,18]] }, expected: [[1,6],[8,10],[15,18]], hidden: false },
      { input: { intervals: [[1,4],[4,5]] },                expected: [[1,5]],               hidden: false },
      { input: { intervals: [[1,4],[2,3]] },                expected: [[1,4]],               hidden: true },
    ],
  },
  {
    id: 'non-overlapping-intervals',
    functionName: 'eraseOverlapIntervals',
    number: 55,
    title: 'Non-overlapping Intervals',
    difficulty: 'Medium',
    topic: 'intervals',
    tags: ['intervals', 'array', 'greedy', 'sorting'],
    leetcodeUrl: 'https://leetcode.com/problems/non-overlapping-intervals/',
    description: 'Given an array of intervals `intervals` where `intervals[i] = [start_i, end_i]`, return the **minimum number of intervals** you need to remove to make the rest non-overlapping.',
    examples: [
      { input: 'intervals = [[1,2],[2,3],[3,4],[1,3]]', output: '1', explanation: 'Remove [1,3] so the rest are non-overlapping' },
      { input: 'intervals = [[1,2],[1,2],[1,2]]',         output: '2' },
    ],
    constraints: ['1 <= intervals.length <= 10^5', 'intervals[i].length == 2', '-5 * 10^4 <= start_i < end_i <= 5 * 10^4'],
    hints: ['Greedy: sort by end, then keep intervals that start at or after the current end.'],
    approaches: [{ name: 'Greedy by Earliest End', summary: 'Sort by end. Walk through, keeping any interval whose start is >= the last kept end. The count of removed intervals is total − kept.' }],
    starterCode: {
      javascript: 'function eraseOverlapIntervals(intervals) {\n  // your code here\n  return 0;\n}',
      python: 'def erase_overlap_intervals(intervals):\n    # your code here\n    return 0',
      java: 'class Solution {\n    public int eraseOverlapIntervals(int[][] intervals) {\n        return 0;\n    }\n}',
      cpp: 'int eraseOverlapIntervals(vector<vector<int>>& intervals) {\n    return 0;\n}',
      go: 'func eraseOverlapIntervals(intervals [][]int) int {\n    return 0\n}',
    },
    solutions: {
      javascript: 'function eraseOverlapIntervals(intervals) {\n  intervals.sort((a, b) => a[1] - b[1]);\n  let kept = 0, lastEnd = -Infinity;\n  for (const iv of intervals) {\n    if (iv[0] >= lastEnd) { kept++; lastEnd = iv[1]; }\n  }\n  return intervals.length - kept;\n}',
      python: 'def erase_overlap_intervals(intervals):\n    intervals.sort(key=lambda x: x[1])\n    kept = 0\n    last_end = float("-inf")\n    for s, e in intervals:\n        if s >= last_end:\n            kept += 1\n            last_end = e\n    return len(intervals) - kept',
      java: 'class Solution {\n    public int eraseOverlapIntervals(int[][] intervals) {\n        java.util.Arrays.sort(intervals, (a, b) -> Integer.compare(a[1], b[1]));\n        int kept = 0;\n        int lastEnd = Integer.MIN_VALUE;\n        for (int[] iv : intervals) {\n            if (iv[0] >= lastEnd) { kept++; lastEnd = iv[1]; }\n        }\n        return intervals.length - kept;\n    }\n}',
      cpp: 'int eraseOverlapIntervals(vector<vector<int>>& intervals) {\n    sort(intervals.begin(), intervals.end(), [](auto& a, auto& b){ return a[1] < b[1]; });\n    int kept = 0, lastEnd = INT_MIN;\n    for (auto& iv : intervals) {\n        if (iv[0] >= lastEnd) { kept++; lastEnd = iv[1]; }\n    }\n    return (int)intervals.size() - kept;\n}',
      go: 'func eraseOverlapIntervals(intervals [][]int) int {\n    sort.Slice(intervals, func(i, j int) bool { return intervals[i][1] < intervals[j][1] })\n    kept, lastEnd := 0, -1<<30\n    for _, iv := range intervals {\n        if iv[0] >= lastEnd { kept++; lastEnd = iv[1] }\n    }\n    return len(intervals) - kept\n}',
    },
    complexity: { time: 'O(n log n)', space: 'O(1) excluding sort' },
    testCases: [
      { input: { intervals: [[1,2],[2,3],[3,4],[1,3]] }, expected: 1, hidden: false },
      { input: { intervals: [[1,2],[1,2],[1,2]] },         expected: 2, hidden: false },
      { input: { intervals: [[1,2]] },                      expected: 0, hidden: true },
    ],
  },
  {
    id: 'interval-list-intersections',
    functionName: 'intervalIntersection',
    number: 56,
    title: 'Interval List Intersections',
    difficulty: 'Medium',
    topic: 'intervals',
    tags: ['intervals', 'two-pointers', 'array'],
    leetcodeUrl: 'https://leetcode.com/problems/interval-list-intersections/',
    description: 'You are given two lists of closed intervals, `firstList` and `secondList`, where `firstList[i] = [start_i, end_i]` and `secondList[j] = [start_j, end_j]`. Each list is individually **non-overlapping** and sorted.\\n\\nReturn the intersection of these two interval lists.',
    examples: [
      { input: 'firstList = [[0,2],[5,10],[13,23],[24,25]], secondList = [[1,5],[8,12],[15,24],[25,26]]', output: '[[1,2],[5,5],[8,10],[15,23],[24,24],[25,25]]' },
      { input: 'firstList = [[1,3],[5,9]], secondList = []', output: '[]' },
    ],
    constraints: ['0 <= firstList.length, secondList.length <= 1000', 'firstList[i].length == secondList[j].length == 2', '0 <= start_i < end_i < 10^9', 'end_i < start_{i+1}'],
    hints: ['Two pointers: the intersection of [a,b] and [c,d] is [max(a,c), min(b,d)] if non-empty.'],
    approaches: [{ name: 'Two Pointers', summary: 'Walk both lists. If they overlap, the intersection is [max start, min end]. Advance whichever interval ends first.' }],
    starterCode: {
      javascript: 'function intervalIntersection(firstList, secondList) {\n  // your code here\n  return [];\n}',
      python: 'def interval_intersection(firstList, secondList):\n    # your code here\n    return []',
      java: 'class Solution {\n    public int[][] intervalIntersection(int[][] firstList, int[][] secondList) {\n        return new int[0][0];\n    }\n}',
      cpp: 'vector<vector<int>> intervalIntersection(vector<vector<int>>& firstList, vector<vector<int>>& secondList) {\n    return {};\n}',
      go: 'func intervalIntersection(firstList [][]int, secondList [][]int) [][]int {\n    return nil\n}',
    },
    solutions: {
      javascript: 'function intervalIntersection(firstList, secondList) {\n  const out = [];\n  let i = 0, j = 0;\n  while (i < firstList.length && j < secondList.length) {\n    const s = Math.max(firstList[i][0], secondList[j][0]);\n    const e = Math.min(firstList[i][1], secondList[j][1]);\n    if (s <= e) out.push([s, e]);\n    if (firstList[i][1] < secondList[j][1]) i++;\n    else j++;\n  }\n  return out;\n}',
      python: 'def interval_intersection(firstList, secondList):\n    out, i, j = [], 0, 0\n    while i < len(firstList) and j < len(secondList):\n        s = max(firstList[i][0], secondList[j][0])\n        e = min(firstList[i][1], secondList[j][1])\n        if s <= e: out.append([s, e])\n        if firstList[i][1] < secondList[j][1]: i += 1\n        else: j += 1\n    return out',
      java: 'class Solution {\n    public int[][] intervalIntersection(int[][] firstList, int[][] secondList) {\n        java.util.List<int[]> out = new java.util.ArrayList<>();\n        int i = 0, j = 0;\n        while (i < firstList.length && j < secondList.length) {\n            int s = Math.max(firstList[i][0], secondList[j][0]);\n            int e = Math.min(firstList[i][1], secondList[j][1]);\n            if (s <= e) out.add(new int[]{s, e});\n            if (firstList[i][1] < secondList[j][1]) i++;\n            else j++;\n        }\n        return out.toArray(new int[0][]);\n    }\n}',
      cpp: 'vector<vector<int>> intervalIntersection(vector<vector<int>>& firstList, vector<vector<int>>& secondList) {\n    vector<vector<int>> out;\n    int i = 0, j = 0;\n    while (i < (int)firstList.size() && j < (int)secondList.size()) {\n        int s = std::max(firstList[i][0], secondList[j][0]);\n        int e = std::min(firstList[i][1], secondList[j][1]);\n        if (s <= e) out.push_back({s, e});\n        if (firstList[i][1] < secondList[j][1]) i++;\n        else j++;\n    }\n    return out;\n}',
      go: 'func intervalIntersection(firstList [][]int, secondList [][]int) [][]int {\n    out := [][]int{}\n    i, j := 0, 0\n    for i < len(firstList) && j < len(secondList) {\n        s := firstList[i][0]; if secondList[j][0] > s { s = secondList[j][0] }\n        e := firstList[i][1]; if secondList[j][1] < e { e = secondList[j][1] }\n        if s <= e { out = append(out, []int{s, e}) }\n        if firstList[i][1] < secondList[j][1] { i++ } else { j++ }\n    }\n    return out\n}',
    },
    complexity: { time: 'O(n + m)', space: 'O(n + m) for output' },
    testCases: [
      { input: { firstList: [[0,2],[5,10],[13,23],[24,25]], secondList: [[1,5],[8,12],[15,24],[25,26]] }, expected: [[1,2],[5,5],[8,10],[15,23],[24,24],[25,25]], hidden: false },
      { input: { firstList: [[1,3],[5,9]], secondList: [] },                                              expected: [],                          hidden: false },
      { input: { firstList: [], secondList: [[4,8],[10,12]] },                                            expected: [],                          hidden: true },
    ],
  },
  {
    id: 'minimum-interval-to-include-each-query',
    functionName: 'minInterval',
    number: 57,
    title: 'Minimum Interval to Include Each Query',
    difficulty: 'Hard',
    topic: 'intervals',
    tags: ['intervals', 'array', 'sorting', 'heap'],
    leetcodeUrl: 'https://leetcode.com/problems/minimum-interval-to-include-each-query/',
    description: 'You are given a 2D integer array `intervals`, where `intervals[i] = [left_i, right_i]` describes the `i`-th interval. The **size** of an interval is the number of integers it contains, i.e. `right_i − left_i + 1`.\\n\\nYou are also given an integer array `queries`. The answer to the `j`-th query is the size of the **smallest** interval `i` such that `left_i <= queries[j] <= right_i`. If no such interval exists, the answer is `-1`.\\n\\nReturn an array `ans` of length `queries.length` where `ans[j]` is the answer to the `j`-th query.',
    examples: [
      { input: 'intervals = [[1,4],[2,4],[3,6],[4,4]], queries = [2,3,4,5]', output: '[3,3,1,4]' },
      { input: 'intervals = [[2,3],[2,5],[1,8],[6,7]], queries = [2,3,4,5]', output: '[-1,3,-1,-1]', explanation: 'intervals like [2,5] do not contain 4' },
    ],
    constraints: ['1 <= intervals.length <= 10^5', '1 <= queries.length <= 10^5', 'intervals[i].length == 2', '1 <= left_i <= right_i <= 10^7', '1 <= queries[j] <= 10^7'],
    hints: ['Sort intervals by left and queries ascending. Use a min-heap of (size, right). Drop expired intervals, then answer the current query.'],
    approaches: [{ name: 'Sort + Min-Heap', summary: 'Sort intervals by left and queries by value. As each query arrives, push every interval whose left <= query, keyed by size. Pop intervals whose right < query. The heap top (if any) is the answer.' }],
    starterCode: {
      javascript: 'function minInterval(intervals, queries) {\n  // your code here\n  return [];\n}',
      python: 'def min_interval(intervals, queries):\n    # your code here\n    return []',
      java: 'class Solution {\n    public int[] minInterval(int[][] intervals, int[] queries) {\n        return new int[0];\n    }\n}',
      cpp: 'vector<int> minInterval(vector<vector<int>>& intervals, vector<int>& queries) {\n    return {};\n}',
      go: 'func minInterval(intervals [][]int, queries []int) []int {\n    return nil\n}',
    },
    solutions: {
      javascript: 'function minInterval(intervals, queries) {\n  intervals.sort((a, b) => a[0] - b[0]);\n  const idx = queries.map((q, i) => [q, i]).sort((a, b) => a[0] - b[0]);\n  const heap = []; const ans = new Array(queries.length);\n  let j = 0;\n  for (const [q, i] of idx) {\n    while (j < intervals.length && intervals[j][0] <= q) {\n      const [l, r] = intervals[j++];\n      heap.push([r - l + 1, r]);\n      heap.sort((a, b) => a[0] - b[0]);\n    }\n    while (heap.length && heap[0][1] < q) heap.shift();\n    ans[i] = heap.length ? heap[0][0] : -1;\n  }\n  return ans;\n}',
      python: 'def min_interval(intervals, queries):\n    import heapq\n    intervals.sort()\n    idx = sorted([(q, i) for i, q in enumerate(queries)])\n    heap, ans, j = [], [0] * len(queries), 0\n    for q, i in idx:\n        while j < len(intervals) and intervals[j][0] <= q:\n            l, r = intervals[j]; j += 1\n            heapq.heappush(heap, (r - l + 1, r))\n        while heap and heap[0][1] < q:\n            heapq.heappop(heap)\n        ans[i] = heap[0][0] if heap else -1\n    return ans',
      java: 'class Solution {\n    public int[] minInterval(int[][] intervals, int[] queries) {\n        java.util.Arrays.sort(intervals, (a, b) -> Integer.compare(a[0], b[0]));\n        Integer[] order = new Integer[queries.length];\n        for (int i = 0; i < order.length; i++) order[i] = i;\n        java.util.Arrays.sort(order, (a, b) -> Integer.compare(queries[a], queries[b]));\n        java.util.PriorityQueue<int[]> heap = new java.util.PriorityQueue<>((a, b) -> Integer.compare(a[0], b[0]));\n        int[] ans = new int[queries.length];\n        int j = 0;\n        for (int idx : order) {\n            int q = queries[idx];\n            while (j < intervals.length && intervals[j][0] <= q) {\n                int l = intervals[j][0], r = intervals[j][1]; j++;\n                heap.offer(new int[]{r - l + 1, r});\n            }\n            while (!heap.isEmpty() && heap.peek()[1] < q) heap.poll();\n            ans[idx] = heap.isEmpty() ? -1 : heap.peek()[0];\n        }\n        return ans;\n    }\n}',
      cpp: 'vector<int> minInterval(vector<vector<int>>& intervals, vector<int>& queries) {\n    sort(intervals.begin(), intervals.end());\n    vector<int> order(queries.size());\n    iota(order.begin(), order.end(), 0);\n    sort(order.begin(), order.end(), [&](int a, int b){ return queries[a] < queries[b]; });\n    auto cmp = [](const pair<int,int>& a, const pair<int,int>& b){ return a.first > b.first; };\n    priority_queue<pair<int,int>, vector<pair<int,int>>, decltype(cmp)> heap(cmp);\n    vector<int> ans(queries.size(), -1);\n    int j = 0;\n    for (int idx : order) {\n        int q = queries[idx];\n        while (j < (int)intervals.size() && intervals[j][0] <= q) {\n            int l = intervals[j][0], r = intervals[j][1]; j++;\n            heap.push({r - l + 1, r});\n        }\n        while (!heap.empty() && heap.top().second < q) heap.pop();\n        if (!heap.empty()) ans[idx] = heap.top().first;\n    }\n    return ans;\n}',
      go: 'func minInterval(intervals [][]int, queries []int) []int {\n    sort.Slice(intervals, func(i, j int) bool { return intervals[i][0] < intervals[j][0] })\n    order := make([]int, len(queries))\n    for i := range order { order[i] = i }\n    sort.Slice(order, func(i, j int) bool { return queries[order[i]] < queries[order[j]] })\n    heap := &pq{}\n    heap.Init()\n    ans := make([]int, len(queries))\n    j := 0\n    for _, idx := range order {\n        q := queries[idx]\n        for j < len(intervals) && intervals[j][0] <= q {\n            l, r := intervals[j][0], intervals[j][1]\n            j++\n            heap.Push([2]int{r - l + 1, r})\n        }\n        for heap.Len() > 0 && (*heap)[0][1] < q { heap.Pop() }\n        if heap.Len() > 0 { ans[idx] = (*heap)[0][0] } else { ans[idx] = -1 }\n    }\n    return ans\n}',
    },
    complexity: { time: 'O((n + m) log n)', space: 'O(n)' },
    testCases: [
      { input: { intervals: [[1,4],[2,4],[3,6],[4,4]], queries: [2,3,4,5] }, expected: [3,3,1,4],         hidden: false },
      { input: { intervals: [[2,3],[2,5],[1,8],[6,7]], queries: [2,3,4,5] }, expected: [-1,3,-1,-1],     hidden: false },
      { input: { intervals: [[1,2]],                    queries: [1,2,3] },   expected: [2,2,-1],         hidden: true },
    ],
  },
];
