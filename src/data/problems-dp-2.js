// dp topic - second batch
export const DP_PROBLEMS_2 = [
  {
    id: 'word-break',
    functionName: 'wordBreak',
    number: 118,
    title: 'Word Break',
    difficulty: 'Medium',
    topic: 'dp',
    tags: ['dynamic-programming', 'hash-set', 'trie'],
    leetcodeUrl: 'https://leetcode.com/problems/word-break/',
    description: 'Given a string `s` and a dictionary `wordDict`, return `true` if `s` can be segmented into a space-separated sequence of one or more dictionary words.',
    examples: [
      { input: 's = "leetcode", wordDict = ["leet","code"]', output: 'true' },
      { input: 's = "applepenapple", wordDict = ["apple","pen"]', output: 'true' },
      { input: 's = "catsandog", wordDict = ["cats","dog","sand","and","cat"]', output: 'false' },
    ],
    constraints: ['1 <= s.length <= 300', '1 <= wordDict.length <= 1000', '1 <= wordDict[i].length <= 20'],
    hints: ['dp[i] = can segment s[0..i-1]. For each i, check every word that ends at i.'],
    approaches: [{ name: 'Bottom-up DP', summary: 'dp[0]=true. For each i from 1 to n, for each word w of length ≤ i, set dp[i]=true if dp[i-|w|] and s[i-|w|..i-1]==w.' }],
    starterCode: {
      javascript: 'function wordBreak(s, wordDict) {\n  // your code here\n  return false;\n}',
      python: 'def word_break(s, word_dict):\n    # your code here\n    return False',
      java: 'class Solution {\n    public boolean wordBreak(String s, List<String> wordDict) {\n        return false;\n    }\n}',
      cpp: 'bool wordBreak(string s, vector<string>& wordDict) {\n    return false;\n}',
      go: 'func wordBreak(s string, wordDict []string) bool {\n    return false\n}',
    },
    solutions: {
      javascript: 'function wordBreak(s, dict) {\n  const set = new Set(dict);\n  const dp = new Array(s.length + 1).fill(false);\n  dp[0] = true;\n  for (let i = 1; i <= s.length; i++)\n    for (let j = 0; j < i; j++)\n      if (dp[j] && set.has(s.slice(j, i))) { dp[i] = true; break; }\n  return dp[s.length];\n}',
      python: 'def word_break(s, word_dict):\n    word_set = set(word_dict)\n    n = len(s)\n    dp = [False] * (n + 1)\n    dp[0] = True\n    for i in range(1, n + 1):\n        for j in range(i):\n            if dp[j] and s[j:i] in word_set:\n                dp[i] = True; break\n    return dp[n]',
      java: 'class Solution {\n    public boolean wordBreak(String s, List<String> wordDict) {\n        java.util.Set<String> set = new java.util.HashSet<>(wordDict);\n        int n = s.length(); boolean[] dp = new boolean[n + 1]; dp[0] = true;\n        for (int i = 1; i <= n; i++)\n            for (int j = 0; j < i; j++)\n                if (dp[j] && set.contains(s.substring(j, i))) { dp[i] = true; break; }\n        return dp[n];\n    }\n}',
      cpp: 'bool wordBreak(string s, vector<string>& wordDict) {\n    unordered_set<string> set(wordDict.begin(), wordDict.end());\n    int n = s.size(); vector<bool> dp(n + 1, false); dp[0] = true;\n    for (int i = 1; i <= n; i++)\n        for (int j = 0; j < i; j++)\n            if (dp[j] && set.count(s.substr(j, i - j))) { dp[i] = true; break; }\n    return dp[n];\n}',
      go: 'func wordBreak(s string, wordDict []string) bool {\n    set := map[string]bool{}\n    for _, w := range wordDict { set[w] = true }\n    n := len(s); dp := make([]bool, n+1); dp[0] = true\n    for i := 1; i <= n; i++ {\n        for j := 0; j < i; j++ {\n            if dp[j] && set[s[j:i]] { dp[i] = true; break }\n        }\n    }\n    return dp[n]\n}',
    },
    complexity: { time: 'O(n * L * maxWord)', space: 'O(n)' },
    testCases: [
      { input: { s: 'leetcode', dict: ['leet','code'] }, expected: true, hidden: false },
      { input: { s: 'applepenapple', dict: ['apple','pen'] }, expected: true, hidden: false },
      { input: { s: 'catsandog', dict: ['cats','dog','sand','and','cat'] }, expected: false, hidden: true },
    ],
  },
  {
    id: 'longest-increasing-subsequence',
    functionName: 'lengthOfLIS',
    number: 119,
    title: 'Longest Increasing Subsequence',
    difficulty: 'Medium',
    topic: 'dp',
    tags: ['dynamic-programming', 'binary-search'],
    leetcodeUrl: 'https://leetcode.com/problems/longest-increasing-subsequence/',
    description: 'Given an integer array `nums`, return the length of the longest **strictly increasing subsequence**.',
    examples: [
      { input: 'nums = [10,9,2,5,3,7,101,18]', output: '4', explanation: 'LIS is [2,3,7,101].' },
      { input: 'nums = [0,1,0,3,2,3]', output: '4' },
    ],
    constraints: ['1 <= nums.length <= 2500', '-10^4 <= nums[i] <= 10^4'],
    hints: ['Patience sorting: keep a tails array. For each x, binary search the smallest tail >= x and replace; if x is larger than all tails, append. Length of tails is the LIS length.'],
    approaches: [{ name: 'Patience-sorting tails', summary: 'Maintain a sorted tails array. Binary search the leftmost tail ≥ x and replace; if no such tail, append x. The length of tails is the LIS.' }],
    starterCode: {
      javascript: 'function lengthOfLIS(nums) {\n  // your code here\n  return 0;\n}',
      python: 'def length_of_lis(nums):\n    # your code here\n    return 0',
      java: 'class Solution {\n    public int lengthOfLIS(int[] nums) {\n        return 0;\n    }\n}',
      cpp: 'int lengthOfLIS(vector<int>& nums) {\n    return 0;\n}',
      go: 'func lengthOfLIS(nums []int) int {\n    return 0\n}',
    },
    solutions: {
      javascript: 'function lengthOfLIS(nums) {\n  const tails = [];\n  for (const x of nums) {\n    let lo = 0, hi = tails.length;\n    while (lo < hi) { const m = (lo + hi) >> 1; if (tails[m] < x) lo = m + 1; else hi = m; }\n    if (lo === tails.length) tails.push(x); else tails[lo] = x;\n  }\n  return tails.length;\n}',
      python: 'def length_of_lis(nums):\n    import bisect\n    tails = []\n    for x in nums:\n        i = bisect.bisect_left(tails, x)\n        if i == len(tails): tails.append(x)\n        else: tails[i] = x\n    return len(tails)',
      java: 'class Solution {\n    public int lengthOfLIS(int[] nums) {\n        java.util.ArrayList<Integer> tails = new java.util.ArrayList<>();\n        for (int x : nums) {\n            int lo = 0, hi = tails.size();\n            while (lo < hi) { int m = (lo+hi)/2; if (tails.get(m) < x) lo = m+1; else hi = m; }\n            if (lo == tails.size()) tails.add(x); else tails.set(lo, x);\n        }\n        return tails.size();\n    }\n}',
      cpp: 'int lengthOfLIS(vector<int>& nums) {\n    vector<int> tails;\n    for (int x : nums) {\n        auto it = lower_bound(tails.begin(), tails.end(), x);\n        if (it == tails.end()) tails.push_back(x); else *it = x;\n    }\n    return (int)tails.size();\n}',
      go: 'func lengthOfLIS(nums []int) int {\n    tails := []int{}\n    for _, x := range nums {\n        lo, hi := 0, len(tails)\n        for lo < hi { m := (lo+hi)/2; if tails[m] < x { lo = m+1 } else { hi = m } }\n        if lo == len(tails) { tails = append(tails, x) } else { tails[lo] = x }\n    }\n    return len(tails)\n}',
    },
    complexity: { time: 'O(n log n)', space: 'O(n)' },
    testCases: [
      { input: { nums: [10,9,2,5,3,7,101,18] }, expected: 4, hidden: false },
      { input: { nums: [0,1,0,3,2,3] }, expected: 4, hidden: false },
      { input: { nums: [7,7,7,7] }, expected: 1, hidden: true },
    ],
  },
  {
    id: 'partition-equal-subset-sum',
    functionName: 'canPartition',
    number: 120,
    title: 'Partition Equal Subset Sum',
    difficulty: 'Medium',
    topic: 'dp',
    tags: ['dynamic-programming', 'subset-sum'],
    leetcodeUrl: 'https://leetcode.com/problems/partition-equal-subset-sum/',
    description: 'Given a non-empty array of positive integers, determine if the array can be partitioned into two subsets with equal sum.',
    examples: [
      { input: 'nums = [1,5,11,5]', output: 'true', explanation: '[1,5,5] and [11].' },
      { input: 'nums = [1,2,3,5]', output: 'false' },
    ],
    constraints: ['1 <= nums.length <= 200', '1 <= nums[i] <= 100'],
    hints: ['Total must be even. Then ask: is there a subset with sum = total/2?'],
    approaches: [{ name: 'Subset-sum DP', summary: 'Let target = sum/2. dp[x] = can reach sum x. Iterate nums; for each n, update dp from target down to n.' }],
    starterCode: {
      javascript: 'function canPartition(nums) {\n  // your code here\n  return false;\n}',
      python: 'def can_partition(nums):\n    # your code here\n    return False',
      java: 'class Solution {\n    public boolean canPartition(int[] nums) {\n        return false;\n    }\n}',
      cpp: 'bool canPartition(vector<int>& nums) {\n    return false;\n}',
      go: 'func canPartition(nums []int) bool {\n    return false\n}',
    },
    solutions: {
      javascript: 'function canPartition(nums) {\n  const total = nums.reduce((a, b) => a + b, 0);\n  if (total % 2 !== 0) return false;\n  const target = total / 2;\n  const dp = new Array(target + 1).fill(false);\n  dp[0] = true;\n  for (const n of nums) for (let i = target; i >= n; i--) dp[i] = dp[i] || dp[i - n];\n  return dp[target];\n}',
      python: 'def can_partition(nums):\n    total = sum(nums)\n    if total % 2: return False\n    target = total // 2\n    dp = [False] * (target + 1); dp[0] = True\n    for n in nums:\n        for i in range(target, n - 1, -1):\n            dp[i] = dp[i] or dp[i - n]\n    return dp[target]',
      java: 'class Solution {\n    public boolean canPartition(int[] nums) {\n        int total = 0; for (int n : nums) total += n;\n        if (total % 2 != 0) return false;\n        int target = total / 2;\n        boolean[] dp = new boolean[target + 1]; dp[0] = true;\n        for (int n : nums) for (int i = target; i >= n; i--) dp[i] |= dp[i - n];\n        return dp[target];\n    }\n}',
      cpp: 'bool canPartition(vector<int>& nums) {\n    int total = 0; for (int n : nums) total += n;\n    if (total % 2) return false;\n    int target = total / 2;\n    vector<bool> dp(target + 1, false); dp[0] = true;\n    for (int n : nums) for (int i = target; i >= n; i--) dp[i] = dp[i] || dp[i - n];\n    return dp[target];\n}',
      go: 'func canPartition(nums []int) bool {\n    total := 0\n    for _, n := range nums { total += n }\n    if total % 2 != 0 { return false }\n    target := total / 2\n    dp := make([]bool, target+1); dp[0] = true\n    for _, n := range nums {\n        for i := target; i >= n; i-- { dp[i] = dp[i] || dp[i-n] }\n    }\n    return dp[target]\n}',
    },
    complexity: { time: 'O(n * target)', space: 'O(target)' },
    testCases: [
      { input: { nums: [1,5,11,5] }, expected: true, hidden: false },
      { input: { nums: [1,2,3,5] }, expected: false, hidden: false },
      { input: { nums: [1,1] }, expected: true, hidden: true },
    ],
  },
  {
    id: 'unique-paths',
    functionName: 'uniquePaths',
    number: 121,
    title: 'Unique Paths',
    difficulty: 'Medium',
    topic: 'dp',
    tags: ['dynamic-programming', 'math', 'combinatorics'],
    leetcodeUrl: 'https://leetcode.com/problems/unique-paths/',
    description: 'There is a robot on an `m x n` grid at the top-left. It can only move right or down. How many unique paths are there to the bottom-right cell?',
    examples: [
      { input: 'm = 3, n = 7', output: '28' },
      { input: 'm = 3, n = 2', output: '3' },
    ],
    constraints: ['1 <= m, n <= 100', 'The answer fits in a 32-bit integer.'],
    hints: ['dp[i][j] = dp[i-1][j] + dp[i][j-1]. Or combinatorially: choose m+n-2 steps.' ],
    approaches: [{ name: '2-D DP', summary: 'Initialize first row and column to 1. dp[i][j] = dp[i-1][j] + dp[i][j-1]. One row can be kept to save space.' }],
    starterCode: {
      javascript: 'function uniquePaths(m, n) {\n  // your code here\n  return 0;\n}',
      python: 'def unique_paths(m, n):\n    # your code here\n    return 0',
      java: 'class Solution {\n    public int uniquePaths(int m, int n) {\n        return 0;\n    }\n}',
      cpp: 'int uniquePaths(int m, int n) {\n    return 0;\n}',
      go: 'func uniquePaths(m int, n int) int {\n    return 0\n}',
    },
    solutions: {
      javascript: 'function uniquePaths(m, n) {\n  const dp = new Array(n).fill(1);\n  for (let i = 1; i < m; i++) for (let j = 1; j < n; j++) dp[j] += dp[j-1];\n  return dp[n-1];\n}',
      python: 'def unique_paths(m, n):\n    dp = [1] * n\n    for _ in range(1, m):\n        for j in range(1, n): dp[j] += dp[j-1]\n    return dp[-1]',
      java: 'class Solution {\n    public int uniquePaths(int m, int n) {\n        int[] dp = new int[n]; java.util.Arrays.fill(dp, 1);\n        for (int i = 1; i < m; i++) for (int j = 1; j < n; j++) dp[j] += dp[j-1];\n        return dp[n-1];\n    }\n}',
      cpp: 'int uniquePaths(int m, int n) {\n    vector<int> dp(n, 1);\n    for (int i = 1; i < m; i++) for (int j = 1; j < n; j++) dp[j] += dp[j-1];\n    return dp[n-1];\n}',
      go: 'func uniquePaths(m int, n int) int {\n    dp := make([]int, n)\n    for i := range dp { dp[i] = 1 }\n    for i := 1; i < m; i++ { for j := 1; j < n; j++ { dp[j] += dp[j-1] } }\n    return dp[n-1]\n}',
    },
    complexity: { time: 'O(m * n)', space: 'O(n)' },
    testCases: [
      { input: { m: 3, n: 7 }, expected: 28, hidden: false },
      { input: { m: 3, n: 2 }, expected: 3, hidden: false },
      { input: { m: 1, n: 1 }, expected: 1, hidden: true },
    ],
  },
  {
    id: 'longest-common-subsequence',
    functionName: 'longestCommonSubsequence',
    number: 122,
    title: 'Longest Common Subsequence',
    difficulty: 'Medium',
    topic: 'dp',
    tags: ['dynamic-programming', 'string'],
    leetcodeUrl: 'https://leetcode.com/problems/longest-common-subsequence/',
    description: 'Given two strings `text1` and `text2`, return the length of their longest **common subsequence** (LCS). A subsequence is a sequence that can be derived by deleting some or no elements without changing the order.',
    examples: [
      { input: 'text1 = "abcde", text2 = "ace"', output: '3', explanation: 'LCS is "ace".' },
      { input: 'text1 = "abc", text2 = "abc"', output: '3' },
    ],
    constraints: ['1 <= text1.length, text2.length <= 1000'],
    hints: ['dp[i][j] = LCS of text1[:i] and text2[:j]. If equal, dp[i][j]=dp[i-1][j-1]+1; else dp[i][j]=max(dp[i-1][j], dp[i][j-1]).'],
    approaches: [{ name: '2-D DP', summary: 'Build dp[i+1][j+1]; if t1[i]==t2[j] dp[i+1][j+1]=dp[i][j]+1; else max of left/top. Can be reduced to 2 rows.' }],
    starterCode: {
      javascript: 'function longestCommonSubsequence(text1, text2) {\n  // your code here\n  return 0;\n}',
      python: 'def longest_common_subsequence(text1, text2):\n    # your code here\n    return 0',
      java: 'class Solution {\n    public int longestCommonSubsequence(String text1, String text2) {\n        return 0;\n    }\n}',
      cpp: 'int longestCommonSubsequence(string text1, string text2) {\n    return 0;\n}',
      go: 'func longestCommonSubsequence(text1 string, text2 string) int {\n    return 0\n}',
    },
    solutions: {
      javascript: 'function longestCommonSubsequence(t1, t2) {\n  const m = t1.length, n = t2.length;\n  const dp = Array.from({length: m + 1}, () => new Array(n + 1).fill(0));\n  for (let i = 1; i <= m; i++) for (let j = 1; j <= n; j++) {\n    if (t1[i-1] === t2[j-1]) dp[i][j] = dp[i-1][j-1] + 1;\n    else dp[i][j] = Math.max(dp[i-1][j], dp[i][j-1]);\n  }\n  return dp[m][n];\n}',
      python: 'def longest_common_subsequence(text1, text2):\n    m, n = len(text1), len(text2)\n    dp = [[0] * (n + 1) for _ in range(m + 1)]\n    for i in range(1, m + 1):\n        for j in range(1, n + 1):\n            if text1[i-1] == text2[j-1]: dp[i][j] = dp[i-1][j-1] + 1\n            else: dp[i][j] = max(dp[i-1][j], dp[i][j-1])\n    return dp[m][n]',
      java: 'class Solution {\n    public int longestCommonSubsequence(String t1, String t2) {\n        int m = t1.length(), n = t2.length();\n        int[] dp = new int[n + 1];\n        for (int i = 1; i <= m; i++) {\n            int prev = 0;\n            for (int j = 1; j <= n; j++) {\n                int tmp = dp[j];\n                if (t1.charAt(i-1) == t2.charAt(j-1)) dp[j] = prev + 1;\n                else dp[j] = Math.max(dp[j], dp[j-1]);\n                prev = tmp;\n            }\n        }\n        return dp[n];\n    }\n}',
      cpp: 'int longestCommonSubsequence(string t1, string t2) {\n    int m = t1.size(), n = t2.size();\n    vector<int> dp(n + 1, 0);\n    for (int i = 1; i <= m; i++) {\n        int prev = 0;\n        for (int j = 1; j <= n; j++) {\n            int tmp = dp[j];\n            if (t1[i-1] == t2[j-1]) dp[j] = prev + 1;\n            else dp[j] = max(dp[j], dp[j-1]);\n            prev = tmp;\n        }\n    }\n    return dp[n];\n}',
      go: 'func longestCommonSubsequence(text1 string, text2 string) int {\n    m, n := len(text1), len(text2)\n    dp := make([]int, n+1)\n    for i := 1; i <= m; i++ {\n        prev := 0\n        for j := 1; j <= n; j++ {\n            tmp := dp[j]\n            if text1[i-1] == text2[j-1] { dp[j] = prev + 1 } else {\n                if dp[j] < dp[j-1] { dp[j] = dp[j-1] }\n            }\n            prev = tmp\n        }\n    }\n    return dp[n]\n}',
    },
    complexity: { time: 'O(m * n)', space: 'O(n)' },
    testCases: [
      { input: { t1: 'abcde', t2: 'ace' }, expected: 3, hidden: false },
      { input: { t1: 'abc', t2: 'abc' }, expected: 3, hidden: false },
      { input: { t1: 'abc', t2: 'def' }, expected: 0, hidden: true },
    ],
  },
  {
    id: 'target-sum',
    functionName: 'findTargetSumWays',
    number: 123,
    title: 'Target Sum',
    difficulty: 'Medium',
    topic: 'dp',
    tags: ['dynamic-programming', 'subset-sum', 'hash-map'],
    leetcodeUrl: 'https://leetcode.com/problems/target-sum/',
    description: 'You are given an integer array `nums` and an integer `target`. You want to build an expression by adding `+` or `-` before each integer, then concatenate them. Return the number of expressions that evaluate to `target`.',
    examples: [
      { input: 'nums = [1,1,1,1,1], target = 3', output: '5' },
      { input: 'nums = [1], target = 1', output: '1' },
    ],
    constraints: ['1 <= nums.length <= 20', '0 <= nums[i] <= 1000', '0 <= sum(nums[i]) <= 1000', '-1000 <= target <= 1000'],
    hints: ['Let P be the set of indices assigned +. Then sum(P) - (total - sum(P)) = target → sum(P) = (target + total) / 2. Reduces to subset-count.'],
    approaches: [{ name: 'Subset-sum DP', summary: 'If (target + total) is odd or < 0, return 0. Otherwise dp[x] = #subsets summing to x; iterate nums updating dp.' }],
    starterCode: {
      javascript: 'function findTargetSumWays(nums, target) {\n  // your code here\n  return 0;\n}',
      python: 'def find_target_sum_ways(nums, target):\n    # your code here\n    return 0',
      java: 'class Solution {\n    public int findTargetSumWays(int[] nums, int target) {\n        return 0;\n    }\n}',
      cpp: 'int findTargetSumWays(vector<int>& nums, int target) {\n    return 0;\n}',
      go: 'func findTargetSumWays(nums []int, target int) int {\n    return 0\n}',
    },
    solutions: {
      javascript: 'function findTargetSumWays(nums, target) {\n  const total = nums.reduce((a, b) => a + b, 0);\n  const sum = target + total;\n  if (sum < 0 || sum % 2 !== 0) return 0;\n  const goal = sum / 2;\n  const dp = new Array(goal + 1).fill(0);\n  dp[0] = 1;\n  for (const n of nums) for (let i = goal; i >= n; i--) dp[i] += dp[i - n];\n  return dp[goal];\n}',
      python: 'def find_target_sum_ways(nums, target):\n    total = sum(nums); s = target + total\n    if s < 0 or s % 2: return 0\n    goal = s // 2\n    dp = [0] * (goal + 1); dp[0] = 1\n    for n in nums:\n        for i in range(goal, n - 1, -1): dp[i] += dp[i - n]\n    return dp[goal]',
      java: 'class Solution {\n    public int findTargetSumWays(int[] nums, int target) {\n        int total = 0; for (int n : nums) total += n;\n        int s = target + total;\n        if (s < 0 || s % 2 != 0) return 0;\n        int goal = s / 2;\n        int[] dp = new int[goal + 1]; dp[0] = 1;\n        for (int n : nums) for (int i = goal; i >= n; i--) dp[i] += dp[i - n];\n        return dp[goal];\n    }\n}',
      cpp: 'int findTargetSumWays(vector<int>& nums, int target) {\n    int total = 0; for (int n : nums) total += n;\n    int s = target + total;\n    if (s < 0 || s % 2) return 0;\n    int goal = s / 2;\n    vector<int> dp(goal + 1, 0); dp[0] = 1;\n    for (int n : nums) for (int i = goal; i >= n; i--) dp[i] += dp[i - n];\n    return dp[goal];\n}',
      go: 'func findTargetSumWays(nums []int, target int) int {\n    total := 0\n    for _, n := range nums { total += n }\n    s := target + total\n    if s < 0 || s%2 != 0 { return 0 }\n    goal := s / 2\n    dp := make([]int, goal+1); dp[0] = 1\n    for _, n := range nums {\n        for i := goal; i >= n; i-- { dp[i] += dp[i-n] }\n    }\n    return dp[goal]\n}',
    },
    complexity: { time: 'O(n * sum)', space: 'O(sum)' },
    testCases: [
      { input: { nums: [1,1,1,1,1], target: 3 }, expected: 5, hidden: false },
      { input: { nums: [1], target: 1 }, expected: 1, hidden: false },
      { input: { nums: [1,2,3], target: 0 }, expected: 2, hidden: true },
    ],
  },
  {
    id: 'interleaving-string',
    functionName: 'isInterleave',
    number: 124,
    title: 'Interleaving String',
    difficulty: 'Medium',
    topic: 'dp',
    tags: ['dynamic-programming', 'string'],
    leetcodeUrl: 'https://leetcode.com/problems/interleaving-string/',
    description: 'Given strings `s1`, `s2`, and `s3`, find whether `s3` is formed by an interleaving of `s1` and `s2`.',
    examples: [
      { input: 's1 = "aabcc", s2 = "dbbca", s3 = "aadbbcbcac"', output: 'true' },
      { input: 's1 = "aabcc", s2 = "dbbca", s3 = "aadbbbaccc"', output: 'false' },
    ],
    constraints: ['0 <= s1.length, s2.length <= 100', 's3.length == s1.length + s2.length'],
    hints: ['dp[i][j] = can s3[0..i+j-1] be formed from s1[0..i-1] and s2[0..j-1].'],
    approaches: [{ name: '2-D DP', summary: 'dp[i][j] = (dp[i-1][j] && s1[i-1]==s3[i+j-1]) || (dp[i][j-1] && s2[j-1]==s3[i+j-1]). One row is enough.' }],
    starterCode: {
      javascript: 'function isInterleave(s1, s2, s3) {\n  // your code here\n  return false;\n}',
      python: 'def is_interleave(s1, s2, s3):\n    # your code here\n    return False',
      java: 'class Solution {\n    public boolean isInterleave(String s1, String s2, String s3) {\n        return false;\n    }\n}',
      cpp: 'bool isInterleave(string s1, string s2, string s3) {\n    return false;\n}',
      go: 'func isInterleave(s1 string, s2 string, s3 string) bool {\n    return false\n}',
    },
    solutions: {
      javascript: 'function isInterleave(s1, s2, s3) {\n  const m = s1.length, n = s2.length;\n  if (m + n !== s3.length) return false;\n  const dp = new Array(n + 1).fill(false);\n  dp[0] = true;\n  for (let j = 1; j <= n; j++) dp[j] = dp[j-1] && s2[j-1] === s3[j-1];\n  for (let i = 1; i <= m; i++) {\n    dp[0] = dp[0] && s1[i-1] === s3[i-1];\n    for (let j = 1; j <= n; j++) {\n      dp[j] = (dp[j] && s1[i-1] === s3[i+j-1]) || (dp[j-1] && s2[j-1] === s3[i+j-1]);\n    }\n  }\n  return dp[n];\n}',
      python: 'def is_interleave(s1, s2, s3):\n    m, n = len(s1), len(s2)\n    if m + n != len(s3): return False\n    dp = [False] * (n + 1); dp[0] = True\n    for j in range(1, n + 1): dp[j] = dp[j-1] and s2[j-1] == s3[j-1]\n    for i in range(1, m + 1):\n        dp[0] = dp[0] and s1[i-1] == s3[i-1]\n        for j in range(1, n + 1):\n            dp[j] = (dp[j] and s1[i-1] == s3[i+j-1]) or (dp[j-1] and s2[j-1] == s3[i+j-1])\n    return dp[n]',
      java: 'class Solution {\n    public boolean isInterleave(String s1, String s2, String s3) {\n        int m = s1.length(), n = s2.length();\n        if (m + n != s3.length()) return false;\n        boolean[] dp = new boolean[n + 1]; dp[0] = true;\n        for (int j = 1; j <= n; j++) dp[j] = dp[j-1] && s2.charAt(j-1) == s3.charAt(j-1);\n        for (int i = 1; i <= m; i++) {\n            dp[0] = dp[0] && s1.charAt(i-1) == s3.charAt(i-1);\n            for (int j = 1; j <= n; j++)\n                dp[j] = (dp[j] && s1.charAt(i-1) == s3.charAt(i+j-1)) ||\n                        (dp[j-1] && s2.charAt(j-1) == s3.charAt(i+j-1));\n        }\n        return dp[n];\n    }\n}',
      cpp: 'bool isInterleave(string s1, string s2, string s3) {\n    int m = s1.size(), n = s2.size();\n    if (m + n != s3.size()) return false;\n    vector<bool> dp(n + 1, false); dp[0] = true;\n    for (int j = 1; j <= n; j++) dp[j] = dp[j-1] && s2[j-1] == s3[j-1];\n    for (int i = 1; i <= m; i++) {\n        dp[0] = dp[0] && s1[i-1] == s3[i-1];\n        for (int j = 1; j <= n; j++)\n            dp[j] = (dp[j] && s1[i-1] == s3[i+j-1]) || (dp[j-1] && s2[j-1] == s3[i+j-1]);\n    }\n    return dp[n];\n}',
      go: 'func isInterleave(s1 string, s2 string, s3 string) bool {\n    m, n := len(s1), len(s2)\n    if m+n != len(s3) { return false }\n    dp := make([]bool, n+1); dp[0] = true\n    for j := 1; j <= n; j++ { dp[j] = dp[j-1] && s2[j-1] == s3[j-1] }\n    for i := 1; i <= m; i++ {\n        dp[0] = dp[0] && s1[i-1] == s3[i-1]\n        for j := 1; j <= n; j++ {\n            dp[j] = (dp[j] && s1[i-1] == s3[i+j-1]) || (dp[j-1] && s2[j-1] == s3[i+j-1])\n        }\n    }\n    return dp[n]\n}',
    },
    complexity: { time: 'O(m * n)', space: 'O(n)' },
    testCases: [
      { input: { s1: 'aabcc', s2: 'dbbca', s3: 'aadbbcbcac' }, expected: true, hidden: false },
      { input: { s1: 'aabcc', s2: 'dbbca', s3: 'aadbbbaccc' }, expected: false, hidden: false },
      { input: { s1: '', s2: '', s3: '' }, expected: true, hidden: true },
    ],
  },
]
