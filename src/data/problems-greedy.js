// Greedy problems — NeetCode 150
export const GREEDY_PROBLEMS = [
  {
    id: 'maximum-subarray',
    functionName: 'maxSubArray',
    number: 22,
    title: 'Maximum Subarray',
    difficulty: 'Medium',
    topic: 'greedy',
    tags: ['kadane', 'dp'],
    leetcodeUrl: 'https://leetcode.com/problems/maximum-subarray/',
    description: 'Given an integer array `nums`, find the subarray with the **largest sum**, and return its sum.',
    examples: [
      { input: 'nums = [-2,1,-3,4,-1,2,1,-5,4]', output: '6', explanation: 'Subarray [4,-1,2,1] has sum 6' },
      { input: 'nums = [1]', output: '1' },
    ],
    constraints: ['1 <= nums.length <= 10^5', '-10^4 <= nums[i] <= 10^4'],
    hints: ['At each index, the best sum ending here is either nums[i] alone or nums[i] + best sum ending at i−1.'],
    approaches: [{ name: 'Kadane\'s Algorithm', summary: 'Track cur = max sum ending at i and maxSum = overall best. Update cur = max(nums[i], cur + nums[i]) each step.' }],
    starterCode: {
      javascript: 'function maxSubArray(nums) {\n  // your code here\n  return 0;\n}',
      python: 'def max_subarray(nums):\n    # your code here\n    return 0',
      java: 'class Solution {\n    public int maxSubArray(int[] nums) {\n        return 0;\n    }\n}',
      cpp: 'int maxSubArray(vector<int>& nums) {\n    return 0;\n}',
      go: 'func maxSubArray(nums []int) int {\n    return 0\n}',
    },
    solutions: {
      javascript: 'function maxSubArray(nums) {\n  let maxSum = nums[0], cur = nums[0];\n  for (let i = 1; i < nums.length; i++) {\n    cur = Math.max(nums[i], cur + nums[i]);\n    maxSum = Math.max(maxSum, cur);\n  }\n  return maxSum;\n}',
      python: 'def max_subarray(nums):\n    cur = max_sum = nums[0]\n    for n in nums[1:]:\n        cur = max(n, cur + n)\n        max_sum = max(max_sum, cur)\n    return max_sum',
      java: 'class Solution {\n    public int maxSubArray(int[] nums) {\n        int cur = nums[0], maxSum = nums[0];\n        for (int i = 1; i < nums.length; i++) {\n            cur = Math.max(nums[i], cur + nums[i]);\n            maxSum = Math.max(maxSum, cur);\n        }\n        return maxSum;\n    }\n}',
      cpp: 'int maxSubArray(vector<int>& nums) {\n    int cur = nums[0], maxSum = nums[0];\n    for (int i = 1; i < (int)nums.size(); i++) {\n      cur = std::max(nums[i], cur + nums[i]);\n      maxSum = std::max(maxSum, cur);\n    }\n    return maxSum;\n}',
      go: 'func maxSubArray(nums []int) int {\n    cur, maxSum := nums[0], nums[0]\n    for _, n := range nums[1:] {\n        if cur+n > n { cur = cur + n } else { cur = n }\n        if cur > maxSum { maxSum = cur }\n    }\n    return maxSum\n}',
    },
    complexity: { time: 'O(n)', space: 'O(1)' },
    testCases: [
      { input: { nums: [-2,1,-3,4,-1,2,1,-5,4] }, expected: 6,  hidden: false },
      { input: { nums: [1] },                     expected: 1,  hidden: false },
      { input: { nums: [5,4,-1,7,8] },            expected: 23, hidden: true },
    ],
  },
  {
    id: 'jump-game',
    functionName: 'canJump',
    number: 23,
    title: 'Jump Game',
    difficulty: 'Medium',
    topic: 'greedy',
    tags: ['array'],
    leetcodeUrl: 'https://leetcode.com/problems/jump-game/',
    description: 'You are given an integer array `nums`. You are initially positioned at the **first index**, and each element `nums[i]` represents the maximum jump length from that position.\n\nReturn `true` if you can reach the last index, or `false` otherwise.',
    examples: [
      { input: 'nums = [2,3,1,1,4]', output: 'true' },
      { input: 'nums = [3,2,1,0,4]', output: 'false' },
    ],
    constraints: ['1 <= nums.length <= 10^4', '0 <= nums[i] <= 10^5'],
    hints: ['Track the farthest index you can reach so far. If your current index ever exceeds that, you\'re stuck.'],
    approaches: [{ name: 'Greedy Farthest Reach', summary: 'Walk the array, maintaining the farthest reachable index. If at any point i > farthest, return false. Otherwise update farthest = max(farthest, i + nums[i]).' }],
    starterCode: {
      javascript: 'function canJump(nums) {\n  // your code here\n  return true;\n}',
      python: 'def can_jump(nums):\n    # your code here\n    return True',
      java: 'class Solution {\n    public boolean canJump(int[] nums) {\n        return true;\n    }\n}',
      cpp: 'bool canJump(vector<int>& nums) {\n    return true;\n}',
      go: 'func canJump(nums []int) bool {\n    return true\n}',
    },
    solutions: {
      javascript: 'function canJump(nums) {\n  let farthest = 0;\n  for (let i = 0; i < nums.length; i++) {\n    if (i > farthest) return false;\n    farthest = Math.max(farthest, i + nums[i]);\n  }\n  return true;\n}',
      python: 'def can_jump(nums):\n    farthest = 0\n    for i, n in enumerate(nums):\n        if i > farthest: return False\n        farthest = max(farthest, i + n)\n    return True',
      java: 'class Solution {\n    public boolean canJump(int[] nums) {\n        int farthest = 0;\n        for (int i = 0; i < nums.length; i++) {\n            if (i > farthest) return false;\n            farthest = Math.max(farthest, i + nums[i]);\n        }\n        return true;\n    }\n}',
      cpp: 'bool canJump(vector<int>& nums) {\n    int farthest = 0;\n    for (int i = 0; i < (int)nums.size(); i++) {\n      if (i > farthest) return false;\n      farthest = std::max(farthest, i + nums[i]);\n    }\n    return true;\n}',
      go: 'func canJump(nums []int) bool {\n    farthest := 0\n    for i, n := range nums {\n        if i > farthest { return false }\n        if i+n > farthest { farthest = i + n }\n    }\n    return true\n}',
    },
    complexity: { time: 'O(n)', space: 'O(1)' },
    testCases: [
      { input: { nums: [2,3,1,1,4] }, expected: true,  hidden: false },
      { input: { nums: [3,2,1,0,4] }, expected: false, hidden: false },
      { input: { nums: [0] },         expected: true,  hidden: true },
    ],
  },
  {
    id: 'jump-game-ii',
    functionName: 'jump',
    number: 24,
    title: 'Jump Game II',
    difficulty: 'Medium',
    topic: 'greedy',
    tags: ['array', 'bfs'],
    leetcodeUrl: 'https://leetcode.com/problems/jump-game-ii/',
    description: 'You are given a `0`-indexed array of integers `nums` of length `n`. You are initially at `nums[0]`. Each element `nums[i]` represents the maximum jump length from that position.\n\nReturn the **minimum number of jumps** required to reach `nums[n - 1]`. You may assume you can always reach the last index.',
    examples: [
      { input: 'nums = [2,3,1,1,4]', output: '2' },
      { input: 'nums = [2,3,0,1,4]', output: '2' },
    ],
    constraints: ['1 <= nums.length <= 10^4', '0 <= nums[i] <= 1000'],
    hints: ['BFS-style: each jump expands your reach. The number of jumps is the number of BFS layers.'],
    approaches: [{ name: 'Greedy Reach BFS', summary: 'Track curEnd (end of current jump) and farthest (best next-jump reach). When i hits curEnd, increment jumps and set curEnd = farthest.' }],
    starterCode: {
      javascript: 'function jump(nums) {\n  // your code here\n  return 0;\n}',
      python: 'def jump(nums):\n    # your code here\n    return 0',
      java: 'class Solution {\n    public int jump(int[] nums) {\n        return 0;\n    }\n}',
      cpp: 'int jump(vector<int>& nums) {\n    return 0;\n}',
      go: 'func jump(nums []int) int {\n    return 0\n}',
    },
    solutions: {
      javascript: 'function jump(nums) {\n  let jumps = 0, curEnd = 0, farthest = 0;\n  for (let i = 0; i < nums.length - 1; i++) {\n    farthest = Math.max(farthest, i + nums[i]);\n    if (i === curEnd) { jumps++; curEnd = farthest; }\n  }\n  return jumps;\n}',
      python: 'def jump(nums):\n    jumps = cur_end = farthest = 0\n    for i in range(len(nums) - 1):\n        farthest = max(farthest, i + nums[i])\n        if i == cur_end:\n            jumps += 1\n            cur_end = farthest\n    return jumps',
      java: 'class Solution {\n    public int jump(int[] nums) {\n        int jumps = 0, curEnd = 0, farthest = 0;\n        for (int i = 0; i < nums.length - 1; i++) {\n            farthest = Math.max(farthest, i + nums[i]);\n            if (i == curEnd) { jumps++; curEnd = farthest; }\n        }\n        return jumps;\n    }\n}',
      cpp: 'int jump(vector<int>& nums) {\n    int jumps = 0, curEnd = 0, farthest = 0;\n    for (int i = 0; i < (int)nums.size() - 1; i++) {\n      farthest = std::max(farthest, i + nums[i]);\n      if (i == curEnd) { jumps++; curEnd = farthest; }\n    }\n    return jumps;\n}',
      go: 'func jump(nums []int) int {\n    jumps, curEnd, farthest := 0, 0, 0\n    for i := 0; i < len(nums)-1; i++ {\n        if i+nums[i] > farthest { farthest = i + nums[i] }\n        if i == curEnd { jumps++; curEnd = farthest }\n    }\n    return jumps\n}',
    },
    complexity: { time: 'O(n)', space: 'O(1)' },
    testCases: [
      { input: { nums: [2,3,1,1,4] }, expected: 2, hidden: false },
      { input: { nums: [2,3,0,1,4] }, expected: 2, hidden: false },
      { input: { nums: [1,2,3] },    expected: 2, hidden: true },
    ],
  },
  {
    id: 'gas-station',
    functionName: 'canCompleteCircuit',
    number: 25,
    title: 'Gas Station',
    difficulty: 'Medium',
    topic: 'greedy',
    tags: ['array'],
    leetcodeUrl: 'https://leetcode.com/problems/gas-station/',
    description: 'There are `n` gas stations along a circular route. You have two integer arrays `gas` and `cost`. Starting with an empty tank at station `i`, you can travel from station `i` to `i + 1` if `tank + gas[i] >= cost[i]`.\n\nReturn the starting station\'s index if you can travel around the circuit once, otherwise return `-1`. The answer is unique if it exists.',
    examples: [
      { input: 'gas = [1,2,3,4,5], cost = [3,4,5,1,2]', output: '3' },
      { input: 'gas = [2,3,4],     cost = [3,4,3]',     output: '-1' },
    ],
    constraints: ['1 <= gas.length == cost.length <= 10^5', '0 <= gas[i], cost[i] <= 10^4'],
    hints: ['If total gas < total cost, no solution. Otherwise the starting point is the index right after the tank first goes negative.'],
    approaches: [{ name: 'Greedy Reset on Negative', summary: 'Sum net = gas − cost over all stations. If total < 0 return −1. Otherwise reset `start` whenever the running tank dips negative.' }],
    starterCode: {
      javascript: 'function canCompleteCircuit(gas, cost) {\n  // your code here\n  return -1;\n}',
      python: 'def can_complete_circuit(gas, cost):\n    # your code here\n    return -1',
      java: 'class Solution {\n    public int canCompleteCircuit(int[] gas, int[] cost) {\n        return -1;\n    }\n}',
      cpp: 'int canCompleteCircuit(vector<int>& gas, vector<int>& cost) {\n    return -1;\n}',
      go: 'func canCompleteCircuit(gas, cost []int) int {\n    return -1\n}',
    },
    solutions: {
      javascript: 'function canCompleteCircuit(gas, cost) {\n  let total = 0, tank = 0, start = 0;\n  for (let i = 0; i < gas.length; i++) {\n    const diff = gas[i] - cost[i];\n    total += diff;\n    tank += diff;\n    if (tank < 0) { start = i + 1; tank = 0; }\n  }\n  return total >= 0 ? start : -1;\n}',
      python: 'def can_complete_circuit(gas, cost):\n    total = tank = start = 0\n    for i, (g, c) in enumerate(zip(gas, cost)):\n        diff = g - c\n        total += diff\n        tank += diff\n        if tank < 0:\n            start = i + 1\n            tank = 0\n    return start if total >= 0 else -1',
      java: 'class Solution {\n    public int canCompleteCircuit(int[] gas, int[] cost) {\n        int total = 0, tank = 0, start = 0;\n        for (int i = 0; i < gas.length; i++) {\n            int diff = gas[i] - cost[i];\n            total += diff;\n            tank += diff;\n            if (tank < 0) { start = i + 1; tank = 0; }\n        }\n        return total >= 0 ? start : -1;\n    }\n}',
      cpp: 'int canCompleteCircuit(vector<int>& gas, vector<int>& cost) {\n    int total = 0, tank = 0, start = 0;\n    for (int i = 0; i < (int)gas.size(); i++) {\n      int diff = gas[i] - cost[i];\n      total += diff;\n      tank += diff;\n      if (tank < 0) { start = i + 1; tank = 0; }\n    }\n    return total >= 0 ? start : -1;\n}',
      go: 'func canCompleteCircuit(gas, cost []int) int {\n    total, tank, start := 0, 0, 0\n    for i := range gas {\n        diff := gas[i] - cost[i]\n        total += diff\n        tank += diff\n        if tank < 0 { start, tank = i+1, 0 }\n    }\n    if total >= 0 { return start }\n    return -1\n}',
    },
    complexity: { time: 'O(n)', space: 'O(1)' },
    testCases: [
      { input: { gas: [1,2,3,4,5], cost: [3,4,5,1,2] }, expected: 3,  hidden: false },
      { input: { gas: [2,3,4],     cost: [3,4,3] },     expected: -1, hidden: false },
      { input: { gas: [3,1,1],     cost: [1,2,2] },     expected: 0,  hidden: true },
    ],
  },
  {
    id: 'hand-of-straights',
    functionName: 'isNStraightHand',
    number: 26,
    title: 'Hand of Straights',
    difficulty: 'Medium',
    topic: 'greedy',
    tags: ['hash-map', 'sorting'],
    leetcodeUrl: 'https://leetcode.com/problems/hand-of-straights/',
    description: 'Alice has some number of cards, and she wants to rearrange the cards into groups so that each group is size `groupSize`, and consists of `groupSize` consecutive cards.\n\nGiven an integer array `hand` where `hand[i]` is the value written on the `i`th card, return `true` if she can rearrange the cards, or `false` otherwise.',
    examples: [
      { input: 'hand = [1,2,3,6,2,3,4,7,8], groupSize = 3', output: 'true' },
      { input: 'hand = [1,2,3,4,5],         groupSize = 4', output: 'false' },
    ],
    constraints: ['1 <= hand.length <= 10^4', '1 <= groupSize <= hand.length', '0 <= hand[i] <= 10^9'],
    hints: ['Sort and try to greedily form groups from the smallest unused card.'],
    approaches: [{ name: 'Sorted Grouping', summary: 'Sort the hand. For each i, if it is the start of a new group, attempt to match groupSize−1 consecutive successors by counting down. If any required value is missing, return false.' }],
    starterCode: {
      javascript: 'function isNStraightHand(hand, groupSize) {\n  // your code here\n  return false;\n}',
      python: 'def is_n_straight_hand(hand, groupSize):\n    # your code here\n    return False',
      java: 'class Solution {\n    public boolean isNStraightHand(int[] hand, int groupSize) {\n        return false;\n    }\n}',
      cpp: 'bool isNStraightHand(vector<int>& hand, int groupSize) {\n    return false;\n}',
      go: 'func isNStraightHand(hand []int, groupSize int) bool {\n    return false\n}',
    },
    solutions: {
      javascript: 'function isNStraightHand(hand, groupSize) {\n  if (hand.length % groupSize !== 0) return false;\n  const count = new Map();\n  for (const c of hand) count.set(c, (count.get(c) || 0) + 1);\n  const sorted = [...count.keys()].sort((a, b) => a - b);\n  for (const k of sorted) {\n    const c = count.get(k);\n    if (c > 0) {\n      for (let i = 0; i < groupSize; i++) {\n        const next = k + i;\n        if ((count.get(next) || 0) < c) return false;\n        count.set(next, count.get(next) - c);\n      }\n    }\n  }\n  return true;\n}',
      python: 'def is_n_straight_hand(hand, groupSize):\n    from collections import Counter\n    if len(hand) % groupSize: return False\n    count = Counter(hand)\n    for k in sorted(count):\n        c = count[k]\n        if c > 0:\n            for i in range(groupSize):\n                if count[k+i] < c: return False\n                count[k+i] -= c\n    return True',
      java: 'class Solution {\n    public boolean isNStraightHand(int[] hand, int groupSize) {\n        if (hand.length % groupSize != 0) return false;\n        java.util.Map<Integer,Integer> count = new java.util.TreeMap<>();\n        for (int c : hand) count.merge(c, 1, Integer::sum);\n        for (int k : count.keySet()) {\n            int c = count.get(k);\n            if (c > 0)\n                for (int i = 0; i < groupSize; i++) {\n                    int next = k + i;\n                    if (count.getOrDefault(next, 0) < c) return false;\n                    count.put(next, count.get(next) - c);\n                }\n        }\n        return true;\n    }\n}',
      cpp: 'bool isNStraightHand(vector<int>& hand, int groupSize) {\n    if (hand.size() % groupSize != 0) return false;\n    std::map<int,int> count;\n    for (int c : hand) count[c]++;\n    for (auto& [k, c] : count) {\n        if (c > 0)\n            for (int i = 0; i < groupSize; i++) {\n                auto it = count.find(k + i);\n                if (it == count.end() || it->second < c) return false;\n                it->second -= c;\n            }\n    }\n    return true;\n}',
      go: 'func isNStraightHand(hand []int, groupSize int) bool {\n    if len(hand)%groupSize != 0 { return false }\n    count := map[int]int{}\n    for _, c := range hand { count[c]++ }\n    keys := make([]int, 0, len(count))\n    for k := range count { keys = append(keys, k) }\n    sort.Ints(keys)\n    for _, k := range keys {\n        c := count[k]\n        if c > 0 {\n            for i := 0; i < groupSize; i++ {\n                if count[k+i] < c { return false }\n                count[k+i] -= c\n            }\n        }\n    }\n    return true\n}',
    },
    complexity: { time: 'O(n log n)', space: 'O(n)' },
    testCases: [
      { input: { hand: [1,2,3,6,2,3,4,7,8], groupSize: 3 }, expected: true,  hidden: false },
      { input: { hand: [1,2,3,4,5],         groupSize: 4 }, expected: false, hidden: false },
      { input: { hand: [1,1,2,2,3,3],       groupSize: 3 }, expected: true,  hidden: true },
    ],
  },
  {
    id: 'merge-triplets-to-form-target',
    functionName: 'mergeTriplets',
    number: 27,
    title: 'Merge Triplets to Form Target',
    difficulty: 'Medium',
    topic: 'greedy',
    tags: ['array', 'greedy'],
    leetcodeUrl: 'https://leetcode.com/problems/merge-triplets-to-form-target-triplet/',
    description: 'A **triplet** is an array of three integers. You are given a 2D integer array `triplets` and an integer array `target = [t1, t2, t3]`.\n\nIn one operation, you may pick two triplets `[a,b,c]` and `[d,e,f]` and merge them into `[max(a,d), max(b,e), max(c,f)]`. Return `true` if you can form the target by merging any subset of triplets, otherwise `false`.',
    examples: [
      { input: 'triplets = [[2,5,3],[1,8,4],[1,7,5]], target = [2,7,5]', output: 'true' },
      { input: 'triplets = [[3,4,5],[4,5,6]],         target = [3,2,5]', output: 'false' },
    ],
    constraints: ['1 <= triplets.length <= 10^5', 'triplets[i].length == target.length == 3', '0 <= triplets[i][j], target[j] <= 1000'],
    hints: ['Skip any triplet that overshoots target on any axis — it can only hurt you.'],
    approaches: [{ name: 'Greedy Max within Bounds', summary: 'Skip any triplet with a value > target. Otherwise take the running max on each axis. Return true iff all three axes match target exactly.' }],
    starterCode: {
      javascript: 'function mergeTriplets(triplets, target) {\n  // your code here\n  return false;\n}',
      python: 'def merge_triplets(triplets, target):\n    # your code here\n    return False',
      java: 'class Solution {\n    public boolean mergeTriplets(int[][] triplets, int[] target) {\n        return false;\n    }\n}',
      cpp: 'bool mergeTriplets(vector<vector<int>>& triplets, vector<int>& target) {\n    return false;\n}',
      go: 'func mergeTriplets(triplets [][]int, target []int) bool {\n    return false\n}',
    },
    solutions: {
      javascript: 'function mergeTriplets(triplets, target) {\n  let a = 0, b = 0, c = 0;\n  for (const [x, y, z] of triplets) {\n    if (x <= target[0] && y <= target[1] && z <= target[2]) {\n      a = Math.max(a, x);\n      b = Math.max(b, y);\n      c = Math.max(c, z);\n    }\n  }\n  return a === target[0] && b === target[1] && c === target[2];\n}',
      python: 'def merge_triplets(triplets, target):\n    a = b = c = 0\n    for x, y, z in triplets:\n        if x <= target[0] and y <= target[1] and z <= target[2]:\n            a, b, c = max(a, x), max(b, y), max(c, z)\n    return a == target[0] and b == target[1] and c == target[2]',
      java: 'class Solution {\n    public boolean mergeTriplets(int[][] triplets, int[] target) {\n        int a = 0, b = 0, c = 0;\n        for (int[] t : triplets) {\n            if (t[0] <= target[0] && t[1] <= target[1] && t[2] <= target[2]) {\n                a = Math.max(a, t[0]);\n                b = Math.max(b, t[1]);\n                c = Math.max(c, t[2]);\n            }\n        }\n        return a == target[0] && b == target[1] && c == target[2];\n    }\n}',
      cpp: 'bool mergeTriplets(vector<vector<int>>& triplets, vector<int>& target) {\n    int a = 0, b = 0, c = 0;\n    for (auto& t : triplets) {\n      if (t[0] <= target[0] && t[1] <= target[1] && t[2] <= target[2]) {\n        a = std::max(a, t[0]); b = std::max(b, t[1]); c = std::max(c, t[2]);\n      }\n    }\n    return a == target[0] && b == target[1] && c == target[2];\n}',
      go: 'func mergeTriplets(triplets [][]int, target []int) bool {\n    a, b, c := 0, 0, 0\n    for _, t := range triplets {\n        if t[0] <= target[0] && t[1] <= target[1] && t[2] <= target[2] {\n            if t[0] > a { a = t[0] }\n            if t[1] > b { b = t[1] }\n            if t[2] > c { c = t[2] }\n        }\n    }\n    return a == target[0] && b == target[1] && c == target[2]\n}',
    },
    complexity: { time: 'O(n)', space: 'O(1)' },
    testCases: [
      { input: { triplets: [[2,5,3],[1,8,4],[1,7,5]], target: [2,7,5] }, expected: true,  hidden: false },
      { input: { triplets: [[3,4,5],[4,5,6]],         target: [3,2,5] }, expected: false, hidden: false },
      { input: { triplets: [[2,5,3],[2,3,4],[1,2,5],[5,2,3]], target: [5,5,5] }, expected: true, hidden: true },
    ],
  },
  {
    id: 'partition-labels',
    functionName: 'partitionLabels',
    number: 28,
    title: 'Partition Labels',
    difficulty: 'Medium',
    topic: 'greedy',
    tags: ['string', 'greedy'],
    leetcodeUrl: 'https://leetcode.com/problems/partition-labels/',
    description: 'You are given a string `s`. Partition it into as many parts as possible so that each letter appears in **at most one part**, and return a list of integers representing the size of these parts.',
    examples: [
      { input: 's = "ababcbacadefegdehijhklij"', output: '[9,7,8]' },
      { input: 's = "eccbbbbdec"',              output: '[10]' },
    ],
    constraints: ['1 <= s.length <= 500', 's consists of lowercase English letters.'],
    hints: ['Precompute the last index of each character, then greedily cut when your index hits the running max.'],
    approaches: [{ name: 'Last-Index Greedy', summary: 'Map each char to its last occurrence. Sweep i from 0; update end = max(end, last[s[i]]). When i == end, cut and reset.' }],
    starterCode: {
      javascript: 'function partitionLabels(s) {\n  // your code here\n  return [];\n}',
      python: 'def partition_labels(s):\n    # your code here\n    return []',
      java: 'class Solution {\n    public java.util.List<Integer> partitionLabels(String s) {\n        return new java.util.ArrayList<>();\n    }\n}',
      cpp: 'vector<int> partitionLabels(string s) {\n    return {};\n}',
      go: 'func partitionLabels(s string) []int {\n    return nil\n}',
    },
    solutions: {
      javascript: 'function partitionLabels(s) {\n  const last = {};\n  for (let i = 0; i < s.length; i++) last[s[i]] = i;\n  const out = []; let start = 0, end = 0;\n  for (let i = 0; i < s.length; i++) {\n    end = Math.max(end, last[s[i]]);\n    if (i === end) { out.push(end - start + 1); start = i + 1; }\n  }\n  return out;\n}',
      python: 'def partition_labels(s):\n    last = {c: i for i, c in enumerate(s)}\n    out, start, end = [], 0, 0\n    for i, c in enumerate(s):\n        end = max(end, last[c])\n        if i == end:\n            out.append(end - start + 1)\n            start = i + 1\n    return out',
      java: 'class Solution {\n    public java.util.List<Integer> partitionLabels(String s) {\n        int[] last = new int[26];\n        for (int i = 0; i < s.length(); i++) last[s.charAt(i) - \'a\'] = i;\n        java.util.List<Integer> out = new java.util.ArrayList<>();\n        int start = 0, end = 0;\n        for (int i = 0; i < s.length(); i++) {\n            end = Math.max(end, last[s.charAt(i) - \'a\']);\n            if (i == end) { out.add(end - start + 1); start = i + 1; }\n        }\n        return out;\n    }\n}',
      cpp: 'vector<int> partitionLabels(string s) {\n    int last[26] = {};\n    for (int i = 0; i < (int)s.size(); i++) last[s[i]-\'a\'] = i;\n    vector<int> out;\n    int start = 0, end = 0;\n    for (int i = 0; i < (int)s.size(); i++) {\n      end = std::max(end, last[s[i]-\'a\']);\n      if (i == end) { out.push_back(end - start + 1); start = i + 1; }\n    }\n    return out;\n}',
      go: 'func partitionLabels(s string) []int {\n    last := [26]int{}\n    for i, c := range s { last[c-\'a\'] = i }\n    out, start, end := []int{}, 0, 0\n    for i, c := range s {\n        if last[c-\'a\'] > end { end = last[c-\'a\'] }\n        if i == end { out = append(out, end-start+1); start = i + 1 }\n    }\n    return out\n}',
    },
    complexity: { time: 'O(n)', space: 'O(1)' },
    testCases: [
      { input: { s: 'ababcbacadefegdehijhklij' }, expected: [9,7,8],  hidden: false },
      { input: { s: 'eccbbbbdec' },              expected: [10],    hidden: false },
      { input: { s: 'abc' },                     expected: [1,1,1], hidden: true },
    ],
  },
  {
    id: 'valid-parenthesis-string',
    functionName: 'checkValidString',
    number: 29,
    title: 'Valid Parenthesis String',
    difficulty: 'Medium',
    topic: 'greedy',
    tags: ['string', 'greedy'],
    leetcodeUrl: 'https://leetcode.com/problems/valid-parenthesis-string/',
    description: 'Given a string `s` containing only three types of characters: `(`, `)`, and `*`, return `true` if the string is **valid**.\n\nRules:\n- Any left parenthesis `(` must have a corresponding right parenthesis `)`.\n- Any right parenthesis `)` must have a corresponding left parenthesis `(`.\n- Left parenthesis `(` must appear **before** the corresponding right parenthesis `)`.\n- `*` could be treated as `(`, `)`, or an empty string `""`.',
    examples: [
      { input: 's = "()"',  output: 'true' },
      { input: 's = "(*)"', output: 'true' },
      { input: 's = "((("', output: 'false' },
    ],
    constraints: ['1 <= s.length <= 100', 's[i] is one of `(`, `)`, `*`.'],
    hints: ['Track the *range* of possible open-counts. hi can never go negative.'],
    approaches: [{ name: 'Range Greedy', summary: 'Track lo (fewest open parens possible) and hi (most open parens possible) as you scan. On `*`, lo-- and hi++. If hi ever < 0, return false. End requires lo == 0.' }],
    starterCode: {
      javascript: 'function checkValidString(s) {\n  // your code here\n  return true;\n}',
      python: 'def check_valid_string(s):\n    # your code here\n    return True',
      java: 'class Solution {\n    public boolean checkValidString(String s) {\n        return true;\n    }\n}',
      cpp: 'bool checkValidString(string s) {\n    return true;\n}',
      go: 'func checkValidString(s string) bool {\n    return true\n}',
    },
    solutions: {
      javascript: 'function checkValidString(s) {\n  let lo = 0, hi = 0;\n  for (const c of s) {\n    if (c === "(") { lo++; hi++; }\n    else if (c === ")") { lo = Math.max(lo - 1, 0); hi--; }\n    else { lo = Math.max(lo - 1, 0); hi++; }\n    if (hi < 0) return false;\n  }\n  return lo === 0;\n}',
      python: 'def check_valid_string(s):\n    lo = hi = 0\n    for c in s:\n        if c == "(": lo += 1; hi += 1\n        elif c == ")": lo = max(lo - 1, 0); hi -= 1\n        else: lo = max(lo - 1, 0); hi += 1\n        if hi < 0: return False\n    return lo == 0',
      java: 'class Solution {\n    public boolean checkValidString(String s) {\n        int lo = 0, hi = 0;\n        for (int i = 0; i < s.length(); i++) {\n            char c = s.charAt(i);\n            if (c == \'(\') { lo++; hi++; }\n            else if (c == \')\') { lo = Math.max(lo - 1, 0); hi--; }\n            else { lo = Math.max(lo - 1, 0); hi++; }\n            if (hi < 0) return false;\n        }\n        return lo == 0;\n    }\n}',
      cpp: 'bool checkValidString(string s) {\n    int lo = 0, hi = 0;\n    for (char c : s) {\n      if (c == \'(\') { lo++; hi++; }\n      else if (c == \')\') { lo = std::max(lo - 1, 0); hi--; }\n      else { lo = std::max(lo - 1, 0); hi++; }\n      if (hi < 0) return false;\n    }\n    return lo == 0;\n}',
      go: 'func checkValidString(s string) bool {\n    lo, hi := 0, 0\n    for i := 0; i < len(s); i++ {\n        switch s[i] {\n        case \'(\': lo++; hi++\n        case \')\': if lo > 0 { lo-- }; hi--\n        default: if lo > 0 { lo-- }; hi++\n        }\n        if hi < 0 { return false }\n    }\n    return lo == 0\n}',
    },
    complexity: { time: 'O(n)', space: 'O(1)' },
    testCases: [
      { input: { s: '()' },  expected: true,  hidden: false },
      { input: { s: '(*)' }, expected: true,  hidden: false },
      { input: { s: '(((' }, expected: false, hidden: true },
    ],
  },
];