// dp topic (continued)
export const DP_PROBLEMS = [
  {
    id: 'climbing-stairs',
    functionName: 'climbStairs',
    number: 5,
    title: 'Climbing Stairs',
    difficulty: 'Easy',
    topic: 'dp',
    tags: ['dynamic-programming', 'fibonacci'],
    leetcodeUrl: 'https://leetcode.com/problems/climbing-stairs/',
    description: 'You are climbing a staircase. It takes `n` steps to reach the top.\n\nEach time you can either climb **1 or 2 steps**. In how many distinct ways can you climb to the top?',
    examples: [
      { input: 'n = 2', output: '2', explanation: '1+1 or 2' },
      { input: 'n = 3', output: '3', explanation: '1+1+1, 1+2, or 2+1' },
    ],
    constraints: ['1 <= n <= 45'],
    hints: ['The answer for step n is the sum of the answers for steps n-1 and n-2.'],
    approaches: [{ name: 'DP / Fibonacci', summary: 'Let dp[i] = ways to reach step i. Then dp[i] = dp[i-1] + dp[i-2]. Base: dp[1]=1, dp[2]=2. You only need the last two values — O(1) space.' }],
    starterCode: {
      javascript: 'function climbStairs(n) {\n  // your code here\n  return 0;\n}',
      python: 'def climb_stairs(n):\n    # your code here\n    return 0',
      java: 'class Solution {\n    public int climbStairs(int n) {\n        return 0;\n    }\n}',
      cpp: 'int climbStairs(int n) {\n    return 0;\n}',
      go: 'func climbStairs(n int) int {\n    return 0\n}',
    },
    solutions: {
      javascript: 'function climbStairs(n) {\n  if (n <= 2) return n;\n  let a = 1, b = 2;\n  for (let i = 3; i <= n; i++) [a, b] = [b, a + b];\n  return b;\n}',
      python: 'def climb_stairs(n):\n    if n <= 2: return n\n    a, b = 1, 2\n    for _ in range(3, n + 1):\n        a, b = b, a + b\n    return b',
      java: 'class Solution {\n    public int climbStairs(int n) {\n        if (n <= 2) return n;\n            int a = 1, b = 2;\n            for (int i = 3; i <= n; i++) { int t = a + b; a = b; b = t; }\n            return b;\n        }\n    }',
      cpp: 'int climbStairs(int n) {\n    if (n <= 2) return n;\n    int a = 1, b = 2;\n    for (int i = 3; i <= n; i++) { int t = a + b; a = b; b = t; }\n    return b;\n}',
      go: 'func climbStairs(n int) int {\n    if n <= 2 { return n }\n    a, b := 1, 2\n    for i := 3; i <= n; i++ { a, b = b, a+b }\n    return b\n}',
    },
    complexity: { time: 'O(n)', space: 'O(1)' },
    testCases: [
      { input: { n: 2 },  expected: 2, hidden: false },
      { input: { n: 3 },  expected: 3, hidden: false },
      { input: { n: 10 }, expected: 89, hidden: true },
    ],
  },
  {
    id: 'min-cost-climbing-stairs',
    functionName: 'minCostClimbingStairs',
    number: 111,
    title: 'Min Cost Climbing Stairs',
    difficulty: 'Easy',
    topic: 'dp',
    tags: ['dynamic-programming', 'array'],
    leetcodeUrl: 'https://leetcode.com/problems/min-cost-climbing-stairs/',
    description: 'You are given an integer array `cost` of length `n`. You start at step index `0` or `1`. To move to the next step, pay the cost of the current step. Return the **minimum cost** to reach the top (past the last index).',
    examples: [
      { input: 'cost = [10,15,20]', output: '15', explanation: 'Start at index 1, pay 15, climb two steps to top.' },
      { input: 'cost = [1,100,1,1,1,100,1,1,100,1]', output: '6' },
    ],
    constraints: ['2 <= cost.length <= 1000', '0 <= cost[i] <= 999'],
    hints: ['dp[i] = min cost to reach step i. dp[i] = min(dp[i-1], dp[i-2]) + cost[i].'],
    approaches: [{ name: 'Linear DP', summary: 'dp[0]=cost[0], dp[1]=cost[1]. For i >= 2: dp[i] = cost[i] + min(dp[i-1], dp[i-2]). Answer = min(dp[n-1], dp[n-2]).' }],
    starterCode: {
      javascript: 'function minCostClimbingStairs(cost) {\n  // your code here\n  return 0;\n}',
      python: 'def min_cost_climbing_stairs(cost):\n    # your code here\n    return 0',
      java: 'class Solution {\n    public int minCostClimbingStairs(int[] cost) {\n        return 0;\n    }\n}',
      cpp: 'int minCostClimbingStairs(vector<int>& cost) {\n    return 0;\n}',
      go: 'func minCostClimbingStairs(cost []int) int {\n    return 0\n}',
    },
    solutions: {
      javascript: 'function minCostClimbingStairs(cost) {\n  for (let i = 2; i < cost.length; i++) cost[i] += Math.min(cost[i-1], cost[i-2]);\n  return Math.min(cost[cost.length-1], cost[cost.length-2]);\n}',
      python: 'def min_cost_climbing_stairs(cost):\n    for i in range(2, len(cost)):\n        cost[i] += min(cost[i-1], cost[i-2])\n    return min(cost[-1], cost[-2])',
      java: 'class Solution {\n    public int minCostClimbingStairs(int[] cost) {\n        for (int i = 2; i < cost.length; i++) cost[i] += Math.min(cost[i-1], cost[i-2]);\n        return Math.min(cost[cost.length-1], cost[cost.length-2]);\n    }\n}',
      cpp: 'int minCostClimbingStairs(vector<int>& cost) {\n    for (int i = 2; i < (int)cost.size(); i++) cost[i] += min(cost[i-1], cost[i-2]);\n    return min(cost.back(), cost[cost.size()-2]);\n}',
      go: 'func minCostClimbingStairs(cost []int) int {\n    for i := 2; i < len(cost); i++ {\n        if cost[i-1] < cost[i-2] { cost[i] += cost[i-1] } else { cost[i] += cost[i-2] }\n    }\n    if cost[len(cost)-1] < cost[len(cost)-2] { return cost[len(cost)-1] }\n    return cost[len(cost)-2]\n}',
    },
    complexity: { time: 'O(n)', space: 'O(1)' },
    testCases: [
      { input: { cost: [10,15,20] }, expected: 15, hidden: false },
      { input: { cost: [1,100,1,1,1,100,1,1,100,1] }, expected: 6, hidden: false },
      { input: { cost: [0,0,0,0] }, expected: 0, hidden: true },
    ],
  },
  {
    id: 'house-robber',
    functionName: 'rob',
    number: 112,
    title: 'House Robber',
    difficulty: 'Medium',
    topic: 'dp',
    tags: ['dynamic-programming', 'array'],
    leetcodeUrl: 'https://leetcode.com/problems/house-robber/',
    description: 'You are a robber planning to rob houses along a street. Each house has a non-negative amount of money. Adjacent houses have security — you **cannot** rob two adjacent houses. Return the maximum amount you can rob.',
    examples: [
      { input: 'nums = [1,2,3,1]', output: '4', explanation: 'Rob house 1 (1) + house 3 (3) = 4.' },
      { input: 'nums = [2,7,9,3,1]', output: '12' },
    ],
    constraints: ['1 <= nums.length <= 100', '0 <= nums[i] <= 400'],
    hints: ['dp[i] = max(dp[i-1], dp[i-2] + nums[i]).'],
    approaches: [{ name: '1-D DP', summary: 'Walk houses; dp[i] = max(rob this house + dp[i-2], skip and take dp[i-1]).' }],
    starterCode: {
      javascript: 'function rob(nums) {\n  // your code here\n  return 0;\n}',
      python: 'def rob(nums):\n    # your code here\n    return 0',
      java: 'class Solution {\n    public int rob(int[] nums) {\n        return 0;\n    }\n}',
      cpp: 'int rob(vector<int>& nums) {\n    return 0;\n}',
      go: 'func rob(nums []int) int {\n    return 0\n}',
    },
    solutions: {
      javascript: 'function rob(nums) {\n  if (nums.length === 0) return 0;\n  let prev2 = 0, prev1 = 0;\n  for (const n of nums) [prev2, prev1] = [prev1, Math.max(prev1, prev2 + n)];\n  return prev1;\n}',
      python: 'def rob(nums):\n    prev2 = prev1 = 0\n    for n in nums:\n        prev2, prev1 = prev1, max(prev1, prev2 + n)\n    return prev1',
      java: 'class Solution {\n    public int rob(int[] nums) {\n        int prev2 = 0, prev1 = 0;\n        for (int n : nums) { int t = Math.max(prev1, prev2 + n); prev2 = prev1; prev1 = t; }\n        return prev1;\n    }\n}',
      cpp: 'int rob(vector<int>& nums) {\n    int prev2 = 0, prev1 = 0;\n    for (int n : nums) { int t = max(prev1, prev2 + n); prev2 = prev1; prev1 = t; }\n    return prev1;\n}',
      go: 'func rob(nums []int) int {\n    prev2, prev1 := 0, 0\n    for _, n := range nums { prev2, prev1 = prev1, max(prev1, prev2+n) }\n    return prev1\n}',
    },
    complexity: { time: 'O(n)', space: 'O(1)' },
    testCases: [
      { input: { nums: [1,2,3,1] }, expected: 4, hidden: false },
      { input: { nums: [2,7,9,3,1] }, expected: 12, hidden: false },
      { input: { nums: [2,1,1,2] }, expected: 4, hidden: true },
    ],
  },
  {
    id: 'house-robber-ii',
    functionName: 'rob',
    number: 113,
    title: 'House Robber II',
    difficulty: 'Medium',
    topic: 'dp',
    tags: ['dynamic-programming', 'array'],
    leetcodeUrl: 'https://leetcode.com/problems/house-robber-ii/',
    description: 'Houses are arranged in a **circle**. Same rules as House Robber, but the first and last houses are adjacent. Return the maximum amount you can rob.',
    examples: [
      { input: 'nums = [2,3,2]', output: '3' },
      { input: 'nums = [1,2,3,1]', output: '4' },
    ],
    constraints: ['1 <= nums.length <= 100', '0 <= nums[i] <= 1000'],
    hints: ['Either rob the first house (and not the last) or rob the last (and not the first). Compute both with the linear House Robber and take the max.'],
    approaches: [{ name: 'Two linear runs', summary: 'Run linear robber on nums[0..n-2] and on nums[1..n-1]; answer is max of the two.' }],
    starterCode: {
      javascript: 'function rob(nums) {\n  // your code here\n  return 0;\n}',
      python: 'def rob(nums):\n    # your code here\n    return 0',
      java: 'class Solution {\n    public int rob(int[] nums) {\n        return 0;\n    }\n}',
      cpp: 'int rob(vector<int>& nums) {\n    return 0;\n}',
      go: 'func rob(nums []int) int {\n    return 0\n}',
    },
    solutions: {
      javascript: 'function linear(a) {\n  let prev2 = 0, prev1 = 0;\n  for (const n of a) [prev2, prev1] = [prev1, Math.max(prev1, prev2 + n)];\n  return prev1;\n}\nfunction rob(nums) {\n  if (nums.length === 1) return nums[0];\n  return Math.max(linear(nums.slice(0, -1)), linear(nums.slice(1)));\n}',
      python: 'def linear(a):\n    p2 = p1 = 0\n    for n in a: p2, p1 = p1, max(p1, p2+n)\n    return p1\n\ndef rob(nums):\n    if len(nums) == 1: return nums[0]\n    return max(linear(nums[:-1]), linear(nums[1:]))',
      java: 'class Solution {\n    int linear(int[] a, int lo, int hi) {\n        int prev2 = 0, prev1 = 0;\n        for (int i = lo; i < hi; i++) { int t = Math.max(prev1, prev2 + a[i]); prev2 = prev1; prev1 = t; }\n        return prev1;\n    }\n    public int rob(int[] nums) {\n        if (nums.length == 1) return nums[0];\n        return Math.max(linear(nums, 0, nums.length-1), linear(nums, 1, nums.length));\n    }\n}',
      cpp: 'int linear(const vector<int>& a, int lo, int hi) {\n    int prev2 = 0, prev1 = 0;\n    for (int i = lo; i < hi; i++) { int t = max(prev1, prev2 + a[i]); prev2 = prev1; prev1 = t; }\n    return prev1;\n}\nint rob(vector<int>& nums) {\n    if (nums.size() == 1) return nums[0];\n    return max(linear(nums, 0, (int)nums.size()-1), linear(nums, 1, (int)nums.size()));\n}',
      go: 'func linear(a []int) int {\n    prev2, prev1 := 0, 0\n    for _, n := range a { prev2, prev1 = prev1, max(prev1, prev2+n) }\n    return prev1\n}\nfunc rob(nums []int) int {\n    if len(nums) == 1 { return nums[0] }\n    a := linear(nums[:len(nums)-1]); b := linear(nums[1:])\n    if a > b { return a }\n    return b\n}',
    },
    complexity: { time: 'O(n)', space: 'O(1)' },
    testCases: [
      { input: { nums: [2,3,2] }, expected: 3, hidden: false },
      { input: { nums: [1,2,3,1] }, expected: 4, hidden: false },
      { input: { nums: [0,0] }, expected: 0, hidden: true },
    ],
  },
  {
    id: 'longest-palindromic-substring',
    functionName: 'longestPalindrome',
    number: 114,
    title: 'Longest Palindromic Substring',
    difficulty: 'Medium',
    topic: 'dp',
    tags: ['dynamic-programming', 'string'],
    leetcodeUrl: 'https://leetcode.com/problems/longest-palindromic-substring/',
    description: 'Given a string `s`, return the **longest palindromic substring** in `s`.',
    examples: [
      { input: 's = "babad"', output: '"bab" or "aba"' },
      { input: 's = "cbbd"', output: '"bb"' },
    ],
    constraints: ['1 <= s.length <= 1000', 's consists of digits and English letters.'],
    hints: ['Expand-around-center: each index can be the center of an odd-length palindrome or the left center of an even-length one.'],
    approaches: [{ name: 'Expand-around-center', summary: 'For each index i, expand around i and around (i, i+1) while the substring is a palindrome. Track the longest.' }],
    starterCode: {
      javascript: 'function longestPalindrome(s) {\n  // your code here\n  return "";\n}',
      python: 'def longest_palindrome(s):\n    # your code here\n    return ""',
      java: 'class Solution {\n    public String longestPalindrome(String s) {\n        return "";\n    }\n}',
      cpp: 'string longestPalindrome(string s) {\n    return "";\n}',
      go: 'func longestPalindrome(s string) string {\n    return ""\n}',
    },
    solutions: {
      javascript: 'function longestPalindrome(s) {\n  let best = [0, 0];\n  function expand(l, r) {\n    while (l >= 0 && r < s.length && s[l] === s[r]) { l--; r++; }\n    if (r - l - 1 > best[1] - best[0] + 1) best = [l+1, r-1];\n  }\n  for (let i = 0; i < s.length; i++) { expand(i, i); expand(i, i+1); }\n  return s.slice(best[0], best[1] + 1);\n}',
      python: 'def longest_palindrome(s):\n    best = [0, 0]\n    def expand(l, r):\n        nonlocal best\n        while l >= 0 and r < len(s) and s[l] == s[r]:\n            l -= 1; r += 1\n        if r - l - 1 > best[1] - best[0] + 1:\n            best = [l+1, r-1]\n    for i in range(len(s)):\n        expand(i, i); expand(i, i+1)\n    return s[best[0]:best[1]+1]',
      java: 'class Solution {\n    int lo = 0, maxLen = 1;\n    void expand(String s, int l, int r) {\n        while (l >= 0 && r < s.length() && s.charAt(l) == s.charAt(r)) { l--; r++; }\n        if (r - l - 1 > maxLen) { maxLen = r - l - 1; lo = l + 1; }\n    }\n    public String longestPalindrome(String s) {\n        for (int i = 0; i < s.length(); i++) { expand(s, i, i); expand(s, i, i+1); }\n        return s.substring(lo, lo + maxLen);\n    }\n}',
      cpp: 'int lo = 0, maxLen = 1;\nauto expand = [&](int l, int r) {\n    while (l >= 0 && r < (int)s.size() && s[l] == s[r]) { l--; r++; }\n    if (r - l - 1 > maxLen) { maxLen = r - l - 1; lo = l + 1; }\n};\nfor (int i = 0; i < (int)s.size(); i++) { expand(i, i); expand(i, i+1); }\nreturn s.substr(lo, maxLen);',
      go: 'func longestPalindrome(s string) string {\n    lo, maxLen := 0, 1\n    expand := func(l, r int) {\n        for l >= 0 && r < len(s) && s[l] == s[r] { l--; r++ }\n        if r-l-1 > maxLen { lo, maxLen = l+1, r-l-1 }\n    }\n    for i := 0; i < len(s); i++ { expand(i, i); expand(i, i+1) }\n    return s[lo:lo+maxLen]\n}',
    },
    complexity: { time: 'O(n^2)', space: 'O(1)' },
    testCases: [
      { input: { s: 'babad' }, expected: 'bab', hidden: false },
      { input: { s: 'cbbd' }, expected: 'bb', hidden: false },
      { input: { s: 'a' }, expected: 'a', hidden: true },
    ],
  },
  {
    id: 'palindromic-substrings',
    functionName: 'countSubstrings',
    number: 115,
    title: 'Palindromic Substrings',
    difficulty: 'Medium',
    topic: 'dp',
    tags: ['dynamic-programming', 'string'],
    leetcodeUrl: 'https://leetcode.com/problems/palindromic-substrings/',
    description: 'Given a string `s`, return the number of **palindromic substrings** in it. A substring is a contiguous sequence of characters in the string.',
    examples: [
      { input: 's = "abc"', output: '3', explanation: '"a", "b", "c".' },
      { input: 's = "aaa"', output: '6', explanation: '"a", "a", "a", "aa", "aa", "aaa".' },
    ],
    constraints: ['1 <= s.length <= 1000', 's consists of lowercase English letters.'],
    hints: ['Expand-around-center, counting all palindromes found.'],
    approaches: [{ name: 'Expand-around-center', summary: 'For every center (odd and even), expand while still a palindrome, counting each one. O(n^2).' }],
    starterCode: {
      javascript: 'function countSubstrings(s) {\n  // your code here\n  return 0;\n}',
      python: 'def count_substrings(s):\n    # your code here\n    return 0',
      java: 'class Solution {\n    public int countSubstrings(String s) {\n        return 0;\n    }\n}',
      cpp: 'int countSubstrings(string s) {\n    return 0;\n}',
      go: 'func countSubstrings(s string) int {\n    return 0\n}',
    },
    solutions: {
      javascript: 'function countSubstrings(s) {\n  let count = 0;\n  function expand(l, r) {\n    while (l >= 0 && r < s.length && s[l] === s[r]) { count++; l--; r++; }\n  }\n  for (let i = 0; i < s.length; i++) { expand(i, i); expand(i, i+1); }\n  return count;\n}',
      python: 'def count_substrings(s):\n    count = 0\n    def expand(l, r):\n        nonlocal count\n        while l >= 0 and r < len(s) and s[l] == s[r]:\n            count += 1; l -= 1; r += 1\n    for i in range(len(s)):\n        expand(i, i); expand(i, i+1)\n    return count',
      java: 'class Solution {\n    int count = 0;\n    void expand(String s, int l, int r) {\n        while (l >= 0 && r < s.length() && s.charAt(l) == s.charAt(r)) { count++; l--; r++; }\n    }\n    public int countSubstrings(String s) {\n        for (int i = 0; i < s.length(); i++) { expand(s, i, i); expand(s, i, i+1); }\n        return count;\n    }\n}',
      cpp: 'int count = 0;\nauto expand = [&](int l, int r) {\n    while (l >= 0 && r < (int)s.size() && s[l] == s[r]) { count++; l--; r++; }\n};\nfor (int i = 0; i < (int)s.size(); i++) { expand(i, i); expand(i, i+1); }\nreturn count;',
      go: 'func countSubstrings(s string) int {\n    count := 0\n    expand := func(l, r int) {\n        for l >= 0 && r < len(s) && s[l] == s[r] { count++; l--; r++ }\n    }\n    for i := 0; i < len(s); i++ { expand(i, i); expand(i, i+1) }\n    return count\n}',
    },
    complexity: { time: 'O(n^2)', space: 'O(1)' },
    testCases: [
      { input: { s: 'abc' }, expected: 3, hidden: false },
      { input: { s: 'aaa' }, expected: 6, hidden: false },
      { input: { s: 'racecar' }, expected: 10, hidden: true },
    ],
  },
  {
    id: 'coin-change',
    functionName: 'coinChange',
    number: 116,
    title: 'Coin Change',
    difficulty: 'Medium',
    topic: 'dp',
    tags: ['dynamic-programming', 'bfs'],
    leetcodeUrl: 'https://leetcode.com/problems/coin-change/',
    description: 'Given an array `coins` of denominations and an integer `amount`, return the **fewest number of coins** needed to make up that amount. If it is impossible, return `-1`. You have an infinite supply of each coin.',
    examples: [
      { input: 'coins = [1,5,10,25], amount = 30', output: '2', explanation: '5 + 25' },
      { input: 'coins = [2], amount = 3', output: '-1' },
    ],
    constraints: ['1 <= coins.length <= 12', '1 <= coins[i] <= 2^31 - 1', '0 <= amount <= 10^4'],
    hints: ['Bottom-up DP: dp[a] = min coins to make amount a. dp[a] = 1 + min(dp[a-c]) for c in coins.'],
    approaches: [{ name: 'Bottom-up DP', summary: 'dp[0] = 0; for a in 1..amount: dp[a] = 1 + min(dp[a-c] for c in coins if a >= c). Return dp[amount].' }],
    starterCode: {
      javascript: 'function coinChange(coins, amount) {\n  // your code here\n  return 0;\n}',
      python: 'def coin_change(coins, amount):\n    # your code here\n    return -1',
      java: 'class Solution {\n    public int coinChange(int[] coins, int amount) {\n        return -1;\n    }\n}',
      cpp: 'int coinChange(vector<int>& coins, int amount) {\n    return -1;\n}',
      go: 'func coinChange(coins []int, amount int) int {\n    return -1\n}',
    },
    solutions: {
      javascript: 'function coinChange(coins, amount) {\n  const dp = new Array(amount + 1).fill(Infinity);\n  dp[0] = 0;\n  for (let a = 1; a <= amount; a++)\n    for (const c of coins) if (a >= c) dp[a] = Math.min(dp[a], 1 + dp[a - c]);\n  return dp[amount] === Infinity ? -1 : dp[amount];\n}',
      python: 'def coin_change(coins, amount):\n    dp = [float(\"inf\")] * (amount + 1)\n    dp[0] = 0\n    for a in range(1, amount + 1):\n        for c in coins:\n            if a >= c: dp[a] = min(dp[a], 1 + dp[a - c])\n    return -1 if dp[amount] == float(\"inf\") else dp[amount]',
      java: 'class Solution {\n    public int coinChange(int[] coins, int amount) {\n        int[] dp = new int[amount + 1];\n        java.util.Arrays.fill(dp, amount + 1);\n        dp[0] = 0;\n        for (int a = 1; a <= amount; a++)\n            for (int c : coins) if (a >= c) dp[a] = Math.min(dp[a], 1 + dp[a - c]);\n        return dp[amount] > amount ? -1 : dp[amount];\n    }\n}',
      cpp: 'int coinChange(vector<int>& coins, int amount) {\n    vector<int> dp(amount + 1, amount + 1);\n    dp[0] = 0;\n    for (int a = 1; a <= amount; a++)\n        for (int c : coins) if (a >= c) dp[a] = min(dp[a], 1 + dp[a - c]);\n    return dp[amount] > amount ? -1 : dp[amount];\n}',
      go: 'func coinChange(coins []int, amount int) int {\n    dp := make([]int, amount+1)\n    for i := range dp { dp[i] = amount + 1 }\n    dp[0] = 0\n    for a := 1; a <= amount; a++ {\n        for _, c := range coins {\n            if a >= c && dp[a-c]+1 < dp[a] { dp[a] = dp[a-c] + 1 }\n        }\n    }\n    if dp[amount] > amount { return -1 }\n    return dp[amount]\n}',
    },
    complexity: { time: 'O(amount * coins)', space: 'O(amount)' },
    testCases: [
      { input: { coins: [1,5,10,25], amount: 30 }, expected: 2, hidden: false },
      { input: { coins: [2], amount: 3 }, expected: -1, hidden: false },
      { input: { coins: [1], amount: 0 }, expected: 0, hidden: true },
    ],
  },
  {
    id: 'maximum-product-subarray',
    functionName: 'maxProduct',
    number: 117,
    title: 'Maximum Product Subarray',
    difficulty: 'Medium',
    topic: 'dp',
    tags: ['dynamic-programming', 'array'],
    leetcodeUrl: 'https://leetcode.com/problems/maximum-product-subarray/',
    description: 'Given an integer array `nums`, find a contiguous subarray that has the largest product, and return the product.',
    examples: [
      { input: 'nums = [2,3,-2,4]', output: '6', explanation: 'Subarray [2,3] has product 6.' },
      { input: 'nums = [-2,0,-1]', output: '0' },
    ],
    constraints: ['1 <= nums.length <= 2*10^4', '-10 <= nums[i] <= 10'],
    hints: ['A negative number flips the sign of max/min. Track both min and max ending at each index.'],
    approaches: [{ name: 'Min/Max DP', summary: 'Track minSoFar and maxSoFar ending at i. On each num: newMax = max(num, num*maxSoFar, num*minSoFar); newMin = min(...). Update answer.' }],
    starterCode: {
      javascript: 'function maxProduct(nums) {\n  // your code here\n  return 0;\n}',
      python: 'def max_product(nums):\n    # your code here\n    return 0',
      java: 'class Solution {\n    public int maxProduct(int[] nums) {\n        return 0;\n    }\n}',
      cpp: 'int maxProduct(vector<int>& nums) {\n    return 0;\n}',
      go: 'func maxProduct(nums []int) int {\n    return 0\n}',
    },
    solutions: {
      javascript: 'function maxProduct(nums) {\n  let minSoFar = maxSoFar = ans = nums[0];\n  for (let i = 1; i < nums.length; i++) {\n    const x = nums[i];\n    const newMax = Math.max(x, x * maxSoFar, x * minSoFar);\n    const newMin = Math.min(x, x * maxSoFar, x * minSoFar);\n    maxSoFar = newMax; minSoFar = newMin;\n    ans = Math.max(ans, maxSoFar);\n  }\n  return ans;\n}',
      python: 'def max_product(nums):\n    min_so_far = max_so_far = ans = nums[0]\n    for x in nums[1:]:\n        new_max = max(x, x*max_so_far, x*min_so_far)\n        new_min = min(x, x*max_so_far, x*min_so_far)\n        max_so_far, min_so_far = new_max, new_min\n        ans = max(ans, max_so_far)\n    return ans',
      java: 'class Solution {\n    public int maxProduct(int[] nums) {\n        int minSoFar = nums[0], maxSoFar = nums[0], ans = nums[0];\n        for (int i = 1; i < nums.length; i++) {\n            int x = nums[i];\n            int newMax = Math.max(x, Math.max(x * maxSoFar, x * minSoFar));\n            int newMin = Math.min(x, Math.min(x * maxSoFar, x * minSoFar));\n            maxSoFar = newMax; minSoFar = newMin;\n            ans = Math.max(ans, maxSoFar);\n        }\n        return ans;\n    }\n}',
      cpp: 'int maxProduct(vector<int>& nums) {\n    int minSoFar = nums[0], maxSoFar = nums[0], ans = nums[0];\n    for (int i = 1; i < (int)nums.size(); i++) {\n        int x = nums[i];\n        int newMax = max(x, max(x * maxSoFar, x * minSoFar));\n        int newMin = min(x, min(x * maxSoFar, x * minSoFar));\n        maxSoFar = newMax; minSoFar = newMin;\n        ans = max(ans, maxSoFar);\n    }\n    return ans;\n}',
      go: 'func maxProduct(nums []int) int {\n    minSoFar, maxSoFar, ans := nums[0], nums[0], nums[0]\n    for _, x := range nums[1:] {\n        newMax := max(x, max(x*maxSoFar, x*minSoFar))\n        newMin := min(x, min(x*maxSoFar, x*minSoFar))\n        maxSoFar, minSoFar = newMax, newMin\n        if maxSoFar > ans { ans = maxSoFar }\n    }\n    return ans\n}',
    },
    complexity: { time: 'O(n)', space: 'O(1)' },
    testCases: [
      { input: { nums: [2,3,-2,4] }, expected: 6, hidden: false },
      { input: { nums: [-2,0,-1] }, expected: 0, hidden: false },
      { input: { nums: [-2,3,-4] }, expected: 24, hidden: true },
    ],
  },
]
